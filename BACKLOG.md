# BACKLOG — TheSkillCodex

> Fichier privé (non publié). Idées et notions à traiter "un jour".
> Pas d'ordre, pas de priorité. On y déverse, on y pioche selon l'inspiration.

## Récent (17/06) — idées émergées en relecture §7

### 26/08 (suite 4) — le module Arduino fermé : deux alt faux, une capture à reprendre, un titre contre ses libellés

- [ ] **La capture de l'*Exemple* de `tinkercad` ne montre pas l'exemple, et c'est un arbitrage d'auteur.** `simulation-en-cours.png` est placée sous « Blink simulé **avec bouton** » et montre le **Blink par défaut** : éditeur affichant le sketch d'usine, LED **intégrée** de la carte allumée, plan de travail **vide**, ni bouton ni résistance. **Trois issues** : reprendre la capture sur le montage réel de l'exemple ; déplacer l'image sous la § *Lancer la simulation*, où elle est juste ; ou la retirer. ⚠ **Seul Tim peut produire la première**, et tant qu'elle n'est pas tranchée, l'alt reste faux des deux côtés.
- [ ] **Deux alt de `tinkercad` affirment ce que l'image ne porte pas — correction FR due.** `plan-de-travail.png` : « Arduino Uno, **LED et résistance câblées** », alors que la plaque d'essai est **vierge** et qu'une flèche figure le glisser-déposer depuis la barre latérale. `simulation-en-cours.png` : « **LED allumée sur le plan de travail** », alors que c'est la LED intégrée. ⚠ **Reportés un pour un côté EN** : la correction s'écrit d'abord côté FR, et elle attend l'arbitrage de capture ci-dessus. ⚠ **Périmètre non mesuré** : aucun balayage d'alt contre image n'a jamais été fait hors des fiches en cours de traduction.
- [ ] **`tinkercad` annonce « deux modes » quand la capture en montre trois.** `editeur-code-texte.png` affiche le menu *MODE D'ÉDITION* à trois entrées — **Blocs / Blocs + Texte / Texte** — et la fiche n'en décrit que deux. **Deux issues** : mentionner *Blocs + Texte* en une ligne, ou dire explicitement qu'on l'ignore. La seconde est plus conforme à la posture de la fiche, qui écarte déjà le mode Blocs.
- [ ] **L'alt de `creer-circuit.png` cite un bouton qui n'existe pas à l'écran.** Il annonce « bouton *Créer un nouveau Circuit* » ; l'écran porte **+ Créer** puis un menu à trois entrées dont **Circuits**. Correction d'une ligne, sans arbitrage.
- [ ] **`simulation-electronique-en` : le titre et trois libellés divergent, et le corpus porte PLUS la forme divergente — arbitrage dû.** `title:` **Circuit simulation**, conforme au §5.3 figé, contre **3 libellés *Electronic simulation*** (`index`, `instruments-de-mesure-en`, `oscilloscope-en`) et **2 *Circuit simulation***. **Trois issues** : aligner les trois libellés sur le titre ; changer le titre **et** l'entrée §5.3 ; ou accepter la coexistence comme pour *PID control* / *PID control on Arduino*. ⚠ **`--libelles` ne le verra jamais** — les deux formes partagent « simulation ». ⚠ **Ne pas aligner sans trancher** : c'est la configuration exacte du 25/08 où la fiche isolée était la source et non la copie.
- [ ] **Le report un pour un a un troisième mode de défaut, et aucun contrôle ne le voit.** Après *copier trop* (typographie importée) et *copier trop peu* (exemption perdue), voici **laisser en français ce qui devait être traduit** : un commentaire de code, symétrique exact des identifiants anglicisés du 26/08 (suite 3). **Ces deux classes se corrigeraient par le même outil** — celui déjà proposé ci-dessous, qui compare un bloc de code FR à son jumeau EN : il suffirait qu'il distingue **jetons de code** et **texte de commentaire**, et qu'il signale un commentaire **inchangé** aussi bien qu'un identifiant **changé**.
- [x] **Les douze embeds absents sont identifiés** : **cinq fichiers distincts** visés des deux côtés du corpus, `arduino-debug/session-debogage.png` l'étant par deux fiches, soit **6 références FR + 6 EN**. Le passage de 11 à 12 est la traduction d'`arduino-debug`, pas un média perdu.
- [x] **Verdicts *cassée* et *ambiguë* des wikilinks relancés et lus** : **0 et 0**, sur 91 mortes, 8 gabarits, 6 alias, 390 ok.
- [x] **Entrée `asservissement` au §5.3 : tranchée par LECTURE et non par décision.** **closed-loop control**, déjà en production dans `chaine-energie-en` et dans un titre de section d'`arduino-moteur-cc-en`. Neuf entrées versées. ⚠ **`réguler` n'a pas de périmètre mesuré** : le motif de balayage fusionnait `régul-` et `régulier`.
- [ ] **Un périmètre dont la règle de comptage ne sépare pas ce qu'elle prétend mesurer se cite comme inexistant, pas comme approximatif.** C110 exige qu'un chiffre porte sa règle ; C118 dit ce qu'on fait quand il n'y a pas de chiffre. **Il manque le cas où la règle est fausse.** ⚠ **La contrainte ASCII de C114 y pousse** : elle oblige à écrire `r.gul` là où on voulait `régul`, et le point avale un mot voisin. **Deux issues** : une ligne à C118, ou une note de méthode sur les balayages ASCII — *tout motif à joker se teste sur un échantillon avant d'être compté*.

### 26/08 (suite 3) — le lot 5 rédigé : un angle mort de contrôle, un chantier de libellés, une sortie à dimensionner

- [ ] **Aucun des cinq contrôles ne voit un identifiant de code traduit, et c'est un angle mort de classe et non d'implémentation.** Le premier jet de `timers-en` rendait `CAPTEUR` par `SENSOR`, `echeance` par `due`, `mesure` par `reading` : ni compteur, ni typographie, ni candidat C109 ne bronche. **Deux issues** : un mode qui **compare les identifiants d'un bloc de code FR à son jumeau EN** et signale tout jeton hors mots-clés du langage qui a changé — les trois compteurs prouvent déjà que les blocs sont appariés un pour un — ou rien, en admettant que la relecture suffise. ⚠ **Elle n'a pas suffi ici** : c'est la lecture d'une fiche de production qui a rattrapé le défaut, pas la relecture du jet.
- [ ] **`--libelles` est à 64 candidats et croit mécaniquement à chaque lot — il lui faut un chantier, pas un traitement au fil de l'eau.** Le compte oscille (55 → 76 → 64) sans jamais redescendre à zéro, parce que **chaque traduction rend jugeables des libellés antérieurs** (famille du 25/08 suite 4). Une bonne moitié sont des reformulations légitimes du type `[[gpio-en|pull resistors]]` et **ne partiront jamais**. **Trois issues** : une séance de balayage qui tranche les 64 une fois ; une **liste d'exemptions** dans l'outil pour les reformulations validées, afin que le compte redevienne un signal ; ou l'accepter comme bruit permanent et cesser de le citer en clôture. ⚠ **La deuxième est la seule qui rende le chiffre lisible d'une séance à l'autre.**
- [ ] **Une sortie de séance de 204 ko ne se lit pas, et la parade retenue à chaud est un ordre d'étapes.** Le bloc verbeux placé en dernier fait tomber son verdict en queue de fichier, là où un `tail` borné le trouve. **Ça a marché, et ça ne scale pas** : à deux blocs verbeux, un seul peut être dernier. **L'issue à coder reste celle du 26/08 (suite 2)** — un **résumé court des verdicts recopié en fin de fichier** par `seance.ps1` lui-même, indépendant de l'ordre des étapes. *L'item existe déjà ci-dessous ; cette séance en est la deuxième occurrence en deux jours.*
- [ ] **Le douzième embed absent n'est pas identifié.** L'audit médias passe de 11 à 12 entre le 26/08 (suite 2) et aujourd'hui, et **la ligne qui le nomme est restée dans le milieu non lu**. Hypothèse non vérifiée : la contrepartie EN du PNG manquant d'`arduino-timers`. ⚠ **Cité comme non mesuré (C118)** ; se lève d'un `audit-medias` isolé.
- [ ] **Les verdicts *cassée* et *ambiguë* des wikilinks n'ont pas été lus cette séance.** Seul le **93 mortes** l'a été. Ils étaient à 0 le 26/08 (suite 2) et **ne se reportent pas** ; à relancer à l'ouverture suivante.
- [x] **`arduino-pid` : titre TRANCHÉ le 26/08 (suite 3), Tim (a) — *PID control on Arduino*.** Forme qualifiée, sur le patron des six titres du lot 5. La production ne portait que des formes courtes, *PID control* ×4 dont le hub et *PID tuning* ×2, donc la règle du 25/08 (suite 7) n'avait pas d'arbitre. ⚠ **Aucun recalage de libellé n'est dû** : les trois *PID control* écrits dans le lot 5 sont la forme courte de parcours, et la coexistence des deux formes est le comportement décrit.
- [ ] **Reste ouvert après cet arbitrage : l'entrée `asservissement` / `réguler` au §5.3.** Elle ne se déduit pas du titre. `asservissement` (925 mots) et `boucle-ouverte` (1 100) sont dans l'anneau 2 et non traduites ; `boucle ouverte / fermée → open loop / closed loop` existe déjà, `asservissement` non. **À lire en production au cadrage** avant d'écrire quoi que ce soit.

### 26/08 (suite 2) — les dates, le trou de lecture, et quatre observations à arbitrer

- [ ] **Un chiffre mesuré juste puis publié après que l'objet a bougé n'est plus une mesure — borne proposée à C119.** Trois cas dans une soirée, aucun n'étant une dérivation : anneau 2 à 120 663 pour **120 666**, dette à 123 960 pour **123 963**, périmètre de relabellisation à 45 pour **74**. C119 interdit de **calculer** sur des chiffres justes ; celle-ci interdirait de les **reporter** après coup. **Deux issues** : une ligne de portée à C119 (*un chiffre se republie à la clôture s'il a pu bouger depuis sa mesure*), ou rien, en admettant que la remesure aux deux bouts couvre déjà le cas quand elle est faite. ⚠ **Elle ne l'était pas ici, et les trois fois le coût était une relance de quelques secondes.**
- [ ] **Aucun contrôle du dépôt ne lit les dates, et c'est pour ça que la dérive a survécu trois séances.** `normalize-pilotage.js` sort `[ok]` sur les cinq fichiers de pilotage à chaque clôture : il vérifie la typographie, jamais la chronologie. **Correctif bon marché** : un mode qui lit les titres `## 2026-MM-DD` du JOURNAL, vérifie l'ordre antichronologique, et **compare le libellé de la première entrée à `Date.now()`** en signalant tout écart supérieur à un jour. *Le dispositif mesure tout sauf ce qui date ses propres mesures.*
- [ ] **La lecture en deux passes de `seance-sortie.txt` laisse un trou quand la sortie s'allonge, et le fichier est écrasé à la série suivante.** Recette et `--controle` ont tourné en série 0 sans être lus, puis ont disparu. **Trois issues** : une sortie par bloc dans des fichiers séparés ; un résumé court des verdicts recopié en fin de fichier, là où la passe de queue le trouve toujours ; ou horodater `seance-sortie.txt` au lieu de l'écraser. **La deuxième est la moins chère et couvre le cas rencontré.**
- [ ] **Quatre observations de relecture sur les six sources FR du lot 5, à arbitrer AVANT la génération.** `arduino-interruptions` §4 porte trois deux-points dans un paragraphe, les deux derniers à une phrase de distance (règle locale d'empilement du 26/08) ; `arduino-eeprom` §1 empile *par page de 1024 octets :* et *Conséquence :* **et** coordonne un infinitif injonctif à une indicative ; `arduino-timers` l.37 rend par un deux-points un tiret à partie droite **infinitive**, que l'amendement du 23/08 exemptait ; `arduino-eeprom` écrit *= oubli du `commit()`* en prose. ⚠ **Corrigées après génération, elles coûtent une paire divergente et un recalage** ; corrigées avant, elles coûtent une édition.
- [ ] **Le titre de piège *Pas d'`EEPROM.commit()` oublié sur ESP32* nie à l'envers.** Le piège **est** l'oubli du `commit()` ; le titre dit qu'il n'y en a pas. Sans rapport avec les passes, antérieur. ⚠ **Périmètre non mesuré** : aucun balayage des têtes de piège à forme négative n'a été fait sur le corpus.
- [ ] **Le périmètre de la relabellisation hors fichiers de pilotage n'est pas mesuré.** `_drafts/relecture-ordre.md` porte 236 renvois datés, `feuille-de-route-captures.md` 92, `traduction-en-regles.md` 44, et plusieurs `tools/*.mjs` en portent dans leurs commentaires — mais **la ventilation par date n'a jamais été sortie**, donc on ignore combien tombent sur les trois jours corrigés. `tools/relabelliser.mjs` le dit en sortie plutôt que de laisser croire à une passe complète. **Compter avant d'étendre.**

### 26/08 (suite) — le serveur MCP tombé, le lot 5 cadré, et la dette qui décroît

- [ ] **Le serveur MCP `filesystem` déclare ses schémas de sortie en `draft-07` et le validateur de session n'accepte que `2020-12` : tous ses appels échouent, redémarrage compris.** Ce n'est pas un incident, c'est une incompatibilité de version, et elle reviendra à chaque session tant que le paquet n'est pas à jour. **Trois issues** : mettre à jour `@modelcontextprotocol/server-filesystem` sur les deux postes (la seule qui ferme la question) ; garder le patch git comme instrument d'édition permanent ; ou n'éditer que depuis le poste où le serveur répond. ⚠ **La deuxième a un coût caché** : `git apply --check` refuse le patch **entier** dès qu'un contexte bouge, là où `edit_file` refusait ancre par ancre — sur un lot de 76 sites, un seul décalage annule tout.
- [ ] **Un outil juste, lancé sur une copie, rend une prédiction et non une mesure — et C119 ne le dit pas.** La convention exige qu'un chiffre publié sorte d'un outil du dépôt ; elle a été écrite quand le bac à sable n'avait aucune copie du dépôt, si bien que « lancé » et « lancé sur l'objet » se confondaient. Ils ne se confondent plus. **Deux issues** : une ligne de portée à C119 (*l'outil se lance sur l'objet, pas sur une copie*), ou rien du tout en admettant que le cas restera exceptionnel. ⚠ **Il ne le restera pas** si le patch git devient l'instrument courant, puisqu'il se vérifie nécessairement sur une copie.
- [ ] **La dette a décru de six en traduisant six fiches, et le commentaire de l'outil dit le contraire.** `dette()` porte en clair *« elle grossit à chaque fiche traduite »*, ce qui est vrai en tendance et faux en solde : les cibles rouges d'un lot bien composé sont souvent **déjà** dans la dette, citées par des fiches traduites plus tôt. ⚠ **Le total seul ne permet pas de le voir** — la sortie n'imprime ni les entrées ni les sorties. **Deux lignes dans le mode** (cibles entrées / cibles sorties depuis la mesure précédente) rendraient la dette lisible comme un flux au lieu d'un stock, et c'est le seul chiffre du dispositif dont le sens de variation ne soit pas évident.
- [ ] **Une seconde collision de nommage français, de forme différente de celle du 26/08 : un libellé égal au `title:` de la fiche voisine.** Le hub Arduino écrit `[[arduino-deep-sleep|Deep sleep]] *(→ notion [[deep-sleep]])*` et la notion s'intitule `Deep sleep`. Ce n'est pas la collision `Programmation non bloquante` (deux `title:` identiques), c'est **le libellé de l'un qui est le titre de l'autre**, sur la même ligne, avec la parenthèse pour seul désambiguïsateur. ⚠ **Périmètre non mesuré** : motif = ligne de hub portant `[[X|L]] *(→ notion [[Y]])*` où `L` égale le `title:` de `Y`. **Compter avant de décider**, et traiter avec le chantier ouvert le 26/08 — les deux relèvent du même geste éditorial.
- [ ] **La charge C109 rapportée au volume vient de casser, et l'échantillon exclut le module comme cause.** 105 pour 7 830 mots sur le lot 5, contre un repère de ~1 pour 100 tenu le 25/08 (suite 7) sur les tutos Arduino (85/8 787) **et** sur les outils de pilotage (35/3 558). `arduino-pid` (15/1 631) et `tinkercad` (14/1 363) restent dans l'ancien régime, donc ce sont ces six fiches-là. **Hypothèse non mesurée** : les six sont des tutos « de concept » (timer, interruption, veille, mémoire, EEPROM, watchdog) là où les quatre premiers lots étaient des tutos « de geste » — et un tuto de concept explique, donc il subordonne. **Motif compté disponible : tirets et points-virgules par cent mots, sur les 33 fiches du module.**
- [ ] **`git apply --verbose` écrit sur stderr, et PowerShell rend sa progression comme un échec.** Sortie enveloppée dans un `RemoteException` avec le message découpé en travers, **code de sortie 0**. C'est le motif du 24/08 pris à l'envers : le flux d'erreur n'est plus avalé, il est défiguré. **Correctif immédiat appliqué** : ne plus passer `--verbose`. ⚠ **Mais la leçon est plus large** — tout outil natif qui écrit sa progression sur stderr produira le même faux signal dans un script de séance, et le lecteur du JOURNAL n'a que la sortie pour juger.
- [x] **~~Les deux postes divergent de deux jours d'horloge~~ — FAUX, et la vraie cause est pire.** Les deux postes lisent le même jour : commit PC pro du 26/08 à 16:47, commit PC perso du 26/08 à 20:11. **Ce qui avait dérivé, de trois jours, c'est le libellé de séance du JOURNAL**, obtenu en incrémentant le précédent au lieu d'être lu sur l'horloge — une dérivation, donc exactement ce que C119 interdit, appliqué à une date. **Mesuré par `git log -S"## 2026-08-2x" -- JOURNAL.md`** : la dérive commence à l'entrée libellée 25/08 (suite 7), introduite par un commit du 25/08 à 20:45, et **rien avant elle n'est fautif**. Trois entrées relabellisées. ⚠ **Le défaut de dispositif restait invisible parce qu'aucun contrôle ne lit les dates** — `normalize-pilotage.js` vérifie la typographie, pas la chronologie.
- [ ] **`tools/seance-lot5.patch` est un artefact jetable de la même nature que `seance.ps1` et `seance-sortie.txt`, mais il n'est pas ignoré.** Une troisième ligne au `.gitignore` fermerait la question, du même arbitrage que celui du 24/08 (option b, deux lignes). **À trancher si le patch git s'installe comme instrument.**

### 26/08 — le lot 4 Arduino, la collision de titre et le foisonnement qui se trompe de sens

- [ ] **Deux fiches portent le même titre dans les deux langues, et dix liens du corpus EN ne les distinguent plus.** `arduino-programmation-non-bloquante` et la notion transverse `programmation-non-bloquante` écrivent toutes deux `title: Programmation non bloquante`, et le corpus FR les cite indifféremment `[[...|programmation non bloquante]]` — `arduino-machine-a-etats` fait les deux dans la même page. Côté EN, **`Non-blocking programming` vise 3 fois la fiche Arduino et 7 fois la notion**. ⚠ **La traduction n'a rien créé** : elle a reporté un pour un, ce qui était la seule option correcte au moment du lot. **Trois issues** : qualifier le titre de la fiche Arduino des deux côtés (le parallèle existe déjà, `arduino-machine-a-etats` dit *sur Arduino* quand sa notion mère ne le dit pas) ; qualifier les seuls **libellés** sans toucher aux titres ; ou assumer, en admettant qu'un popover levé au survol distingue les deux. **La première est la seule qui répare le graphe et pas seulement l'affichage.**
- [ ] **Le foisonnement ne se contente plus de résister au classement : il joue à l'inverse du sens annoncé.** Le 25/08 (suite 8) avait constaté que la liste des causes n'**ordonne** pas les paires. Le lot 4 va plus loin : **`moteur-cc` sort en négatif** (1 246 → 1 236) alors qu'un lien nu y a été rempli, effet qui **ajoute** des mots par construction. La cause était donc présente, mesurable, de signe connu, et le résultat penche dans l'autre sens. Hypothèse non mesurée à tester sur les paires disponibles : **les tutos de câblage procèdent par chaînes nominales que l'anglais soude** (*la sécurité thermique du L298N* → *the L298N's thermal protection*, 5 mots → 4). Si elle tient, c'est la première cause de foisonnement qui dépende de la **syntaxe** de la fiche et non de son vocabulaire. **Motif compté : occurrences de `de la` / `du` / `de l'` par cent mots**, disponible sur 138 paires.
- [ ] **Une exemption C109 perdue reste invisible à l'outil, et cette séance montre que seule la prédiction chiffrée la rattrape.** Le correctif proposé le 25/08 (suite 5) — signaler `nEn < nFr` en candidat à lire — **aurait mordu ici** : 15 contre 16, sur `moteur-pas-a-pas`. Sans lui, la garde est la publication de la décomposition **fiche par fiche** avant lancement, qui repose entièrement sur ma rigueur d'écriture du script. ⚠ **Et le coût de l'oubli n'est pas nul** : une exemption perdue est une divergence FR/EN définitive que plus aucun contrôle ne signalera jamais. **À remonter d'un cran dans la file : c'est le seul défaut du chantier dont la détection dépende d'une prédiction que je choisis d'écrire.**
- [ ] **Lire les libellés en production APRÈS la génération pollue la lecture, et il a fallu filtrer à la main.** Onze des trente-neuf occurrences rendues venaient des six squelettes fraîchement écrits, encore en français. Elles se repèrent à l'œil (`Piloter un servomoteur`, `moteur CC`) **parce qu'elles sont en français**, mais rien ne garantit que ce sera toujours le cas — un squelette dont la source porte déjà un libellé anglais passerait inaperçu. **Deux issues** : lire les libellés **avant** le lancement 2, ce qui déplace la lecture au cadrage ; ou faire exclure par le geste de lecture les fiches EN dont le `source_sha256` est plus récent que le début de séance. **La première est gratuite et suffit.**
- [ ] **`Special case — ` est en production depuis le 25/08 sans figurer au §5.1, et la même chose vaut pour `Example — `.** Les deux formes ont été écrites quatre fois dans ce seul lot, sur des titres de section `Cas particulier — X` et `Exemple — X`. Elles se déduisent, mais **le §5.1 est justement la liste de ce qui ne doit pas se déduire deux fois de la même manière**. ⚠ **Périmètre non mesuré** : motif = titre de section commençant par `Cas particulier` ou `Exemple` suivi d'un tiret. **Compter, puis deux lignes au §5.1.**

### 25/08 (suite 8) — trois lots Arduino, C120, et la typographie importée par le report

- [ ] **Le contrôle de typographie devrait se relancer après toute exemption reportée, et il ne le fait qu'au lancement 3.** Les six espaces françaises du lot 3 sont nées du **report un pour un** d'exemptions C109, geste que le protocole prescrit et qui **copie donc la ponctuation avec son espacement**. Elles n'ont été vues qu'au contrôle final, après rédaction des six fiches. **Deux issues** : ajouter au §5.3 une consigne de relecture ciblée sur chaque exemption reportée (coût nul, dépend de ma vigilance, donc du même ordre que les huit échecs précédents), ou **faire rendre par `--style` un verdict séparé sur les lignes qui portent une exemption connue** — l'outil sait déjà quelles lignes il exempte. La seconde ferme la question par l'outil, comme le correctif du chevron l'a fait le 25/08.
- [ ] **`--libelles` et le signalement de liens nus comptent des occurrences, et tous les chiffres publiés depuis le 25/08 se lisent donc de travers.** `arduino-debug` rend `cpp` deux fois, `choisir-le-materiel-en` rend trois fois le même libellé vers la même cible. **Le « 50 candidats » du 25/08 (suite 7) n'est pas un effectif de liens à relire.** ⚠ **Conséquence pratique** : la charge de relecture est **surestimée** par ces totaux, et personne ne sait de combien. Deux lignes dans le mode suffiraient à rendre les deux nombres (occurrences et cibles distinctes).
- [ ] **Le foisonnement résiste à toute tentative de le classer, et il faudrait décider si on continue à le publier comme un indicateur.** Quatre lots, quatre degrés de propreté très différents, **écarts internes de 1,5 / 1,7 / 1,7 / 1,9 sans corrélation avec la propreté**. Et **deux fois dans la même journée la fiche la plus chargée en causes d'exclusion est sortie la plus basse de son lot**. La liste des six causes sert à **écarter un chiffre**, jamais à **ordonner des paires** — ce qui était l'usage implicite depuis le 23/08. **À trancher** : le ratio de lot reste-t-il un instrument de dimensionnement, ou n'est-il plus qu'une trace ?
- [ ] **C113 a maintenant trois bornes, et elles ne se composent pas en un critère unique.** *Dire contre désigner* (24/08), *ce que l'image affiche* (24/08 suite), *ce dont la forme porte une contrainte matérielle* (25/08 (suite 8), chaines sans accents pour le moniteur série). Les trois se sont trouvées une par une, chacune sur un cas réel, et **rien ne dit qu'il n'y en a pas une quatrième**. ⚠ **Le cas mixte est déjà rencontré** : `arduino-serie` garde une chaîne et en traduit une autre **dans la même fiche**. Une réécriture de C113 en **question unique** (que perd le lecteur anglophone si je traduis ?) couvrirait-elle les trois ? **À tenter à froid**, pas au milieu d'un lot.
- [ ] **La forme `**Titre.** Phrase entière. Phrase entière — glose` est le plus gros gisement d'angles morts du corpus, et `arduino-debug` en portait quatre à elle seule.** Le proxy est mesuré depuis le 23/08 (suite 4) : **142 puces à tête de plus de 100 caractères**. Jusqu'ici elles sortaient une par une ; le lot 3 en a rendu quatre d'un coup, toutes dans la même section d'une même fiche. ⚠ **L'exemption positionnelle les masque entièrement** : aucun balayage ne les rendra jamais. **Elles ne se trouvent qu'à la lecture**, ce qui rend le coût d'un lot proportionnel à ma vigilance et non à sa taille. **Un mode `--puces-longues` qui listerait les têtes à verbe conjugué coûterait peu et rendrait le gisement mesurable.**

### 25/08 (suite 7) — C119, les six outils de pilotage, l'ouverture du module Arduino

- [ ] **C119 ne couvre pas le pourcentage de foisonnement, et c'est exactement le genre d'interstice où elle se fera déborder.** La convention vise « les chiffres que le dépôt sait produire » ; **aucun outil ne rend un ratio**, qui se calcule à partir de deux valeurs mesurées. Règle d'usage adoptée sans arbitrage : **le publier toujours flanqué de ses deux valeurs brutes**. Trois issues à trancher un jour — étendre C119 aux grandeurs **dérivées** d'un couple de mesures publiées (et alors le ratio doit sortir d'un script), la borner explicitement aux grandeurs **absolues**, ou faire imprimer le foisonnement par `compter-mots --lot` quand on lui donne deux listes. **La troisième est la moins chère et ferme la question par l'outil**, comme l'amendement du 23/08 (suite 4) l'a fait pour la règle de comptage.
- [ ] **La glose de puce de *Raccrochage projet* tombe sous C109 et personne ne sait combien de fois.** Forme `**Étape N de la [[x|phase]]** — <explication à verbe conjugué>`, invisible à l'outil par la règle positionnelle, fautive par le critère du verbe conjugué. C'est une **glose de template**, donc présente dans toutes les fiches à *Raccrochage projet*. ⚠ **Périmètre non mesuré** (C118) : motif = puce dont la tête est en gras et dont la partie droite porte un verbe conjugué, dans une section terminale. **Compter avant de décider quoi que ce soit**, et la réponse engage le template autant que les fiches.
- [ ] **Le module `arduino/` pèse 45 047 mots sur 32 fiches, soit six lots, et rien ne dit ce qui se passe à mi-parcours.** Les modules restants sont mesurés (25/08 (suite 7), série 4) et **quatre d'entre eux — `cpp/`, `esp32/`, `stm32/`, `teensy/`, 80 484 mots — sont sous l'arbitrage C110 en attente sur les blocs en chevron**. Traduire dedans avant l'arbitrage, c'est publier des foisonnements dont on sait d'avance qu'ils ne mesurent rien. **Question d'ordonnancement** : trancher C110 avant d'y entrer, ou accepter une dette de mesure sur plus de la moitié de l'anneau.
- [ ] **`conduite/proj/` est le seul module qui ferme une branche, et il est repoussé.** Ses 13 fiches / 13 678 mots achèveraient `conduite/` (ese + meo + proj), donc **un anglophone lirait toute la conduite de projet sans un lien rouge**. Écarté au profit d'`arduino/` (arbitrage Tim du 25/08 (suite 7)). ⚠ **Il porte aussi les six liens rouges du triplet NF X50-151** : l'arbitrage dû sur ces six cibles se posera au moment où ce module reviendra, pas avant.
- [ ] **Une cible peut porter plusieurs libellés en production, et rien ne dit lequel fait titre.** Mesuré sur le module Arduino : jusqu'à **quatre** libellés distincts pour une seule cible, répartis entre la forme **courte** du hub (position dans un parcours) et la forme **qualifiée** des fiches-notion sœurs. La règle retenue — *le titre se prend sur la forme qualifiée* — tient parce qu'elle coïncide avec la traduction du `title:` français. **Le cas non résolu est `arduino-temporisation`**, dont le titre français `Temporiser` est un infinitif nu que rien ne dicte et dont les trois libellés en production (*Timing delays*, *delay() vs millis()*, *delays on Arduino*) ne convergent pas.
- [ ] **La charge C109 par mot est la même sur deux familles que tout oppose, et c'est un repère de dimensionnement neuf.** 35 occurrences pour 3 558 mots (outils de pilotage, `conduite/proj/`) contre **85 pour 8 787** (tutos, `arduino/`) : un peu moins d'une pour cent mots dans les deux cas. ⚠ **Le résidu, lui, n'est pas comparable** — 2 exemptions contre 13 — parce que les tutos procèdent par énumérations nominales et par listes `**Libellé** — explication`. **Deux chiffres à ne jamais confondre en prévision de lot** : la charge se prédit au mot, le résidu à la forme.

### 25/08 (suite 6) — l'anneau 2, trois lots

- [ ] **L'anneau 2 est le dernier, et le plan par anneaux arrive donc à son terme sans qu'on ait décidé ce qui vient après.** Mesure : **119 fiches restantes dans l'anneau 2 sur les 134 du corpus**, donc **quinze fiches seulement hors anneaux 0-2**. Le §7 des règles annonce des « anneaux suivants, recalculés depuis le front atteint » : **il n'y en aura pas**. Ce qui reste à arbitrer n'est plus un ordre d'anneaux mais un **ordre de modules** à l'intérieur d'un seul anneau — `arduino/` (33 fiches), `micropython/` (21), `esp32/` (12), `cpp/` (7), `conduite/proj/` (19), `stm32/`, `teensy/`, `raspberry-pi/`, `xiao/`, `simulation/`, `easyeda` seule à 9 773 mots. ⚠ **Les effectifs ci-dessus sont comptés à l'œil sur la sortie et ne sont pas citables** — ils sortiront de `compter-mots --lot` au cadrage. **À réécrire au §7 quand l'ordre sera arbitré.**
- [ ] **La dette et l'anneau ne divergent que par le voisinage des fiches traduites hors ordre, et la mesure vient de le montrer sur pièce.** À la première mesure, la dette valait **anneau 2 restant + kicad + micropython-bibliotheques** — une seule fiche d'écart, et *atteignables par aucun parent traduit* rendait zéro. **En fin de séance elle en compte trois** : le lot des six notions a ouvert `micropython-module` et `micropython-shield`. **Le décalage croît donc à chaque lot qui cite le module MicroPython**, traduit hors anneau comme module d'épreuve le 24/08. **Question ouverte** : ces trois fiches se traitent-elles à part, pour refermer une dette qui ne se refermera jamais toute seule, ou attendent-elles leur rang dans un module ?
- [ ] **`--anneau` sait déjà rendre les cibles orphelines, et il ne sait pas rendre les cibles à fort degré entrant.** Le mode imprime *atteignables par aucun parent traduit*, qui vaut zéro depuis sa création. **L'information symétrique manquerait moins** : quelles cibles de l'anneau 2 sont visées par le plus de fiches déjà traduites ? Ce sont elles qui ferment le plus de liens rouges par mot traduit, **et c'est le critère qui avait donné au lot 2a son « meilleur rapport liens fermés par mot »**. Coderait en trois lignes dans `dette()`, qui parcourt déjà le bon graphe.
- [ ] **La famille `Famille — Sujet` a mordu pour la première fois dans un lot, et le correctif reste non codé.** Quatre des sept emplacements de résidu du troisième lot sont des faux positifs de cette famille (`Arduino — bibliothèques`, `MicroPython — shield`…), mesurée le 24/08 (suite 3) à **15 puces sur 9 fiches, toutes dans `mcu/`**. Le correctif probable est écrit depuis un mois — **masquer les libellés de wikilink avant de chercher le tiret de glose** — et il vaut aussi pour la famille `VCC–GND` / `D0`–`D8`, qui **s'est répétée deux fois dans une même fiche** ce jour. ⚠ **Les deux familles sont unilatérales** (faux positifs seulement) donc non bloquantes, **mais elles polluent désormais la lecture d'un résidu**, qui ne se lit plus sans décomposition.
- [ ] **La borne textuelle du §6 va être éprouvée dix fois d'affilée, et rien ne prévoit ce qui se passe si elle casse.** Les sept `*-prise-en-main` de l'anneau 2, plus `falstad`, `ltspice`, `wokwi`, `tinkercad`, `easyeda` et `kicad` post-refonte, citent tous des chemins d'interface. **La borne n'est éprouvée que sur une fiche.** Le cas non rencontré qui la testera : un libellé dont la forme anglaise diffère **d'un caractère** (accent, espace, casse) — la borne dit qu'il se glose, ce qui produira une parenthèse quasi identique à son original. **À relire après le premier lot de simulation.**
- [ ] **Six liens rouges côté français, et le côté anglais les porte déjà suffixés.** `[[FP]]`, `[[FS]]`, `[[FC]]`, `[[critere]]`, `[[niveau]]`, `[[flexibilite]]` ne résolvent rien, et `specification-technique-en` écrit pourtant `[[critere-en|criterion]]`. **Le générateur a suffixé des cibles inexistantes dans les deux langues, et aucun des trois compteurs ne le voit** — ils comptent les liens sans les résoudre. ⚠ **C'est un contrôle qui manque et qui serait bon marché** : `--controle` a déjà la table de résolution de `graphe()` sous la main. **Question de fond à trancher d'abord** : ces six notions méritent-elles une fiche, ou les liens sont-ils à défaire ?

### 25/08 (suite 5) — les trois trames transverses

- [ ] **Les 291 000 mots du corpus incluent les notes d'édition privées, et personne ne l'avait vu.** `compter-mots.mjs` exclut le front matter et les blocs cloturés, **pas les commentaires HTML**. Mesuré : **3 446 mots enfermés dans 37 blocs sur six fiches**, dont **2 750 dans les trois templates** que `--recette` ne compte même pas comme fiches sources — deux instruments, deux périmètres, le même écart que le 243 contre 242 déjà ouvert. Les 696 mots restants vivent dans les trois trames transverses. ⚠ **La question n'est pas théorique depuis (c)** : les fiches EN n'ont plus leurs commentaires, les FR les gardent, donc **le foisonnement de ces trois paires est faux de sept points** si on le lit brut, et **le foisonnement de corpus est biaisé** de 696 mots. Trois issues : exclure les commentaires de C110 (**et publier sa note de discontinuité**, tous les chiffres de mots depuis le 22/08 bougeraient), les laisser et retrancher à la main lot par lot, ou retirer les commentaires des sources FR aussi. **Aucune n'est gratuite.**
- [ ] **La parade au point-virgule créé par la traduction n'est pas un contrôle, c'est un geste.** Les six du 25/08 (suite 5) sont tous nés au même endroit : un segment d'énumération **portant des virgules internes**, où le point-virgule paraît lever l'ambiguïté. Le français avait résolu les deux cas **par une parenthèse**, et je ne l'avais pas relu. Le cinquième contrôle de `--style` les attrape **après coup** ; ce qui les empêcherait, c'est de relire la phrase française à chaque énumération au lieu de trancher seul. **Candidat à une note au §4 des règles**, à côté de l'espace française devant `;` dont le geste est déjà identifié depuis cinq séances.
- [ ] **Une exemption C109 perdue à la traduction n'est signalée par rien.** Le différentiel du cinquième contrôle ne compte que les **créations** : rendre en virgules une incise que le français exempte fait **baisser** le compte EN, ce qui passe au vert. Quatre cas le 25/08 (suite 5), trois corrigés à la relecture, **un laissé délibérément**. Sans garde, le report un pour un repose entièrement sur ma relecture. **Correctif possible et bon marché** : signaler aussi `nEn < nFr`, en candidat à lire et non en verdict, puisqu'une baisse peut être licite (glose d'un terme anglophone qui tombe, §5.3).
- [ ] **Le foisonnement a maintenant huit lignes non interprétables sur 92 paires, et le test de corrélation n'a toujours pas été lancé.** Aux cinq déjà nommées (`machine-a-etats`, `grafcet`, `chaine-energie`, `schema-cinematique`, `cahier-des-charges-fonctionnel`) s'ajoutent **les trois trames transverses**, dont la base FR doit perdre ses mots de commentaire avant division. **La liste des paires à exclure est écrite, nommée, et s'allonge à chaque lot** — c'est maintenant **un tiers d'un lot type**. Le test de densité de code inline, écrit le 24/08, tourne sur 92 paires disponibles.
- [ ] **Il reste une seule zone que le masquage ne franchit pas, et son périmètre est compté depuis le 25/08 (suite).** Le **chevron de citation** : 68 blocs, 34 fiches, toutes dans `embarque/mcu/`. Le commentaire HTML vient d'être refermé par le même geste (une alternative de plus dans `masquerHorsProse`), **et le chevron demandera l'inverse** — retirer un préfixe de ligne avant de segmenter, pas ajouter un motif. ⚠ **Le chevron a deux symptômes et le commentaire n'en avait qu'un** : il touche aussi le **troisième compteur**, qui sous-compte en silence. C'est le plus grave des deux et le seul qui reste.

### 25/08 (suite 3) — la fiche pivot `cahier-des-charges-fonctionnel`

- [ ] **Une fiche dont le titre est un terme du §5.2 fabrique des faux positifs `--libelles` par construction, et la famille va grandir.** `cahier-des-charges-fonctionnel-en` s'intitule *Cahier des charges fonctionnel* ; les fiches EN qui la citent écrivent *functional requirements specification*, comme le §5.2 le prescrit. Résultat : **cinq candidats neufs le jour même** (35 → 40), sur `faire-communiquer-en`, `fiabiliser-et-deboguer-en` et `schema-bloc-fonctionnel-en` ×3, **tous légitimes et permanents**. Le mécanisme est structurel et se reproduira à chaque fiche à titre §5.2 traduite — `bete-a-cornes`, `pieuvre`, `ecodesign`, `amdec`, `afnor-nfx50-151`. ⚠ **Périmètre non mesuré** : motif = wikilink EN dont la cible porte un `title:` figurant au §5.2. **Compter avant de coder** (leçon du 24/08). Exemption possible : une liste fermée des slugs à titre §5.2, ou un test de recouvrement contre la **glose** du terme et non contre son titre.
- [ ] **Le retrait des `aliases:` côté EN est un comportement d'outil que personne n'a décidé, et il vient d'être mesuré.** `creer-fiche-en.mjs` supprime le bloc à la génération et l'annonce en sortie (*aliases retirés (2 lignes)*). **11 fiches FR portent un alias, 2 sont déjà traduites** (`generateur-de-signaux`, `precision-de-mesure`) **et leurs jumelles n'en portent aucun** : le précédent est posé depuis le 25/08 (suite), en silence. Le choix est défendable — un alias qui ne résout aucun lien n'est pas neutre (22/08 suite 2) — **mais il n'est écrit ni au §2 des règles de traduction, qui dit « traités au cas par cas », ni nulle part ailleurs**. À fermer d'une ligne au §2, ou à rouvrir si un alias EN devient utile. **9 fiches à alias restent à traduire.**
- [ ] **Une décomposition se publie en emplacements, un balayage rend des occurrences.** Le cadrage a annoncé **12 emplacements**, `--style` en a rendu **17** : la fiche porte cinq incises encadrées, qui comptent deux tirets chacune. Aucun écart réel, mais **la comparaison a demandé une conversion faite à la main après coup**, et un lecteur pressé y aurait lu un échec de prédiction. **Candidat à une note sous C110** : quand une prédiction et une sortie d'outil ne comptent pas la même unité, la règle de conversion se publie **avec** la prédiction, pas après la mesure.
- [ ] **Le foisonnement d'une fiche à forte densité §5.2 n'est pas une mesure de langue, et trois paires sont maintenant dans ce cas.** `cahier-des-charges-fonctionnel` sort à **+11,7 %** dont **~64 mots dérivés sur 154** viennent de gloses imposées et du remplissage de liens nus. Après `machine-a-etats` (24/08) et `chaine-energie` / `schema-cinematique` (25/08 suite 2), ça fait **cinq lignes du tableau des 87 paires qui ne mesurent pas l'expansion de langue**, toutes pour la même raison de fond : une décision éditoriale comptée comme du texte. ⚠ **Le test de corrélation code inline / foisonnement, écrit le 24/08 et jamais lancé, devrait les exclure** — la liste des paires à exclure existe désormais, elle est nommée, et elle s'allonge à chaque lot.
- [ ] **Le §5.2 impose des gloses que le foisonnement paye, et personne n'a mesuré combien de fiches restantes les portent.** Une fiche qui cite `bête à cornes`, `pieuvre`, `CdCF`, `AMDEC`, `GRAFCET`, `écodesign` ou `FP/FS/FC` doit gloser **chacun à sa première occurrence**, soit ~7 mots par terme. Les six dernières de l'anneau 1 sont **toutes dans `conduite/proj/`**, donc probablement les plus chargées du corpus sur cet axe. **Motif de mesure** : occurrences des termes du §5.2 par fiche non traduite. Sans ce chiffre, le foisonnement des prochains lots se lira de travers, comme celui-ci l'aurait été.

### 24/08 (suite 3) — le lot des huit notions transverses

- [ ] **C109 est aujourd'hui un prérequis de traduction, et personne n'a décidé si ça devait le rester.** La passe précède la génération, fiche par fiche : le travail C109 restant se confond donc avec les **168 fiches non traduites** et n'a pas de calendrier propre. Si le français doit être propre **indépendamment** de l'anglais, c'est un chantier de l'ordre de **2 100 emplacements** — 2 768 candidats mesurés moins les exemptions, au taux de 77 % traités observé sur les huit du jour — soit une vingtaine de séances au rythme de cette séance. ⚠ **Rien n'enregistre mécaniquement qu'une fiche a reçu sa passe** : la charge résiduelle ne permet pas de reconstituer l'historique, et les huit fiches passées aujourd'hui en portent encore 31. **Les deux réponses sont défendables ; elles n'engagent pas la même chose.** À ouvrir en début de séance, pas en clôture.
- [ ] **« Débugger » contre « déboguer » : 12 lignes contre 120, et deux fiches déjà traduites.** Le titre de `micropython-debug` est **« Débugger un programme MicroPython »** quand `debugger-embarque` écrit « déboguer » partout et pointe cette même fiche sous le libellé « **Déboguer** en MicroPython » : le même objet porte deux orthographes selon qu'on lit son titre ou un lien vers lui. **Périmètre mesuré** : 12 lignes sur 9 fiches, toutes dans `embarque/mcu/` — `arduino/` 5 fiches, `micropython/` 3, `cpp/` 1 — contre 120 lignes en « débog- ». ⚠ **Ce n'est pas une édition purement française** : `arduino` et `micropython` ont déjà leur jumelle anglaise depuis le 25/08, donc toucher leur FR les fera sortir en `DÉRIVÉ` avec report manuel puis `--recaler`. **Passe dédiée, pas un bouche-trou entre deux fiches.** Le chantier anglais n'est pas concerné, les deux formes donnant *debugging*.
- [ ] **Le masquage ne franchit pas le chevron de citation — un défaut, deux symptômes, un correctif.** Un bloc de code clôturé logé dans un callout (`> ```cpp`) n'est **ni compté** par le troisième compteur, **ni masqué** par le tamis C109. Mesuré sur `cpp-logs` : le compteur rend **7 blocs pour 8 présents**, et les deux `;` de `delay(10);` et `digitalWrite(13, etat);` remontent en candidats C109 à chaque passe, en français **comme en anglais** puisque le code ne change pas. **Faux positifs permanents et sous-comptage silencieux**, le second étant le plus grave : un bloc perdu à la traduction dans un callout ne serait pas vu. **Correctif probable** : retirer le préfixe de citation avant de segmenter, ce qui règle les deux à la fois. ⚠ **Périmètre non mesuré** — combien de blocs clôturés vivent derrière un chevron dans `content/` ? À compter avant de coder.
- [ ] **Trente-six lignes de la forme `**Libellé** — explication` restent hors de C115.** Périmètre total mesuré : **44 lignes sur 33 fiches**, dont **8 dans `cpp-logs`** traitées le 24/08 (suite 3). Les 36 autres se répartissent à raison d'une ou deux par fiche (`multimetre` 2, `arduino-bibliotheques` 2, `esp8266-prise-en-main` 2, `instruments-de-mesure` 2, puis des unités). **Passe mécanique et bon marché**, résolution figée par C115 : point après le gras fermant, jamais deux-points. ⚠ **Ne pas confondre avec la forme `Cas 1 — sujet`**, où le tiret vit **à l'intérieur** du gras : **26 lignes sur 14 fiches**, glose nominale, **exemptée**.
- [ ] **Remplir le libellé d'un lien nu gonfle mécaniquement le compte EN, et C110 ne le dit pas.** `[[processeur]]` fait **un** jeton sous la règle C110 ; `[[processeur-en|processor]]` en fait **deux**, et davantage quand le libellé est multi-mots (`[[choisir-le-materiel-en|choosing the hardware]]` en fait quatre). Sur `microcontroleur` et ses **onze liens nus**, le seul remplissage pèse **+19 mots sur +72**, dérivé emplacement par emplacement — **un quart du foisonnement affiché n'est pas du texte**. Le corpus porte **175 liens sans libellé**. **Conséquence à écrire** : un foisonnement de fiche ne se lit pas sans son compte de liens nus, ou bien C110 exclut les slugs du comptage — ce qui changerait tous les chiffres publiés et demande sa note de discontinuité. **Candidat à une extension de C110.**
- [ ] **Deux outils ne voient pas le même corpus : 243 contre 242.** `--recette` compte **243 fiches sources**, `compter-mots.mjs` en publie **242**. Un fichier de `content/` sort d'un périmètre et pas de l'autre. L'écart ne change aucune décision aujourd'hui, mais **deux instruments dont les périmètres divergent d'un finiront par se contredire sur un chiffre qui compte**. À trancher d'une lecture de source, pas de mémoire.
- [ ] **`--front` compte des fichiers présents, pas des fiches rédigées.** `cpp-logs` était comptée « déjà traduite » dès la génération de son squelette, avant le premier mot d'anglais : le RESTANT est donc juste au fichier près et faux au travail près dès qu'un squelette existe sans sa traduction. **Sans gravité tant que génération et rédaction se suivent dans la même séance** — ce que C116 garantit — mais **le piège se rouvre au premier lot interrompu en cours de route**. Piste : distinguer squelette et traduction sur un critère lisible (présence d'un `title:` traduit, par exemple).

### 25/08 — les sept hubs de famille

- [ ] **Le périmètre des légendes C74 n'est pas mesuré, et l'arbitrage (a) porte sur le corpus entier.** La forme `Libellé — apposition nominale` est désormais une glose exemptée de C109, en légende de figure comme en chapô de section. **Éprouvée sur quatre emplacements seulement**, tous rencontrés ce jour. C74 imposant une légende italique centrée à toute figure autonome, le corpus en porte un nombre inconnu. **Motif à compter** : ligne entièrement en italique commençant par `*` et contenant un cadratin. Sans ce chiffre, on ne sait pas si la borne couvre trois cas ou trois cents.
- [ ] **La borne libellé-glose contredit délibérément le second tamis, et il faudra bien un jour choisir lequel prime.** L'amendement du 24/08 dit qu'un segment nominal **qui commente tombe** ; `la carte de référence du panorama` commente, et il **reste**. Le motif retenu est que la forme est celle du tiret de glose de `Voir aussi`, licite au §4, et non celle d'une incise de prose. **C'est tenable tant que la tête est un libellé court.** Si une légende se présente dont la tête est une phrase et l'apposition un commentaire lourd, les deux règles se contrediront frontalement. **À rouvrir au premier cas de ce genre, pas avant.**
- [ ] **Un motif de défaut récurrent n'a pas de convention : la décomposition inventée sous un verdict juste.** Trois fois dans la même session, une prédiction dont le **total** était bon a nommé des **éléments** qui n'existaient pas (un tiret dans un alt qui n'en porte pas, deux liens dits nus qui portent un libellé). C110 impose qu'un chiffre se lise au lieu de se recompter, **et elle ne dit rien des listes**. Le symptôme est le même — affirmer sans lire — mais il échappe au contrôle du chiffre parce que le chiffre, lui, est juste. **Candidat à une extension de C110, ou à une convention propre.**
- [ ] **Le foisonnement a maintenant sept mesures sur un échantillon homogène, et aucune hypothèse ne survit.** Sept hubs de famille, même type, même jour, même main : **+1,9 / +0,1 / +5,7 / +3,1 / +5,6 / +0,1 / +3,0**. L'homogénéité de l'échantillon **écarte la nature de la fiche** comme cause, ce qu'aucune mesure antérieure ne pouvait faire. Restent la densité de code inline (test écrit le 24/08, jamais lancé, **61 paires disponibles**), la part de tableaux et de listes, et la longueur moyenne de phrase du FR. ⚠ **Quatre hypothèses ont déjà été posées puis démenties**, dont une par moi après deux points seulement. **Ne rien poser de cinquième sans avoir lancé le test de corrélation.**
- [ ] **`tools/README.md` a de nouveau trois sections de retard.** Il ignore le mode `--front`, le contrôle hors-latin de `--style` et l'exemption `step N` de `--libelles`. **Même constat qu'au 23/08 (suite 4)**, où trois sections manquaient déjà depuis le 22/08 : la doc d'outillage se périme à chaque session où un mode s'ajoute, et elle n'est jamais dans la même passe que le code. **À traiter en fin de session d'outillage plutôt qu'en session dédiée.**
- [ ] **`seance.ps1` a deux modes d'échec silencieux, tous deux découverts à l'usage.** **(1)** `Write-Host` dans un bloc `Etape` écrit sur la console hôte et non dans le pipeline : l'étape rend **vide avec un code 0**, indiscernable d'un contrôle passé. **(2)** Deux lancements successifs écrasent la sortie du premier, y compris quand c'est le premier qui a fait le travail. Les deux contraintes sont écrites en tête du script, **mais C114 ne les porte pas** — or elle porte déjà l'ASCII strict et la bannière par commande, qui sont de même nature. **À verser dans C114 à sa prochaine épreuve.**

### 24/08 (suite) — deux sous-lots du 2c

- [x] **Le compteur d'embeds sous-compte — CORRIGÉ le 25/08.** Motif porté à un niveau d'imbrication de crochets, appliqué aussi aux zones d'alt de `--style`. **Recette 395 → 397 sans qu'aucune fiche ait bougé**, note de discontinuité écrite au JOURNAL et au TODO ; `machine-a-etats-en` passe de 2 à 4 embeds **sans casser aucune paire**, le correctif comptant mieux des deux côtés à la fois. La contrainte induite sur les alt anglais tombe donc, mais **elle n'a pas été retirée du texte** : les crochets gardés pour satisfaire l'ancien compteur sont toujours là, et ils ne gênent rien. *Ancien libellé ci-dessous, conservé pour trace.* — **Le compteur d'embeds sous-compte, et le remettre d'aplomb change les chiffres publiés.** Son motif `!\[[^\]]*\]\([^)]+\)` s'arrête au premier `]` du texte alternatif : **un alt contenant un crochet rend l'embed invisible**. Mesuré sur tout `content/` : **écart de 4, porté par la seule paire `machine-a-etats`** (2 comptés pour 4 images de chaque côté). L'égalité FR/EN restant vraie, **le contrôle passe au vert en ne regardant que la moitié des embeds de cette fiche** : un embed perdu à la traduction n'y serait pas vu. ⚠ **Corollaire gênant, déjà subi** : il a fallu **garder des crochets dans les alt anglais** pour que la paire ne diverge pas — *un compteur défectueux impose sa forme au texte*. ⚠ **Et corriger le motif ferait bouger le 395 de la recette**, publié depuis le 22/08 et vérifié à chaque lancement : le chiffre deviendrait 397 sans que rien n'ait changé dans le dépôt. **À traiter avec sa note de discontinuité**, sinon une session future lira l'écart comme un corpus qui a bougé.
- [x] **Le différentiel FR/EN des caractères hors latin — PORTÉ le 25/08 dans `--style`.** Sixième famille du mode, **verdict mécanique** qui sort en code 1. **61 paires, 0 suspecte**, puis **1 sur banc de réinjection** — une copie de `pcb-en` déposée **hors de `content/`** pour que Quartz ne l'indexe jamais, avec un `U+4E2D` construit par son point de code. *Ancien libellé ci-dessous, conservé pour trace.* — **Le différentiel FR/EN des caractères hors latin n'existe que dans un script jetable.** Né d'un idéogramme chinois glissé dans `pcb-en`, **invisible aux cinq contrôles existants** — ni lien, ni embed, ni bloc de code, ni typographie, ni C109 — et rattrapé par la seule relecture. **La bonne forme a été trouvée à la deuxième tentative** : une plage de caractères rend deux faux positifs (l'ohm, les emojis du corpus), **la comparaison à la source n'en rend aucun** puisqu'un caractère présent des deux côtés est délibéré par construction. **54 paires, 0 suspecte, zéro prouvé par réinjection.** À porter dans `creer-fiche-en.mjs`, en sixième famille de `--style` ou en contrôle de `--controle`. **Vingt lignes, et le seul filet contre une corruption de génération.**
- [x] **`--libelles` — EXEMPTÉ le 25/08, et le chiffre annoncé était faux.** Le brief disait **quatorze** occurrences du patron `step N` : **il y en a seize**, comptées dans la liste complète et non reprises du total. Exemption codée sur le **motif** `^step \d+$` et non sur une liste de fiches, qui vieillirait à chaque étape ajoutée au parcours. **25 → 9 candidats à lire + 16 positions de parcours**, comptées à part au lieu d'être supprimées. Les **neuf restants ont tous été lus et sont légitimes**. *Ancien libellé ci-dessous, conservé pour trace.* — **`--libelles` devient bruyant à mesure qu'il gagne en couverture, et le patron responsable est identifié.** 3 candidats le 23/08, **25 le 24/08 (suite)**, dont **quatorze du seul motif `step N`** (`[[concevoir-l-electronique-en|step 3]]`, `[[fiabiliser-et-deboguer-en|step 6]]`…). Ces libellés désignent une **position dans le parcours** et non un titre, ils sont délibérés, et le patron se répétera à chaque fiche de parcours traduite — les cinq étapes de `realisation/` en portent déjà l'essentiel. **Exemption à coder** : libellé de la forme `step \d+` ou `étape \d+`. Sans elle, l'outil apprendra à être ignoré, exactement comme `--style` à son premier jet.
- [ ] **Le foisonnement résiste à une deuxième hypothèse, et deux mesures sont désormais polluées.** Après la densité de gloses, **l'hypothèse des tableaux est tombée deux fiches après avoir été posée** : `concevoir-l-electronique` (cinq tableaux) sort à +0,1 %, `programmer-l-embarque` (un seul) à **−1,0 %**, et `alimentation-electronique` à **−2,7 %**. Ce sont les **deux premières valeurs négatives du chantier**, toutes deux sur des fiches longues. ⚠ **Deux lignes du tableau des 54 paires ne mesurent plus la langue** : `machine-a-etats` (+9,4 %) et `grafcet` (+9,6 %) portent le paragraphe de correspondance des libellés ajouté par l'arbitrage (b), soit une décision éditoriale comptée comme expansion. **Le test de corrélation code inline / foisonnement du 24/08 tourne désormais sur 54 paires**, et devrait exclure ces deux-là.
- [x] **Les parenthèses — ÉCRITES le 25/08 au §8, et elles se spécialisent.** Troisième voie de résolution de C109, **huit emplois sur le lot des sept hubs**, presque tous au même endroit : un renvoi de fin de segment, `— voir [[x]]`, que ni le découpage en phrases ni la virgule ne rendent bien. Sur `teensy`, quatre puces de la section Écosystème se règlent ainsi. *Ancien libellé ci-dessous, conservé pour trace.* — **Un troisième mode de résolution de C109 sert sans être écrit : les parenthèses.** C109 prévoit le découpage en phrases (22/05) puis la phrase longue à virgules (22/08 suite). Un cas a demandé mieux : `des étapes — ce que le système fait — reliées par des transitions` devient ambigu en double virgule, où « reliées » semble se rattacher à l'incise. **Les parenthèses préservent la portée là où la virgule la brouille.** À écrire dans C109 ou à laisser au jugement, mais **pas à redécouvrir à chaque lot**.
- [ ] **La glose de `Voir aussi` attire la prose, et le §4 ne la couvre qu'à moitié.** Deux points-virgules à verbe conjugué y ont été trouvés en une séance (`alimentation-electronique`, `chronogramme`). Le §4 déclare licite le **tiret** de glose, pas la ponctuation **à l'intérieur** de la glose — or c'est le seul endroit d'une liste où l'auteur explique au lieu d'énumérer, donc le seul où la prose s'installe. **1 314 gloses de `Voir aussi` dans le corpus** (mesure du 22/08) : un balayage des `;` à l'intérieur des gloses dirait si le motif est isolé ou systématique.
- [ ] **Deux mots français à deux objets repérés mais non figés au glossaire.** **`broche`** = *pin* en électronique, *spindle* en usinage (rencontré sur `grafcet`, poste de perçage) ; **`trame`** = *packet* pour un message radio, *frame* pour une liaison série. Je les ai rendus différemment selon les fiches **sans que la règle soit écrite nulle part**, ce qui est exactement le défaut que le §5.3 existe pour empêcher. Cinquième et sixième du chantier après contrôleur, incertitude, valider et fin de course. ⚠ **`broche` est le plus dangereux** : les deux sens cohabitent dans la branche embarquée, et le glossaire ne porte aujourd'hui que `brochage → pinout`.
- [ ] **Une mention de la langue source n'a pas de règle, et le cas s'est présenté deux fois.** `programmer-l-embarque` et `algorithme` écrivent tous deux qu'un comportement « qu'on ne sait pas raconter **en français** » n'est pas prêt à être codé. Traduire par *in French* est absurde pour un anglophone, *in plain English* est idiomatique mais bizarre dans une fiche traduite lue par des non-anglophones. **Forme retenue deux fois : *in plain words*** — l'idée est le langage naturel opposé au code, pas une langue particulière. **À écrire au §5.3 sous forme de règle et non d'entrée** : *une mention de la langue source se rend par la langue naturelle, jamais par le nom d'une langue*.

### 24/08 — module d'épreuve MicroPython

- [ ] **Le foisonnement dépend peut-être de la densité de code inline, et le test tient en une ligne de script.** Le module sort à **+4,5 %**, le plus bas jamais mesuré, et **la longueur n'y trie rien** : `micropython-controle` est la 2ᵉ plus courte et sort à +2,6 %, `micropython-fonctions` à peine plus longue sort à +8,5 %. L'hypothèse de la densité de gloses, qui expliquait mal le lot 2b, **n'explique rien du tout ici**. **Hypothèse de remplacement** : C110 compte le code inline comme des mots (`ticks_ms()`, `global`, `while True:`), et **ces mots-là sont identiques dans les deux langues**. Une fiche saturée de code inline porte donc une fraction invariante qui dilue mécaniquement le foisonnement du texte alentour. Le classement y répond bien — `lire-un-programme` et `controle`, où presque chaque phrase cite un mot-clé, sont les deux plus basses ; `fonctions`, la plus prosaïque, est la plus haute. ⚠ **Trois autres causes tiennent aussi la route et aucune des quatre n'est mesurée** : la glose perdue de `micropython-repl` (*Read-Eval-Print Loop*, dont la moitié française n'a plus d'objet), la part de listes à puces, la longueur des titres de section. **TEST QUI TRANCHERAIT** : compter les occurrences de code inline par fiche (motif : segments entre backticks simples, hors blocs clos) et corréler au foisonnement, sur les 41 paires existantes — l'échantillon suffit déjà. **Ne rien écrire dans les règles avant.**
- [ ] **Les 48 fiches de l'anneau 1 ne ressemblent à aucun lot déjà mesuré.** Hubs de famille, notions transverses, colonne `realisation/` et lourdes de `conduite/proj` : **peu de code inline**, contrairement au module d'épreuve. Si l'hypothèse ci-dessus tient, leur foisonnement sera **plus haut** que +4,5 %, probablement plus proche du +5,7 % du lot 2b. **Ne pas transporter le chiffre du module**, et remesurer au premier sous-lot plutôt qu'à la fin.
- [x] **Le front n'est pas un ensemble fixe — `--front` ÉCRIT le 25/08, et l'écart est tranché.** L'anneau 1 vaut **82 cibles**, le chiffre du brief : le 79 du §7 et le 78 de ma mesure du 24/08 étaient l'un et l'autre faux. La règle de résolution est écrite dans le code et réimprimée à chaque lancement — chemin complet, puis suffixe de chemin, puis nom de fichier **unique**, un nom porté par plusieurs fiches sortant en AMBIGU. Décomposition vérifiée des 8 fiches EN hors anneau : six MicroPython, `decouplage`, et **`content/index` lui-même**, que rien n'atteint en wikilink depuis C111. *Ancien libellé ci-dessous, conservé pour trace.* — **Le front n'est pas un ensemble fixe, et rien dans l'outillage ne le dit.** Traduire une fiche déplace le front d'un cran : les 31 traductions des lots 2a et 2b ont ouvert **74 cibles neuves**, si bien que le front courant fait **122 fiches** quand le reste de l'anneau 1 en fait **48**. Le §7 des règles dit « le reste du front », formule qui se lit dans les deux sens. À réécrire en « les 48 restantes de l'anneau 1 ». ⚠ **Écart non résolu à tracer** : l'anneau 1 depuis les quatre index mesure **82 fiches** à ma règle de résolution, le §7 en annonçait **79**. L'écart ne change aucune décision, mais il ne doit pas se propager tel quel — **soit la règle de résolution diffère, soit le corpus a bougé depuis le 22/08**. Un mode `--front` dans `creer-fiche-en.mjs` réglerait la question une fois pour toutes et rendrait le périmètre citable comme les autres compteurs.

### 23/08 (suite 4) — outillage avant le lot 2c

- [ ] **Le contrôle de typographie ne regarde que les espaces en trop, jamais celles qui manquent.** `--style` attrape `75 %` écrit à la française ; il ne dira rien de `3mm`, `5V` ou `60°C` collés, alors que le §5.3 impose que **les unités SI gardent leur espace des deux côtés**. C'est le défaut **symétrique** de celui qui est traité, et il devient probable précisément parce que la règle du `%` pousse à coller. Écarté à la livraison faute de savoir estimer le bruit (`3D`, `5V` en sérigraphie, références de boîtiers). **À mesurer avant de coder** : combien d'occurrences de `\d(mm|cm|V|A|mA|kHz|MHz|ms|µs|kΩ|°C|kg|h|s)` dans `content/en/`, et combien sont légitimes.
- [ ] **L'exemption de glose garde un angle mort, retourné mais non supprimé.** La règle de **position** (le premier tiret d'une puce est la glose) exempte aussi une **incise de prose** quand la tête de puce est une phrase entière. Proxy mesuré le 23/08 (suite 4) : puces à tiret dont la tête dépasse 100 caractères, **5 côté EN, 142 côté FR**. Le compromis est délibéré dans ce sens — ne jamais signaler une glose licite, au prix de manquer une incise en tête de puce — mais les 142 FR disent que le reddéploiement C109 ne peut pas s'appuyer sur ce seul motif. **À rouvrir si une passe C109 de lot 2c laisse passer un tiret qu'une relecture attrape.**

### 23/08 (suite 3) — fermeture du lot 2b

- [x] **Aucun outil ne relit un jet EN pour lui-même — FAIT le 23/08 (suite 4), mode `--style`.** Porté dans `creer-fiche-en.mjs`, quatre familles : espace française devant `; : ! ? %` et virgule décimale en **verdict mécanique**, virgule ambiguë et C109 de prose en **candidats à lire**, plus un cinquième contrôle non prévu au brief — **la comparaison FR/EN du décompte C109**, parce qu'une occurrence reportée du français a déjà été arbitrée quand une occurrence **créée par la traduction** ne l'a jamais été. Premier passage sur les 35 fiches : **1 verdict réel** (`75 %` dans un alt de `pwm-en`), 1 virgule ambiguë, 0 créée, 34 candidats. ⚠ **Le premier jet rendait 6 verdicts dont 3 faux**, et le plus vicieux venait de mon masquage : remplacer le code par des espaces faisait lire `` `return`: `` comme une espace française devant deux-points. **L'instrument fabriquait le défaut qu'il signalait.**
- [x] **Le motif de glose de liste — CORRIGÉ le 23/08 (suite 4), et le correctif n'est pas celui qu'on attendait.** Il ne consiste pas à mieux reconnaître le gras. L'ancien motif exigeait une tête de puce d'une **forme** donnée (wikilink ou gras initial) ; le nouveau ne juge que la **position** — sur une puce, le premier tiret est la glose, tous les suivants sont de la prose. Le faux positif d'`integration-et-tests` disparaît, et le second tiret « logé derrière le tiret de glose » du 23/08 (suite 2), celui que l'exemption masquait, est désormais attrapé. **Angle mort résiduel mesuré, voir l'item du 23/08 (suite 4) plus haut.**
- [x] **Un chemin long casse le copier-coller PowerShell — ÉCRIT le 23/08 (suite 4) dans le README.** La forme sûre (`$p = "..."` puis `--recaler $p`) figure désormais dans la section `creer-fiche-en.mjs` du `tools/README.md`, avec son motif. Reste que **le message d'usage du script lui-même** donne toujours la forme longue : à corriger le jour où le script sera rouvert pour autre chose.
- [ ] **Le jargon d'édition en production n'a jamais été cherché ailleurs que là où il est tombé.** Trois occurrences de `H4` trouvées sur deux fiches, **par la traduction et non par relecture**. Rien ne dit qu'il n'y en a pas d'autres, et le vocabulaire d'édition du dépôt est étroit : `H2`, `H3`, `H4`, `front matter`, `callout`, `slug`, `wikilink`, `embed`. **Un balayage de dix minutes** dirait si le défaut est isolé ou systématique. Sans urgence, mais à faire avant la publication aux étudiants.
- [ ] **Le foisonnement varie d'un facteur trois à l'intérieur d'un même lot, et on ne sait pas pourquoi.** +2,5 % à +7,6 % sur cinq trames de calibre voisin. L'hypothèse de la densité de gloses explique les deux extrêmes et **échoue sur le milieu** : `concept` et `preuve-de-concept` portent deux gloses chacune et sortent à +7,6 % et +4,3 %. **Autres variables plausibles, aucune mesurée** : proportion de tableaux et d'énumérations nominales (l'anglais y est plus compact), densité de noms propres et de références catalogue, longueur moyenne de phrase du FR. Le lot 2c donnera **70 000 mots de matière** pour trancher, avec en plus un régime inédit — les blocs de code, hors comptage C110, mais dont les commentaires se traduisent. **Ne pas réécrire l'hypothèse avant d'avoir ces chiffres.**

### 23/08 (suite) — encart d'accueil et sélecteur de langue

- [ ] **`locale: "fr-FR"` — ce n'est pas un correctif qui manque, c'est un arbitrage écrit.** Le chrome de Quartz (« Rechercher », « Rétroliens », « Graphe », les dates) reste français sur les pages EN. Le coût est **accepté par défaut depuis le 22/08 et n'a jamais été tranché**, si bien qu'il ressort à chaque session comme s'il restait à faire. Trois issues : **(a)** refus tracé — une dizaine de mots contre deux builds, c'est-à-dire contre la simplicité que toute l'architecture a achetée ; **(b)** un `lang` par page si Quartz 4.5 le lit en front matter, **à vérifier en source avant d'en discuter**, pas de mémoire ; **(c)** un composant de layout, même famille que le sélecteur de langue ci-dessus, donc même session. **Ne pas rouvrir sans avoir fait (b).**
- [ ] **Le sélecteur de langue devrait être un composant Quartz, pas deux callouts écrits à la main.** C111 borne la pratique aux deux pages d'accueil, parce qu'un sélecteur par fiche produirait aujourd'hui **212 liens vers des fiches EN inexistantes**, invisibles à `audit-wikilinks.mjs` qui ne lit que les wikilinks. Un composant de layout déduirait la jumelle du chemin (`X.md` ↔ `en/X-en.md`, les huit `index.md` en cas particulier) et **se masquerait quand elle n'existe pas** : le problème des 404 disparaît au lieu d'être géré, et le sens FR → EN s'ouvre fiche par fiche au rythme de la traduction, sans une seule édition de contenu. À rouvrir **quand les 242 existeront**, ou plus tôt si un étudiant Erasmus se plaint de ne pas retrouver la version française depuis une fiche EN.
- [x] **Les libellés de wikilink ont désormais un outil — FAIT le 23/08 (suite 4), mode `--libelles`.** Heuristique livrée : un libellé qui ne recoupe aucun mot significatif du `title:` de sa cible est un candidat. Le bruit était **tenable après deux correctifs et pas avant** : 12 candidats au premier jet, ramenés à **3 sur 279 liens jugeables** en écartant la morphologie (*machined* / *Machining*, cinq cas) et les sigles (*PoC*, *ADC*). Les trois restants sont tous légitimes (*hub of this wiki*, *V-model*, *Finished product*). ⚠ **Le motif `Welding` ayant été corrigé le 23/08 (suite), il n'existe plus dans le corpus : seule sa réinjection a pu valider l'outil**, et le détecteur mord dès qu'on remet la fiche dans son état d'avant correction. **536 des 815 wikilinks à libellé visent une fiche non encore traduite** et restent injugeables — la couverture de l'outil grandira mécaniquement avec le corpus EN.
- [x] **Le foisonnement FR → EN vaut +7,4 % sur le lot 2a — remesuré à +4,3 % le 23/08 (suite 2), et c'est ce second chiffre qui dimensionne le reste.** 6 996 mots EN pour 6 515 mots FR, règle C110 des deux côtés. Le chiffre ne vaut que pour des notions de 150 à 500 mots, où la glose des termes conservés (§5.2) pèse proportionnellement lourd. **À remesurer au lot 2b** : si les trames s'en écartent, c'est la glose qui explique l'écart, et le reste du chantier se redimensionne à partir de là plutôt que depuis les 288 050 mots FR. **Réponse : elles s'en écartent, et c'est bien la glose.** `preuve-de-concept` sort à **+4,3 %** (6 689 mots EN pour 6 413 FR) parce qu'une trame de 6 400 mots ne porte que **deux gloses**, `CdCF` et `pieuvre`, soit une trentaine de mots dilués sur l'ensemble. Les **288 050 mots FR restants** sortent donc autour de **300 400 mots EN** et non 309 400. Ratio recontrôlé après la passe de vocabulaire C112 : **inchangé**. ⚠ **Un troisième régime reste possible au lot 2c**, où les fiches portent des blocs de code : le code ne se traduit pas et sort du comptage C110, mais les commentaires et la prose d'encadrement, si.

- [x] **C65 — la compensation est éprouvée le 23/08 (suite 2), elle tient, et ce n'est pas une prescription morte.** Test sur `preuve-de-concept-en`, 6 689 mots. Le contraste « on » / « tu » disparaît bien, comme le 22/08 l'annonçait, mais les tournures impersonnelles ne sonnent pas creux **à condition d'alterner passif et sujet collectif** : *the bench is characterised* pour un geste procédural, *the team learns to rule* quand la décision engage quelqu'un. Le passif enchaîné sur trois phrases, lui, tire vers la prose administrative. **C'est donc un arbitrage par paragraphe, pas une règle mécanique**, et c'est sous cette forme qu'il faut le lire au §4. Effet inattendu : **la contrainte a amélioré un passage** — « L'équipe apprend à statuer. Pas à constater, à statuer » devient *The team learns to rule. Not to observe, to rule*, où le verbe anglais unique force une distinction que le français étalait sur deux mots proches. ⚠ **Éprouvée sur une trame seulement** : les quatre autres du lot 2b diront si l'alternance se tient sans y penser ou s'il faut l'écrire comme procédé. **Réponse le 23/08 (suite 3) : elle se tient sans y penser, sur les cinq trames.** Aucune des quatre fiches suivantes n'a demandé d'arbitrage de registre, et le passif enchaîné ne s'est pas réinstallé. **Item clos, la formulation du §4 est la bonne et n'a pas besoin d'être durcie en procédé.**

### 22/08 (suite) — chantier anglais

- [x] **La version anglaise perd le contraste de registre C65, et personne ne l'a compensé.** Le français oppose le « on » des trames du V au « tu » des fiches de réalisation ; l'anglais n'a qu'un « you ». **Perte acceptée le 22/08**, compensation minimale prescrite (tournures impersonnelles côté V, impératif direct côté réalisation) mais **jamais éprouvée sur une fiche réelle**. À vérifier au lot 1, qui contient précisément les cinq trames du V : si la compensation ne se sent pas à la lecture, l'écrire comme perte assumée plutôt que de la maintenir en prescription morte.
- [ ] **Les 236 `<desc>` de SVG sont une ressource dormante pour les non-francophones.** Chaque schéma porte une description d'accessibilité de 60 à 150 mots, exhaustive — celle de `gpio/modes.svg` décrit les trois montages broche par broche. Les images restant en français, **l'étudiant Erasmus n'a que l'alt** pour lire un diagramme, et C74 borne l'alt à une phrase. **Piste** : traduire le `<desc>` sous l'embed en callout repliable, sur les seuls schémas structurants. La matière existe déjà, il n'y a rien à produire. À éprouver sur deux ou trois fiches du lot 1 avant d'en faire une règle.
- [x] **Ordre des lots — réserve levée le 22/08 (suite).** Tim revient à la proposition de base : **entrée → 53 fiches courtes → modules**. Le glossaire se rode donc sur 16 000 mots au lieu des 65 000 de `conduite/proj`, et le risque de dérive rattrapée sur du volume déjà écrit tombe. Reste à figer le glossaire §5.3 avant la fiche 1, ce qui vaut dans tous les cas.
- [x] **`FolderPage()` — vérifié au clic-test du 22/08 (suite) : `/ressources/` a bien disparu.** Le `draft: true` sur le seul `index.md` suffit, l'émetteur de pages de dossier ne réémet rien. **Précédent à retenir** : pour dépublier un dossier qui contient des assets, `draft: true` sur son index, jamais `ignorePatterns`.
- [ ] **Le chrome du site restera français pour les anglophones.** `locale` est un réglage global de Quartz : « Rechercher », « Rétroliens », « Graphe », « Table des matières ». Une dizaine de mots. Le corriger imposerait **deux builds**, donc la complexité que l'architecture à un seul build a précisément permis d'éviter. **Accepté par défaut, jamais explicitement arbitré** — à confirmer quand un étudiant Erasmus aura utilisé le site pour de vrai.
- [ ] **La recherche et le graphe mélangeront les deux langues.** Conséquence directe du build unique : un francophone qui tape « gpio » verra deux résultats, dont un illisible pour lui. Aucun remède propre dans Quartz 4.5 sans passer à deux builds. **Bruit accepté**, à mesurer au premier usage réel plutôt qu'à anticiper.

### 22/08 (suite) — clôture pré-publication

- [x] **`FolderPage()` — point clos, voir plus haut.** Vérifié au clic-test du 22/08 (suite).
- [ ] **Les 18 alias inertes créent la seconde porte que le refus du 21/08 voulait éviter.** `[[watchdog]]`, `[[CdCF]]`, `[[justesse]]` sont résolvables alors que personne ne les écrit. C'est exactement le mécanisme qui a produit 69 faux positifs d'audit le 20/08. **Tim a tranché (c)** — seul l'auto-alias `filtrage` retiré, les 18 restés en place faute de défaut mesuré. À rouvrir **seulement** si un audit remonte de nouveau du bruit imputable à un alias.
- [ ] **La dette de captures porte 29 items, pas 3.** Corrigé le 22/08 (suite). Répartition : `kicad` 3, MicroPython 11, STM32 9, Teensy 4, Raspberry Pi 2. Toutes reportées post-rentrée par arbitrage, toutes visibles en production sous forme de mentions C29 assumées (C90). **Ne plus écrire « 3 ».**

### 22/08 — rédaction `easyeda`
- [ ] **Le style C109 n'existe que sur une fiche, et il n'y a aucun outil pour mesurer l'écart.** « Une phrase, une idée » est éprouvée sur `easyeda` ; les 241 autres fiches gardent les tirets d'incise. **Avant d'arbitrer un redéploiement, il faut le volume** : combien de fiches, combien d'occurrences, et surtout **quelle proportion de tirets licites** (glose de `Voir aussi`, séparateur de liste, plage numérique comme « 544-2400 µs »). Un simple `grep` sur « — » rendra du bruit à hauteur de ce qu'il ignore, quatrième occurrence annoncée du motif. **Compter d'abord, trancher ensuite.**
- [ ] **Les quatre photos de provenance web de `easyeda` sont un mode dégradé daté, pas un acquis.** Bon/mauvais footprint, planche de condensateurs, cartes à straps, types de connecteurs. L'une porte un **filigrane « dreamstime »** visible au pixel mais pas à l'œil. Remplaçant retenu : **fabriquer volontairement des cartes portant les défauts à montrer** (empreinte trop large, trop étroite, montage vertical contraint) plutôt qu'espérer retrouver le bon raté dans un lot d'anciens projets. Consigne pour la séance de rentrée : **photographier les cartes ratées avant de les rendre**.
- [ ] **`differents-types-connecteurs.jpg` porte une mention de coût gravée dans les pixels.** « C'est la solution la moins couteuse », légende écrite par Tim dans le montage Word, donc **prose et non interface tierce** : C71 s'y applique pleinement et la rédaction ne peut pas la corriger. **Arbitrage (c) accepté le 22/08**, tracé ici. Reprise possible en même temps que les photos ci-dessus, avec une légende technique (« ça ne demande aucune pièce, mais ça ne se démonte pas »).
- [ ] **`recherche-connecteurs.png` est la seule capture de `easyeda` restée en 6.5.23.** Elle affiche `User Contributed`, `System(210)`, `Types: Symbol` en anglais, quand la fiche explique deux sections plus haut que la classe s'appelle *Contributions des utilisateurs*. **Non gêné par Tim** (arbitrage 22/08), donc pas un défaut — mais c'est le genre d'écart qu'une relecture future reposera. Reprise à coût nul si le logiciel est ouvert pour autre chose.
- [ ] **Le `.json` EasyEDA n'a aucune fiche, alors qu'il est le livrable de fabrication de l'école.** `pcb` parle désormais de « fichier de fabrication, dont le format dépend de qui fabrique », et `easyeda` explique le geste d'export. Personne ne dit **ce que le format contient** ni pourquoi l'atelier le préfère au Gerber. Ce n'est probablement pas une fiche à part entière, plutôt un encart dans `pcb` — à juger à la clôture, quand la question « quel format pour quel fabricant » sera relue d'un bloc.

### 21/08 — mesure de recherche, extraits L298, C71 résiduel
- [ ] **La borne de C71 sur les logiciels libres n'est pas tranchée.** L'expurgation du 21/08 a retiré « ~10 € », « pas à acheter pour le projet » et « peu coûteux », cas francs. Restent des mentions de **gratuité logicielle** — « gratuit » sur PulseView dans `analyseur-logique` (retiré au passage, « suite libre » le couvrait) et « (gratuite, iOS/Android) » sur nRF Connect dans `esp32-ble` (**laissé**). Question de fond : C71 vise le **coût du matériel que l'école fournit** ; un logiciel qu'on télécharge soi-même n'est pas dans ce périmètre, et « gratuit » y porte une information pratique réelle (aucune licence à demander). **Ne pas trancher à la volée** — un balayage « gratuit / libre / licence » sur les 242 fiches donnerait le volume avant de décider. Deux occurrences connues, ce qui est probablement le plancher.
- [ ] **Le critère de mesure de la recherche est écrit (C105), l'instrument ne l'est pas.** La mesure du 21/08 s'est prise à la main sur quatre mots. Si le canal recherche entre au clic-test comme le suggère l'item du 20/08 ci-dessous, il lui faut une **liste de mots stabilisée** — les quatre symptômes du parcours étudiant en sont le noyau naturel, plus les mots de vocabulaire enseignant (« milestone », « PoC »). À cadrer avec la clôture pré-publication, pas avant.
- [ ] **#38 — la spec demande l'inobservable, et la reprise vaut mieux qu'un recadrage.** Le traceur série trace en index d'échantillon : « échantillons régulièrement espacés **dans le temps** » ne se voit pas. Ce qui se voit est l'**intervalle mesuré** tracé lui-même (`micros()` entre deux ticks), plat sous `TimerOne`, baveux sous `millis()` quand la boucle travaille — c'est **l'argument d'ouverture d'`arduino-timers` rendu visible**, et ça suppose d'ajouter le sketch à la fiche. La prise étant reportée post-rentrée, l'arbitrage se prendra au moment du shoot.
- [ ] **`arduino-pid` n'a aucun exemple exécutable, et #37 va lui en donner un.** La fiche déclare `double lireVitesse();` « fournie par le capteur » et ne l'implémente jamais : aucun de ses trois blocs ne compile en l'état. Le sketch à procédé simulé arbitré le 21/08 comble ce trou **indépendamment de l'image**. À écrire même si la capture glissait.

### 20/08 (suite 2) — parcours étudiant
- [ ] **Le canal *recherche* n'a jamais été un objet de relecture, et c'est un angle mort de méthode.** Le clic-test tient depuis juin sur le rendu visuel (clair / sombre / mobile) ; la recherche `Ctrl+K`, elle, n'a jamais été testée sur autre chose que des noms de fiches. Or c'est **le seul canal qui court-circuite tout le maillage** : un étudiant qui tape son mot n'a besoin d'aucune porte. **Piste** : ajouter au clic-test une ligne « tape le mot d'un étudiant, note **quel résultat arrive en tête** ». Le piège à éviter est celui du 20/08 suite 2 — la recherche étant plein texte, **demander « est-ce que ça rend quelque chose » ne mesure rien**.
- [ ] **`cpp` s'annonce comme un cours quand un étudiant bloqué cherche un dépannage.** Le hub écrit « à suivre dans l'ordre : chaque étape suppose la précédente », et l'item 7 (*Lire et comprendre les erreurs*) porte `prerequis: cpp-execution`. Le titre de l'item est assez explicite pour qu'on passe outre — constaté à la traversée #4, **friction et non blocage**. À rouvrir seulement si un étudiant rapporte s'être senti obligé de commencer par l'item 1.
- [ ] **`ressources/index` — page publiée que personne ne peut atteindre.** Trois lignes, page d'atterrissage du dossier de médias, **aucun lien entrant ni sortant**. Ce n'est pas un défaut de maillage (elle n'a rien à dire), et elle est désormais **hors des compteurs de santé** de `parcours-etudiant.mjs`. Reste la question de fond : faut-il la **dépublier** (`draft: true`), comme `templates/` l'a été le 18/08 ? Elle apparaît dans le graphe et la recherche des étudiants pour rien. Une ligne, à trancher à la clôture pré-publication.
- [ ] **Les alias sont une seconde porte, et personne ne les inventorie.** `cahier-des-charges-fonctionnel` porte `aliases: [CdCF]`, `fonction` porte FP/FS/FC, `caracteriser-une-exigence` porte critere/niveau/flexibilite — **25 alias déclarés** au total dans le dépôt (comptés par le script). Ils créent des entrées de recherche invisibles depuis les liens, et personne ne sait **lesquels manquent**. Candidats évidents repérés à la traversée : `PoC` sur `preuve-de-concept`, `BOM` sur `bom`, `reproductibilité` / `intermittent` sur `fiabiliser-et-deboguer`. **Inventaire d'abord, ajouts ensuite** — `parcours-etudiant.mjs --json` les rend déjà.
- [ ] **« Milestone » n'existe pas dans le wiki, alors que les profs l'emploient.** Le scénario 7 partait de « on est au **milestone** PoC » ; le wiki dit **jalon** partout (`jalons.md`). Le chemin cliqué aboutit quand même, mais l'étudiant qui tape le mot du prof ne trouve rien. **Même famille que l'inventaire d'alias ci-dessus** : ni un anglicisme à admettre (C1 le proscrirait) ni un mot à ignorer — un **alias** règle le cas sans toucher à la prose. À vérifier d'abord : les enseignants disent-ils vraiment « milestone », ou est-ce le scénario qui l'a introduit ?

### 19/08 (suite 4) — lot « porte borgne »
- [ ] **`schema-bloc-fonctionnel` — deux entrées de *Voir aussi* non liées alors que les cibles existent.** « Boucle ouverte / boucle fermée » et « Capteurs et actionneurs » sont en texte nu, quand `[[boucle-ouverte]]` est **déjà liée** plus haut dans la même fiche. Repéré en posant le retarget PID. Deux lignes à câbler, ou à retirer si l'absence de lien est délibérée — mais alors la mention plus haut la contredit.
- [ ] **`gpio` — la section *L'état des broches à l'allumage* ne renvoie qu'à Arduino.** La porte a été fermée **par la *Voir aussi*** (les deux `-gpio-boot` y figurent désormais), ce qui suffit à l'audit mais pas au lecteur : celui qui lit la section sur le boot voit toujours le seul `[[arduino-gpio-boot|le tuto GPIO au démarrage]]` en prose. Choix assumé pour ne pas alourdir, à rouvrir si le parcours étudiant bute dessus.
- [ ] **Le § *Cas particulier — La limite : vers un RTOS* d'`arduino-programmation-non-bloquante` ne dit rien de propre.** Contrairement à son jumeau MicroPython, qui porte le **second cœur RP2040** (`_thread`), la version Arduino répète la transverse en plus court **et** renvoie à FreeRTOS **sur ESP32** depuis une fiche Arduino — le lecteur sort de sa famille pour rien. Proposé en réduction à une phrase le 19/08 suite 4, **non tranché** : l'arbitrage (b) portait sur le seul § *À quoi ça sert ?*.
- [ ] **Trois SVG pour une seule idée, et ils ne sont pas fusionnables.** `arduino-programmation-non-bloquante/bloquant-vs-non-bloquant`, son jumeau MicroPython et `programmation-non-bloquante/bloquant-vs-cooperatif` disent la même chose. **C92 interdit le réemploi** : les deux premiers nomment `delay()` et `sleep()` à l'écran, l'écran n'est donc pas muet sur la famille. Coût assumé, pas un défaut — tracé pour qu'une relecture future ne rouvre pas la question.

### 19/08 (suite 3) — fiches de lecture C85
- [ ] **Les encarts « Comment lire ce code » existent sous deux formes, sans règle écrite.** L'inventaire des 244 fiches en rend **33 sur 30 fiches**, dont **24 en callout** (`> [!info] Comment lire ce code`) et **9 en gras de paragraphe** (`**Comment lire ce code.**`). Les deux formes cohabitent à l'intérieur d'un même module, parfois à deux fiches d'écart (`arduino-capteur-analogique` en gras, `arduino-eeprom` en callout). C77 ne tranche pas, et rien n'indique que l'écart soit intentionnel. **Ni bloquant ni visible par l'étudiant** — mais c'est le genre d'écart qu'une relecture future reposera. Deux sorties : normaliser sur le callout (le plus fréquent, et le seul qui se replie), ou **écrire le critère** qui justifie l'une ou l'autre (le gras convient peut-être mieux quand l'encart introduit une liste à puces, ce qui est le cas de 3 des 9). À traiter en grooming, pas en session.
- [ ] **Va-et-vient par inversion de signe — mitoyen écarté, trace.** Les encarts servomoteur des deux familles expliquent le patron `sens = ±1` puis inversion en butée. C'est identique dans n'importe quel langage, donc « langage » au sens strict de C85 — mais ce n'est pas un *mécanisme de langage*, c'est un patron algorithmique, et aucun bloc du wiki ne les porte. **Arbitrage Tim 19/08 : hors périmètre**, les encarts restent locaux. À rouvrir seulement si un jour une fiche d'algorithmique élémentaire se justifie — le risque étant de transformer les fiches de lecture en cours d'algo.
- [ ] **`cpp.md` — la *Voir aussi* ne liste aucun item de son propre parcours.** Constaté au maillage du 19/08 : la section ne pointe que hors parcours (`arduino`, `micropython-langage`, `fonction-informatique`, transverses), si bien qu'ajouter une sous-fiche y crée une redite avec la liste numérotée. `micropython-langage` a la même structure. **C'est probablement délibéré et sain** — le parcours *est* la liste, la *Voir aussi* dit l'ailleurs. À confirmer comme convention de hub de parcours plutôt qu'à corriger.

### 19/08 (suite 2) — notions `[T]` non bloquante et boucle ouverte, audit des portes de famille
- [ ] **LOT « porte borgne » — 22 liens à compléter sur 14 fiches.** Le défaut B de l'audit du 19/08 : la notion `[T]` existe et ne cite qu'une famille alors que le jumeau existe. **Mécanique, bon marché, une session courte** — on ajoute un lien, on n'écrit pas de fiche. Répartition : `potentiometre` (4), `asservissement` (3), `filtrage` (2), `lire-une-datasheet` (2), puis `gpio` et `machine-a-etats` (`gpio-boot`), `shield` (`module`), `chronogramme`, `alimentation-electronique`, `decouplage`, `ascii`, `wokwi`. **Trois sont hors dossier embarqué** : `fast`, `schema-bloc-fonctionnel` et `specification-technique` pointent `arduino-pid` sans `micropython-pid`. Liste régénérable à tout moment par `node tools/audit-portes-famille.mjs`.
- [ ] **Les deux créneaux vides restants : `chien-de-garde` et `temporisation`.** Même motif que `programmation-non-bloquante`, traité le 19/08 : jumeaux Arduino **et** MicroPython présents, aucune notion `[T]` au-dessus. **8 liens s'y rabattent** — `watchdog` depuis `firmware`, `fiabiliser-et-deboguer`, `interruption` et `timer` ; `temporisation`/`timers` depuis `timer`, `debugger-embarque`, `interruption` et `manipulation-de-bits`. Gabarit `interruption` / `timer`, **aucun bloc de code** (la notion sert deux familles). ⚠ **`fiabiliser-et-deboguer` porte un lien `arduino-watchdog` à retargeter** dès que `chien-de-garde` existe — posé sciemment le 19/08 sur arbitrage Tim.
- [ ] **Alléger les deux jumeaux non bloquants, désormais en redite avec la notion transverse.** `arduino-` et `micropython-programmation-non-bloquante` portent des sections *À quoi ça sert* et *la limite vers le RTOS* quasi mot pour mot identiques entre elles — et maintenant redites par la notion `[T]`. La sortie n'est pas de les amputer mais de les **recentrer sur leur famille** (le patron `millis()` d'un côté, `ticks_diff` et `asyncio` de l'autre) et de renvoyer le concept à la transverse. À faire **avec** le lot porte borgne : même geste, mêmes fiches ouvertes.
- [ ] **Asymétrie `cpp` / `micropython-langage` — laissée telle quelle (arbitrage Tim 19/08).** `programmer-l-embarque` §2 oppose `[[cpp]]`, fiche-langage autonome dans son propre dossier, à `[[micropython-langage]]`, fiche interne au module MicroPython. Ce n'est pas le défaut des portes de famille — MicroPython *est* à la fois le langage et la famille — mais la structure du wiki dit deux choses différentes de deux langages. À rouvrir seulement si la question se repose d'elle-même.
- [ ] **`asservissement` — réciproque `filtrage` toujours non posée.** Item du 18/08 suite 3, **non traité le 19/08 alors que la fiche était ouverte** : la réciproque `boucle-ouverte` a été posée, celle de `filtrage` non, pour ne pas mélanger deux lots. À faire avec le lot porte borgne, qui touche la même fiche.

### 18/08 (suite 3) — fiche `filtrage`, expurgation C71 du hub MCU
- [ ] **`microcontroleur` — « veille ~1 mA (carte) » sur la ligne Pico, jamais vérifiée en source.** Repérée pendant l'expurgation C71, laissée en place faute de mandat : elle est hors périmètre de la convention, mais c'est exactement le genre de valeur que C82 proscrit d'asséner de mémoire. Le chiffre plausible pour un Pico au repos dépend fortement du mode (`dormant` vs boucle active) et de la carte porteuse. À vérifier en doc Raspberry Pi Ltd, ou à passer en qualitatif comme l'a été le profil de conso de `micropython-deep-sleep`.
- [ ] **`asservissement` — réciproque `filtrage` non posée.** Les deux réciproques évidentes ont été posées (`adc`, `precision-de-mesure`), mais `asservissement` reste sans lien retour alors que `filtrage` lui consacre un paragraphe entier (le déphasage d'un filtre déstabilise une régulation). Non traité faute d'avoir lu la fiche cible cette session — à faire avec le balayage des portes famille, dont c'est le même geste.
- [ ] **`filtrage` — un cas d'usage manque : le fil rouge.** La fiche est illustrée sur des capteurs génériques (LDR, ultrason) mais ne dit rien du **bras 3 axes**, alors que le filtrage d'un retour de position y est un vrai sujet — et que la convention de fil rouge le prévoit pour les fiches-trame. Calibre notion `[T]`, donc pas obligatoire ; à trancher si le fil rouge doit descendre jusqu'aux notions transverses.
- [ ] **Faut-il une fiche `filtrage` côté famille, un jour ?** La fiche transverse ne porte volontairement aucun code (voir §8, note du 18/08 suite 3). Si les étudiants réclament du code prêt à coller, la sortie n'est pas d'en ajouter dans la transverse — ce serait la basculer côté Arduino — mais de créer `arduino-filtrage` et `micropython-filtrage` sur le modèle C47. À n'ouvrir que sur demande réelle : deux fiches de plus pour une moyenne glissante, c'est cher.

### 18/08 (suite 2) — triage de l'inventaire, promotion de C29, clôture de la relecture
- [ ] **Motif de balayage à élargir, et un outil à en tirer.** Le balayage du 18/08 a **raté** le placeholder de `shield.md` parce qu'il commence par « Prendre **photo** de » et non « Prendre capture » — il attrapait pourtant « Prendre capture d'écran **ou photo** de » et les paragraphes tout en italique. **101 est donc un plancher.** Motif cible : `Prendre (capture|photo|vidéo)` + `Intégrer une vidéo` (C75). À verser dans `tools/` sur le modèle des deux audits, avec la garde qui manquait : **rapporter le nombre de fiches balayées**, pas seulement le nombre de résultats. 5ᵉ occurrence du motif « l'opération réussit, le périmètre est tronqué » en deux jours — et la première trouvée par une question posée sur autre chose, pas par un audit.
- [ ] **Quatre SVG d'instruments à produire (lot D du triage)** — indépendants de toute prise de vue, donc mobilisables dès qu'une session courte se présente : `multimetre/face-avant` (écran, sélecteur, trois bornes COM / V-Ω / 10 A), `multimetre/mesure-5v` (pointes sur GND et 5 V d'un Arduino), `oscilloscope/face-avant` (écran quadrillé + blocs vertical / horizontal / trigger **en position relative typique**), `oscilloscope/ecran-pwm` (trace carrée, période cotée, valeurs de l'étape 5 de la fiche : ≈ 490 Hz, `analogWrite(128)` → 50 %). Plus le complément de `micropython-alimentation/alimentation-separee` (branche USB à côté de VSYS) et `arduino-alimentation/trois-sources`. **Précaution de dessin** : blocs fonctionnels, pas un instrument idéalisé à quatre boutons — sinon l'étudiant ne reconnaît pas le vrai.
- [ ] **`teensy-usb:74` — GIF à rouvrir sur demande étudiante.** Le placeholder « un éditeur de texte recevant *Teensy!* à chaque appui » a été **supprimé** au triage (rang 3, priorité basse). C'est pourtant le seul endroit du wiki où une carte agit **sur l'ordinateur** au lieu de l'inverse, et aucun texte ne rend ça. Arbitrage Tim : à rouvrir si un groupe travaille l'USB HID cette année. Forme cible : **GIF court** (C87, le mouvement est le message).
- [ ] **Le dry-run se lit aussi pour le rendu de ce qu'on insère.** Deux dry-runs de la session ont révélé non pas un anchor manquant mais une **imbrication d'italiques dans mon propre `newText`** (ligne de trace au §8, phrase ajoutée à un blockquote déjà italique au §16). La formulation actuelle de la discipline C14 parle de vérifier la **cible** ; elle gagnerait à dire aussi de **relire l'inséré**. À fondre dans C14 au prochain grooming, pas une convention neuve.
- [ ] **Anchor court, toujours.** Un anchor de ~1400 caractères a échoué en fin de session sur `TODO.md` ; refait en trois anchors courts, tout est passé du premier coup. Le coût d'un anchor long n'est pas le risque de corruption du fichier mais le **risque de transcription** — une seule divergence invisible annule le batch entier (atomicité). À rappeler dans la chaîne C14, dont c'est le corollaire pratique le plus utile.

### 18/08 — refonte `ltspice`, clôture du §8, inventaire des captures
- [ ] **Triage de l'inventaire des captures — 101 placeholders, 57 fiches** (`_drafts/inventaire-captures.txt`, produit le 18/08). **Chantier prioritaire de la prochaine session.** Trois gisements, chacun avec sa sortie propre : **(1) les six prise-en-main = 31 placeholders pour ~6 gestes** répétés d'une famille à l'autre — une seule session de prise de vue les couvre, et `esp32-prise-en-main:38` / `teensy-prise-en-main:38` demandent **la même image** (page `arduino.cc/en/software`), donc une prise pour deux emplois (C76) ; **(2) ~18 captures de sortie texte** (moniteur série, Shell Thonny, console) → **blocs de code**, plus légers, cherchables, lisibles sur mobile — exception : les traceurs/Plotters, qui produisent des **courbes** ; **(3) faces avant d'instruments** (`multimetre:19`, `oscilloscope:19`) → candidates **SVG annoté**, indépendantes du modèle posé sur la paillasse. **Précédent chiffré** : le lot `ltspice` est passé de 10 à 3 sans aucune perte pédagogique. Ne rien shooter avant le triage.
- [ ] **`lire-une-datasheet` — 4 extraits de datasheet ST à shooter : question de droit à poser.** Les quatre placeholders demandent des **reproductions de figures** de la datasheet L298N (boîtiers, brochage, table de vérité, *Absolute maximum ratings*). La reprise de doc constructeur à fin pédagogique est généralement tolérée, mais le wiki est **publié en ligne** et aucune mention d'attribution n'est prévue — alors que le dépôt en pose déjà pour les brochages Raspberry Pi (CC BY-ND) et PJRC. **À trancher avant de shooter** : attribution en légende, recadrage minimal sur l'élément commenté, ou redéssin maison des figures les plus simples.
- [ ] **Chapeau de délégation du calendrier dans `conduite/index`** — reliquat optionnel de l'arbitrage du 18/08 : la chronopédagogie est écartée parce que le calendrier appartient au cours de gestion de projet des collègues, mais **rien ne le dit à l'étudiant**. Une phrase dans le hub — « le découpage du semestre et les jalons vous sont donnés en cours de gestion de projet ; ce wiki dit *comment* faire, pas *quand* » — transforme un silence en délégation lisible. Une ligne, pas une page.
- [ ] **Poids cumulé des dossiers médias des fiches-outils scénarisées** — `falstad/` 6,6 Mo + `ltspice/` 1,2 Mo, soit près de 8 Mo pour deux fiches. Le pronostic du 17/08 (« ça se repaiera sur `ltspice` ») s'est vérifié **à la baisse** — un seul GIF au lieu de quatre. Rien d'alarmant, mais **C88 est coûteuse en médias par construction** : à intégrer au calcul si d'autres fiches-outils passent en refonte (`kicad`, `tinkercad`, `wokwi` déjà dense).
- [ ] **`low-pass-analyse.gif` — candidat MP4, 2ᵉ réserve C87 consécutive.** 1 min 20, 502 ko, montage complet + relevé au curseur. Deux objections posées (fond : une construction filmée se regarde au lieu de se faire ; format : ni pause ni retour arrière sur 80 s, et un MP4 serait **plus léger et plus utilisable**), **arbitrage Tim : on garde**. Mitigé par le placement — après le relevé, en vérification et non en modèle. **Le motif est désormais récurrent** (`construire-passe-haut.gif` le 17/08) : si un troisième cas se présente, rouvrir C87 plutôt que de tracer une réserve de plus.
- [ ] **SVG « la directive commande l'analyse » (`ltspice`) — écarté au profit des captures, pas invalidé.** Un même schéma se ramifiant en trois tracés selon la directive posée (`.op` → valeurs aux nœuds, `.tran` → chronogramme, `.ac` → Bode). Justification C84 intacte : **aucune capture ne peut le montrer**, un écran n'affichant qu'une analyse à la fois. Reporté par arbitrage Tim (« priorité aux captures ») ; à reprendre si le §8 repasse en polish.
- [ ] **Libellé du champ d'amplitude petit signal (LTspice) — jamais vu à l'écran.** La fiche emploie une périphrase (« l'amplitude pour l'analyse petit signal ») faute d'avoir pu lire le dialogue *Advanced* de la source. Exact et non inventé, donc publiable en l'état — mais à remplacer par le libellé réel si Tim rouvre l'outil. Même statut que les deux « captures manquantes non bloquantes » de `falstad`.

### 17/08 (suite 3) — refonte pédagogique `falstad`
- [ ] **Bump des actions GitHub en `@v5`** (`.github/workflows/deploy.yml`) — `actions/checkout@v4`, `setup-node@v4`, `upload-artifact`, `deploy-pages@v4` ciblent **Node 20, déprécié** ; les runners les forcent sur Node 24, ce qui **marche mais est une béquille**. Le jour où GitHub la retire, le workflow casse d'un coup — probablement un jour de publication pressée. Modification de ~4 lignes, à faire **une journée calme** (surtout pas pendant une panne de la plateforme : on ne distinguerait pas ses propres erreurs de celles de GitHub). Repéré le 17/08 sur l'échec de déploiement — lequel était un **503 côté GitHub** (incident majeur du jour), pas un problème du dépôt.
- [x] ~~**L'assistant conversationnel comme outil de travail étudiant — décision à prendre au-delà de `falstad`.**~~ ✅ *Tranché le 18/08 — voie (a), c'est une porte qu'on ouvre.* Et la borne a **changé de critère** au passage : `falstad` cadrait par le **sens** de l'usage (l'IA propose, la simulation tranche), ce qui interdisait implicitement l'usage inverse — or `ltspice` demandait précisément de faire **lire** une netlist par un assistant. Nouveau critère, **C89** : *l'affirmation produite est-elle **vérifiable à l'écran** ?* — il vaut dans les deux sens et se transpose hors simulation. `falstad` **back-patchée** d'un paragraphe. **Reste à faire** : réemployer C89 dans les fiches de lecture de programme (C85), où « faire expliquer un programme puis vérifier soi-même » est le même geste.
- [x] ~~**`construire-passe-haut.gif` — candidat au retour en MP4** (C87)~~ ✅ *Réserve levée au clic-test du 17/08 : Tim a validé les 13 embeds, GIF compris.* Le point reste **documenté comme précédent** : c'était la seule séquence longue du lot, et un GIF n'offre ni pause ni retour arrière pour relire un intitulé de sous-menu. Si le cas se représente sur `ltspice` — séquence longue **plus dense en libellés** (boîtes de dialogue de directives, fenêtre de tracé) — arbitrer d'emblée pour le MP4 (`<video controls>`, C75) plutôt qu'après coup.
- [ ] **Captures manquantes de `falstad`, non bloquantes** — deux endroits où la fiche reste volontairement évasive faute d'avoir vu l'écran : (a) le **menu Dessiner déroulé** (les intitulés de sous-menus sont cités d'après la transcription de Tim, jamais lus à l'image) ; (b) la **boîte d'édition de la source alternative** (la fiche dit « faire varier la fréquence » sans montrer où). Ni l'une ni l'autre ne bloque la lecture ; à shooter si l'occasion se présente.
- [ ] **Poids du dossier `falstad/`** — 6,6 Mo, dont 6,4 en quatre GIF : **dossier média le plus lourd du dépôt** (précédent : `arduino-prise-en-main`, 5,6 Mo). Rien d'alarmant pour GitHub Pages, mais c'est le prix de la scénarisation et il se **repaiera sur `ltspice`**. À surveiller quand le dépôt comptera trois ou quatre fiches-outils scénarisées ; piste si besoin, recadrage plus serré et `fps` plus bas.

### 17/08 (suite 2) — fin de la relecture de fond (§12, §15, §16)
- [x] ~~**Balayage systématique des portes famille sur les notions transverses**~~ ✅ *Fait le 19/08 (suite 2), et outillé : `tools/audit-portes-famille.mjs`.* L'intuition était juste et la méthode proposée insuffisante — la liste de onze notions à auditer à la main aurait raté la moitié du gisement. **132 fiches non-famille balayées sur 132**, **30 asymétries réelles** (45 brutes, 15 faux positifs structurels), et surtout **deux défauts distincts** là où l'item n'en voyait qu'un : le **créneau vide** (pas de notion `[T]`, on écrit une fiche) et la **porte borgne** (la notion existe et ne cite qu'une famille, on ajoute un lien). Le motif déborde le dossier embarqué — trois fiches PROJ pointent `arduino-pid`. **Suites en tête de ce fichier** (lot des 22, les deux créneaux restants). **Règle écrite en C94**, ce qui était le vrai manque : la pratique était appliquée d'office depuis le 18/08 sans être nulle part énoncée.
- [ ] **Fiche `teensy-gpio` ? Non — mais vérifier que C56 tient au rendu.** Le module Teensy est délibérément *lean-Bases* (C56) : pas de `teensy-gpio` ni de `teensy-serie`, le hub délègue aux [T] et à `teensy-arduino-core`. Constaté cohérent à la lecture 17/08 suite 2 ; à reconfirmer quand Tim déroulera le module sur matériel réel — si la porte `-arduino-core` ne suffit pas à couvrir GPIO/UART en pratique, C56 sera à rediscuter pour cette famille.
- [ ] **Peigne SVG — 6 SVG neufs du §12** (`teensy/comparaison-arduino`, `teensy-arduino-core/paliers-d-acces`, `teensy-prise-en-main/montage-led-externe`, `teensy-audio/empilage-audio-shield`, `teensy-usb/montage-bouton`, `teensy-usb/montage-potentiometre`) : à verser à la passe de polish pré-publication après le clic-test. `empilage-audio-shield` est le plus exposé — vue en écorché de deux cartes empilées, densité à surveiller sur mobile.

### 17/08 — relecture de fond §9 ESP32
- [x] ~~**Fiches-tuto « lire un programme » (C/C++ et Python) — orientation Tim 17/08**~~ ✅ *Conception arrêtée 17/08 — voir C85 et l'item de production ci-dessus.* Les cinq points ouverts sont tranchés : (a) périmètre = **méthode de lecture + mécanismes de langage répétés**, pas la syntaxe (déjà aux parcours `cpp` / `micropython-langage`) ; (b) les **idiomes métier restent en encart local** — C77 amendée, pas remplacée ; (c) **pas de rétroactivité** — les encarts de §7 et §13 sont des encarts d'idiome ; (d) emplacement = **fin des deux parcours langage**, pas de fiche transverse ; (e) le volet « commentaires sur chaque ligne utile » de C77 est **conservé intégralement**.
- [ ] **Produire `cpp-lire-un-programme` et `micropython-lire-un-programme`** — conception arrêtée 17/08 (Tim délègue le choix à Claude), consignée en **C85** + notes dans `conventions.md` §8. Deux fiches-tuto en **fin de parcours langage**, `aa: []`, dupliquant assumément leur section de méthode (copie locale C47). Contenu visé : (1) *par où entrer* dans un programme inconnu — repérer l'état, suivre le flux, séparer mécanique et intention ; (2) les **mécanismes de langage** que les encarts C77 répètent aujourd'hui de module en module ; (3) une lecture commentée d'un programme complet du wiki, pris tel quel. **Reste à faire au moment de produire** : poser les renvois depuis les fiches famille (le premier concerné est `arduino-programmation-non-bloquante`, seul encart de mécanique pure inventorié), et les liens réciproques avec `arduino-debug` / `micropython-debug` (frontière lire/déboguer) et `revue-de-code` (la lecture en est le prérequis). **Pas de passe rétroactive** sur §7 et §13 : leurs encarts sont des encarts d'idiome, légitimes sous C85.
- [ ] **Fiche `esp32-programmation-non-bloquante` (candidate, C47)** — repérée 17/08 en relecture d'`esp32-prise-en-main` : le corrigé de l'Exercice 1 renvoie à `[[arduino-programmation-non-bloquante]]`, une fiche d'une **autre famille**, faute d'équivalent ESP32 et de notion transverse [T] sur le motif `millis()`. Le renvoi a été **glosé** (« traité côté Arduino, se transpose tel quel ») plutôt que retiré. Deux voies à trancher plus tard : créer le jumeau ESP32 (parcours autonome C47), ou extraire une **notion transverse** du motif non bloquant, que les deux familles référenceraient — à arbitrer quand le module ESP32 sera relu en entier. À recouper avec `[[esp32-freertos]]` (concurrence par tâches, réponse voisine mais distincte).

### 30/06 (suite) — relecture de fond §14 Raspberry Pi
- [ ] **Bloc « Caméra → SBC » sur `architecture-bicephale.svg`** (enrichissement optionnel, faible priorité, proposé 30/06) : le fil rouge parle d'« un bras augmenté d'une caméra » et `raspberry-pi-projet` mentionne la vision, mais le schéma n'a pas de bloc caméra en **entrée** du SBC (la « Vision · détection de cible » est listée dans la boîte SBC). Ajouter un petit bloc *Caméra* fléché vers le Raspberry Pi compléterait le récit visuel. Non bloquant — Tim a clos la session sans le prendre.

### 30/06 — relecture de fond §13 MicroPython terminée
- [ ] **Audit des liens entrants `[[micropython-eeprom]]`** — la fiche a été renommée `micropython-stockage` ; un lien cassé corrigé dans `micropython-memoire` (30/06), mais d'autres *Voir aussi* / renvois entrants peuvent subsister dans le module (et hors module). Grep `[[micropython-eeprom]]` sur `content/` et repointer vers `[[micropython-stockage]]`. À faire en grooming, pas urgent.
- [ ] **Captures C29 du §13 (review 30/06)** — placeholders matériels laissés en place dans les fiches relues, à shooter par Tim : Shell Thonny / débogueur (`micropython-debug`, `-timers` Plotter), multimètre en veille (`micropython-deep-sleep`), REPL `gc.mem_free()` (`micropython-memoire`), enfichage carte porteuse (`micropython-shield`), alimentation étape 3 (`micropython-alimentation`). Complètent les SVG-concepts.
- [ ] **Relire l'ajout « Servos à retour de position » de `micropython-servomoteur`** — gros ajout 30/06 (déc. a : retour ADC `read_u16` + calibration + variante Hall/PWM `time_pulse_us`) : **relecture humaine du contenu** (le rendu est validé). Pendant du même ajout côté `arduino-servomoteur` (déjà signalé 17/06).

### 25/06 — intégration des médias familles MCU (dépôt Martin)
- [x] ~~**Régénérer 6 SVG divergents contre le code de la fiche**~~ ✅ *Fait 27/06.* (audit 25/06, violation C80 — produits d'après la cartographie générique, pas le code) : `micropython-sortie-tor/montage-relais.svg` (→ LED GP8 + buzzer/transistor GP9 + relais GP10), `micropython-capteur-numerique/montage-capteur.svg` (→ HC-SR04 : Trig GP9, Echo via pont diviseur GP10), `micropython-spi/branchement-spi.svg` (→ SCK GP2 / MOSI GP3 / MISO GP4 / CS GP5), `micropython-moteur-cc/branchement-pont-h.svg` (→ DRV8833 AIN1 GP12 / AIN2 GP11), `micropython-moteur-pas-a-pas/branchement-stepper.svg` (→ ULN2003 GP2-GP5), `raspberry-pi-gpio/montage-led-bouton.svg` (→ bouton GPIO2, pas GP27). Leur placeholder « photo » est resté dans la fiche. Claude régénère direct (écriture MCP).
- [x] ~~**Replacer 2 SVG mal ciblés**~~ ✅ *Fait 27/06.* : `esp32-wifi/roles-ble.svg` → fiche `esp32-ble` (schéma BLE, hors sujet Wi-Fi) ; `esp8266/brochage-d1-gpio.svg` → probablement `esp8266-arduino-core` (le hub `esp8266` renvoie les broches à cette fiche). Décider la cible + insérer.
- [x] ~~**Attributions des 2 images officielles**~~ ✅ *Fait 27/06.* : `micropython-gpio/brochage-pico.png` (Raspberry Pi, **CC BY-ND** — légende d'attribution requise ; 2e placeholder de `micropython-gpio` laissé à cet effet) ; `teensy/brochage.png` (**PJRC** — attribution + **fiche cible Teensy à choisir**, module lean).
- [x] ~~**Brochages STM32 — cible(s) + cartographie**~~ ✅ *Fait 27/06.* : `stm32/brochage-blue-pill.svg` et `brochage-nucleo-64.svg` (générique Nucleo-64 « éléments communs ») — quelle(s) fiche(s) les embarque(nt) ? + **MAJ `_drafts/cartographie-medias-familles.md`** (1 brochage STM32 prévu → 2).
- [ ] **Captures matérielles laissées en placeholder** (pour Tim, hardware/logiciel) : Shell Thonny (`micropython-capteur-numerique`), écran d'oscilloscope (`micropython-sortie-pwm`), navigateur + IP au moniteur série (`esp32-wifi`), vue Registers de CubeIDE en debug (`stm32-registres`).
- [ ] **7 embeds placés au jugé (sans placeholder) — confirmer la position au rendu** : `micropython-temporisation` (sleep-vs-ticks), `micropython-programmation-non-bloquante` (frise-taches), `esp32-gpio` (brochage + montage), `esp32-wifi` (sta-vs-ap), `raspberry-pi-gpio` (brochage-40), `stm32-registres` (registre MODER).
- [ ] **Peigne SVG familles MCU** : les ~26 SVG du dépôt Martin (ESP32/ESP8266/STM32/Teensy/RPi/MicroPython/Pico) **n'ont jamais été rendus** — à intégrer à la passe de polish + rendu (clair/sombre/mobile) au même titre que les jets antérieurs.
- [ ] **`micropython-gpio/brochage-pico.png` — image à reprendre avant publication définitive** (signalé par Tim au rendu 27/06 ; embed + attribution Raspberry Pi Ltd / CC BY-ND en place, **acceptable en l'état**). Piste : régénérer un brochage Pico maison en SVG, ou retravailler l'image officielle.

### Suite 3 (17/06) — fin §7 Arduino + notion asservissement
- [ ] **Fiche `esp32` stockage / Preferences** — le bloc de code Preferences **retiré d'`arduino-eeprom`** (C47, section ESP32 allégée en place d'un tuto) doit trouver sa maison dans une future fiche `esp32-*` : NVS/Preferences (`begin` / `putX` / `getX` / `commit`), caveat usure flash. À créer avec le module ESP32.
- [ ] **Suites de la notion `asservissement`** (créée 17/06 suite 3, `content/embarque/asservissement.md`) : (a) **relecture humaine** de la fiche neuve + clic-test de son SVG `boucle-fermee` ; (b) **liens réciproques** vers `[[asservissement]]` depuis `schema-bloc-fonctionnel`, `timer`, `pwm`, `arduino-moteur-cc`, `arduino-capteur-analogique` (la notion pointe vers eux, pas l'inverse) ; (c) **emplacement à confirmer** (`embarque/` racine vs `embarque/mcu/`) — le wikilink résout quel que soit le dossier, simple `move_file` si Tim préfère.
- [ ] **Notion `watchdog` / `chien-de-garde` (candidate, basse priorité)** — **non créée** 17/06 suite 3 : le WDT étant un compteur dédié, `arduino-watchdog` s'appuie sur `[[timer]]` comme parent légitime. À créer seulement pour la symétrie avec les autres notions famille, si souhaité ; sinon laisser raccroché à `timer`.
- [ ] **Note de portabilité familles MCU (candidate convention, corollaire C47)** — systematisée 17/06 suite 3 (`timers`/`eeprom`/`deep-sleep`/`memoire`/`watchdog`) : quand l'API d'un mécanisme est propre à l'AVR et qu'aucune fiche cible n'existe, **caveat bref + renvoi `[[esp32]]`** plutôt que détailler. Pendant « sortant » de C47. À promouvoir en corollaire numéroté si confirmé sur les modules famille à venir.

- [ ] **Bibliothèque de composants (notion par composant)** — *futur lointain.* Beaucoup de composants (LDR, LM35, FSR, MQ-x, résistance, LED, diode…) n'ont pas de fiche-notion propre et restent expliqués **inline** là où on les utilise. Le **potentiomètre** a fait exception (notion créée 17/06) car **générique, réutilisé et porteur du diviseur** (archétype transverse). **Ligne de périmètre** : n'ouvrir une notion-composant que pour un composant générique/réutilisé/transverse ; sinon inline. Ne pas lancer d'encyclopédie de composants avant la couverture du socle + publication. (Tim 17/06 : « à rajouter dans un futur lointain ».)
- [ ] **Passe de contrôle C79 (rétroactif)** — C79 (« un montage/schéma de principe par bloc de code ») émerge le 17/06. Repasser les **fiches déjà relues** pour vérifier que chaque bloc de code a un montage ou un schéma de principe adjacent (ou un renvoi). Candidats probables : tutos MicroPython, fiches algorithme. À faire en grooming, pas en clôture.
- [ ] **Filigrane « robocraze » sur le GIF IR** (`how-ir-sensor-works.gif`, `arduino-capteur-numerique`) — image tierce avec watermark ; décider avant publication (garder / remplacer par un schéma maison / recréer en SVG). Vérifier au passage les autres médias fournis (webp `how-ultrasonic-sensor-works`, `how-potentiometer-works`…) pour d'éventuels watermarks.
- [ ] **Exemple d'une fiche shield/module piloté par la bibliothèque (candidate convention)** — décidé sur `arduino-shield` (17/06) : l'exemple d'une fiche dont l'intérêt est l'abstraction (shield, module à bibliothèque) se pilote **par sa bibliothèque**, pas par bit-banging de broches — le pilotage bas niveau est renvoyé à la fiche driver dédiée (ex. `arduino-moteur-cc`). Recoupe C47 (couche d'abstraction famille) et C79. À confirmer comme convention distincte ou simple cas d'application de C47 après 1-2 autres occurrences (afficheur I2C, capteurs à bibliothèque). **2ᵉ occurrence confirmée 17/06** (`arduino-afficheur` : exemples pilotés par `Adafruit_SSD1306` / `LiquidCrystal_I2C`, zéro bit-banging) — reste ≈ 1 cas (capteur à bibliothèque, ex. BMP280/DHT) avant promotion en convention numérotée.
- [ ] **Notion transverse `[[pont-en-h]]` (candidate)** — repéré 17/06 en relecture de `arduino-moteur-cc`. Le pont en H (inversion de polarité, table de vérité sens/frein/roue libre) y est expliqué inline, et `arduino-moteur-cc` en est bien la fiche-maison côté Arduino (cf. décision `arduino-shield`). Mais c'est un **concept d'électronique transverse** que les autres familles MCU (ESP32, STM32, MicroPython) re-référenceront — candidat à une **notion partagée** sur le modèle de `[[uart]]`/`[[i2c]]`/`[[spi]]` (couche [T], C47) : la notion porterait le principe (4 interrupteurs, shoot-through, freinage), les fiches famille gardant l'implémentation (driver, broches, code). À créer plus tard.

## Fiches-trame à rédiger (phases du cycle en V)

Ces fiches forment l'épine dorsale du parcours étudiant. Voir le hub `content/hub/index.md` pour la trame complète.

- [ ] `hub/specification-technique` — phase 1 du V
- [x] ~~`hub/concept` — phase 2 du V~~ — squelette fait le 22/05 dans `content/fiches/proj/concept.md`
- [x] ~~`hub/preuve-de-concept` — phase 3 du V (point pivot du V)~~ — squelette fait le 22/05 dans `content/fiches/proj/preuve-de-concept.md` ; **approfondi 25/05 suite 7** (5 étapes + Pièges 11 entrées + Équipe 4 paragraphes + cohérence finale 4 passes + 6 nouvelles cibles wiki-links sur outils)
- [x] ~~`hub/dossier-technique` — phase 4 du V~~ — squelette fait le 22/05 dans `content/fiches/proj/dossier-technique.md` ; **approfondi 25/05 suite 8** (5 étapes [étape 3 pré-rédigée par autre instance Claude] + Pièges 11 entrées + Équipe 4 paragraphes + cohérence finale 4 passes à 0 patch + round 2 PCB gravure à l'anglaise + alignement v2.1)
- [x] ~~`hub/integration-et-tests` — phase 5 du V~~ — squelette fait le 22/05 dans `content/fiches/proj/integration-et-tests.md`
- [x] ~~`hub/gestion-de-projet` — fil transverse~~ — squelettisée + approfondie 25/05 dans `content/fiches/proj/gestion-de-projet.md`. 5 conventions transverses fixées à propager aux 2 autres transverses.
- [x] ~~`hub/ecoconception` — fil transverse~~ — squelettisée + approfondie 25/05 suite dans `content/fiches/proj/ecoconception.md`. 3 blocs co-actifs Évaluer / Réduire / Tracer, calque transverse établi sur gestion-de-projet.
- [x] ~~`hub/securite-et-qualite` — fil transverse~~ — créée ex nihilo + approfondie 26/05 dans `content/fiches/proj/securite-et-qualite.md`. 3 blocs co-actifs Sécurité produit / Sécurité projet / Qualité documentaire. **Phase 1 du wiki complète côté trames du V + fils transverses.**

À noter : l'emplacement acté implicitement par la session du 22/05 est `content/fiches/proj/` (et non `content/hub/`). Le hub reste un point d'entrée vers ces fiches, pas leur conteneur.

## Fiches à rédiger — par domaine

> **MAJ 12/06** — la **branche Conduite de projet** (fiches PROJ + MEO + ESE) est **intégralement créée et relue** (relecture humaine close 12/06). Beaucoup d'entrées non cochées des sections PROJ/MEO/ESE ci-dessous correspondent à des fiches **existantes et validées** (`caracteriser-une-exigence`, `etat-de-l-art-technique`, `decomposition-fonctionnelle`, `fast`, `mind-map`, `mecatronique`, `bom`, `amdec`, `unite-si`, `relation-client`, `archivage-projet`, `revue-de-code`, `cable-management`, `marquage-ce`, `basse-tension`, `emc`, `iso-12100`, `epi`…). À cocher lors d'un grooming BACKLOG dédié (éviter un balayage à risque en clôture).

### EEE — Électronique
- [x] ~~PWM (modulation de largeur d'impulsion)~~ — popover `pwm` créé 28/05 suite 4 (brique MCU).
- [x] ~~PID (régulateur proportionnel-intégral-dérivé)~~ — tuto `arduino-pid` relu/coché 17/06 (suite 3) ; notion mère `asservissement` créée.
- [x] ~~Asservissement~~ — notion transverse `content/embarque/asservissement.md` créée 17/06 (suite 3) (boucle fermée / correcteur / PID + SVG `boucle-fermee`). À relire + liens réciproques (cf. Récent suite 3).
- [ ] **`cpp-tableaux`** (repéré 12/06 en relecture de la grappe langage) : les tableaux sont utilisés dans `cpp-boucles` (chenillard `LEDS[]`) et dans les tutos aval (multi-voies, moyennes glissantes) sans jamais être enseignés dans le parcours. Fiche-tuto candidate en étape 6bis du hub `cpp` (déclaration, indices 0..N-1, parcours au `for`, dépassement d'indice) ; pendant MicroPython = listes, déjà couvertes par `micropython-types`. À cadrer — l'explication inline du chenillard suffit en attendant.
- [ ] **`microcontroleur`** — **hub mère** (re-cadrage 26/05 suite 5). Page d'aiguillage vers hubs filles familles MCU (mini-hubs imbriqués, convention C18). Familles priorité 1 : `arduino`, `esp32`, `esp8266`, `raspberry-pi` (sous étiquette élargie *plateformes embarquées*). Familles priorité 2 : `stm32`, `teensy`, `pic`. Chaque famille = hub fille + tutos d'utilisation. Pose les notions communes (registres, périphériques, bus, IDE) puis renvoie vers les tutos spécifiques.
- [ ] **`arduino`** — **hub fille** de `microcontroleur` (re-cadrage 26/05 suite 5). Uno + Mega, panorama écosystème + tutos enfants : `arduino-prise-en-main`, `tinkercad` (simu spécifique). Popover posé dans `preuve-de-concept.md` Stock standard, 25/05 suite 7.
- [x] ~~**`stm32`** — hub fille priorité 2.~~ — **hub + 1ʳᵉ grappe faits 06/06 (suite 6)** : hub `eee/mcu/stm32/stm32.md` (décalque C18, thèse **deux portes** STM32duino/native, table variantes 7 lignes par cœur Cortex-M **`[!info]` à confronter au ST product selector**, callout 3,3 V/broches FT, 4 paliers [A]/[T], SVG couches embarqué ; lien rouge `[[stm32]]` de `microcontroleur` résolu, aucun patch) + **5 enfants** : vague 1 `stm32-prise-en-main` (CubeIDE+Nucleo) / `stm32-arduino-core` (STM32duino sur HAL) ; vague 2 pivots `stm32-cubemx` (arbre d'horloge, génération) / `stm32-hal` (3 modes scrutation-IT-DMA, HAL/LL) / `stm32-registres` (CMSIS, blink bare-metal, BSRR vs ODR → `manipulation-de-bits`). 2 SVG conceptuels. **Tally inchangé 79 %** (largeur, multi-couverture C20). Décision **lean-Bases** (pas de `stm32-gpio`/`-serie`, §8 C56). **Restent (enrichissement)** : tutos enfants additionnels (périphériques : `stm32-timers`/PWM, ADC, I2C/SPI), `micropython`-STM32 éventuel. **En attente relecture (garde-fou)** : exactitude API STM32 (HAL, broches Nucleo, macros CMSIS, URL STM32duino, modes flashage) + 2 SVG au rendu + captures C29.
- [x] ~~**`esp32`** — **hub fille** de `microcontroleur`~~ — **hub fait 02/06 (suite)** (`eee/mcu/esp32/esp32.md` ; 4 paliers + [A]/[T], alerte 3,3 V ; C25/C26 2/N). **Restent les tutos enfants** (rouges) : `esp32-prise-en-main`, `esp32-arduino-core`, `esp32-idf`, `wokwi`, `esp32-gpio`/`-serie`/`-wifi`/`-ble`/`-deep-sleep`/`-freertos`. ⚠️ Tableau des variantes (ESP32/S2/S3/C3/C6) à confronter à la doc Espressif avant publication. **→ Enfants écrits + tableau variantes refresh fait 06/06 (suite) ; reste relecture utilisateur.**
- [x] ~~**`esp8266`** — hub fille priorité 1.~~ — **hub lean + 2 enfants faits 06/06 (suite 8)** : hub `eee/mcu/esp8266/esp8266.md` (décalque C18, thèse « petit frère Wi-Fi de l'ESP32 » — Wi-Fi seul, pas de BLE, un seul ADC ; une porte ESP8266 core ; table variantes NodeMCU/D1 mini/ESP-01 `[!info]` ; 2 callouts 3,3 V + broches de boot/ADC ; 4 paliers [A]/[T] ; lien rouge `[[esp8266]]` de `microcontroleur` résolu) + **2 enfants** : `esp8266-prise-en-main` (core ESP8266, pilote USB-série, NodeMCU, blink LED active-bas) / `esp8266-arduino-core` (Dxx≠GPIO, broches de boot, ADC unique, watchdog/yield, exemple Wi-Fi → renvoi `esp32-wifi`). **Pas de SVG** (famille « moins »). **Tally inchangé 79 %** (largeur, multi-couverture C20). **lean + un cran** : pas de `esp8266-wifi`/`-ble` (Wi-Fi dans l'exemple + renvoi esp32) → **C56 3/N**, nuance C47 (renvois cross-famille). **Restent (enrichissement)** : enfants additionnels (serveur web/MQTT, OTA, deep-sleep détaillé) si besoin. **En attente relecture (garde-fou)** : exactitude API ESP8266 (URL core, pilote USB-série, LED active-bas, mapping Dxx, broches de boot, ADC, `ESP8266WiFi`).
- [x] ~~**`raspberry-pi`** — hub fille priorité 1 (SBC Linux).~~ — **module complet fait 06/07** : hub `eee/mcu/raspberry-pi/raspberry-pi.md` (notion, thèse **MCU vs SBC**, panorama Pi 5/4/Zero `[!info]`, warning 3,3 V, distinction explicite Pico→`micropython`, **paliers adaptés** ≠ C25 ≠ clone C57) + **3 fiches** : `-prise-en-main` (Imager + Pi OS Lite + **headless/SSH**, aparté C55 sysadmin délégué, `aa:[]`) / `-gpio` (`gpiozero`/`RPi.GPIO`(KO Pi 5)/`lgpio`, LED+bouton, **« pas de temps réel dur »**, `aa:PROJ/5`) / `-projet` (**architecture bicéphale** SBC+MCU sur bras 3 axes, `aa:PROJ/5`). **3 SVG conceptuels**. **Tally inchangé 79 %** (largeur, C20). **lien rouge `[[raspberry-pi]]` de `microcontroleur` résolu, aucun patch → couche familles MCU/SBC complète.** **En attente relecture (garde-fou)** : exactitude OS/API (Imager, `gpiozero`/Pi 5, numérotation BCM, courant/broche, alim Pi 5) + 3 SVG au rendu + captures C29.
- [x] ~~**`micropython`** — fiche-tuto approche scriptée haut niveau (positionnement à trancher : tuto autonome ou enfant de hub MCU ?).~~ — **module complet ouvert 06/06 (suite 9), au format Arduino, ancré Pico 2, en 4 vagues.** **Vague 0 faite** (`eee/mcu/micropython/`) : hub `micropython.md` + `micropython-prise-en-main` (Thonny/firmware/REPL) + `micropython-simulation` (Wokwi) + hub langage `micropython-langage` + 5 filles (`-repl`/`-types`/`-controle`/`-fonctions`/`-modules`). Partage [A]/[T] : hub langage propre remplace `cpp` ; concepts agnostiques référencés [T] ; « Communication » (bases) → `micropython-repl` (pas de fiche `-serie`, à confirmer). **V1 Bases hands-on faite** (06/06 suite 10 — 11 fiches : gpio, entree-tor, sortie-tor, capteur-numerique, capteur-analogique, sortie-pwm, temporisation, bibliotheques, module, shield, alimentation ; + SVG `micropython-modele-execution` embarqué dans le hub). **V2 Avancées faite** (06/06 suite 11 — 12 fiches : uart/i2c/spi, servomoteur/moteur-cc/moteur-pas-a-pas, afficheur, debug, gpio-boot, prog-non-bloquante, machine-a-etats, stockage/eeprom ; filtrage laissé rouge, pas de jumeau arduino). **V3 Ingénieur faite** (06/06 suite 12 — 6 fiches : interruptions, timers, deep-sleep, pid, memoire, watchdog ; manipulation-de-bits reste [T]). **✅ MODULE MicroPython COMPLET** (hub + 37 enfants = 38 fiches, 4 vagues) — seul `micropython-filtrage` reste rouge (optionnel, pas de jumeau arduino). **En attente relecture (bloc final)** : API MicroPython COMPLÈTE (tout le module) + captures C29. Motif « clone de curriculum + hub langage substitué » **mûr → promotion en C57 à acter** (conventions §8).
- [ ] **`pico-sdk`** — fiche-tuto **optionnelle** (reportée 06/07, hors module SBC) : programmer le Raspberry Pi **Pico** en **C/C++** via le Pico SDK (2ᵉ porte du Pico, à côté de MicroPython). Public marginal (l'école passe par MicroPython) ; à produire un jour si besoin.
- [x] ~~**`teensy`** — hub fille priorité 2.~~ — **hub + 1ʳᵉ grappe faits 06/06 (suite 7)** : hub `eee/mcu/teensy/teensy.md` (décalque C18, thèse « Arduino mais rapide + couteau-suisse USB/audio », une seule porte Teensyduino ; table variantes par cœur **`[!info]` à confronter doc PJRC** ; callout 3,3 V — **4.x NON tolérant 5 V** ; 4 paliers [A]/[T]) + **4 enfants** : vague 1 `teensy-prise-en-main` (Teensyduino + Teensy Loader) / `teensy-arduino-core` (Arduino musclé, noyau PJRC sur registres NXP, pas de HAL) ; vague 2 distinctifs `teensy-audio` (Audio Library + Audio System Design Tool + DMA, pas de DAC sur 4.x) / `teensy-usb` (USB Type : clavier/MIDI/manette…). 2 SVG conceptuels (`teensy-audio-flux`, `teensy-usb-personnalites`). **Tally inchangé 79 %** (largeur, multi-couverture C20). **lean-Bases** reconduit (pas de `teensy-gpio`/`-serie`) → **C56 2/N + élargie** (porte Arduino, pas native). Pas de fiche perf séparée. **Restent (enrichissement)** : enfants additionnels (périphériques, Audio Shield détaillé, Ethernet/SD sur 4.1). **En attente relecture (garde-fou)** : exactitude API Teensy (Teensyduino, broches, USB Type, Audio Library, pas-de-DAC 4.x) + 2 SVG au rendu + captures C29.
- [x] ~~**`pic`** — hub fille priorité 2.~~ — **fait 06/06 (suite 9) en fiche notion** (`eee/mcu/pic.md`, pas de module) : positionnement (ancien µC Microchip MPLAB/XC, pas d'Arduino-core natif, détrôné par STM32 en industrie / Arduino en école) ; `[[pic]]` rouge de `microcontroleur` résolu. **Choix : pointeur léger** (PIC hors usage école). Pas de relecture lourde nécessaire.
- [ ] **`algorithme`** — mini-hub mère (acquis 26/05 suite 5). Regroupe 3 méthodes de description du flot de comportement : `logigramme`, `machine-a-etats` (MAE), `grafcet`. Critique pour la couverture du critère `RA-EEE-C03-2/EEE/5`.
- [ ] **`logigramme`** — fiche-notion fille hub algorithme. Représentation graphique d'une séquence d'instructions/décisions.
- [ ] **`machine-a-etats`** — fiche-notion fille hub algorithme, alias `MAE`. Automate fini, états et transitions sur événements.
- [ ] **`grafcet`** — fiche-notion fille hub algorithme. Graphe de commande étape-transition, norme IEC 60848.
- [ ] **`chronogramme`** — fiche-tuto transverse (acquis 26/05 suite 5). Jonction datasheet ↔ algorithme ↔ oscilloscope. Diagramme temporel, X = temps, Y = signaux. **Hors mini-hub algorithme** (sémantique différente : signaux dans le temps vs flot de comportement). Contribue au critère `RA-EEE-C03-2/EEE/5`.
- [x] ~~**`bus-de-communication`** + `uart` / `i2c` / `spi`~~ — **fait 28/05 suite 4** (grappe batch `eee/mcu/bus/` ; hub tableau comparatif + aide au choix + SVG topologies).
- [ ] **Enrichissement grappes bus / sans-fil** (déféré 28/05 suite 4) : SVG de brochage par fille bus (`uart`/`i2c`/`spi`), trame UART + chronogrammes I2C/SPI (jonction `chronogramme`), approfondissement des filles sans-fil (actuellement popover). Optionnel bus : `can`, `rs-485`.
- [x] ~~**`techno-sans-fil`** + `wifi` / `ble` / `zigbee` / `xbee` / `lora`~~ — **fait 28/05 suite 4** (grappe batch `eee/mcu/sans-fil/` ; hub tableau + carte portée×débit + SVG comparaison).
- [x] ~~**`lire-une-datasheet`** — fiche-tuto transverse compétence pro~~ — **fait 28/05 suite 3** (procédure générique 5 étapes + *Exemple* L298N autonome 2 moteurs CC + 2 SVG ; C19 variante-(c), épreuve faible — vrai test multi-techno reste `analyse-de-schema-electronique` / `firmware`).
- [ ] **`analyse-de-schema-electronique`** — fiche-tuto transverse compétence pro. Critère NC `RA-PROJET-C03-3/EEE/1` (couvert dans Phase 1 squelette critique AA). À éprouver pour la convention C19 (fiche transverse multi-techno).
- [x] ~~**`firmware`** — fiche transverse pure~~ — **fait 02/06 (suite)** (`eee/mcu/firmware.md` ; typé **notion** ; progression super-loop → RTOS + section « selon la famille » ; **C19 vrai test → option (c)** ; `aa: .../PROJ/5` effleuré ; SVG couches = candidat).
- [x] ~~**`debugger-embarque`** — fiche-tuto compétence pro. SWD, JTAG, printf, breakpoints.~~ — **fait 06/06 (suite 3)** (eee/mcu/, tuto transverse [T], « Déboguer un système embarqué » ; deux approches printf vs JTAG/SWD + méthode d'enquête ; aa PROJ/5).
- [x] ~~**`pcb`** — mini-hub mère léger (gravure école, monoface interne / double face JLCPCB ; enfants kicad/easyeda).~~ — **fait 06/06 (suite 3)** (eee/pcb/, hub C18, aa EEE/5 *dédiée* ; flux schéma→Gerber + SVG ; **`easyeda` reste à faire**).
- [x] ~~**`kicad`** — fiche-tuto outil PCB principal (open source, double face). Simu intégrée mention possible.~~ — **fait 06/06 (suite 3)** (eee/pcb/, fille hub pcb, aa:[]).
- [ ] **`easyeda`** — fiche-tuto outil PCB altern (en ligne, JLCPCB-friendly). Embranchement de choix outil.
- [ ] **`pcb-gravure-ecole`** — fiche-tuto spécifique monoface interne école. À produire ultérieurement (alimentation continue).
- [ ] **`circuitverse`** — fiche-tuto simu numérique en ligne (retenu 26/05 suite 5 après test utilisateur). Squelette transverse.
- [ ] **`ltspice`** — fiche-tuto simu analogique desktop. Squelette transverse.
- [ ] **`tinkercad`** — fiche-tuto simu Arduino (embranchement Arduino).
- [ ] **`wokwi`** — fiche-tuto simu ESP32 (embranchement ESP32).
- [ ] **`falstad`** — fiche-tuto simu altern, à évaluer après CircuitVerse. Squelette transverse.
- [x] ~~**`multimetre`** — fiche-tuto compétence pro (mesures tension/courant/résistance/continuité, choix d'échelle, pièges).~~ — **fait 06/06 (suite 3)** (eee/mesure/, fille du hub instruments-de-mesure, aa:[], + SVG série/parallèle).
- [x] ~~**`oscilloscope`** — fiche-tuto compétence pro (lecture d'écran, sonde, déclenchement, mesures).~~ — **fait 06/06 (suite 3)** (eee/mesure/, fille du hub instruments-de-mesure, aa:[] ; frontière lecture conceptuelle → [[chronogramme]]).
- [x] ~~**`alimentation-electronique`** — fiche-tuto compétence pro (re-cadrage 26/05 suite 5, ex `alimentation-stabilisee`). Régulation, découplage, masse, mode CV/CC, sécurité court-circuit.~~ — **fait 06/06 (suite 4)** : finalement **notion transverse [T]** (`eee/alimentation-electronique.md`), 5 principes + exemple bras 3 axes + 3 SVG ; carto `RA-EEE-C03-2/EEE/3` **E→C**. **2 SVG candidats restants** (différés du 1er jet) : courbe CV/CC, placement du découplage.
- [ ] **`bom-electronique`** — fiche-tuto compétence pro. Au-delà du `bom` générique projet, focus composants élec.
- [ ] Alimentation d'un système embarqué
- [x] ~~Pull-up / pull-down~~ — couvert par `gpio` (02/06) : modes de tirage interne/externe, logique inversée, schéma push-pull/drain ouvert/tirage.
- [x] ~~**`niveau-logique`** — compatibilité 3,3 V / 5 V, level shifters~~ — **fait 28/05 suite 4** sous le slug **`niveaux-de-tension`** (fiche-notion + 2 SVG ; popover posé dans `integration-et-tests.md` 26/05).
- [x] ~~ADC~~ / DAC — popover `adc` créé 28/05 suite 4 (brique MCU) ; `DAC` reste à traiter.
- [ ] Filtrage analogique de base
- [ ] Masse électrique, plan de masse
- [ ] Découplage (condensateurs)
- [ ] **`cable-management`** — fiche-tuto (popover posé dans `integration-et-tests.md` section Équipe, 26/05). Routage des câbles, attaches, repérage, anti-arrachement, protection en mouvement.

### MIA — Informatique embarquée
- [x] ~~**`firmware`** — fiche-notion~~ — **fait 02/06 (suite)**, fusionnée avec l'entrée EEE ci-dessus (une seule fiche `eee/mcu/firmware.md` couvre architecture + définition/cycle de vie).
- [ ] **`revue-de-code`** — fiche-notion (popover posé dans `securite-et-qualite.md` § Qualité documentaire, 26/05). Pratique de qualité logicielle : revue croisée entre équipiers ou avec l'encadrant, focus défauts manifestes (gestion interrupts, machine à états, fonctions de sécurité), 30 min par jalon.
- [ ] Architecture logicielle d'un objet connecté
- [ ] Boucle principale (`loop()`) vs interruptions
- [ ] Lecture capteur (polling vs interrupt)
- [ ] Communication série (UART, I2C, SPI)
- [ ] MQTT pour IoT
- [ ] Versionning du code embarqué
- [ ] Structuration d'un projet Arduino / PlatformIO
- [ ] Debug embarqué (sortie série, LED de status)

### PROJ — Démarche projet
- [x] ~~Cahier des charges fonctionnel~~ — fait 25/05 suite 2 dans `content/fiches/proj/cahier-des-charges-fonctionnel.md` (fiche-notion complète au standard `bete-a-cornes.md` hybridé avec section « Structure type du document école »).
- [ ] **`caracteriser-une-exigence`** (fiche-tuto **prioritaire**, décision 23/05 suite 2 : 1 fiche unique avec `aliases: [critere, niveau, flexibilite]` dans le front matter). Embarquera le triplet, le triptyque mauvais/moyen/bon (pattern `bete-a-cornes.md`), l'échelle F0/F1/F2/F3 NF X50-151. Première phrase = définition du triplet pour popover cohérent au survol des 3 alias.
- [ ] **`unite-si`** (fiche-notion, popover posé étape 4 de `specification-technique.md` le 23/05 suite 2). Convention typographique des unités SI (espace insécable, format ± X mm, bornes, plages). Traitera aussi la mention `Ctrl+Maj+Espace` dans Obsidian.
- [x] ~~Bête à cornes~~ — fait le 20/05 (fiche-notion complète + 4 SVG)
- [ ] **Décomposition fonctionnelle** (notion-mère, distincte de `schema-bloc-fonctionnel` qui n'est qu'un des outils possibles — popover posé dans `concept.md` étape 1 le 22/05)
- [x] ~~**Matrice de décision**~~ — fait 25/05 suite 2 dans `content/fiches/proj/matrice-de-decision.md` (fiche-notion brève rédigée + 2 SVG : tableau générique 3 sol × 5 critères + matrice choix d'alimentation station météo).
- [x] ~~Analyse fonctionnelle (FAST, SADT, pieuvre) — découper ?~~ — décision 23/05 suite : on découpe en fiches séparées (entrées `pieuvre` et `fast` ci-dessous, SADT à voir si besoin)
- [x] ~~**`pieuvre`** (notion, **triplement prioritaire** : popover posé étape 3 de `specification-technique.md` + placeholder image cassée dans la trame + **aliases `[FP, FS, FC]` à poser** dans le front matter — décision 23/05 suite 2)~~ — fait 24/05 (suite 2) : fiche-notion légère produite (stub méthodologique). **Aliases déplacés sur la nouvelle fiche `fonction.md`** (décision 24/05 suite 2 : FP/FS/FC pointent sémantiquement vers la catégorie de fonction, pas l'outil pieuvre). Approfondissement au standard `bete-a-cornes.md` restant.
- [x] ~~**`fonction`** (nouveau, aliases `[FP, FS, FC]`)~~ — fait 24/05 (suite 2) : fiche-notion brève complète (option b) avec trichotomie FP/FS/FC, format d'énoncé verbe + complément, tableau d'exemples correct/incorrect, 3 pièges.
- [ ] **`fast`** (tuto, popover posé en ouverture de l'étape 3 de `specification-technique.md` le 23/05 suite — la pieuvre donne le *quoi*, FAST donne le *comment*). À traiter en phase 2 concept.
- [ ] **`revue-de-cdcf`** (notion candidate — vocabulaire acté 24/05, utilisé 4-5 fois dans `specification-technique`. À monitorer ; produire une fiche-notion légère si l'utilisation se répète aux phases 2-5 ou si une fiche dédiée aux revues de jalon émerge).
- [ ] Mécatronique (fiche-notion racine, emplacement à arbitrer)
- [ ] Frontière du système
- [ ] Revue de projet
- [ ] Approche-test / itération
- [ ] Critères de comparaison de solutions
- [ ] **`etat-de-l-art-technique`** (tuto, popover posé dans l'objectif de `specification-technique.md` le 23/05). Méthode N×M (N solutions × M critères pondérés) pour étude comparative chiffrée. À distinguer d'une simple revue bibliographique. Fusionne l'ancienne entrée « État de l'art (méthodologie) ».
- [ ] Schéma cinématique
- [ ] Chaîne d'énergie
- [ ] Pré-dimensionnement (méthodologie)
- [ ] **`bom`** / Nomenclature (tuto, popover posé étape 2 de `specification-technique.md` le 23/05 suite). Acronyme + structure d'un BOM. Évolution attendue : BOM préliminaire (spec) → consolidée (concept) → finale (dossier technique).
- [ ] **`amdec`** — fiche-notion (popover posé dans `securite-et-qualite.md` § Sécurité produit, 26/05). Méthode d'analyse des risques produit : modes de défaillance, gravité, probabilité, parade. Variante allégée pédagogique envisageable ; méthode complète déléguée au cours dédié.
- [ ] Plan de qualification produit
- [ ] Retour d'expérience (REX)

### MEO — Méthodes et organisation
- [ ] Outils collaboratifs (ODJ, CR, planning)
- [ ] Outils de créativité (brainstorm, SCAMPER, etc.)
- [ ] **`mind-map`** (notion, popover posé étape 3 de `specification-technique.md` le 23/05 suite). Outil méthodo générique mobilisable bien au-delà de la pieuvre (structuration d'idées, analyse de besoin, brainstorming).
- [ ] **`fablab`** (notion candidate — terme utilisé 2 fois dans la section Équipe de `specification-technique.md` (24/05 suite). À monitorer ; produire fiche-notion légère si l'utilisation se généralise dans les fiches de fabrication ou de mise à disposition de moyens école).
- [ ] Prise de décision collective
- [ ] Gamme de fabrication
- [ ] **Relation client** (tuto) — valider la compréhension du besoin, posture en réunion client, gestion des changements de demande
- [ ] **Archivage projet** (tuto) — CR de réunion, traçabilité documentaire, versioning des livrables
- [ ] **Définir un objectif SMART** (tuto)
- [x] ~~**`retroplanning`** (fiche-tuto détaillée)~~ — fait 25/05 suite 2 : fiche-tuto brève rédigée (5 étapes méthode) + 2 SVG (axe temps avec flèche à rebours + rétroplanning station météo 15 sem.).
- [x] ~~**`jalons`** (fiche-tuto détaillée)~~ — fait 25/05 suite 2 : **type:notion** après clarif Q4 25/05 suite 2 (notion = court/popover). Fiche-notion brève rédigée + 2 SVG (frise générique cycle en V + jalons station météo sur 15 semaines).
- [x] ~~**`gantt`** (fiche-tuto détaillée)~~ — fait 25/05 suite 2 : fiche-tuto brève rédigée + 2 SVG (mini-Gantt générique 4 tâches × 8 sem. + Gantt station météo 6 × 15 sem. avec dépendances). Approfondissement avec captures GanttProject/Trello/Excel reste à faire ultérieurement.
- [x] ~~**`wbs`** (fiche-notion légère)~~ — fait 25/05 suite 2 : fiche-notion brève rédigée + 2 SVG (arbre générique 3 niveaux + WBS station météo par sous-système).
- [x] ~~**`matrice-de-risques`** (fiche-notion légère)~~ — fait 25/05 suite 2 : fiche-notion brève rédigée + 2 SVG (grille 3×3 P×G générique colorée + 6 risques cotés sur projet station météo avec parades).

### MME — Matériaux / mécanique (interface collègues)
- [ ] Liens vers cours mécanique
- [ ] Liens vers cours fabrication
- [ ] **`gabarit`** — fiche-notion (popover posé dans `integration-et-tests.md` étape 2 H4-2, 26/05). Pièce de vérification dimensionnelle réutilisable, simple à fabriquer en interne, accélère les contrôles répétitifs sur séries de pièces identiques. Probablement délégable au cours fabrication des collègues.
- [ ] **`pied-a-coulisse`** — fiche-notion (popover posé dans `preuve-de-concept.md` outillage, 25/05 suite 7). Instrument de mesure dimensionnelle de base, lecture vernier, plage 0-150 mm typique. Court, popover-only.
- [ ] **`comparateur`** — fiche-notion (popover posé dans `preuve-de-concept.md` instruments de mesure, 25/05 suite 7). Aliases possibles `[comparateur-micrometrique]`. Mesure de variation dimensionnelle (jeu, défaut de planéité, dérive), précision typique 0,01–0,02 mm.

### ESE — Écoconception, sécurité produit, normes
- [ ] ACV (analyse cycle de vie) — principes
- [ ] Liens vers cours dédié
- [ ] **`marquage-ce`** — fiche-notion (popover posé dans `securite-et-qualite.md` Objectif + § Sécurité produit + § Qualité documentaire + § Équipe, 26/05). Conformité européenne, périmètre projet pédagogique, renvoi cours normatif.
- [ ] **`basse-tension`** — fiche-notion (popover posé dans `securite-et-qualite.md`, 26/05). Directive 2014/35/UE, périmètre, applicabilité projet étudiant. Délégué cours normatif.
- [ ] **`emc`** — fiche-notion alias `[cem, compatibilite-electromagnetique]` (popover posé dans `securite-et-qualite.md`, 26/05). Compatibilité électromagnétique, principes, renvoi cours dédié.
- [ ] **`iso-12100`** — fiche-notion (popover posé dans `securite-et-qualite.md`, 26/05). Norme sécurité des machines, principes, périmètre projet pédagogique.
- [ ] **`epi`** — fiche-notion (popover posé dans `securite-et-qualite.md` § Sécurité projet, 26/05). Équipement de protection individuelle, recensement par environnement (atelier, fablab, salle élec), articulation avec les responsables d'équipement.

## Idées de fiches transversales / méta

- [ ] **Convention mécatronique "par fonction"** — acquis 21/05 suite : en mécatronique, une fonction mobilise toujours plusieurs disciplines. Pas de test purement disciplinaire. À expliciter dans une fiche-notion sur l'**essence mécatronique** (ce qui distingue la mécatronique d'un assemblage élec + méca + info).

- [ ] Glossaire d'acronymes (PWM, PID, PCB, PPM, etc.) — à décider : fiche unique ou fiches courtes ? `ppm` est un prospect typique (acronyme périphérique, pas digne d'une fiche dédiée)
- [ ] FAQ projet mécatronique
- [x] ~~"Premiers pas" (page d'onboarding pour étudiant qui arrive sur le site)~~ — l'accueil refondu fait office d'onboarding (« Par où commencer » = 3 branches), 08/06.
- [x] ~~Mode d'emploi du site (popovers, recherche, graph)~~ — couvert par la refonte de l'accueil (section « Comment utiliser ce site »), 08/06.

## Idées d'illustrations / médias à produire

- [ ] **SVG colonne « Système embarqué » (à produire, voie A)** — diagramme des 7 étapes de réalisation montrant l'**orthogonalité au cycle en V** (deux colonnes : gestion de projet [le V] vs ingénierie embarquée [la colonne], reliées par le mapping par étape). Renforcerait le hub `eee/index`. Session SVG dédiée (voie A, hand-codé). Posé 09/06.
- [ ] Schéma annoté d'une couveuse réelle (référencé dans schema-bloc-fonctionnel)
- [ ] Illustration des conventions de flèches (info / énergie / matière)
- [x] ~~**SVG bête à cornes générique**~~ — fait le 20/05 (4 SVG produits : générique + 3 exemples bras robotique). Placeholder dans `specification-technique.md` étape 1 remplacé.
- [ ] SVG bête à cornes — version simplifiée 3-cases (variante éventuelle pour fiche très courte ou résumé carte mentale)
- [x] ~~SVG pieuvre générique (pour étape 3 de `specification-technique`) — **désormais doublement prioritaire** : placeholder cassé dans la trame depuis le 23/05 suite.~~ — fait 24/05 (suite 2) : `pieuvre-generique.svg` produit (forme rayonnante classique AFNOR, tous liens même style, étiquettes Fx). Première version « traversante FP/FS/FC » refondue suite à retour utilisateur sur la convention dominante.
- [x] ~~SVG pieuvre incarné bras 3 axes~~ — fait 24/05 (suite 2) : `pieuvre-bras-3-axes.svg` produit (système BRAS 3 AXES + 5 milieux + 4 fonctions FP1/FS1/FC1/FC2). Référencé dans l'`[!example]` étape 3 de `specification-technique.md` et dans la section Exemple de `pieuvre.md`.
- [ ] Photos de projets étudiants antérieurs avec accord (anonymisées si besoin)
- [ ] Capture d'écran de schéma Fritzing / KiCad type
- [ ] Vidéo courte de démo (intégration via embed ?)
- [x] ~~Schéma cycle en V v2 avec boucles de retour itératives~~ — fait le 21/05 suite 2 (`cycle-v-projet.svg` enrichi de 4 rétroactions ambrées, version v1 archivée)

## Améliorations site / Quartz

- [ ] **Masquer / filtrer l'explorateur de fichiers Quartz** — l'arborescence alphabétique des fiches (`arduino-*` notamment) paraît « en vrac » côté étudiant (signalé 02/06), alors que l'ordre pédagogique vit dans les hubs. Agir sur le **composant Explorer du layout Quartz** (le retirer, ou lui donner un filtre de dossiers). **Ne PAS utiliser `draft: true`** : en Quartz une page `draft` est exclue du build (dépubliée), ce qui casserait tous les wiki-links/popovers entrants. À traiter en pré-publication.
- [ ] **Reprise visuelle des images SVG** (pas du contenu) avant publication — **très peu urgent, à faire juste avant l'ouverture aux élèves**. Concerne tous les SVG produits depuis le début du projet :
  - **MAJ 16/06 — migration C73 faite** : tous les SVG listés ci-dessous sont désormais dans `content/ressources/img/<slug-fiche>/` (descripteur = nom **sans le préfixe de fiche** ; règle de placement en conventions §8 C76). Les noms à plat ci-dessous **identifient encore** les SVG mais leurs **chemins ont changé** ; `peigne-svg-prepublication.md` est aussi à rafraîchir. À refléter au début de la passe d'affinage voie A.
  - **Premiers jets fragiles, affinage utilisateur attendu** (produits le 25/05 suite 2, génériques + exemples station météo) : `jalons-generique.svg`, `jalons-station-meteo.svg`, `wbs-generique.svg`, `wbs-station-meteo.svg`, `retroplanning-generique.svg`, `retroplanning-station-meteo.svg`, `gantt-generique.svg`, `gantt-station-meteo.svg`, `matrice-de-risques-generique.svg`, `matrice-de-risques-station-meteo.svg`, `matrice-de-decision-generique.svg`, `matrice-de-decision-station-meteo.svg`. Ce sont des premiers jets Claude, à reprendre visuellement avant publication (alignements, hauteurs de lignes, positionnements de textes proches des bords, lisibilité smartphone).
  - **Premiers jets EEE/mcu (28/05)** : `microcontroleur-architecture.svg` (schéma bloc, 28/05 suite 2), `lire-une-datasheet-generique.svg` (anatomie datasheet), `lire-une-datasheet-l298n.svg` (L298N deux mondes). 1ers jets Claude — affinage avant publication (alignements, lisibilité smartphone, sens des deux flèches de rotation moteur sur le SVG L298N).
  - **Premiers jets EEE/mcu (02/06)** : `gpio-modes`, `gpio-flottant`, `gpio-courant-max` (#B23A2E), `interruption-chronogramme`, `timer-compteur`, `programmation-non-bloquante` — 6 SVG hand-codés à valider au rendu Quartz. **+ 2 SVG candidats non encore produits** : schéma-bloc PID (consigne→erreur→P+I+D→système→retour) pour `arduino-pid`, profil de consommation deep-sleep (pics de réveil + vallées) pour `arduino-deep-sleep`.
  - **Premiers jets EEE/mcu (02/06 suite)** : **aucune SVG produite** — `cpp`, `fonction-informatique`, `manipulation-de-bits`, `firmware`, `esp32` rédigées sans schéma (tables + code + motifs binaires monospace). **Les 2 SVG conceptuels candidats ont été produits 06/06** (`manipulation-de-bits-masquage`, `firmware-architectures`).
  - **Premiers jets EEE/mcu (05/06 suite)** : **2 SVG conceptuels produits** pour le module `cpp` (relecture parcours Arduino), à valider au rendu — `cpp-execution-cycle.svg` et `cpp-portee-locale-globale.svg` : schéma du cycle `setup()`/`loop()` (mise sous tension → `setup()` une fois → `loop()` en boucle) pour `cpp-execution` ; visuel portée locale vs globale (variable recréée/détruite à chaque tour de `loop()` vs persistante) pour `cpp-portee`.
  - **Premiers jets 06/06** : module `simulation` — 4 SVG conceptuels (`simulation-electronique-cycle`, `simulation-electronique-lecture-courbe`, `manipulation-de-bits-masquage`, `firmware-architectures`), embeds posés, à valider au rendu Quartz. **SVG méca relus 06/06 en séance** (fil rouge bras 3 axes) : `schema-cinematique-generique`/`-bras-3-axes` **validés** ; `chaine-energie-generique`/`-bras-3-axes` **corrigés** (flèche de mesure re-routée depuis le bloc Agir).
  - **Premiers jets 06/06 (suite)** : module `esp32` — 2 SVG conceptuels (`esp32-deep-sleep-reveil` = cycle réveil=RESET + sources de réveil ; `esp32-freertos-ordonnancement` = préemption de 2 tâches), à valider au rendu Quartz. **Note** : la liste de classes SVG `.th/.tl/.tf` du § 3 de `conventions.md` paraît obsolète — les SVG récents embarquent leur propre `<style>` auto-contenu ; à nettoyer.
  - **Premiers jets 06/06 (suite 3)** : Phase 3 squelette pro — 3 SVG conceptuels (`multimetre-serie-parallele` = voltmètre en parallèle / ampèremètre en série ; `instruments-de-mesure-confrontation` = mesure dans plage attendue → validé ; `pcb-flux` = schéma→Gerber→fabrication avec frontière), embeds posés, à valider au rendu Quartz (**géométrie du circuit multimètre en priorité**, lisibilité smartphone).
  - **Premiers jets 06/06 (suite 4 + suite 5)** : EEE `alimentation-electronique` — 3 SVG (`alimentation-electronique-regulation` linéaire vs découpage ; `-masses` duo étoile/chaînage C31 ; `-bras-3-axes` archi incarnée 2 rails / fusible / diode anti-inversion / découplage / masse étoile) **non listés jusqu'ici**. MME `optimisation-mecanique` — 1 SVG (`optimisation-mecanique-generique` = avant/après allègement, profilé plein → ajouré, masse ≈ 70 %). Tous 1ers jets auto-contenus + mode sombre, à valider au rendu Quartz.
  - **Premiers jets 06/06 (suite 6)** : module `stm32` — 2 SVG conceptuels (`stm32-abstraction-couches` = 4 couches STM32duino/HAL/LL/registres-CMSIS + silicium, CubeMX « génère » HAL&LL, axe abstraction↑ — la thèse du module ; `stm32-cubemx-flux` = 6 boîtes Configurer→Générer→Compléter→Compiler→Flasher→Déboguer + boucle « USER CODE préservé »), embeds posés (chemins racine-absolus), à valider au rendu Quartz (lisibilité smartphone des axes/boîtes).
  - **Premiers jets 06/06 (suite 7)** : module `teensy` — 2 SVG conceptuels (`teensy-audio-flux` = chaîne audio, objets reliés par cordons, traitement DMA en tâche de fond ; `teensy-usb-personnalites` = un Teensy → identités USB choisies à la compilation), embeds posés dans `teensy-audio`/`teensy-usb`, à valider au rendu Quartz (lisibilité smartphone des chaînes/fan-out).
  - **Premiers jets 06/07** : module `raspberry-pi` — 3 SVG conceptuels (`raspberry-pi-mcu-vs-sbc` = comparaison MCU/SBC 2 panneaux, la thèse ; `raspberry-pi-gpio-pile` = l'OS qui s'intercale entre programme et broche, point « pas de temps réel dur » ; `raspberry-pi-architecture-bicephale` = SBC haut niveau + MCU temps réel sur bras 3 axes), embeds posés (chemins racine-absolus), à valider au rendu Quartz.
  - **Premiers jets 06/10** : 3 SVG voie A (`schema-bloc-fonctionnel-generique`, `schema-bloc-fonctionnel-couveuse` [remplace le Mermaid hors charte], `concevoir-l-electronique-bras-3-axes` [schéma de principe annoté, pastilles d'étapes]) — **déjà validés au rendu par Tim en séance, smartphone compris** : hors passe d'affinage, listés pour mémoire. Acquis technique : une règle CSS de classe écrase les attributs de présentation SVG (`text-anchor`, `font-size`) — les poser par élément, jamais en `<style>`.
  - **Premiers jets 13/06 (module XIAO ESP32-S3)** : 7 SVG hand-codés à valider au rendu Quartz (clair + sombre + smartphone) — `xiao-esp32-s3-brochage` (carte fonctionnelle, disposition logique des pads), `xiao-esp32-s3-variantes` (arbre de décision S3/Sense/C6/C5/C3), `xiao-esp32-s3-extendeur-i2c` (C67, PCF8574/MCP23017 + tirage), `xiao-esp32-s3-antenne` (interne céramique vs externe U.FL), `xiao-prise-en-main-bootloader` (flux de récupération 4 étapes), `xiao-alimentation-paths` (USB-C/accu → charge + régulateur 3V3), `xiao-sense-peripheriques` (caméra/micro/SD + bus). Gabarit auto-contenu dark, markers en classes C69, text-anchor par élément.
  - **Premiers jets 16/06 (relecture §7 Arduino)** : 4 SVG hand-codés à valider au rendu Quartz (clair + sombre + smartphone) — `ascii/caractere-vers-code` (bande caractère→code + piège '4'=52), `arduino-entree-tor/rebond` (chronogramme anti-rebond : lecture brute → fenêtre 30 ms → état stable), `arduino-sortie-tor/montage` (carte d'interfaces broche→charge, remplace une photo — C78). **⚠ `arduino-sortie-tor/transistor-bas-cote` (étage NPN) : symbole correct mais À REPRENDRE avant mise en ligne** (signalé par Tim — peaufinage géométrie / symbole transistor dans le cercle). Gabarit auto-contenu dark, markers en classes C69, text-anchor par élément.
  - **Premiers jets 17/06 (relecture §7 Arduino — capteur/PWM, bus, actionneurs, afficheur, debug)** : SVG hand-codés à valider au rendu (clair/sombre/smartphone) — bus `arduino-uart/cablage-croise`+`pont-serie`, `arduino-i2c/branchement-i2c`, `arduino-spi/branchement-sd` ; actionneurs `arduino-servomoteur/retour-position`, `arduino-moteur-cc/branchement-l298n`, `arduino-moteur-pas-a-pas/branchement-28byj48` ; `arduino-afficheur/branchement-i2c` ; `arduino-debug/ou-est-le-probleme` (triage) ; + matinée `arduino-capteur-analogique/branchement-potentiometre`+`pont-diviseur-ldr`, `arduino-sortie-pwm/branchement-led-pwm`, `potentiometre` (triptyque webp), `arduino-temporisation/delay-vs-millis`+`cadences-paralleles`, `arduino-bibliotheques/couches-abstraction`, `arduino-module/branchement-dht11`+`pullups-paralleles`, `arduino-alimentation/alimentation-separee`. **⚠ Défaut `\u` corrigé** sur 6 SVG (write_file écrit littéralement les échappements, ≠ edit_file ; vrais caractères rétablis — conventions §8 note 17/06 suite) : re-vérifier au rendu qu'aucun `\u00xx` ne subsiste dans les notes de pied. Gabarit auto-contenu dark, markers en classes C69.
  - **Premiers jets 17/06 (suite 3) — fin §7 Arduino (gpio-boot → watchdog) + notion asservissement** : 14 SVG hand-codés à valider au rendu (clair/sombre/smartphone) — `arduino-gpio-boot/sequence-boot`+`branchement-relais-pullup`, `arduino-programmation-non-bloquante/frise-3-taches`, `arduino-machine-a-etats/montage`+`diagramme-etats`, `arduino-eeprom/carte-memoire-eeprom`, `arduino-interruptions/montage`+`chronogramme-comptage`, `arduino-timers/cadence-millis-vs-timer`, `arduino-deep-sleep/profil-courant-veille`, `arduino-pid/boucle-fermee-pid`, `asservissement/boucle-fermee` (notion), `arduino-memoire/carte-memoire-sram`, `arduino-watchdog/chronogramme-watchdog`. Tous en **vrais caractères UTF-8** (aucun `\u`), relus post-écriture. Gabarit auto-contenu dark, markers en classes C69.
  - **Premiers jets 27/06 (relecture/correction médias familles MCU)** : 2 SVG **fonctionnellement corrects** (broches = code, **validés au rendu** clair/sombre/mobile) mais signalés par Tim **à retravailler visuellement avant publication** — `micropython-sortie-tor/montage-relais.svg` (trois interfaces LED/transistor/relais, densité du schéma) et `micropython-moteur-pas-a-pas/branchement-stepper.svg` (ULN2003). Acceptables en l'état. Gabarit auto-contenu dark, markers en classes C69.
  - **Versions stabilisées antérieures** : `cycle-v-projet.svg`, `bete-a-cornes-generique.svg` + 3 exemples, `pieuvre-generique.svg`, `pieuvre-bras-3-axes.svg`.
  - **Critères de la passe d'affinage** : homogénéité visuelle (poids des traits, tailles relatives, alignement des éléments), positionnement texte (centrages, gaps autour des bords), cohérence palette, lisibilité smartphone. Le contenu pédagogique reste inchangé — passe de polissage esthétique uniquement. Posée 24/05 (suite 2), enrichie 25/05 suite 2.

- [ ] Charte couleur / identité visuelle complète (au-delà de la palette callouts v1)
- [ ] Thème couleur école / ICAM
- [ ] Page personnalisée 404
- [ ] Favicon custom
- [ ] Logo en haut à gauche
- [ ] Tester l'export PDF académique (Pandoc + xelatex)
- [ ] Tester l'export HTML pour Moodle
- [ ] Ajouter un bouton "Suggérer une amélioration" (lien vers GitHub Issues ?)
- [ ] Lien permanent vers la version PDF de chaque fiche
- [ ] Sitemap publié pour SEO

## Décisions hiérarchie en attente (à arbitrer)

*Migrées depuis TODO 27/05 suite 3 (Q4 cleanup documentation).*

- [ ] Convention de tags AA (granularité, syntaxe)
- [ ] Niveau d'écart accepté entre référentiel et contenu produit (peut-on aller au-delà ?)
- [ ] Couleurs / charte graphique : faut-il aligner sur la charte école ?
- [x] ~~**Catégorie « Hors scope par délégation »**~~ — **instaurée 06/06** (arbitrage utilisateur, autonomie déléguée). Distincte de C15. Reçoit les 3 critères design (`PROJ-C03-3/1`, `/2`, `MME-C03-1/1`). Actée dans `conventions.md` §7 + `couverture-en-cours.md`.
- [x] ~~**Statut des 4 critères MME effleurés sans fiche centrale**~~ — **tranché 06/06** : les **5** critères (`RA-MME-C02-1/MME/2`, `/4`, `/6`, `RA-MME-C03-1/MME/2`, `/4`) actés *Effleuré terminal par délégation* — pas de fiche MME phase 2.
- [x] ~~**Statut de la fiche `schema-cinematique`**~~ — **tranché 06/06** : fiche **créée** dans le wiki (`mme/`, frontière interface + renvoi cours méca), `RA-MME-C02-1/MME/5` NC→C, wiki-link du hub résolu.

## Manipulations manuelles différées

*Migrées depuis TODO 27/05 suite 3. Limites techniques MCP filesystem (pas d'écriture binaire, pas de delete) ou configurations utilisateur ponctuelles.*

- [ ] **Déposer manuellement `cdcf-ecole-template.docx`** dans `content/ressources/templates/` (limite technique MCP filesystem : pas d'écriture binaire).
- [ ] **Supprimer `content/callouts-demo.md`** — fiche jetable, mission accomplie (validation visuelle palette v2). À faire à la main (pas d'outil delete MCP).
- [ ] **Supprimer le dossier `content/fiches/mia/` et son `index.md`** — MIA fusionné dans EEE depuis 26/05/2026. Patches navigation déjà effectués sur `content/index.md`, `content/hub/index.md` et `content/fiches/eee/index.md`. À faire à la main (limite MCP : pas d'outil delete).
- [ ] **Désactiver Dark Reader sur localhost et le site déployé**
- [ ] Configurer le plugin Templates d'Obsidian (`content/templates`)
- [ ] Installer **Pandoc + MiKTeX** pour export PDF académique (pas urgent)
- [ ] Produire le schéma d'illustration des conventions de flèches (mentionné dans `schema-bloc-fonctionnel`)
- [ ] Produire / trouver une photo de couveuse annotée (mentionné dans `schema-bloc-fonctionnel`)

## Vérifications visuelles différées (rendus Quartz + smartphone)

*Migrées depuis TODO 27/05 suite 3. Accumulées au fil des fiches, à conduire en passe dédiée avant publication aux élèves.*

- [ ] **Clic-test des wikilinks de forme `x/index` au rendu** — un wikilink `eee/index` ne résout pas par slug (404 ; 10 occurrences corrigées 10/06 en `fiches/eee/index`) et **le scan bash de la source ne peut pas détecter ce type d'échec** — seul le clic au rendu le voit. À vérifier avant publication sur les liens équivalents restants (homepage, `hub/index`, `mme/index`, `proj/index`, Voir aussi des hubs).

- [ ] **Clic-tests branche Conduite de projet (12/06)** — au rendu Quartz + mobile : SVG `fils-transverses-generique` (sur les 3 transverses, clair + sombre), `jalons-generique` (6 losanges), `mecatronique-generique`, `matrice-de-risques-station-meteo` (légende colonne unique 720×600), `mind-map-bras-3-axes` (feuille opérateur), `retroplanning-generique` (légende) ; **ancres double-tiret** `#cas-particulier--projet-école-sans-client-réel` (`bete-a-cornes`, `cahier-des-charges-fonctionnel` — C70) ; **liens slug-nu** des 3 index de domaine (`proj`/`meo`/`ese`) ; valeur `phases: integration-et-tests` (`acv-simplifiee`, `cable-management`) au filtre Explorer ; `text-anchor` en classes sur les 8 SVG planification + `matrice-de-risques` (vérifier le non-décalage des libellés).

- [ ] Vérifier le rendu des 4 squelettes (`concept`, `preuve-de-concept`, `dossier-technique`, `integration-et-tests`) en local Quartz + smartphone
- [ ] Vérifier le rendu de la fiche hub sur smartphone (vrai test responsif)
- [ ] Vérifier le rendu de la fiche `templates/callouts.md` (charte v2 + révision v2.1) en ligne et sur mobile
- [ ] Vérifier le rendu des templates `templates/fiche-trame.md` et `templates/fiche-notion.md` en ligne et sur mobile
- [ ] Vérifier le rendu de l'étape 4 de `specification-technique.md` (sous-liste F0/F1/F2/F3, callouts côte à côte)
- [ ] Vérifier le rendu de l'étape 6 + section Équipe de `specification-technique.md` (callout `[!info]` première utilisation, densité popovers TdM H4 Structurer)
- [ ] Vérifier le rendu de `bete-a-cornes.md` (3 SVG dans 3 callouts côte à côte)
- [ ] Vérifier le rendu de `gestion-de-projet.md` (3 blocs co-actifs, callouts v2.1, sous-listes Continu/Jalonné)
- [ ] Vérifier le rendu de `ecoconception.md` (3 blocs co-actifs, callouts v2.1, popovers wiki-links — `microcontroleur`, `pla`, `rohs`)
- [ ] Vérifier le rendu des 7 nouvelles fiches du 25/05 suite 2 (densité tableau `matrice-de-decision`, légende `matrice-de-risques-station-meteo`, mode sombre des 12 SVG)
- [ ] Vérifier le rendu de `pieuvre.md` approfondi (tableau 5 familles de milieux, double H3 Familles + Topologie)
- [ ] Vérifier le rendu de `concept.md` étapes 1-2 (2 H4 par étape, callouts v2.1, tableau matrice 3 solutions × 5 critères + scores pondérés dans l'`[!example]` étape 2)
- [ ] Vérifier le rendu de `concept.md` étape 3 (3 H4 pivot dense, tableau de conflits 4 colonnes × 2 lignes dans `[!example]`, callouts warning + tip + example + livrable)
- [ ] Vérifier le rendu de `concept.md` étape 4 (2 H4 économes, tableau de pré-dim 6 colonnes × 5 lignes dans `[!example]` — point de vigilance mobile)
- [ ] Vérifier le rendu de `concept.md` étape 5 (3 H4 clôture documentaire, TdM type 5 sections + TdM instanciée dans `[!example]`)
- [ ] Vérifier le rendu de `concept.md` section Pièges (8 entrées gras court + explication, format calque spec-tech)
- [ ] Vérifier le rendu de `concept.md` section Équipe (4 paragraphes thématiques, wiki-links rouges sur normes RoHS/REACH/DEEE et procédés usinage/impression-3d/soudure)
- [ ] Vérifier le rendu de `concept.md` post-cohérence (passe 25/05 suite 6, densité wiki-links après audit exhaustif convention 6)
- [ ] Vérifier le rendu de `preuve-de-concept.md` approfondi (5 étapes complètes, callouts v2.1, 6 nouveaux wiki-links sur outils/instruments)
- [ ] Vérifier le rendu de `preuve-de-concept.md` section Pièges (11 entrées gras court + explication)
- [ ] Vérifier le rendu de `preuve-de-concept.md` section Équipe (4 paragraphes thématiques, wiki-links rouges sur 6 nouvelles cibles)
- [ ] Vérifier le rendu de `integration-et-tests.md` étape 1 (2 H4 économes, `[!example]` 3 paragraphes structurés par branche, calendrier semaine n°12-15)
- [ ] Vérifier le rendu de `integration-et-tests.md` étape 2 (**3 H4 disciplinaires** élec/méca/info — cas inédit C11, `[!example]` 4 paragraphes structurés par discipline)
- [ ] Vérifier le rendu de `integration-et-tests.md` étape 3 (**3 H4 pivot dense pyramide compressée** niveau 1 / niveaux 2-3 / niveau 4, `[!example]` 5 paragraphes structurés par niveau + 3 statuts qualification)
- [ ] Vérifier le rendu de `integration-et-tests.md` étape 4 (2 H4 économes bilans, `[!example]` 4 paragraphes structurés par bilan + REX)
- [ ] Vérifier le rendu de `integration-et-tests.md` étape 5 (2 H4 économes rapport+soutenance, `[!example]` 3 paragraphes structurés)
- [ ] Vérifier le rendu de `integration-et-tests.md` section Pièges (11 entrées gras court + explication, ratio ~55 %)
- [ ] Vérifier le rendu de `integration-et-tests.md` section Équipe (4 paragraphes thématiques, wiki-links rouges sur nouvelles cibles : `cable-management`, `gabarit`, `firmware`)
- [ ] Vérifier le rendu de `securite-et-qualite.md` (3 blocs co-actifs Sécurité produit / Sécurité projet / Qualité documentaire, callouts v2.1, popovers nouvelles cibles wiki-links rouges)
- [ ] Vérifier le rendu de `securite-et-qualite.md` Bloc 1 (`[!example]` 4 axes pincement/couple/énergie/parades, calendrier semaine n°2-15)
- [ ] Vérifier le rendu de `securite-et-qualite.md` Bloc 2 (3 risques cotés, cas dérogation refusée semaine n°12, `[!warning]` règles non négociables)
- [ ] Vérifier le rendu de `securite-et-qualite.md` Bloc 3 (plan 6 points revues, tags Git v0.1→v1.0.1, `[!tip]` tracer au moment de la décision)
- [ ] Vérifier le rendu de `securite-et-qualite.md` section Pièges (11 entrées gras court + explication, ratio ~45 %)
- [ ] Vérifier le rendu de `securite-et-qualite.md` section Équipe (3 axes articulation cadence/matrice de risques/piloter sans écraser)
- [ ] Vérifier le rendu de `decomposition-fonctionnelle.md` (4 SVG arborescences 3 niveaux + triptyque mauvais/moyen/bon, densité SVG-3-axes en 920×500 = point de vigilance mobile)
- [ ] Vérifier le rendu du patch `concept.md` étape 1 (arborescence descendante en lieu et place de schema-bloc-fonctionnel, SADT/IDEF0 en alternative formelle)
- [ ] Vérifier le rendu du patch `schema-bloc-fonctionnel.md` *Voir aussi* (lien réciproque vers decomposition-fonctionnelle en première position)

## Idées éditoriales

- [ ] **Lexique étudiant : termes à proscrire** — démarrage suite à la décision du 22/05 de bannir « dérisquer » (non français) et « point dur » (peu lisible) dans toute production étudiante. À constituer comme petit document méthodologique (markdown court dans `_templates/` ou page de référence) listant les termes à éviter et leurs alternatives recommandées : « dérisquer » → « lever une incertitude » / « valider le fonctionnement », « point dur » → « incertitude », « phase N » → noms en toutes lettres dans la prose. À enrichir au fil des sessions.
- [ ] Mini-séries thématiques (ex: "tout pour piloter un moteur DC en 5 fiches")
- [ ] Parcours guidé : "Tu démarres ton projet ?" → suite logique de fiches
- [ ] Encadré "Vu en projet" (callout terrain) systématisé
- [ ] Page "ressources externes recommandées" (datasheets utiles, sites de référence)
- [ ] Version anglaise des fiches (très long terme, après stabilisation du contenu français)

## Discussions / décisions en attente

- [x] ~~**Harmoniser le registre tu / on (colonne realisation vs trames du V)**~~ — **tranché 10/06 : contraste conservé** (trames du V = on/infinitif ; colonne réalisation + hubs de branche = tu), **candidate C65** posée en conventions §8. Contexte d'origine : la colonne « Système embarqué » (hub `eee/index` + 5 fiches-étape `eee/realisation/`) est en **« tu »** (adresse directe, point d'entrée étudiant), les **trames du V** en **« on »/infinitif**. Décision à prendre : garder le contraste (colonne tu / V on) ou uniformiser tout le wiki (en tu ? en on ?). Signé 09/06 (production colonne embarquée). La pilote `concevoir-l-electronique` a déjà été reconvertie de « on » vers « tu ».
- [x] ~~**`aa:` de la colonne realisation**~~ — **tranché 10/06 : `aa: []` confirmé vide pour les 6 pages** (exception « instruments » sur `fiabiliser` rejetée ; C64-c confirmée, conventions §8). Contexte d'origine : le hub `eee/index` et les 5 fiches-étape sont en `aa: []` (fiches de méthode qui orchestrent les fiches outils ; les critères vivent dans les outils). À trancher en relecture : rattacher des critères EEE (ex. `PROJ/5`, `EEE/5`) ou laisser vide ? Source de vérité `_drafts/referentiel/couverture-en-cours.md`.
- [ ] **Normaliser le Voir aussi de `schema-bloc-fonctionnel`** — seuls liens relatifs du corpus (`[[../proj/…]]`) à passer en slugs nus ; relevé en relecture 10/06, jamais arbitré.
- [ ] **Inventaire systématique des fiches stub/placeholders avant publication aux élèves** — tâche structurante pour la phase de pré-ouverture. Posée 24/05 (suite 2) en conséquence de la décision « `draft: false` par défaut » : le BACKLOG est désormais le pilote de la maturité éditoriale. Passe à conduire en fin de phase de rédaction (avant ouverture aux élèves) sur l'ensemble du contenu : identifier les fiches stub, les sections `*[À rédiger]*`, les liens rouges, les images placeholder. Sortie : checklist d'éléments à finaliser ou à retirer. **➜ Liens rouges : CLOS 07/06 (suite 3)** — re-scan exhaustif `bash` (221 fiches, 100 % de `content/`), **0 lien rouge réel** confirmé + **43 annotations périmées** nettoyées (statut retiré / descripteur conservé, C59). **Restent ouverts** (3 autres volets de l'inventaire) : fiches **stub** (`afnor-nfx50-151` recensé), sections `*[À rédiger]*`/placeholders des templates, **images placeholder** (peigne Quartz des SVG — liste « Vérifications visuelles différées » ci-dessus).

- [ ] Convention des tags AA (séparer domaine vs code complet)
- [ ] **C50 — numéro de convention orphelin** : listé comme « promouvable » dans la note §8 du 06/07 mais **jamais défini** (introuvable dans conventions.md, JOURNAL, archive, BACKLOG). Soit slip de numérotation (un numéro avancé sans convention attachée), soit définition perdue lors d'un archivage ancien. Référence neutralisée en §8 le 10/06. À trancher : laisser C50 définitivement vacant (le réutiliser créerait une ambiguïté historique) — recommandation : **ne jamais réaffecter C50**, numéroter les nouvelles conventions à partir du plus haut atteint (C65).
- [ ] **Harmoniser le libellé des hubs en *Voir aussi*** — « Hub du parcours projet » (employé sur les trames du V validées) vs « hub du tutoriel » (variante dans la conclusion d'`integration-et-tests`) et autres renvois vers `hub/index`. Signalé en relecture §1 (09/06). Passe d'harmonisation globale à conduire d'un bloc (grep des libellés de hub), pas fiche par fiche.
- [ ] **Harmoniser le commentaire `draft` des templates** (`fiche-tuto.md`, et vérifier `fiche-notion.md` / `fiche-trame.md`) : le commentaire indique de commencer une nouvelle fiche en `draft: true` tant qu'elle n'est pas relue, ce qui contredit la convention `draft: false` par défaut (24/05, maturité pilotée par le BACKLOG) appliquée de fait sur toutes les fiches récentes. Incohérence signalée 28/05 suite 3.
- [ ] Faut-il un glossaire séparé pour les acronymes ?
- [ ] Quoi faire des fiches MME / ESE : pages très courtes pointant vers les collègues, ou ne rien créer du tout ?
- [x] ~~Pliage des callouts~~ — **résolu 05/06** : le pliable `[!type]-` fonctionne sur Quartz **à condition de ne pas être imbriqué** dans un autre callout (sinon figé ouvert ; cf. C40). Les corrigés d'exercice du module `cpp` l'utilisent en callout **frère** de l'énoncé. Le fond des blocs de code dans les callouts a aussi été corrigé (CSS `var(--light)`, cf. conventions §8). Reste ouvert : faut-il replier `[!example]-` **par défaut** dans les fiches-trame ? (préférence à trancher à l'usage).
- [ ] **Mode sombre des callouts v2** — non décliné actuellement. À traiter quand on aura du recul d'usage. SCSS Quartz à étendre, équivalent Obsidian éventuel à décider.
- [ ] **Convention de gras dans la section Pièges des fiches-notion** : « \*\*Piège en gras court.\*\* Phrase d'explication. » — figé dans le template `fiche-notion.md` mais pourrait constituer une « exception structurelle » dans la règle de gras minimal (« uniquement sur 1ère occurrence des concepts-clé »). À documenter formellement quand on écrira un guide éditorial unifié.
- [x] ~~**Convention de triptyque mauvais/moyen/bon dans les fiches-notion** : utilise `[!failure]` / `[!warning]` / `[!example]` (vu dans `bete-a-cornes.md`, mentionné dans `fiche-notion.md`). À figer formellement dans `callouts.md` si on confirme le pattern après 2-3 fiches-notion supplémentaires.~~ — promue § 2 conventions le 27/05 suite après épreuve 3/N (bete-a-cornes + caracteriser-une-exigence + decomposition-fonctionnelle).
- [ ] **Politique extensions navigateur pour validation visuelle** : recommander aux utilisateurs/collègues de désactiver Dark Reader (et similaires) sur le domaine du site — à documenter dans une page "comment lire ce site" éventuelle
- [ ] **Palette couleurs disciplines (élec / méca / info)** : 3 couleurs ad-hoc définies pour le flowchart-2-concept (bleu `#D6E8F5`, ocre `#F5E8D6`, vert `#D6F5E0`). À harmoniser avec la **palette callouts v2** (figée 22/05 suite) dans une vraie itération design, puis à appliquer de manière cohérente sur **toutes les phases avec éclatement disciplinaire** (concept, PoC, dossier technique, intégration probable).
- [ ] **Convention de suffixe disciplinaire** `(élec)` / `(méca)` / `(info)` en bout de label des nœuds : à acter comme convention si retenu après plusieurs flowcharts produits.
- [ ] **Layout flowchart phase 2 — non résolu** : le subgraph BRANCHES (étude par discipline) reste chaotique malgré plusieurs tentatives (sous-subgraphs LR/TB, liens invisibles `~~~`). Pistes à explorer plus tard : (a) tester le renderer ELK via `%%{init: {'flowchart': {'defaultRenderer': 'elk'}} }%%` (peut nécessiter une mise à jour de `@mermaid-js/mermaid-cli`), (b) abandonner la grille pure et accepter un autre mode de représentation, (c) sortir complètement de Mermaid pour cette phase (SVG main, draw.io, Excalidraw). **Confirmé 21/05 suite** : le même découpage en grille 3×3 régulière (phase 3) passe sans problème. C'est spécifiquement le 2×3 rectangulaire qui coince. Si on doit résoudre ph2, symétriser en 3×3 (ajouter un 3è étage : matrice + ?) pourrait suffire.
- [ ] **Règle de visualisation des flowcharts** : préférer le SVG généré par `npm run flowcharts` au rendu Obsidian quand le diagramme devient large (Obsidian ne permet pas le scroll horizontal facile). Le SVG s'ouvre dans n'importe quel visualiseur d'image avec zoom natif.

- [ ] **Mode d'application convention 6 (wiki-link à la 1ère occurrence par section/sous-section/callout)** : observation 25/05 suite 6 — 20 patches sur 27 de la session de cohérence finale concept ont porté sur la convention 6 (74 %). Hypothèse : la convention 6 s'applique mieux en **passe dédiée en sortie de fiche** qu'en application au fil de la rédaction. À confirmer sur `preuve-de-concept` : si le ratio passe wiki-links / total reste similaire, formaliser le mode d'application dans `conventions.md` § 2 (Mise en forme — Popovers et wiki-links).

- [x] ~~**Pattern MARKER + N segments** comme procédure standard pour déplacements de gros blocs (>30 ko) via MCP~~ — **documenté formellement dans `conventions.md` § 7** (26/05 suite). Capitalisation faite. 2ᵉ cas d'usage non encore rencontré mais pattern doc tracé pour réutilisation.

- [ ] **Re-calibrage seuil C14 dans la zone 30-40 ko** — valeur exacte du seuil pratique payload MCP non clairement identifiée. Épreuves connues : 24 ko net passe (26/05 suite, seg2 archive), 50 ko échoue silencieusement (25/05 suite 8). À éprouver à froid en testant des payloads progressivement croissants (35, 40, 45 ko) pour préciser la limite. Résultat utile pour calibrer la segmentation MARKER + N segments.

- [x] ~~**Convention emplacement `_drafts/referentiel/`** pour les documents école externes (référentiel compétences, grilles d'évaluation, etc.)~~ — **confirmée sur 2e cas 26/05 suite 2** : production en session du fichier de travail `_drafts/referentiel/couverture-en-cours.md` + `_drafts/referentiel/referentiel-normalise.md`. Convention de rangement stabilisée pour les documents de pilotage interne (hors site Quartz). À documenter formellement dans `conventions.md` § 6 (Publication / Quartz) si repéré sur un 3e cas.

- [ ] **Synthèse globale de `couverture-en-cours.md` à compléter en fin de passe B+A** — section placeholder déjà en place dans le fichier (26/05 suite 2). Contenu cible : (a) **table de couverture par domaine** (EEE/ESE/MEO/MME/PROJ) avec statut par critère ; (b) **liste des Hors scope** explicités ; (c) **liste des trous phase 1** à corriger (patches à appliquer aux fiches existantes) et **trous phase 2** prévus (alimente le TODO phase 2) ; (d) **note explicative sur sécurité-qualité** = trame sans critère central AA (insight 26/05 suite 2, le cadre AA n'est pas le seul critère de pertinence d'une fiche). Production estimée 0,5 session après passe B+A finie.

- [ ] **Écriture champ `aa:` dans front matter** sur ~22 fiches en passe groupée — opération mécanique après passe B+A finie. Structure du champ : sous-clés `aa.couvert`, `aa.effleure`, `aa.hors_scope` (liste de codes courts `<Code_RA>/<AA_DOMAIN>/<N°critère>`). Source de vérité = `couverture-en-cours.md`. À conduire en 1 passe niveau B mécanique. Estimé 0,5 session.

- [x] ~~**Amendement C14 — verrou Windows EPERM**~~ — **documenté formellement dans `conventions.md` § 7** (26/05 suite). Piège distinct du seuil de payload, symptôme explicite EPERM, remède = changer d'onglet Obsidian.

*Note : 4 conventions à éprouver précédemment listées ici (popovers sigles génériques, alias Quartz CrawlLinks, 2 images par fiche-notion d'outil, fil rouge station météo) sont désormais portées par `conventions.md` section « En cours d'éprouvage ». Pas de duplication.*

## Points ouverts des flowcharts (21/05 suite)

*Note : à arbitrer en relecture avant ouverture aux élèves, dans le cadre de l'inventaire systématique des fiches stub/placeholders.*

### Phase 4 — dossier technique
- [ ] **3 cercles S2 parallèles (S2e / S2m / S2r)** : si le layout est trop chaotique après rendu, alternative possible = cercle unique avec note textuelle sur les 3 validateurs (au prix de perdre l'enseignement).
- [ ] **D1 + 3 flèches `non`** vers les 3 branches : même problème potentiel qu'en phase 2.
- [ ] **L1 (dossier validé) vs L2 (commandes émises)** : maintenir 2 livrables séparés est défendable mais peut alourdir. À évaluer si on garde la séparation ou si on fusionne en "Dossier technique + commandes".
- [ ] **Validation écoconception non matérialisée** : transversale aux 3 validateurs sur leur périmètre. Peut donner l'impression que l'éco "tombe" sans contrôle. Option alternative : ajouter une mention "validation transversale éco" sur les 3 cercles S2 (labels chargés).
- [ ] **Étape E7 (passer commandes)** : action opérationnelle, parfois faite par le responsable projet et pas par l'équipe étudiante. À clarifier : qui clique sur "valider la commande" dans l'outil école ?

### Phase 5 — intégration et tests
- [ ] **Double sortie de D1** (`banc inadapté` vers E3 + `pièce défaillante` vers E2) : visuellement chargé. À évaluer après rendu.
- [ ] **D4 → L1 dans les deux cas** (oui/non, écart documenté) : visuellement bizarre. Alternative : un seul nœud `L1` précédé d'un nœud "documenter écarts si nécessaire".
- [ ] **Soutenance de mi-parcours / jalons intermédiaires** : pas matérialisés. À ajouter si l'école impose des points de contrôle en cours de phase.
- [ ] **Train final L1 → 3 bilans → E8 → E9 → S1 → L2 → L3** : 8 nœuds en série après L1, longueur LR potentiellement excessive. Si trop long, empiler L2 et L3 en colonne plutôt qu'en ligne.
- [ ] **Sous-graphe APPRO_FAB déséquilibré (2 branches)** : si le déséquilibre rend mal visuellement, possibilité d'ajouter un nœud info "Préparer environnement de déploiement final" pour symétriser.
