# TODO — TheSkillCodex

> Fichier privé (non publié). Listes courtes et actionnables. Items faits :
> supprimer plutôt qu'archiver — l'historique est dans `JOURNAL.md`.

## Prochaines sessions (ordre logique)

> **➤ MAJ 23/08** — **LOT 2a : LES 26 PASSES C109 FAITES, 18 FICHES TRADUITES SUR 26.** PC perso. **REMESURE D'OUVERTURE SOUS C110, CORPUS RECONSTITUÉ EN BAC À SABLE** (252 `.md` reconstruits depuis 10 lectures MCP, intégrité vérifiée à l'octet contre `get_file_info`). **QUATRE CHIFFRES HÉRITÉS CONFIRMÉS AU CHIFFRE PRÈS** : 242 fiches FR, 4 EN, 395 embeds, 376 blocs de code, 29 placeholders C29 sur 15 fiches. ⚠ **DEUX DIVERGENT** : **wikilinks 4 322 et non 4 323** (l'écart d'un préexistait entre le JOURNAL et la recette du script) ; **mots FR 291 099 et non 319 000**, soit **−8,7 %** — le chiffre hérité était annoncé sans sa règle. **Médiane 1 088 et non 1 192, `easyeda` 9 775 mots et non 13 028.** **Reste à traduire : 288 050 mots.** ⚠ **LE LOT 2a PÈSE 6 515 MOTS ET NON 5 000** (+30 %), dont 5 826 pour les 23 fiches courtes. ⚠ **LE MOTIF DE BASCULE DE `conduite/proj/index` AU LOT 2b TOMBE** : 405 mots, **4ᵉ du lot**, derrière `i2c` (521), `spi` (493) et `uart` (477). ⚠ **AUCUNE ANCRE INTRA-PAGE ET AUCUN BLOC DE CODE DANS LES 26 FICHES** — l'avertissement hérité ne mord pas ici, et le contrôle de fin de fiche se réduit à deux compteurs sur trois. **PASSE C109 : 104 occurrences de prose traitées (77 tirets, 27 points-virgules), 0 résiduelle**, résidu classé = 26 items de liste numérotée + 8 alt + 3 titres, **tous hors périmètre**. Coût : **+39 mots sur 6 476**, le texte est redécoupé, pas délayé. **TROIS ARBITRAGES TIM** — **AMDEC (c)** promue au §5.2, glose recalée en *the French acronym for FMEA* ; **les quatre entrées figées validées en bloc (a)**, périmètres mesurés avant validation (`dossier technique` 224/50, `écart` 103/39, `soutenance` 63/13, `REX` 26/5) ; **maître / esclave → controller / peripheral (b)** avec mention du terme historique une fois par fiche, 37 occurrences sur 9 fiches. **DEUX OUTILS NEUFS** : `tools/lot-2a.ps1` (génération groupée + `--controle`, sortie résumée) et `tools/draft-en.ps1` (bascule du flag `draft` sur `content/en/`, UTF-8 sans BOM et LF, quatre index du lot 1 exclus par défaut). **26 SQUELETTES GÉNÉRÉS EN UN LANCEMENT, 30 FICHES EN CONTRÔLE, 0 DIVERGENTE.** ⚠ **DÉFAUT DU SCRIPT TROUVÉ : UN WIKILINK DONT LE LIBELLÉ CONTIENT DU CODE INLINE ÉCHAPPE AU SUFFIXAGE** — le backtick casse le parsing. **29 occurrences sur 22 fiches**, aucune dans le lot 2a sauf `micropython-langage` (corrigée à la traduction). **À traiter dans `creer-fiche-en.mjs` avant le lot 2c**, qui en porte l'essentiel. ⚠ **CONDITION DE COURSE ENTRE SCRIPT ET ÉCRITURE, CRÉÉE PAR MA CONSIGNE** (« lance pendant que je traduis ») : `draft-en.ps1` est passé entre deux `write_file`, **la sortie du script annonçait trois bascules dont deux n'ont pas tenu**. *Une sortie d'outil dit ce qu'il a fait, pas ce que le fichier contient après coup.* ⚠ **UNE URGENCE QUE J'AI INVENTÉE ET QUE MON PROPRE OUTIL DÉMENTAIT** : j'ai présenté deux fois la mesure C105 comme non rattrapable alors que `draft-en.ps1`, écrit une heure plus tôt, rend la bascule réversible dans les deux sens. **Tim a par ailleurs rappelé que les doublons de recherche ne sont pas un souci** — objection refermée, `draft: false` conservé. ⚠ **ERREUR DE CHEMIN DE MA PART** : un `write_file` parti sur `content/en/memoire-en-PLACEHOLDER.md`, sorti de `content/` et supprimé par Tim. **18 FICHES TRADUITES, 5 340 MOTS EN, 186 LIENS, 10 EMBEDS, corpus EN à 30 fiches toutes en `draft: false`.** **Cinq entrées de glossaire dégagées en cours de lot** (*On the wire*, *In practice*, *Putting it to work is covered in*, marqueur C32 → *concept page*, *(transverse)* → *(cross-cutting)*), plus **« contrôleur » → microcontroller** là où le mot désigne le MCU et non le maître de bus. **RESTE 8 FICHES** : les 5 `conduite/meo` et les 3 index de branche. **RESTE À TIM** : `node tools/normalize-pilotage.js` puis commit/push.

> **➤ MAJ 22/08 (suite 2)** — **OUTILLAGE BILINGUE LIVRÉ ET ÉPROUVÉ, LOT 1 TRADUIT, LE SITE EST BILINGUE EN PRODUCTION.** PC perso. **DEUX OUTILS ÉCRITS PUIS EXÉCUTÉS AVANT LIVRAISON** — le corpus a été reconstitué à l'identique dans un bac à sable pour faire tourner les scripts sur les 243 fiches **avant** de les déposer, au lieu de publier une prédiction. `tools/creer-fiche-en.mjs` (**recette : 243 fiches, 4 323 liens, 395 embeds, 376 blocs de code, 0 divergente**, confirmée au chiffre près sur le dépôt réel) et `tools/derive-traduction.mjs`. **QUATRE MODES** : génération, `--recette`, `--controle`, `--recaler`. **LE SQUELETTE N'EST PAS UNE TRADUCTION** : c'est la fiche française avec les seules transformations structurelles appliquées, ce qui rend les trois compteurs égaux **par construction** et fait porter le contrôle de fin de fiche sur ce que la **traduction** a cassé. ⚠ **LE MARQUEUR DE COMMIT PRÉVU AU §9 ÉTAIT INAPPLICABLE** : la fiche EN se crée **après** la passe C109, donc sur un fichier FR non committé — `git log -1` y rend le commit d'*avant* la passe et tout le lot serait signalé dérivé dès le premier push. **Arbitrage Tim (a) : `source_sha256` de contenu**, le script n'appelle plus git du tout. ⚠ **LE SUFFIXE `-en` N'A D'EFFET DE BORD QUE SUR LES HUIT `index.md`** : `index-en.md` ne sert plus `/en/conduite/`, Quartz y réémet une `FolderPage()`. **Mesure qui tranche : zéro wikilink du dépôt ne vise `[[index]]` en forme courte**, les 37 passent par le chemin complet. **Arbitrage Tim (b) : index non suffixés.** ⚠ **QUATORZE ANCRES INTRA-PAGE `[texte](#section)` SONT INVISIBLES AU SUFFIXAGE** — ce ne sont pas des wikilinks, et le titre visé est traduit : elles auraient cassé en silence, **12 des 14 sont dans le lot 1**. Contrôle ajouté au script. **LOT 1 FAIT** : passe C109 sur les quatre index (**33 occurrences, dont 32 dans `embarque/index`**), génération, traduction, **2 939 mots**. ⚠ **DEUX CHIFFRES DU PROMPT ÉTAIENT FAUX** : **l'ordre des cinq trames** — `preuve-de-concept` pèse **46 574 o contre 44 154** pour `specification-technique`, c'est **la plus lourde et non la troisième**, la liste avait été triée décroissante puis réétiquetée dans l'ordre du V ; et **le point-virgule de C109 ne se reproduit pas** — **1 168 mesurés contre 798 annoncés** (le tiret tient : 2 235-2 377 contre 2 287). **242 fiches et 29 placeholders C29 confirmés**, décomposition exacte. ⚠ **C71 : « peu coûteux » TROUVÉ DANS `embarque/index`** — le hub le plus fréquenté de la branche, qui avait traversé la relecture de fond **et** l'expurgation du 18/08. **3ᵉ occurrence du motif** après `multimetre` et `analyseur-logique`. **ARBITRAGE TIM : `draft: false` PENDANT LE CHANTIER** — contre ma recommandation, motif accepté : le wiki n'est connu que de son auteur et le clic-test en ligne vaut mieux qu'un aller-retour `build --serve` par fiche. **Constante `DRAFT_EN` dans le script.** **À REBASCULER quand le corpus EN dépasse la vingtaine de fiches**, au-delà les doublons anglais pèsent sur la bande top-3. ⚠ **OBSERVATION DE TIM QUI AFFAIBLIT RÉELLEMENT MON ARGUMENT** : chercher `project` ne rend pas les fiches FR, le vocabulaire général diverge — **mais le lexique technique, lui, se recouvre** (`concept`, `interface`, `module`, `PID`, `GPIO`, `PWM`, `I2C`, `firmware`, `PCB`, `GRAFCET`), et c'est là que sont les requêtes. **`quartz.config.ts` : `RemoveDrafts` RENDU CONDITIONNEL** à `QUARTZ_DRAFTS` — sépare « pas publié » de « pas constructible en local », resservira à la rebascule. **CLIC-TEST PASSÉ — LE PARI CENTRAL DE L'ARCHITECTURE EST GAGNÉ** : l'embed s'affiche depuis `/en/conduite/` alors que le fichier image n'a pas bougé d'un octet. **Les 395 embeds suivront sans travail supplémentaire.** **ARBITRAGE TIM (b) : PROGRESSION PAR PROXIMITÉ DE LIEN**, plus module par module. ⚠ **LE PREMIER ANNEAU DEPUIS LES QUATRE INDEX FAIT 79 FICHES ET 104 000 MOTS, SOIT LE TIERS DU CORPUS** — « aucun lien mort depuis l'accueil » coûte cher, et il valait mieux le savoir avant de s'engager. **Découpe : lot 2a = 23 fiches courtes (≈ 5 000 mots, 23 liens fermés pour le prix d'une trame), 2b = les 5 trames (≈ 28 600), 2c = le reste (≈ 70 000).** **CONVENTION : C110** (une mesure de volume se publie avec sa règle de comptage). **La numérotation atteint 110.** **RESTE À TIM** : `node tools/normalize-pilotage.js` puis commit/push. ⚠ **ET LA MESURE C105 DE RÉFÉRENCE, CETTE SEMAINE** — voir l'item dédié : elle n'est plus prenable une fois le corpus EN grossi.

> **➤ MAJ 22/08 (suite)** — **CLÔTURE PRÉ-PUBLICATION FAITE, ET UN CHANTIER NEUF OUVERT : LE WIKI EN ANGLAIS.** PC perso, session de décisions sans production de fiche. **LES CINQ POINTS SONT TRANCHÉS.** ⚠ **LE CHIFFRE QUI RASSURAIT ÉTAIT LE PLUS FAUX** : « 3 placeholders C29, tous dans `kicad` » en vaut **29** — 3 KiCad + 13 du 20/08 (Teensy 4, STM32 9) + 13 du 20/08 suite (MicroPython 11, Raspberry 2). Le « 3 » datait du 19/08 et a traversé trois prompts. **La dette de captures n'est pas close, elle porte 29 items.** **C109 EN (a) GLOBAL, MAIS PONCTUATION SEULE** — arbitrage Tim : les puces ne sont **pas** reprises, *la liste est le lieu où l'écriture mécanique est légitime*. Périmètre mesuré : **3 085 occurrences de prose sur 236 fiches** (2 287 tirets + 798 points-virgules), les 1 314 gloses de `Voir aussi` restant licites. ⚠ **J'AI PUBLIÉ 3 045 PUIS CORRIGÉ EN 3 085** — dictionnaire clé par nom de fichier, les **huit `index.md`** s'écrasaient. **`ressources/index` DÉPUBLIÉE** (`draft: true`, pas `ignorePatterns` qui aurait cassé les 296 embeds) : 0 lien entrant, 0 sortant, 158 caractères. **CANAL RECHERCHE (c) NON INSTRUMENTÉ.** **ALIAS : 25 sur 11 fiches, mais 18 NE RÉSOLVENT RIEN** — six seulement sont employés comme cible sur 4 322 liens, et **zéro cible non résolue** dans le dépôt, donc **aucun alias ne manque**. Seul l'**auto-alias `filtrage` retiré** (il émettait une redirection de la page vers elle-même). **C71 LOGICIEL : 25 mentions sur 14 fiches et non 2 — TIM TRANCHE, ON NE TOUCHE À RIEN.** **CHANTIER ANGLAIS OUVERT** : wiki entier, **un dépôt / une branche / un build**, `content/en/` slugs suffixés `-en`, **images laissées en français** (arbitrage Tim, étudiants Erasmus). **C'est cette décision sur les images qui supprime les branches git.** **FUSION C109 + TRADUCTION, UNE PASSE PAR FICHE** (idée de Tim). ⚠ **JE ME SUIS TROMPÉ SUR L'EM-DASH ANGLAIS** : natif en prose éditée, rare dans l'écrit courant et devenu marqueur de texte généré — **C109 s'applique aux deux langues.** **VOLUMÉTRIE : 319 000 mots, médiane 1 192/fiche, 40 à 50 SESSIONS.** **LIVRABLE : `_drafts/traduction-en-regles.md`.** **RESTE À TIM** : `node tools/normalize-pilotage.js` puis commit/push ; **clic-test** — vérifier après build que `/ressources/` a disparu du graphe et de la recherche, et que `FolderPage()` n'émet pas de page de dossier auto-générée. **PROCHAINE SESSION = OUTILLAGE DE TRADUCTION PUIS LOT 1.** **➤ CORRECTIF POST-CLÔTURE** — arbitrage 13 revenu à la proposition de base : **entrée → 53 fiches courtes (16 000 mots) → modules**, le glossaire se rode sur du volume faible. **CLIC-TEST PASSÉ : `/ressources/` a bien disparu, `FolderPage()` ne réémet rien.** Commit fait.

> **⚠ REFUS TRACÉ — CANAL RECHERCHE NON INSTRUMENTÉ (22/08 suite).** Arbitrage **(c)** de Tim. Une liste de dix mots avait été proposée et fondée sur mesure (`marche parfois` et `milestone` **absents du corpus**, `reproductibilité` dans **1 seule fiche**, `via` sortant **104 fiches** par collision avec la préposition). **Motif du refus : le coût de la mesure répétée dépasse son rendement**, C105 ayant déjà montré le 21/08 que les quatre requêtes aboutissent dans la bande de tête sans rien changer. **Ne pas rouvrir sans un échec de parcours observé en vrai.**

> **⚠ REFUS TRACÉ — C71 NE S'ÉTEND PAS À LA GRATUITÉ LOGICIELLE (22/08 suite).** Balayage complet fait : **25 occurrences sur 14 fiches** (`ltspice` 4, `kicad` 3, `falstad` 3, `tinkercad` 2, `micropython-simulation` 2, `simulation-electronique` 2, `stm32-prise-en-main` 2, puis `stm32`, `stm32-arduino-core`, `gantt`, `specification-technique`, `analyseur-logique`, `pcb`, `esp32-ble`). **Tim tranche : état actuel conservé, rien à expurger.** Le balayage est fait, **ne pas le refaire**.

> **➜ MAJ 22/08** — **`easyeda` ÉCRITE DE BOUT EN BOUT : 62 ko, 53 MÉDIAS, LA PLUS VOLUMINEUSE DU WIKI.** PC perso. **LA DÉCISION DU 21/08 REPOSAIT SUR UNE PRÉMISSE FAUSSE, ET C'EST LE `.docx` DE TIM QUI L'A MONTRÉ.** J'avais recommandé (a) « retirer la mention EasyEDA » en croyant la fabrication externalisée, donc `kicad` et `easyeda` redondantes. **L'école grave elle-même**, à la Datron du Labo02, et le livrable n'est pas un Gerber mais un **`.json` remis en main propre**. Le contraste avec KiCad n'est ni « en ligne » ni le catalogue LCSC : c'est **simple face, fraisée, soudée à la main, dans le couloir d'à côté** — pastilles 3 × 1,5 mm, perçage 1 mm, piste 1 mm, isolement 0,5 mm, straps, miroir, tirage papier. **Arbitrage 1 (b) fiche assumée ICAM, 3 (a) fiche unique sans plafond, 4 (c) captures en mixte.** **PATRON À DEUX TEMPS** — `installer → construire` (21 sections) `→ échanger` : première fiche C88 sans temps `lire`, motif écrit (un outil hors ligne sans dépôt public n'a rien à donner à lire). **LES DEUX POINTS D'ÉCHEC ÉTUDIANT SONT LE CŒUR DE LA FICHE** : le **mode hors ligne** (n° 1, symptôme indirect — il manque une classe dans la recherche trois étapes plus loin, d'où un **auto-test par la barre de titre**) et le **choix CMS au lieu de traversant** (n° 2, section dédiée « Traversant, et rien d'autre » + piège replié). ⚠ **LE TEST D'ALT+U A RÉÉCRIT LE PIÈGE CENTRAL** : ce n'est **pas** une perte de données comme annoncé des deux côtés, mais **l'entonnoir vers l'erreur de mode** — le logiciel exige un DRC, puis demande de se connecter et de repasser en ligne. **Les deux problèmes de la fiche n'en font qu'un**, séparés par un quart d'heure. Corollaire : refaire `Convertir` n'est pas une précaution pédagogique, c'est **le seul chemin disponible** hors ligne. **CONVENTION : C109** (une phrase, une idée — tiret d'incise et point-virgule de milieu de phrase proscrits en prose de fiche, + corollaires de registre) + 7 notes §8. **La numérotation atteint 109.** **`pcb` RECALÉE** : ligne de tableau devenue lien, choix d'outil scindé fabricant extérieur / gravure à l'école, **frontière de fabrication généralisée** (« le fichier de fabrication, dont le format dépend de qui fabrique »), résidu C71 « payer » retiré, `Voir aussi` complété. ⚠ **UN EMBED CASSÉ RATTRAPÉ PAR LISTAGE, PAS PAR LE RENDU** — `verifier-changer-mode-offline.png` converti en `.jpg` entre deux tours. **Contrôle de fin de fiche posé : 53 embeds contre 53 fichiers, chacun utilisé une fois.** ⚠ **QUATRE NOMS DE FICHIERS CORRIGÉS** dont un **sans nom du tout** (`.png`, extension seule). **MODE DÉGRADÉ ACTÉ** : quatre photos de provenance web restent en place jusqu'à la rentrée, dont une **filigranée « dreamstime »** (illisible à l'œil, lue au pixel). **CLIC-TESTS VALIDÉS** (fiche + prose). **KICAD REPORTÉ POST-RENTRÉE (arbitrage Tim 22/08, gain de temps).** **PROCHAINE SESSION = CLÔTURE PRÉ-PUBLICATION.** **RESTE À TIM** : `node tools/normalize-pilotage.js` puis commit/push ; **supprimer** `_drafts/retire-modes.svg`, `_drafts/retire-revenir-hors-ligne.svg` et l'ancien `download-website.png` s'il traîne.

> **⚠ REDESSIN DE STYLE C109 — CHANTIER OUVERT, NON LANCÉ (22/08).** La règle « une phrase, une idée » est **éprouvée sur `easyeda` uniquement**. Les 241 autres fiches sont écrites à l'ancien style, tirets d'incise compris. **Ce n'est pas un bloquant de publication** : les deux styles se lisent, et rien n'est faux. À arbitrer à la clôture : redeploiement global, redeploiement borné aux fiches de tête, ou statu quo avec application aux fiches neuves seulement. *Ne pas lancer un balayage de tirets sans cet arbitrage — le tiret de glose des `Voir aussi` et le tiret de liste sont licites, un remplacement mécanique les casserait.*

> **➜ MAJ 21/08 (suite)** — **LES SIX DERNIÈRES : LE LOT DE PRISES DE VUE EST CLOS.** PC perso. **4 CAPTURES DÉPOSÉES ET INTÉGRÉES** — **#37** (traceur PID sur **procédé simulé**), **#40** (NodeMCU, montage IDE + photo), **#44** (nRF Connect), **#47** (GIF servo, **vidéo d'archive de Tim**). **#48 REPORTÉE POST-RENTRÉE** (Tim n'a pas d'Uno, et la carte **est** le sujet de la photo — une Nano n'a pas d'embase de shield). **#49 SUPPRIMÉE** — l'objet n'existe pas, et annoter quatre organes sur une photo trouvée en ligne présenterait des **suppositions comme des faits** ; la fiche est complète sans elle (C68 satisfaite deux fois). **ÉTAT : 27 déposées, 0 restante, 30 reportées, 1 supprimée** — contrôle 27 + 30 + 1 = **58** = #1→#60 moins #8 et #51. **CONVENTIONS : C107** (un montage n'est licite que si la couture se voit) et **C108** (annoter, c'est affirmer) + 6 notes §8. **La numérotation atteint 108.** ⚠ **LES DEUX PRÉCAUTIONS HÉRITÉES ÉTAIENT L'UNE FAUSSE, L'AUTRE À CÔTÉ** (C106 appliquée le lendemain de son écriture) : nRF Connect affiche **toujours** l'hexadécimal et y **ajoute** le texte — le test est que `(0x) 31-32` soit **suivi de** `"12"` ; et `arduino-pid` **contredisait son propre piège**, ses deux blocs cadencés écrivant `tCalcul = millis();` là où le pas constant est déclaré « non négociable » → corrigé en `tCalcul += DT_MS`. ⚠ **`arduino-pid` reçoit le sketch du procédé simulé** en **bloc distinct (b)** sous l'embed : la fiche sort à **14 483 o**, soit **483 o au-dessus** de la bande `tuto` de C95 — **dépassement arbitré (a), pas un précédent**. Le gain du modèle était **contraint par la fiche** : l'intégrale bornée à 200 ne laisse le terme I porter que `Ki × 200` = 120, donc un moteur réaliste aurait figé la boucle à 127,7 au lieu de 150. **LE MEILLEUR MOMENT EST UN DÉSACCORD OÙ PERSONNE N'AVAIT TORT** : la plage par défaut d'`attach` vaut **544-2400 µs sur AVR** (mon chiffre, vérifié en source) mais **1000-2000 µs sur ESP32Servo** (le geste de Tim, juste) — y élargir à 500-2500 **double exactement** la plage, et le GIF a été tourné sur ESP32. Bloc neuf dans `arduino-bibliotheques`. ⚠ **RÉSERVE TRACÉE SUR #40** : le montage réunit un sélecteur *Generic* et une console produite sous *NodeMCU 1.0* — **accepté par Tim**, même binaire, même LED. ⚠ **GIF #47 gardé à 4,61 Mo sans réencodage** (arbitrage Tim), sous la barre C87. **RESTE À TIM** : `node tools/normalize-pilotage.js`, puis commit/push ; **clic-tests** — `arduino-pid`, `arduino-bibliotheques`, `esp32-ble` (largeur 480, seul le rendu dira si les UUID sont lisibles), `esp8266-prise-en-main` (embed à 640). **PROCHAINE SESSION = `easyeda`, TUTORIEL SCÉNARISÉ C88.**

> **⚠ JOURNAL À 161 ko — 61 % au-dessus du seuil de §6 (21/08 suite).** L'archivage est reporté post-rentrée par décision (voir *Tâches techniques*), et le flux 1-pour-1 **stabilise sans faire maigrir** : au rythme de ~5 ko par session, le fichier atteindra ~200 ko à la rentrée. Ce n'est pas un bloquant — seule la lecture d'ouverture en pâtit, et elle se fait en `head=250`. À rouvrir si une session démarre lentement.

> **➜ MAJ 21/08** — **SESSION COURTE : LA MESURE DE RECHERCHE, #42/#43, ET LE PLAN JUSQU'À LA RENTRÉE.** PC perso. **LA MESURE EST FAITE ET ELLE A DISQUALIFIÉ MON PROPRE CRITÈRE.** Les quatre mots tapés dans `Ctrl+K` — **aucune** des deux conditions de fermeture que j'avais écrites n'est remplie (`bruit` rend `filtrage` en **3ᵉ** et non en 1ʳᵉ, `couple` ne rend **aucune** fiche Méca), **et les quatre requêtes aboutissent quand même** : `couple` → moteur pas à pas en 2ᵉ, `marche parfois` → chien de garde puis `fiabiliser-et-deboguer`, `bruit` → alimentation / capteur numérique / filtrage, qui forment un **diagnostic différentiel**. **Une palette `Ctrl+K` n'est pas une redirection, c'est une liste courte — la bonne unité est le top 3, pas le rang 1.** **ARBITRAGE TIM : (a) rien de plus**, le callout *défaut intermittent* suffit. ⚠ **J'AI PLAIDÉ CONTRE MA PROPRE PROPOSITION (b)** : un `aliases:` n'est **pas un mot-clé de recherche**, c'est une **table de résolution de liens** — `[[reproductibilité]]` deviendrait un wikilink valide. C'est ce qui a produit mes 69 faux positifs du 20/08. **Refus tracé avec son motif ci-dessous — ne pas rouvrir sans chiffre neuf.** **#42 ET #43 DÉPOSÉES ET INTÉGRÉES** (extraits datasheet L298) ; embeds, alts et attributions C74-c étaient posés d'avance, rien d'autre à éditer. **RÉVISION RELEVÉE : édition de JANVIER 2000**, pas la « révision 5 (octobre 2023) » que la fiche annonçait — **chiffre jamais sourcé**, corrigé. ⚠ **LE RISQUE ANNONCÉ N'ÉTAIT PAS LE RISQUE RÉEL** : le manifeste alertait sur la renumérotation des tables, or la fiche **ne cite aucun numéro de table** — elle cite **onze valeurs**, toutes vérifiées lisibles dans les extraits. **QUATRE FICHES ÉDITÉES** — `lire-une-datasheet` (révision, alt de #42 recalé sur l'image, piège *Télécharger n'importe quel PDF* recentré sur *savoir laquelle on lit*, **100 nF non inductif** sur Vs et pas sur Vss — précision venue de la table `PIN FUNCTIONS` lue à part), `analyseur-logique` et `multimetre` (**expurgation C71**). ⚠ **DEUX DÉFAUTS C71 RÉSIDUELS TROUVÉS EN PRÉPARANT UNE PRISE** — « ~10 € » et « pas à acheter pour le projet » dans `analyseur-logique`, « peu coûteux » dans `multimetre` ; les trois sœurs (`oscilloscope`, `generateur-de-signaux`, `instruments-de-mesure`) sont **indemnes**. L'expurgation du 18/08 avait visé le hub MCU, pas les instruments. **MONTAGE #42 PROPOSÉ PUIS RETIRÉ PAR MOI-MÊME** — la fiche trie les broches en **quatre familles** là où la table ST les ordonne par numéro : un index sous une explication qui le surclasse. **ARBITRAGES TIM** — **#37 en (a) procédé simulé** et **reste active** (aucun matériel) ; **#12, #36, #38, #45 reportées post-rentrée** ; **#44 à la prochaine séance** ; **`easyeda` (b) produire la fiche** contre ma préférence (a) ; **contrôle Phase 0 en (a) léger** ; ordre : **images → EasyEDA → KiCad → clôture**. **CONVENTIONS : C105** (une mesure de recherche se prend sur la bande de tête) et **C106** (une alerte héritée se vérifie contre la fiche avant d'être exécutée) + notes §8. **La numérotation atteint 106.** **ÉTAT : lot actif 29, déposées 23, restantes 6** — #37, #40, #44, #47, #48, #49, **aucune ne dépend plus de l'école**. Contrôle de cohérence : 29 + 29 reportées = 58 = #1→#60 moins #8 et #51. **RESTE À TIM** : commit/push (engagement pris) ; **lancer d'abord `node tools/normalize-pilotage.js`** — j'ai posé **74 NBSP** dans les quatre fichiers de pilotage (27 TODO, 29 JOURNAL, 14 conventions, 4 BACKLOG) et le hook de pre-commit les a arrêtés. ⚠ **La liste `TARGETS` du script ignorait les trois feuilles de route de `_drafts`**, dont le manifeste des captures, aussi ancré que le TODO : trois chemins ajoutés le 21/08. **À refaire à chaque création de fichier de pilotage.** **Puis clic-test de `lire-une-datasheet`** (deux images neuves + trois passages réécrits) et d'`analyseur-logique` (tableau *S'équiper* refait).

> **⚠ REFUS TRACÉ — `aliases:` DE RECHERCHE, ne pas rouvrir sans chiffre neuf (21/08).** Proposition écrite par moi le 20/08 comme « un patch de deux minutes », **retirée par moi le 21/08 et refusée par Tim**. Motif : dans Quartz **comme** dans Obsidian, un `aliases:` est une **table de résolution de wikilinks**, pas un index de recherche — poser `reproductibilité` sur `fiabiliser-et-deboguer` rendrait `[[reproductibilité]]` résolvable, et créerait une **seconde porte invisible depuis les liens** (4ᵉ occurrence du motif, cf. les 69 faux positifs du 20/08). Gain nul en face : l'étudiant en panne tape « marche parfois », et ça rend `chien-de-garde` puis `fiabiliser-et-deboguer` en 1ʳᵉ et 2ᵉ.

> **➜ MAJ 20/08 (suite 2)** — **PARCOURS ÉTUDIANT : LES DOUZE SCÉNARIOS TRAVERSÉS, LES QUATRE LOTS PATCHÉS.** PC perso. **Les trois sessions du régime tenues d'affilée** sur décision de Tim. **LIVRABLES NEUFS** : `tools/parcours-etudiant.mjs` (graphe des wikilinks + BFS sur 26 couples), `_drafts/parcours-etudiant.md` (39,3 ko, les douze traversées), `_drafts/parcours-etudiant-rendu.md` (grille des cinq canaux, **prédictions sous séparateur** pour rester réfutables). **LE RÉSULTAT EN UNE PHRASE : LE WIKI RELIE BIEN, IL ÉTIQUETTE MAL.** Graphe sain — 241 fiches atteignables sur 242, 4 290 liens, **aucun scénario n'échoue faute de lien**. Les trois qui peinent le font parce que **le mot de l'étudiant n'est pas sur la porte**. ⚠ **MON OUTIL A RENDU 69 FAUX POSITIFS SUR 70** : `fonction.md` porte `aliases: [FP, FS, FC]` et `caracteriser-une-exigence.md` `aliases: [critere, niveau, flexibilite]` — Quartz **et** Obsidian résolvent les alias. **C'est l'incohérence avec les 5 liens morts d'`audit-wikilinks.mjs` du 18/08 qui a déclenché la vérification**, pas un doute de principe. Un seul réel : `microcontroleur → [[xiao]]` (le hub s'appelle `xiao-esp32-s3`). **QUATRE LOTS PATCHÉS, 6 FICHES, 10 ÉDITIONS** : **A** lien mort ; **B** les portes mal étiquetées (« preuve de concept » liée aux livrables 3/7 et 5/7 du hub embarqué où le mot était en texte nu ; callout « attention au mot déboguer » à l'étape 6 ; AMDEC nommée sur `conduite/index` ; frontière Méca ouverte **depuis le piège** de `arduino-moteur-pas-a-pas`) ; **C** borne de périmètre AMDEC levée sur `securite-et-qualite` ; **D** callout *défaut intermittent* dans `fiabiliser-et-deboguer` (câblage, alimentation, découplage, PCB) + frontière compilation/exécution en miroir. **MESURE APRÈS PATCH** : **#4 et #9 passent de 3 à 2 clics** depuis l'accueil, #4 de 2 à **1** depuis le hub, **0 lien mort, 0/0/0 en santé**, +13 liens dont la décomposition tombe juste (2+1+1+1+3+5). **ARBITRAGES TIM** : exception **(c)** au gel, bornée à l'erreur de microstepping (`arduino-moteur-pas-a-pas` donnait « microstepping plus fin » pour **augmenter** le couple et écrivait 40 lignes plus bas qu'il le **réduit**) ; grille de rendu **validée** au titre de la relecture de fond. **CONVENTIONS : C103** (une porte se juge à son libellé, pas à son existence) et **C104** (la délégation hors périmètre se pose **depuis le piège**, pas en section séparée) + 6 notes §8. **La numérotation atteint 104.** ⚠ **LE LOT D N'EST PAS CLOS** : l'indexation par symptôme reste à trancher (voir item dédié ci-dessous). **CLIC-TESTS VALIDÉS.** **RESTE À TIM** : commit/push. **Recette du prochain lancement : 242 / 26 / 0 lien mort / 0-0-0.**

> **➔ MAJ 20/08 (suite)** — **SECONDE SESSION DE PRISE DE VUE : LE LOT LOGICIEL EST VIDE.** PC perso. **7 CAPTURES DÉPOSÉES ET INTÉGRÉES** : #9 et #60 (`arduino-bibliotheques`), #11 (`cpp-logs`), #10 (`esp32-serie`), #50 (`ide`), #28 (`esp32-prise-en-main`), #31 (`esp32-wifi`). **2 dossiers créés** (`cpp-logs\`, `esp32-serie\`). ⚠ **SIX CHIFFRES FAUX HÉRITÉS DU PROMPT** : « 4 Teensy » comptait un réemploi (3+4+9 = 16 pour un total annoncé de 15), et « lot actif 44 / 12 déposées » étaient **décalés de deux en parallèle** — la soustraction tombait juste, donc l'erreur était invisible. **Un total juste ne prouve rien sur ses termes.** ⚠ **LE MANIFESTE N'AVAIT JAMAIS REÇU SA SECTION « STM32 REPORTÉ »** alors que les 9 embeds étaient bien reconvertis dans `content/` : arbitrage exécuté, non consigné — écrite. **QUATRE FICHES ONT GAGNÉ DU CONTENU NEUF, PAS DES IMAGES** : `arduino-bibliotheques` (boîte des dépendances + piège « Install without dependencies réussit », message exact), `cpp-logs` (**colonne fausse corrigée** dans *Anatomie*, entrée `fatal error` absente des cinq listées, **trois cas verbatim remplaçant deux fabriqués**, encart *COPY ERROR MESSAGES*), `esp32-serie` (**deux localisations fausses** corrigées, piège du plancher ADC + test de bon fonctionnement, sortie réelle de l'étape 4 et piège du sélecteur de fin de ligne), `esp32-wifi` (le « Non sécurisé » enfin expliqué). **ARBITRAGES TIM** : **#51 annulée** ; **MicroPython (11) et Raspberry Pi (2) reportés post-rentrée**, 13 embeds reconvertis ; alt de #9 recalé sur l'image ; embed #11 maintenu sous *Anatomie* en registre spatial. ⚠ **UNE FALSIFICATION DE VALEURS DÉTECTÉE ET ANNULÉE AVANT COMMIT** (des `1` substitués à des `0` sur #10) — périmètre vérifié, **une seule image**, toutes les autres retouches étant des encadrés et flèches. **CONVENTIONS : C101** (masquage obligatoire des données personnelles) et **C102** (soustraire / superposer permis, **altérer interdit** ; un montage se déclare dans l'alt) + 7 notes §8. **La numérotation atteint 102.** **ÉTAT : lot actif 33, déposées 21, restantes 12** — les douze demandent toutes du matériel d'école. **CLIC-TESTS VALIDÉS** sur les 6 fiches du jour. **RESTE À TIM** : commit/push. ⚠ **À VÉRIFIER** : un `é` illisible dans `micropython-prise-en-main` et `micropython-repl` (normalisation Unicode mixte ?) — **ne pas « corriger » avant d'avoir regardé au rendu**.

> **➔ MAJ 20/08** — **PREMIÈRE SESSION DE PRISE DE VUE RÉELLE. Tim shoote, Claude cadre et vérifie.** PC perso. **LIVRABLE NEUF** : `_drafts/guide-prise-de-vue-par-outil.md` — le manifeste relu **par outil puis par page**, l'axe qui manquait à la feuille de route triée par logistique. ⚠ **DEUX CHIFFRES FAUX CORRIGÉS À L'OUVERTURE** : « 49 lignes dont 8 réemplois » était faux (lire **49 pivots + 8 réemplois**, ensembles disjoints), et la liste des dossiers manquants héritée du 19/08 en oubliait un — **`micropython-prise-en-main`, qui porte 4 prises**. Les deux venaient d'une soustraction. **9 IMAGES DÉPOSÉES ET INTÉGRÉES** : #1 `ide`, #2 #3 #4 #52 #53 #57 #58 #39 #59 sur `esp32-prise-en-main`, #5 #54 #55 #56 sur `esp8266-prise-en-main`. **DEUX FICHES SUBSTANTIELLEMENT RÉÉCRITES** — `esp32-prise-en-main` (étape 3 en deux chemins, étapes 4-5 refondues, **4 pièges neufs**) et `esp8266-prise-en-main` (bascule sur *Generic ESP8266 Module*, étape 4 en deux chemins, piège des étiquettes retourné, exercice 2 et corrigé passés de `D5` à `14`). **SEPT DÉFAUTS DE FOND TROUVÉS PARCE QUE LE MATÉRIEL ÉTAIT BRANCHÉ**, aucun trouvable à froid : paquet leurre « Arduino ESP32 Boards », port `Unknown` sur tout pont tiers (**CH340 comme CP2102**), reset propre d'esptool qui invalidait la manoeuvre BOOT décrite (**correction apportée par Tim**), `Wrong boot mode detected (0x13)`, `'LED_BUILTIN' was not declared in this scope`, liaisons série Bluetooth expliquant les cinq `Unknown`, et *Generic* qui ne définit pas `D0`-`D8`. **ARBITRAGES TIM** : bilinguisme FR/EN assumé dans les captures ; **Teensy et STM32 reportés post-rentrée** ; #1 scindée en trois ; une seule capture de moniteur série dans le wiki, maintenue ; erreur visuelle de #55 acceptée et tracée. **CONVENTIONS : C99** (une prise de vue est une relecture technique) et **C100** (format et poids des médias) + 5 notes §8. **La numérotation atteint 100.** ⚠ **13 EMBEDS RECONVERTIS EN PLACEHOLDERS C29** : reporter une famille dont les embeds sont posés d'avance laisserait des **images cassées** en production — seul cas que C90 ne couvre pas. ⚠ **Le poids des médias a coûté plus de temps que les captures** : GIF de 7,2 Mo abandonné, photos à 1,76 Mo, passage en PNG multipliant le poids par six. **PROCHAINE SESSION = SUITE DU LOT IDE ARDUINO** (#9 à #12, puis #36-38 au traceur) — prompt rédigé. **RESTE À TIM** : commit/push ; **supprimer `content/ressources/img/esp32-prise-en-main/blink-code.png`** (écartée — le code est déjà en bloc copiable) ; **clic-test d'`esp32-prise-en-main` et `esp8266-prise-en-main`**, dont les étapes 3 à 5 ont entièrement bougé.

> **➔ SÉQUENCE JUSQU'À LA RENTRÉE — voir `_drafts/feuille-de-route-sessions.md`** (établi le 19/08, enveloppe **10-15 sessions**). Ordre : ~~notion `[T]` non bloquante~~ ✓ → ~~`boucle-ouverte`~~ ✓ → ~~fiches de lecture C85 (×2)~~ ✓ → **intégration des captures (3-5 sessions élastiques) ← PROCHAINE, Tim shoote à partir du 19/08** → parcours étudiant → clôture pré-publication, **marge de 2-5 sessions laissée vide**. ⚠ **Les 79 % de couverture AA ne sont pas un chantier** : 0 non-couvert, les 21 % restants sont des délégations et hors-scope actés. Le socle référentiel est **fini**.

> **➔ MAJ 19/08 (suite 4)** — **LOT « PORTE BORGNE » : LES 22 LIENS POSÉS.** PC perso. **LE SCRIPT A TOURNÉ POUR LA PREMIÈRE FOIS.** Recette confirmée sur le fond, **démentie sur un chiffre** : **134 fiches non-famille et non 132** — `boucle-ouverte` et `cpp-lire-un-programme` ont été écrites **après** le passage de l'audit, le corpus avait bougé de deux fiches. **Un test de recette se date : un écart peut venir du corpus et non du portage.** 241 fiches indexées (`templates/` exclu), **30 candidats**, **19 faux positifs** (15 + 4 apportés par `cpp-lire-un-programme`). ⚠ **Le « 24 liens » du prompt valait 26** : trois lignes réclament plus d'un jumeau (`alimentation-electronique` et `decouplage` → micropython **+ xiao**, `wokwi` → trois). **LIVRABLES** : **19 fiches éditées**, 22 portes borgnes fermées sur **15 fiches**, **2 renvois C85** (les encarts « garde composée » des deux `machine-a-etats` pointent désormais les fiches de lecture), **réciproque `filtrage` depuis `asservissement`** (3ᵉ mention au TODO, enfin posée), **allègement (b) des deux jumeaux non bloquants**. **ARBITRAGES TIM** : **(b)** retarget vers la notion `[T]` sur les trois fiches PID ; **(b)** forme compacte sur `potentiometre` ; **(b)** allègement borné au seul § *À quoi ça sert ?*. ⚠ **5 des 26 « liens » sont des retargets, pas des ajouts** — `arduino-pid` → `asservissement` ×3, `arduino-sortie-pwm` → `pwm`, `arduino-gpio-boot` → `gpio` : quand la fiche emploie le **concept** et non le **geste**, la porte disparaît au lieu de s'élargir. ⚠ **1 refus motivé** : `wokwi` ne prendra pas `raspberry-pi-gpio` — Wokwi simule le **Pico**, pas le **SBC** ; exclusion à écrire dans le `.mjs`. ⚠ **Trois formes de jumeau en service**, chacune alignée sur sa fiche d'accueil (entrée par famille à la `timer` / parenthèse en fin de ligne / familles séparées par `·`) — candidate §8. ⚠ **Mon chiffrage de l'allègement était faux d'un facteur 3** (**−379 et −228 octets** mesurés pour ≈ −1 200 annoncés) : j'ai compté ce que je retirais sans compter ce que j'écrivais. **PROCHAINE SESSION = LES 2 CRÉNEAUX VIDES** (`chien-de-garde`, `temporisation`) — prompt rédigé. **RESTE À TIM** : commit/push ; **relancer le script** (prédiction **241 / 134 / 9 candidats**) ; **clic-test de `asservissement` et `potentiometre`**, dont les *Voir aussi* ont le plus bougé.

> **➔ MAJ 19/08 (suite 3)** — **LES DEUX FICHES DE LECTURE C85.** PC perso. **INVENTAIRE C77 D'ABORD** : **244 fiches balayées sur 244** (tout `content/`, `templates/` compris). ⚠ **Le « 243 » du prompt était faux** : 242 le 19/08 session A + 2 fiches créées en suite 2 = **244** — une soustraction et non un comptage, **3ᵉ occurrence** du motif après le « 31 » qui valait 35. **33 encarts « Comment lire ce code » sur 30 fiches**, 17 en §7 et 16 en §13, **0 ailleurs**, sous **deux formes** (`> [!info]` ×24, gras de paragraphe ×9). **LE TRI INVALIDE LA PRÉMISSE DU BRIEF** : 26 encarts sont des **idiomes métier**, **1 seul est un mécanisme de langage franc** (`arduino-programmation-non-bloquante`, celui déjà connu), 6 mitoyens. Le bloc 2 a donc été **rédigé, pas compilé** — la matière réelle est dans les **paires jumelles** Arduino/MicroPython, pas dans les encarts. C85 l'avait écrit dès le 17/08 (« la quasi-totalité des encarts existants sont des encarts d'idiome »). **ARBITRAGES TIM** : garde composée **(b)** bornée (le bloc 2 couvre « lire une condition composée », les encarts ne bougent pas) ; gestion de ressource **(a)** ; exceptions **(a)** ; va-et-vient **(b)**, hors périmètre. **LIVRABLES** : `cpp-lire-un-programme.md` (**16,7 ko**) et `micropython-lire-un-programme.md` (**18,4 ko**) + **6 fiches de maillage** (`cpp` item 8, `micropython-langage` item 6, `arduino-programmation-non-bloquante` renvoi C85, `arduino-debug`, `micropython-debug`, `revue-de-code`). Blocs 2 et 3 **disjoints** entre sœurs (feu tricolore côté C++, ordonnanceur à 3 tâches côté MicroPython) ; seule la méthode est dupliquée (copie locale C47). **CONVENTION : C95** (calibre typé par type de fiche — notion 5-10 ko, tuto 8-14, tuto scénarisé C88 sans plafond). ⚠ **Dépassement signalé et arbitré** : 16,7 et 18,4 ko contre 10-12 annoncés, **Tim garde — arbitrage tracé, pas précédent**. ⚠ **Deux erreurs factuelles dans mes propres fiches**, prises à la relecture après écriture : « cinq numéros de broches » pour six, et un extrait `global` annonçant deux affectations en n'en montrant qu'une. **2 fiches validées au clic-test.** **PROCHAINE SESSION = LOT « PORTE BORGNE »** (22 liens sur 14 fiches, mécanique) — prompt rédigé. **RESTE À TIM** : commit/push ; lancer une fois `node tools/audit-portes-famille.mjs` (recette **132 / 45 / 30**).

> **➔ MAJ 19/08 (suite 2)** — **NOTION `[T]` NON BLOQUANTE + `boucle-ouverte` + BALAYAGE DES PORTES DE FAMILLE.** PC perso. **BALAYAGE INVERSE** sur les 8 fiches du brief (5 étapes de `embarque/realisation/` + 3 transverses de `conduite/`) : **les 3 trames transverses sont indemnes**, le défaut vit dans les **notions `[T]` du dossier `mcu/`**. Le motif n'est pas un accident à deux occurrences mais un **quatuor de créneaux** — `programmation-non-bloquante`, `chien-de-garde`, `temporisation` vides, `timer` correctement bâti et qui sert de contre-modèle. **AUDIT COMPLET ENSUITE** : **132 fiches non-famille balayées sur 132**, 237 liens vers une fiche de famille, **45 asymétries brutes → 30 réelles** (15 faux positifs, dont **12 pour le seul module `cpp/`** — le C++ *est* l'écosystème Arduino). **DEUX DÉFAUTS DISTINCTS** : **A, le créneau vide** (aucune notion `[T]` au-dessus — 8 occurrences, toutes sur `watchdog` et `temporisation`/`timers`) et **B, la porte borgne** (la notion existe mais ne cite qu'une famille — **22 occurrences sur 14 fiches**), dont `filtrage` ×2 et **trois fiches PROJ** pointant `arduino-pid`. Le défaut B est **mécanique et bon marché** : ajouter le lien jumeau, pas écrire de fiche. **LIVRABLES** : `embarque/mcu/programmation-non-bloquante.md` (12,2 ko, **aucun bloc de code**, 1 SVG) + **8 patches de maillage** ; `embarque/boucle-ouverte.md` (7,4 ko, 1 SVG) + **4 réciproques** ; **`tools/audit-portes-famille.mjs`** + section README. **CONVENTION : C94** (une porte de famille s'ouvre sur **toutes** les familles qui portent le concept). ⚠ **Mon premier rapport d'audit annonçait 45 défauts pour 30 réels** — 3ᵉ occurrence du motif *un audit qui ignore une convention du dépôt produit du bruit à hauteur de ce qu'il ignore*. ⚠ **Le script Node versionné n'a jamais été exécuté** (portage du Python de contrôle) : **test de recette = 132 / 45 / 30**. ⚠ **Un lien `arduino-watchdog` a été posé sciemment** dans `fiabiliser-et-deboguer` (arbitrage Tim) : **à retargeter** le jour où `chien-de-garde` existera. **PROCHAINE SESSION = fiches de lecture C85 (×2)** — prompt rédigé. **RESTE À TIM** : commit/push ; **clic-test des 2 fiches neuves et de leurs 2 SVG** (clair/sombre/mobile) ; lancer une fois `node tools/audit-portes-famille.mjs` pour valider le portage.

> **➔ MAJ 19/08 (suite)** — **RANG 1, SESSIONS B ET C : LES 23 CONVERSIONS ET LES 8 SVG.** PC perso, deux sessions menées d'affilée. **RE-BALAYAGE DE CONTRÔLE** : 242 fiches sur 242, **35 placeholders et non 31** — le 31 du prompt était une soustraction (108 − 16 − 60), pas un comptage ; le 35 se recompose sur les verdicts (23 conversions + 8 SVG + 3 KiCad + 1 non posé). **Recalage : 5 fichiers périmés et non 18** (suppressions et embeds se sont compensés). **LIVRABLES** : **23 blocs de code ou tableaux** écrits, **0 placeholder résiduel** dans ces fiches ; **8 SVG** créés et intégrés (`multimetre/face-avant` + `fusible-grille`, `oscilloscope/face-avant` + `ecran-pwm`, `generateur-de-signaux/face-avant`, `arduino-alimentation/trois-sources`, `micropython-alimentation/deux-sources`, `arduino-module/serigraphies-i2c`) ; `micropython-simulation:52` **supprimé** ; feuille de route des captures recalée en une passe ; **`_drafts/feuille-de-route-sessions.md` créé**. **10 fiches validées au clic-test par Tim, aucun défaut.** **CONVENTION : C93** (le placeholder dit un **besoin**, pas un emplacement ni un contenu — volet placement + volet contenu) + **extension de C69** (aucun élément en couleur dure, pas seulement les markers) + 5 notes §8. **La numérotation atteint 93.** ⚠ **Deux régimes (a)/(b) reclassés à la lecture du code** : `teensy-arduino-core` **sort** des valeurs au banc (aucune mémoire libre imprimée, seulement `F_CPU`), `esp32-arduino-core` **y entre** (`esp_get_free_heap_size()`). ⚠ **Le dry-run a servi à relire ma propre redite** — 4 fiches repassées, l'amorce du bloc existait déjà en prose. **ÉTAT : 3 placeholders restants dans tout `content/`** (KiCad, reportés). **La dette de médias se réduit aux seules prises de vue.** **PROCHAINE SESSION = notion `[T]` « programmation non bloquante » + balayage inverse des trames transverses** — prompt rédigé. **RESTE À TIM** : commit/push ; **remplacer `cdcf-ecole-template.docx`** ; les prises de vue quand il voudra.

> **➤ MAJ 19/08** — **RANG 1, SESSION A : RE-BALAYAGE, ARBITRAGES, EMBEDS POSÉS D'AVANCE.** PC perso. **242 fiches balayées sur 242** `.md` de `content/` (compte vérifié contre `search_files`, pas contre un code de retour) : **107 placeholders sur 60 fiches**, les 101 du triage retrouvés à la ligne exacte, **+6 nets** — tous en forme « photo » (`Prendre photo de`, `Prendre une photo de`, `Intégrer un GIF ou une photo de`), invisibles au motif du 18/08. Un **108ᵉ** trouvé en commentaire HTML (`schema-bloc-fonctionnel:67`). Contrôle de second rideau sans nouvelle prise : amorces `Ajouter / Illustrer / Montrer / Filmer / Enregistrer`, italiques courts, 41 lignes résiduelles à mot-média. **ARBITRAGES (9, format C91)** : GIF servo → à shooter ; module I²C et face avant de GBF → **SVG** ; `lire-une-datasheet:124` et `:201` → supprimés ; photo d'empilage `shield` → à shooter ; commentaire couveuse → placeholder visible ; **portée des embeds = (c-large)** ; **pivot #3 et pivot #48 → suppressions**. **LIVRABLE PRINCIPAL** : `_drafts/feuille-de-route-captures.md` **18,9 → 36,3 ko**, avec deux sections neuves — **`# À apporter par Tim — suivi`** (cases à cocher : prises par session, 8 valeurs à relever au banc, ce que Tim rédige lui-même, décisions ouvertes) et **`# Manifeste des embeds`** (chemin et largeur figés pour les 49 pivots + 9 réemplois — **le nom de fichier est le contrat** : shoote sous ce nom, l'image apparaît sans autre édition). **60 des 61 embeds posés**, 16 suppressions exécutées, sur ~40 fiches. **~59 absents attendus à `audit-medias.mjs` jusqu'aux prises de vue — régime nominal de (c-large), pas une régression.** **CONVENTION : C92** (un réemploi cross-dossier exige une capture **neutre de famille**) + notes §8. **La numérotation atteint 92.** ⚠ **Cinq réemplois sur douze sont tombés** au même motif : l'écran nommait une famille (Espressif, Paul Stoffregen, un Uno, « Hello, ESP32! »). ⚠ **Les numéros de ligne du manifeste sont périmés dans 18 fichiers** (16 suppressions + 2 attributions ajoutées) — **recalage en ouverture de session B**. **PROCHAINE SESSION = RANG 1, SESSION B** (23 conversions de captures de sortie texte en blocs de code) — prompt rédigé. **RESTE À TIM** : `micropython-simulation:54` à reclasser (dernier item non posé) ; les prises de vue quand tu voudras, feuille de route en main.

> **➤ MAJ 18/08 (suite 3)** — **FICHE TRANSVERSE `filtrage` CRÉÉE + EXPURGATION C71 DU HUB `microcontroleur`.** PC perso. **Plus aucun lien rouge connu dans le wiki** : les huit derniers pointaient vers `filtrage`. **LIVRABLES** : `content/embarque/mcu/filtrage.md` (notion `[T]`, gabarit `timer`, **aucun bloc de code** — la fiche sert Arduino *et* MicroPython, un bloc C++ en aurait fait une fiche Arduino déguisée) + **2 SVG calculés** (`bruit-et-retard`, `trois-bruits`) ; hub `microcontroleur` expurgé (tableau **7 → 6 colonnes**, argumentaires ESP8266 et PIC **reconstruits**, critère 6 « Prix » remplacé par **Encombrement et intégration**, maîtrise de l'équipe versée au critère 5) ; `positionnement.svg` corrigé (**2 mentions C71 invisibles depuis le markdown**) ; `cycle-v-projet.svg` patché (chevauchement de texte, `<desc>` faux, markers en classes C69) ; réciproques `filtrage` posées depuis `adc` et `precision-de-mesure`. **CONVENTION : C91** (arbitrage à la chaîne — une phrase, options (a)/(b)/(c), préférence facultative ; demande Tim, bornée aux décisions répétitives) + 6 notes §8. **La numérotation atteint 91.** ⚠ **Le brief hérité était faux sur trois points** — `content/index.md` ne porte **aucune image**, `conduite/index` embarque en **absolu** et non en `../`, et le `content/hub/` de l'exception §6 **n'existe plus**. ⚠ **Recodage du cycle en V écarté par Tim** : dette esthétique interne, non visible par l'étudiant ; seul le défaut réel a été patché, **et le TODO ne doit plus en reparler**. **PROCHAINE SESSION = RANG 1, SESSION A** (re-balayage + arbitrages + suppressions + réemplois) — prompt rédigé. **Puis session B** (23 conversions en blocs de code) **et session C** (6 SVG). **RESTE À TIM** : commit/push ; **clic-tests** — les 2 SVG de `filtrage`, le hub `microcontroleur` (tableau 6 colonnes + `positionnement.svg`), `conduite/index` (`cycle-v-projet.svg` patché), clair/sombre/mobile.

> **➜ MAJ 18/08 (suite 2)** — **TRIAGE DE L'INVENTAIRE DES CAPTURES + PROMOTION DE C29 + CLÔTURE DE LA RELECTURE.** Session d'inventaire et d'arbitrage, PC perso : **aucune prise de vue, aucune production de fiche**. **LIVRABLE PRINCIPAL** : `_drafts/feuille-de-route-captures.md` (18,9 ko) — les 101 placeholders triés un par un, groupés **par session de prise de vue** (un logiciel ouvert = une session) et non par fiche. **101 → 46 prises + 3 reportées** (KiCad), 23 blocs de code, 6 SVG, 12 réemplois cross-dossier, 11 suppressions. La seule session « IDE Arduino » couvre **12 prises**, soit un quart de la dette. **LES TROIS GISEMENTS VÉRIFIÉS** : (1) les 31 des prise-en-main sont réels mais **ne se ramènent pas à 6 prises — 15**, trois gestes traversant des logiciels différents et neuf placeholders n'appartenant à aucun geste répété ; **2ᵉ paire strictement identique trouvée** (`esp32-prise-en-main:92` / `teensy-prise-en-main:81`) ; (2) sorties texte légèrement sous-estimées (**21** convertibles, dont un tableau de broches capturé en image) ; (3) instruments largement sous-estimés (**6** candidats SVG, pas 2). **ARBITRAGE STRUCTURANT (Tim, contre ma recommandation)** : sur les **familles de tête** (Arduino, ESP32, ESP8266) et les notions `[T]`, **le volume d'usage l'emporte sur le critère d'opacité** — les images de confirmation (LED allumée, compilation réussie) y sont conservées. Borne **écrite dans C29**, sinon la prochaine relecture les resupprimerait. **PRIORITÉ DE PRODUCTION** posée : rang 1 = notions `[T]` (6) + `arduino-*` (5) + `esp32/esp8266-*` (12) ; rang 2 = `micropython-*` (8, très peu d'étudiants) ; rang 3 = `stm32/teensy/raspberry-*` (12) ; rang 4 = `kicad` (3). **C29 + C33 PROMUES AU §3** — fusionnées, augmentées du critère « interface opaque », de la borne de trafic et de la dérivation des chiffres ; §8 à **57 conventions**, numérotation inchangée **C90**. **RELECTURE CLOSE — 212/213** : 11 fiches cochées (§8, §11, §12, `pcb`), seule `kicad` reste ouverte par décision. Deux incohérences de pilotage levées (scorie de clic-test sur `registre-gpio`, bandeau §16 périmé → remplacé par un **pointeur**, pas un chiffre). ⚠ **L'INVENTAIRE N'EST PAS EXHAUSTIF — 101 EST UN PLANCHER** : `shield.md` porte un placeholder que le balayage n'a pas vu, il commence par « Prendre **photo** de » et non « Prendre capture ». Re-balayage à motif élargi **avant toute prise de vue**. **PROCHAINE SESSION = `cycle-v-projet` EN VOIE A**, puis les fiches de lecture C85, puis la transverse `filtrage`. **RESTE À TIM** : commit/push ; si un défaut sort au premier `npx quartz build --serve`, le risque porte sur les **6 SVG neufs du §12**, jamais rendus — le signaler pour décocher.

> **➜ MAJ 18/08 (suite)** — **SESSION DÉDIÉE DETTE TECHNIQUE : audits mécaniques, hygiène de dépôt, groomings.** Aucune production de contenu (PC perso). **5 outils écrits dans `tools/`** : `audit-medias.mjs`, `audit-wikilinks.mjs`, `sommaire-conventions.mjs`, `groom-todo.mjs`, `groom-conventions.mjs` (+ README). **AUDIT EMBEDS — dépôt SAIN** : 296 embeds et liens ressources, **0 absent, 0 casse, 0 hors-gabarit** ; la migration C73 et la dizaine de renommages n'ont rien cassé. **Orphelins : 2** — `ch340-gestionnaire.mp4` (3,4 Mo, jamais référencé, format écarté par C87) **supprimé par Tim**, `archive/cycle-v-projet-v1.svg` laissé. **AUDIT WIKILINKS — 5 morts sur 67 annoncés** (61 faux positifs = pipes échappés de tableau, 8 = placeholders de gabarits) : `filtrage` ×8 **laissé rouge** (approche A) ; `bus-de-terrain` ×2 et `raspberry-pi-pico` ×2 **déliés** ; `boucle-fermee` **retargé vers `asservissement`** (libellé ajusté — la fiche ne traite que la boucle fermée) ; `programmation-non-bloquante` **retargé vers `arduino-programmation-non-bloquante`**. `[[del|LED]]`, `[[micropython-filtrage]]`, `[[pont-en-h]]` **absents du dépôt** ; **audit `[[micropython-eeprom]]` CLOS**, zéro résiduel. **PORTES FAMILLE [T] : 6 défauts sur 11 notions**, patchés d'office (`adc`, `pwm`, `bibliotheque` = Voir aussi ; `memoire`, `shield` = prose + Voir aussi, les fiches `micropython-*` étaient absentes partout ; `firmware` = bullet STM32 seul sans lien + 2 hubs manquants ; `deep-sleep` = triade en prose). **`entree-sortie` laissé sans lien famille** (route par gpio/adc/pwm, arbitrage). **WORKFLOW GITHUB** — le « @v5 » du TODO était **faux pour trois actions sur quatre** : cibles vérifiées en source = checkout **@v7**, setup-node **@v7**, upload-pages-artifact **@v5**, deploy-pages **@v5** ; ⚠ trouvaille hors périmètre — `runs-on: ubuntu-22.04` entre en **dépréciation le 17/09/2026** (fenêtres où les jobs échouent), passé en **ubuntu-24.04**. **`templates/` dépublié** (`ignorePatterns`) — il servait 8 liens rouges de gabarit aux étudiants. **GROOMINGS** : `conventions.md` **162,4 → 144,2 ko** (§8 de 127 → 99 ko, **75 → 59 conventions**, 16 promues + 8 blocs condensés) ; `TODO.md` **62,2 → 22,3 ko** (25 → 3 flèches) ; **`.bak` de la racine supprimés** (531 ko). **JOURNAL-archive** : seuil de découpe fixé à **1 Mo**, par trimestre (514 ko aujourd'hui, ~1 an de marge). **PROCHAINE SESSION = TRIAGE DE L'INVENTAIRE DES CAPTURES + CLÔTURE DE LA RELECTURE** — prompt déjà rédigé. **RESTE À TIM** : commit/push, **surveiller le premier déploiement** (onglet Actions — seul changement risqué de la session) ; **clic-tests en attente** inchangés — §11, §12, `pcb`, `simulation-electronique` ; vérifier que `templates/` a bien disparu du graphe et de la recherche.

> **➜ MAJ 18/08** — **REFONTE PÉDAGOGIQUE DE `ltspice` (2ᵉ épreuve de C88) — §8 CLOS 4/4 + INVENTAIRE GLOBAL DES CAPTURES.** Une seule fiche reprise de bout en bout (PC perso). **5,9 → 24,3 ko, 8 médias**, rendu validé par Tim. Méthode identique à `falstad` : **discussion d'abord** (3 scénarios argumentés → arbitrage), production ensuite. **Scénario B + miroir en dispositif** retenu — **C88 transposée avec une antichambre** : `installer` → `lire` → `modifier` → `construire` + section `exporter la netlist`. L'objection « page vide » est tombée en source (*File → Open Examples… → Educational* livre des `.asc` portant déjà leur directive) ; le scénario C (refaire le passe-haut de `falstad`) a été **écarté** — perte d'autoportance. **Conventions : C89** (assistant conversationnel — le critère est « l'affirmation est-elle **vérifiable à l'écran** », pas le sens de l'usage ; **`falstad` back-patchée**) et **C90** (**placeholder C29 visible en production assumé**, transparence pédagogique — arbitrage Tim contre ma recommandation) + 9 notes §8. **La numérotation atteint 90.** ⚠ **Libellés périmés, 3 fois dans la même fiche** : *Edit Simulation Cmd*→**Configure Analysis**, *SPICE Netlist*→**Update and View SPICE Netlist**, *Run*→**Run/Pause** ; Tim tourne sous **LTspice 26.0.2**, mes sources parlaient de la 24. ⚠ **`interface.png` légendée de travers** faute d'avoir pu l'ouvrir (403 ko > limite de lecture) — règle : un média illisible ne se légende pas, **l'auteur écrit l'alt** (fait pour le GIF de 502 ko). ⚠ **`lecture-courbe.svg` mentait sur ses repères** — Bézier à l'œil croisant le 63 % 64 px avant τ ; remplacé par une **exponentielle échantillonnée** (formule en commentaire). **INVENTAIRE CAPTURES** : balayage complet de `content/` → **101 placeholders sur 57 fiches** (contre 15 et 13 aux deux compteurs manuels) — fichier `_drafts/inventaire-captures.txt`. **PROCHAINE SESSION = DÉDIÉE DETTE TECHNIQUE** (arbitrage Tim en clôture, voir prompt de lancement) — audits mécaniques d'abord (chemins et **casse** des embeds : 404 en production, invisibles en local ; wikilinks morts ; portes famille des notions `[T]`), puis hygiène de dépôt (bump des actions GitHub avec **vérification des versions en source** — le « @v5 » du TODO n'est pas vérifié pour les quatre actions, un tag inexistant casse le déploiement immédiatement ; `.bak` de la racine, 531 ko à deux), puis groomings en fin de session (`conventions.md` à **168 ko**, `TODO.md` à **64 ko** contre ~10 ko au dernier grooming du 11/06). **SESSION N+1 = TRIAGE DE L'INVENTAIRE DES CAPTURES + CLÔTURE DE LA RELECTURE** — prompt déjà rédigé, à reprendre tel quel. **RESTE À TIM** : commit/push ; **clic-tests en attente** — §11 (`trois-modes`, `flux`, `abstraction-couches`, hub `stm32`), §12 (6 SVG neufs + 3 largeurs), `pcb` (1 largeur), `simulation-electronique` (`cycle` + `lecture-courbe` corrigé). **Décisions prises** : KiCad **reporté en post-publication** ; chronopédagogie **écartée** (le calendrier appartient au cours de gestion de projet des collègues — fausse lacune) ; reprise visuelle SVG **non prioritaire sauf `cycle-v-projet`** ; dette technique **non prioritaire sauf bump GitHub `@v5`**.

> **➜ MAJ 17/08 (suite 3)** — **REFONTE PÉDAGOGIQUE DE `falstad` : FICHE-OUTIL DESCRIPTIVE → TUTORIEL SCÉNARISÉ.** Une seule fiche, reprise entièrement (PC perso). **5,4 → 20,4 ko**, **13 médias validés au rendu par Tim** dont **4 GIF — les premiers du wiki** (clair/sombre/mobile OK, `construire-passe-haut.gif` compris). Méthode : **discussion d'abord** (3 scénarios argumentés → arbitrage Tim), production ensuite, puis 8 allers-retours au fil des captures déposées. **Scénario B + dispositif C** retenu : **lire** un circuit qui tourne → **modifier** un circuit de la bibliothèque → **construire** depuis une page blanche, + **4ᵉ section** *Importer un circuit décrit en texte* ; « prédire puis vérifier » en `[!question]` ×3 ; **2 exercices + corrigés**. **Corrections de fond** : (1) **l'interface est en français** — tout le vocabulaire à refaire, chemins d'accès compris (*Fichier → Nouveau circuit vide*, pas *Circuits → Blank Circuit* ; *Vitesse d'Animation*, pas *Current Speed*) ; (2) **callout d'erreur du solveur retiré** — source = doc de l'**applet Java**, périmée, **démentie par le test de Tim** ; (3) masse dans *Entrées et générateurs → Ajouter terre* (`g`), pas au premier niveau ; (4) raccourci fil `W`→**`w`** ; (5) l'enregistrement automatique existe ; (6) 3 oscilloscopes déjà posés à l'ouverture. **Décisions Tim** : section 2 sur le **circuit réel de la bibliothèque** (2 sources 5 V sur le curseur, pot 1 kΩ — 10 mA/branche à mi-course, **50 mA vs 5,6 mA** au dixième) ; section 3 **basculée du transitoire RC au filtre passe-haut** (35 Ω / 10 µF → **fc ≈ 455 Hz** ; τ = 0,1 s et 3,15 V **ne sont plus le contenu**) ; **zéro SVG** ; **plafond de calibre levé** ; **`ltspice` partira aussi en refonte**. **Conventions : C87** (GIF pour la boucle courte, vidéo pour la séquence longue + corollaire de poids) et **C88** (fiche-outil scénarisée lire→modifier→construire, le scénario commande les médias — inversion C81) + 6 notes §8. **La numérotation atteint 88.** ⚠ **MP4 de 40 Mo intercepté avant commit** (git garde tout à jamais) ; ⚠ **renommage de casse silencieux** (`circuit-RC`→`circuit-rc` a renvoyé « succès » sans rien changer — 404 en production si non détecté). **PROCHAINE SESSION = REFONTE `ltspice`** (2ᵉ épreuve de C88) : même méthode — scénarios argumentés → arbitrage → liste de captures dérivée du scénario → Tim shoote → intégration ; angle de contraste à tenir : **Falstad ne fait pas d'analyse fréquentielle**, LTspice déclare `.ac`, balaie et trace le gain où le −3 dB se relève. **RESTE À TIM** : commit/push ; **clic-tests en attente** §12 (6 SVG + 3 largeurs), §11 (`trois-modes`, `flux`, `abstraction-couches`, hub `stm32`), §8 (`cycle`, `lecture-courbe`). **Captures : lot ramené à 12** (LTspice 3, §10 six, KiCad 3). **Nouveau** : bump des actions GitHub en `@v5` (Node 20 déprécié). Pendances inchangées (fiche transverse `filtrage`, fiches de lecture C85, audit `[[micropython-eeprom]]`, `.bak` de la racine, pile de flèches TODO).

> **➤ MAJ 17/08 (suite 2)** — **RELECTURE DE FOND TERMINÉE — §12 TEENSY (5/5), §15 PIC (1/1), §16 PCB (2/2) + RATTRAPAGE §11.** Fin du chantier : **les 20 sections du parcours étudiant sont lues**. 8 fiches relues + corrigées, **6 SVG créés**. **§12 Teensy** — **C86 : trois occurrences dans `teensy-arduino-core`** (l'« occurrence unique » du 17/08 ne valait que pour le périmètre relu), corrigées **en mixte** (variable d'état au sketch d'ouverture, **`digitalToggleFast`** à l'Exemple et au corrigé — primitive vérifiée dans `PaulStoffregen/cores`) + **piège « relire une broche de sortie »** ajouté ; **ADC faux corrigé** (« 12 bits et plus » → 12 matériels / **10 par défaut** / ~10 utiles, plage figée 0-3,3 V, `analogReference()` sans effet) — **3ᵉ exemplaire** après Uno R4 et STM32duino, la fiche **se contredisait avec `teensy-usb`** ; **C71 reconstruit** sur trois angles (3,3 V strict / aucune radio / écosystème mince), **panorama réduit aux 4.1-4.0** (LC, 3.2, 3.5, 3.6 **arrêtées depuis 2023**) ; `aa: PROJ/5` (**4ᵉ trou de parité**) ; version d'outil précisée (paquet « for Arduino IDE 2.0.4 or later », **1.8.x abandonné sous macOS depuis Teensyduino 1.60**) ; 2 embeds C74 + **alts-fleuves raccourcis** ; `phases` +dossier-technique sur `-usb`. **§15 PIC** — **C71 tranchée absolue** (arbitrage Tim, contre ma recommandation) : 4 mentions retirées, y compris le coût unitaire en série ; rachat d'**Atmel par Microchip en 2016** ajouté ; contrôle « parcours fantasmé » **RAS**. **§16 PCB** — **C45 confirmée 4ᵉ épreuve** ; **ERC nommé à l'étape 1** du hub (le sigle n'apparaissait que dans les pièges) ; **« soutenue par le CERN » périmé** → **Linux Foundation depuis 2019**, CERN au passé (dons clos 09/2023) ; format C29 normalisé ×3 ; bibliothèque CERN (17 000 composants, mai 2026) + IPC-2581. **§11 rattrapé** — contradiction JOURNAL/dashboard levée sur demande de Tim : relecture de contrôle de `stm32-cubemx`/`-hal`/`-registres`, **elles étaient bien relues le 17/08**, seule la clôture s'était arrêtée ; 3 annotations + bandeau écrits, **aucune correction**. Conventions : **aucune numérotée (reste C86)** + 7 notes §8. **PROCHAINE SESSION = `falstad`, REFONTE PÉDAGOGIQUE** (décision Tim, révisée en fin de session) : **pas une intégration de captures** — Tim n'en a aucune et n'en veut pas encore. Session de **scénarisation** : reprendre la fiche entièrement, la transformer en **vrai tutoriel** (progression, gestes montrés, modification de circuits, exercices). Échange d'abord (options argumentées → arbitrage Tim), production ensuite. **Inversion assumée de la méthode `wokwi`** : ici le scénario **détermine** la liste des captures à shooter, au lieu que les captures déterminent le texte. **RESTE À TIM** : commit/push ; **clic-test des 6 SVG neufs** (`teensy/comparaison-arduino`, `teensy-arduino-core/paliers-d-acces`, `teensy-prise-en-main/montage-led-externe`, `teensy-audio/empilage-audio-shield`, `teensy-usb/montage-bouton` + `montage-potentiometre`) **et des 3 embeds à largeur modifiée** (`teensy-audio/flux`, `teensy-usb/personnalites`, `pcb/flux`) → cocher §12 et `pcb` ; clic-tests **§11 et §8 toujours en attente** (`trois-modes`, `flux`, `abstraction-couches`, `cycle`, `lecture-courbe`, hub `stm32`) ; **captures : lot porté à 15** (Falstad 2, LTspice 3, §10 six, **KiCad 3**). `pic` **cochée sans clic-test** (aucun média). **Pendances inchangées** : fiche transverse `filtrage`, fiches de lecture C85, audit liens `[[micropython-eeprom]]`, `.bak` de la racine, **pile de flèches TODO à regroomer**.

### 1. Cadrage pédagogique (suite)
- [ ] **Session « chronopédagogie »** — **ÉCARTÉE le 18/08 (arbitrage Tim)** : le calendrier du projet est enseigné par le **cours de gestion de projet des collègues**, et le wiki est une *interface* vers leurs cours. Y mettre un rétroplanning reviendrait à refaire leur travail, ce que le cadrage projet interdit. L'étudiant se réfère à la fiche dont il a besoin au moment où son cours le lui indique. *Reliquat optionnel* : une phrase dans le hub `conduite/index` disant que le calendrier vient du cours de gestion de projet, pour que celui qui cherche « en semaine combien ? » sache où regarder.
- [ ] **Format des compétences (AA)** : discussion sur la granularité et la convention de tag — en attente validation hiérarchie. **Statut à reconfirmer** — même question que la chronopédagogie : est-ce un vrai chantier ou une délégation ?
- [ ] **Trame v2 — boucles itératives** : enrichir le schéma du cycle en V pour afficher honnêtement les retours arrière (PoC échec → revoir spec/concept, qualif échec → revoir dossier). **Statut à reconfirmer** au même titre.

### 2. Rédaction de fiches

- [x] **Lot actif des prises de vue — CLOS le 21/08 (suite). 27 déposées, 0 restante, 30 reportées, 1 supprimée.** Plus aucune prise de vue en attente avant la rentrée. Ce qui reste hors manifeste : les captures des sessions `easyeda` et `kicad`, **non chiffrées** tant que les scénarios C88 ne sont pas arbitrés (précédents : `falstad` 13 médias, `ltspice` 8). Les 30 reportées post-rentrée : 3 Teensy + 9 STM32 + 11 MicroPython + 2 Raspberry Pi + #12, #36, #38, #45 + **#48** (pas d'Uno). *Ancien libellé ci-dessous, conservé pour trace.* — **Lot actif des prises de vue — 29 après les cinq reports. 23 déposées, 6 restantes (MAJ 21/08).** Les six : **#37** (PID, **procédé simulé**, aucun matériel), **#40** (NodeMCU), **#44** (BLE + nRF Connect, **pas de l'I²C** — groupée à tort avec #45), **#47** (servo, GIF), **#48** (Uno + shield), **#49** (couveuse). **Aucune ne dépend plus du matériel de l'école.** Cinquième report (21/08) : #12 (sonde), #36 et #38 (potentiomètre, et carte AVR + TimerOne pour #38), #45 (analyseur logique). ⚠ **Trois précautions écrites au manifeste avant le prochain shoot** : #37 exige que le **sketch complet soit ajouté à `arduino-pid`** (qui déclare `lireVitesse()` sans jamais l'implémenter) **et** que le procédé simulé soit **déclaré dans l'alt** (C102) ; #44 tombe sous **C101** (un scan BLE affiche les appareils nommés des gens autour) ; **la spécification de #38 est fausse** — le traceur trace en index d'échantillon, pas en temps, donc « échantillons régulièrement espacés dans le temps » est **inobservable** en l'état. *Ancien libellé ci-dessous, conservé pour trace.* — **33 après les quatre reports. 21 déposées, 12 restantes.** Le manifeste vit dans `_drafts/feuille-de-route-captures.md`, le cadrage prise par prise dans **`_drafts/guide-prise-de-vue-par-outil.md`** (tableau de recomptage en tête = celui qui fait foi). **Reportées** : 3 KiCad (18/08), 3 Teensy + 1 réemploi coupé et 9 STM32 (20/08), 11 MicroPython et 2 Raspberry Pi (20/08 suite). **Le lot logiciel est vide** — **dix des douze restantes** demandent du matériel ou un montage : #12 (sonde JTAG), #36-38 (traceur, montages réels), #40 (NodeMCU), #44 (nRF Connect), #45 (analyseur logique), #47-49 (paillasse). ⚠ **Correction du 20/08 suite 2 : #42 et #43 ne sont PAS bloquées sur du matériel** — ce sont deux extraits de la **datasheet L298N**, shootables depuis un PDF dès maintenant, sous réserve de noter la révision du document (§C du manifeste). **Arbitrage Tim : à traiter après les sessions parcours étudiant** — elles sont faites, l'item est donc mobilisable dès la prochaine session courte.

- [ ] **`kicad` — REPORTÉE POST-RENTRÉE (arbitrage Tim 22/08).** La refonte C88 de `kicad` était prévue juste après `easyeda` ; Tim la repousse **par gain de temps**. Conséquence sur l'ordre : la **clôture pré-publication** devient la prochaine session. `kicad` garde ses **3 placeholders C29** dans `content/`, seuls placeholders résiduels du corpus, et reste la seule fiche non cochée de la relecture de fond (212/213). ⚠ **Le contraste est désormais écrit dans `pcb` et dans `easyeda`** : KiCad sert le **fabricant extérieur** et les Gerber, EasyEDA la **gravure à l'école** et le `.json`. La refonte de `kicad` devra s'y tenir, sinon les deux fiches se remettent à se doublonner.

- [ ] **MESURE C105 DE RÉFÉRENCE — TOUJOURS À PRENDRE, MAIS PLUS URGENTE (recalée le 23/08).** Taper `couple`, `reproductibilité`, `marche parfois` et `bruit` dans `Ctrl+K` et **noter la bande top-3** pendant que le corpus EN ne compte que 4 fiches publiées. ⚠ **ELLE N'EST PLUS NON RATTRAPABLE.** J'avais écrit deux fois qu'elle devenait imprenable dès qu'une fiche EN repasse en `draft: false` — **faux depuis que `tools/draft-en.ps1` existe** : la bascule est réversible en une commande dans les deux sens, et `RemoveDrafts` est conditionnel à `QUARTZ_DRAFTS` depuis le 22/08. **L'outil que je venais d'écrire invalidait l'urgence que j'invoquais pour le justifier.** Procédure : `draft-en.ps1 -Etat true`, build, mesure, `draft-en.ps1 -Etat false`. ⚠ **ET L'OBJECTION ELLE-MÊME EST PLUS ÉTROITE QUE JE NE L'AI PORTÉE** — Tim l'a rappelé le 23/08 : la recherche se fait en anglais d'un côté et en français de l'autre, les requêtes divergent, et le 22/08 avait déjà mesuré que `project` ne rend aucune fiche FR. **Le recouvrement réel est borné aux sigles techniques identiques** (`PWM`, `I2C`, `GPIO`, `firmware`, `PCB`), et l'effet est une gêne de lecture, pas une panne. **Travailler en `draft: false` n'est pas un problème** — arbitrage Tim, ne pas rouvrir.

- [ ] **Encart d'accueil anglais sur `content/index.md` — bloqué par une dépendance, pas par le travail (22/08 suite 2, demande Tim).** Orienter le nouvel arrivant anglophone depuis la page d'accueil française. ⚠ **Ne peut pas s'écrire tant que le lot d'arrivée n'est pas navigable** : l'encart doit pointer `[[en/index]]`, et un renvoi vers un parcours dont tous les liens sont morts est pire que pas de renvoi. **À poser quand le lot 2a est traduit.** Connaître aussi sa conséquence : modifier `content/index.md` fera passer `en/index.md` en `DERIVE` — c'est le cas d'usage exact de `--recaler`, à lancer **après** avoir reporté l'ajout côté anglais.

- [x] **Glossaire de traduction — les quatre entrées sans mandat sont validées, plus AMDEC et maître/esclave. FAIT le 23/08.** Arbitrages Tim : **(a)** les quatre entrées `dossier technique` / `soutenance` / `REX` / `écart` **validées en bloc**, périmètres mesurés avant validation (224/50, 63/13, 26/5, 103/39) ; **(c)** AMDEC promue au §5.2 avec la glose *the French acronym for FMEA*, l'ancienne « the French FMEA » fabriquant une variante nationale qui n'existe pas ; **(b)** maître / esclave → *controller* / *peripheral*, terme historique mentionné une fois par fiche. **Trois formes dérivées figées en plus** : `dossier technique CE` → *CE technical file* (le *technical file* seul est le terme réglementaire du marquage CE), séance de REX → *lessons learned review*, « soutenance finale » qui ne se redouble pas. Tout est écrit aux §5.2 et §5.3 de `_drafts/traduction-en-regles.md`. **Ne pas rouvrir.**

- [ ] **`creer-fiche-en.mjs` — un wikilink à libellé contenant du code inline échappe au suffixage (23/08).** Le backtick casse le parsing du libellé : dans `micropython-langage`, l'item 5 de la liste d'apprentissage est ressorti **sans son `-en`** quand ses cinq voisins l'avaient. **Mesuré sur tout le corpus : 29 occurrences sur 22 fiches**, concentrées dans les modules Arduino et MicroPython (deux chacune sur `micropython-gpio`, `micropython-pid`, `micropython-timers`, `arduino-pid`, `arduino-timers`, `arduino-programmation-non-bloquante`, `micropython-lire-un-programme`). **Aucune dans le lot 2a** sauf celle-là, corrigée à la main. **Ce n'est pas un lien mort aujourd'hui**, la cible française existant, mais côté EN le lien pointera une fiche française. **À corriger avant le lot 2c**, qui porte l'essentiel des 29.

- [ ] **`locale: "fr-FR"` est global dans `quartz.config.ts` (22/08 suite 2).** Les pages EN afficheront leurs dates et les libellés d'interface de Quartz **en français**. Ne bloque rien aujourd'hui, se corrigera mal une fois 243 fiches en place. À regarder avant que le corpus EN grossisse : Quartz accepte-t-il un `lang`/`locale` par page en front matter, ou faut-il un composant ?

- [ ] **Deux pièges de téléversement Nano à écrire, avec leurs messages exacts (21/08 suite).** Rencontrés au banc en shootant #37, absents du wiki. **(1)** `Error: cannot open port \\.\COM13: Accès refusé.` puis `unable to open port COM13 for programmer arduino` — le port est **déjà ouvert** par le moniteur ou le traceur série, qui ne se partagent pas. Le remède tient en une phrase et n'est écrit nulle part, alors que la manoeuvre « ouvrir le traceur, corriger, retéléverser » est exactement ce que #36-38 demandent. **(2)** `not in sync: resp=0x00` répété dix fois — clone Nano à ancien bootloader, *Outils → Processeur → ATmega328P (Old Bootloader)*. ⚠ La dernière ligne `unable to open port` est un **résidu trompeur** d'avrdude qui referme le port, à ne pas lire comme un retour de l'erreur (1). Fiche d'accueil probable : `arduino-prise-en-main` (`cpp-logs` traite la **compilation**, pas le téléversement).

- [ ] **Contrôle de fratrie servo, suite au bloc `attach` du 21/08 (suite).** Le décalage des plages d'impulsion par défaut — **544-2400 µs sur Arduino AVR**, **1000-2000 µs sur ESP32Servo** — est désormais écrit dans `arduino-bibliotheques`. **À vérifier ailleurs** : `arduino-servomoteur`, `micropython-servomoteur`, et tout renvoi servo depuis la famille ESP32. C'est le genre de piège qui traverse les familles sans être écrit nulle part, et l'étudiant qui change de carte le rencontre le premier. Même motif que l'expurgation C71 du 21/08, qui visait une fiche et en a trouvé deux.

- [ ] **Recalage des `fiche:ligne` du manifeste — borné aux fiches réécrites le 20/08.** Deux références périmées sur trois vérifiées le 21/08 : `esp8266-prise-en-main` **77 → 93** et `arduino-bibliotheques` **130 → 136**, toutes deux corrigées. `esp32-ble:99` et `schema-bloc-fonctionnel:67` étaient exacts. **Le décalage ne touche que les fiches réécrites** — inutile de balayer tout le corpus ; `esp32-prise-en-main` est la principale suspecte restante (refonte de l'étape 3 + six prises neuves le 20/08).

- [ ] **#50 et #51 — CLOS le 20/08 (suite). Trace, pas une tâche.** #50 est **déposée et intégrée** (boîte *Select Other Board and Port*, cartes et ports côte à côte). **#51 est ANNULÉE** : la lecture de `interface-annotee.png` a montré que le moniteur série y était **déjà annoté** — le *où* est livré dans `ide`, le *ce qu'il affiche* par #10, et une troisième capture contredisait l'arbitrage « une seule capture de moniteur dans le wiki ». L'alt de `ide:19` a été **recalé à la place** : il ne promet plus de sélecteur de port (l'IDE 2.x n'en a qu'un, combiné) et nomme *Verify* et *Upload* comme l'image les annote. **Corriger un alt peut supprimer une prise de vue.** ⚠ **Ne pas rouvrir #51** — un audit qui la signalerait daterait d'avant le 20/08 suite.

- [ ] **Libellés d'interface — portée à mesurer, sans urgence.** Le bilinguisme est **clos côté images** (arbitrage Tim 20/08). Reste le versant texte : la prose cite des libellés **en français entre guillemets** (« URL de gestionnaire de cartes supplémentaires », « Vérifier » / « Téléverser ») quand les captures montrent l'anglais. **L'IDE 2.x étant localisé en français, la prose n'est pas fausse** — c'est la machine de prise de vue qui est en anglais. Décalage bénin, ne bloque aucune prise. À mesurer (combien de fiches citent un libellé) avant de trancher entre forme canonique bilingue, anglais seul, ou statu quo. ⚠ **Première manifestation sur un cas réel, 21/08 (suite)** : Tim a demandé ce qu'était le « traceur » dont je parlais — c'est le **Serial Plotter**, que sa machine affiche en anglais alors que toutes les fiches écrivent « traceur série ». Le décalage a donc coûté un échange, ce qui est le premier coût mesuré depuis l'ouverture de l'item.

- [ ] **Propager `const int` aux autres familles.** Deux fois le même arbitrage le 20/08 : `D5` → `14` (ESP8266), `LED_BUILTIN` → `const int LED = 2;` (ESP32). Les alias de broches ne sont définis que par certaines définitions de carte. **`teensy-prise-en-main` utilise encore `LED_BUILTIN` partout** — légitime sur Teensy (broche 13, constante définie), mais à revoir si la fiche repasse en production.

- [ ] **Réserve visuelle sur #55** (`esp8266-prise-en-main/selection-port-com.png`) : la colonne d'options y montre encore celles du cœur ESP32 (*CPU Frequency 240MHz*, impossible à 160 MHz max). **Erreur acceptée par Tim**, tracée au manifeste. Un recadrage à la hauteur de #54 la supprimerait sans reshoot, si l'occasion se présente avant publication.

- [ ] **Captures d'écran — TRIÉES le 18/08 (suite 2) : voir `_drafts/feuille-de-route-captures.md`.** Les 101 placeholders ont reçu un verdict un par un, groupés **par session de prise de vue** (un logiciel ouvert = une session) : **46 prises à shooter** + 3 KiCad reportées, 23 conversions en blocs de code ou tableaux, 6 SVG, 12 réemplois cross-dossier, 11 suppressions. **Ce n'est pas un bloquant de publication** (C90). **ÉTAT AU 19/08** : re-balayage fait (**242 fiches, 107 placeholders**, +6 nets) ; **60 des 61 embeds posés d'avance** (c-large) et **16 suppressions exécutées**. Le suivi vit désormais dans **`# À apporter par Tim — suivi`** de la feuille de route, le nommage dans **`# Manifeste des embeds`**. Restent **3 placeholders** dans `content/` : les trois KiCad, reportées post-publication. **Les 23 conversions (session B) et les 8 SVG (session C) sont faits le 19/08** ; `micropython-simulation:52` a été supprimé (image ESP32/C++ sous une phrase Pico). **La dette de médias se réduit désormais aux prises de vue.** **Ordre de production** : rang 1 = notions `[T]` (6) + `arduino-*` (5) + `esp32/esp8266-*` (12) ; rang 2 = `micropython-*` (8) ; rang 3 = `stm32/teensy/raspberry-*` (12) ; rang 4 = `kicad` (3). **La session la plus rentable est « IDE Arduino »** : 12 prises d'un coup, un quart de la dette. ⚠ **RE-BALAYAGE FAIT LE 19/08** — motif élargi, **242 fiches sur 242**, 107 placeholders, 6 formes « photo » récupérées. Item clos. ⚠ **Contrôle de format C29** : le défaut du paragraphe tout-italique touchait **17 occurrences sur 9 fiches** et non 5 ; il en reste **14 sur 6 fiches** après les embeds et suppressions du 19/08. ⚠ **`micropython-simulation`** : le piège « `Pin("LED")` *peut* ne pas être reconnu » est à trancher **au simulateur**, pas de mémoire.

  *Hypothèses de triage du 18/08, conservées en trace — les trois ont été corrigées à la vérification : les 31 des prise-en-main donnent **15 prises et non 6**, les sorties texte sont **21** et non ~18, les instruments **6** candidats SVG et non 2 :* **(1) les six prise-en-main concentrent 31 placeholders** qui se ramènent à ~6 gestes répétés d'une famille à l'autre (page de téléchargement, Préférences avec URL, gestionnaire de cartes, menu Outils, compilation réussie, carte branchée) — **une session unique « je déroule les cinq installations »** les couvre, et **deux sont strictement identiques** (`esp32-prise-en-main:38` et `teensy-prise-en-main:38` demandent la même page `arduino.cc/en/software` → une prise de vue, réemploi cross-dossier C76) ; **(2) ~18 placeholders demandent une capture de sortie texte** (moniteur série, Shell Thonny, console) — convertibles en **blocs de code**, plus légers, cherchables et lisibles sur mobile ; exception légitime : les **traceurs/Plotters**, qui produisent des courbes ; **(3) les faces avant d'instruments** (`multimetre:19`, `oscilloscope:19`) sont des candidates **SVG annoté** plutôt que photo — plus lisibles et indépendantes du modèle posé sur la paillasse. **Méthode de prise de vue éprouvée** (`wokwi` 17/08, `ltspice` 18/08) : Tim shoote et dépose dans `content/ressources/img/<slug>/` en kebab-case, Claude **lit les images** (`read_media_file`) avant de rédiger (C81), puis remplace les placeholders et cale les largeurs sur les dimensions réelles. ⚠ Un média trop lourd pour la lecture (> ~350 ko) doit être **allégé ou légendé par Tim** — jamais intégré avec un alt inventé.

- [ ] **KiCad — reporté en post-publication** (arbitrage Tim 18/08). 3 placeholders (fenêtre de démarrage + deux éditeurs côte à côte ; Pcbnew en cours de routage 2 couches ; schéma de la carte capteur annoté). La fiche **reste publiée telle quelle**, placeholders visibles (C90) — pas de `draft: true`, pas d'amputation. À reprendre quand le module PCB passera en production de médias.

- [ ] **Les 2 créneaux vides du défaut A** — **CLOS le 19/08 (suite 5), mais pas comme prévu.** `chien-de-garde` **écrite** (13,3 ko) et ses 5 entrants retargetés, dont le lien `arduino-watchdog` posé sciemment dans `fiabiliser-et-deboguer`. ⚠ **`temporisation` NE SERA PAS ÉCRITE — décision, pas oubli.** Le critère C94 la refuse : son seul contenu propre (la soustraction de dates, juste en C, fausse en MicroPython) est **déjà le contenu propre de `programmation-non-bloquante`**, écrite le 19/08 suite 2 ; le reste est porté par `timer`. La fiche aurait résumé ses voisines. Les 4 liens concernés ont été traités **sans fiche neuve** : doublages sur `timer` et `interruption`, formulation généralisée puis doublée sur `debugger-embarque`. **Ne pas rouvrir ce créneau** — un audit qui le signalerait daterait d'avant le 19/08 suite 2.
- [ ] **C62 — BALAYAGE FAIT, OUTILLAGE REFUSÉ le 19/08 (suite 6). Trace de refus, pas une tâche.** ⚠ **NE PAS ROUVRIR SANS CHIFFRE NEUF.** Motif mesuré : sur les **242 fiches publiées**, **62 portent un tableau**, **522 lignes de tableau** inspectées, **61 wikilinks correctement échappés** contre **1 seul non échappé** — `gpio.md` ligne 34 (`[[i2c|I2C]]` dans le tableau des modes de broche), corrigé le jour même. Motif adjacent (pipe nu dans un *code span* de tableau) : **0**. La convention est donc comprise et appliquée à **61/62** ; un contrôle mécanique attraperait environ **un défaut par an** au rythme observé (`interruption` en juin, `gpio` en août). L'argument qui restait en faveur de l'outil n'était pas la fréquence mais le **coût de détection** — les deux occurrences ont traversé une relecture humaine, le défaut ne se voyant qu'au rendu ; **Tim a tranché contre l'outillage**. `_drafts/` balayé aussi : **15 fichiers, 332 lignes de tableau, aucun wikilink**, donc zéro défaut possible par construction — inutile d'y repasser tant que ses tableaux restent sans liens. `JOURNAL`, `TODO` et `BACKLOG` non balayés. Historique du 19/08 suite 5 : Le tableau des sources d'`interruption` portait **6 wikilinks à libellé non échappés** (`gpio`, `uart`, `adc`, `arduino-interruptions`, `arduino-timers`, `arduino-watchdog`) : chaque pipe non échappé **ajoute une colonne** à sa ligne, la table était donc décalée en production. Symétrique de l'incident du 18/08 : `audit-wikilinks.mjs` a été corrigé pour ignorer les pipes **échappés**, rien ne détecte les **non échappés** — liens valides pour le graphe, cellules cassées pour le rendu. ⚠ **La fiche avait été relue le 12/06 sans que ça sorte** : le défaut se lit mal en source, il ne se voit qu'au rendu — donc **toute fiche portant un tableau à wikilinks est suspecte**, et un balayage est à prévoir. Motif à détecter : `[[…|…]]` non échappé sur une ligne commençant par `|`. ~~À cadrer~~ — **arbitré contre le 19/08 (suite 6)**, motif mesuré ci-dessus.
- [ ] **Session « parcours étudiant »** — **FAITE le 20/08 (suite 2). Trace, pas une tâche.** Les douze scénarios sont traversés côté fichiers (`_drafts/parcours-etudiant.md`), la grille au rendu existe (`_drafts/parcours-etudiant-rendu.md`), les quatre lots de correction sont patchés. ⚠ **NE PAS ROUVRIR** — un audit qui signalerait les portes de #4, #6 ou #9 daterait d'avant le 20/08 suite 2.

- [ ] **INDEXATION PAR SYMPTÔME — la seule décision que le parcours étudiant laisse ouverte.** Quatre des douze scénarios sont des **symptômes** (« mon capteur renvoie n'importe quoi », « mon code ne compile pas », « mon moteur manque de couple », « mon robot marche parfois ») et **aucun des deux hubs n'indexe sur cet axe** — le V indexe par livrable, l'embarqué par étape et par objet. Le wiki **a pourtant toutes les réponses** : `cable-management` ouvre sur « pannes **intermittentes** (faux contacts, fils arrachés) », `pcb` sur « contacts intermittents », `alimentation-electronique` sur « pour que le microcontrôleur ne se réinitialise pas », `cpp-logs` sur la lecture des erreurs, et les sept pièges d'`arduino-capteur-analogique` forment un diagnostic différentiel complet. **Rien n'est à écrire, tout est à désigner.** Ce qui est **déjà fait** : le callout *défaut intermittent* de `fiabiliser-et-deboguer` désigne quatre de ces fiches là où l'étudiant passe. Ce qui **reste à trancher**, et qui dépend d'une mesure non faite : **(a)** rien de plus, le callout suffit ; **(b)** des `aliases:` de recherche (`reproductibilité`, `intermittent` sur `fiabiliser-et-deboguer` — deux minutes) ; **(c)** une entrée par symptôme sur un hub, qui engage le parcours et se discute. ⚠ **LA MESURE QUI MANQUE** : taper `couple`, `reproductibilité`, `marche parfois` et `bruit` dans `Ctrl+K` et noter **quelle fiche arrive en tête** — pas si ça rend des résultats (la recherche de Quartz est plein texte, tout mot en rend). Si `bruit` remonte `filtrage` en 1ʳᵉ, #3 est réglé sans rien faire ; si `couple` remonte une fiche Méca, la frontière de #10 change de nature.
- [ ] **Arbitrer la promotion de C58** (pointeur d'interface léger) vers §6 + template. Éprouvée sur **14 pointeurs** (6 Méca + 8 ESE), les deux branches d'interface sont closes, et la note du 13/08 la qualifie de *promotion-ready* depuis. Écartée du lot promu le 18/08 parce qu'elle ne figure pas dans la liste des contrôles d'office — mais son cas est mûr. La promotion suppose d'écrire le **template de fiche-pointeur** correspondant, donc une session template plutôt qu'un grooming.
- [ ] **Fiche-porte `raspberry-pi-pico`** (notion courte, demande Tim 12/06) : la carte et ses variantes (Pico / Pico W / Pico 2), le PIO, renvoi au module `micropython` comme parcours de cours + note Pico-SDK C/C++ (à raccrocher à l'item « Porte Pico-SDK » des fiches restantes, cf. BACKLOG). Ligne déjà ajoutée au panorama de `microcontroleur` + pastille SVG de positionnement (lien rouge `[[raspberry-pi-pico]]` posé, approche A).
- [ ] **Fiche `bus-de-terrain`** (notion, demande Tim 12/06) : RS-485 et surtout **CAN** (trame, arbitrage, paire différentielle, résistances de terminaison 120 Ω — le « si nécessaire » des terminaisons vit là), positionnement face aux bus de carte (UART/I2C/SPI). Lien rouge `[[bus-de-terrain]]` posé dans `bus-de-communication` (prose de clôture + Voir aussi, approche A). Calibre à cadrer : notion popover-long suffit probablement (culture générale, hors socle AA).
- [ ] **Fiche `cpp-tableaux`** — étape 6bis candidate du parcours langage (repéré 12/06 : tableaux utilisés dans le chenillard de `cpp-boucles` sans être enseignés) — détail au BACKLOG.
- [ ] **Enrichir `lire-une-datasheet` partie 2** (« Lecture de la datasheet du L298N », restructurée 11/06) — souhait Tim 11/06 : approfondir la lecture commentée plus tard (autres extraits annotés, exercices, etc. — périmètre à cadrer en session dédiée).
- [ ] **Session de validation cohérence d'ensemble** : relecture bout en bout, ajustements de périmètre entre phases. *(Largement portée par la relecture humaine en cours via `_drafts/relecture-ordre.md`.)*

*Fiches restantes (alimentation continue, non bloquantes pour la publication) :*
- [ ] `circuitverse` (fiche-tuto simu **numérique/logique** en ligne — niche distincte de `falstad`, pertinence à évaluer)
- [ ] `easyeda` (fiche-tuto outil PCB altern, embranchement de choix) — **promesse posée** : le tableau « Choisir un outil » de `pcb` annonce « EasyEDA *(à venir)* » (arbitrage Tim 17/08 suite 2 : mention conservée, item tracé ici). Pas un lien rouge, donc rien ne casse au rendu — mais la promesse est écrite.
- [ ] `bom-electronique` (fiche-tuto compétence pro)
- [ ] `pcb-gravure-ecole` (fiche-tuto spécifique monoface interne)
- [ ] Porte **Pico-SDK C/C++** (optionnelle, hors module Raspberry — cf. BACKLOG)

#### Pré-publication MVP

> Passe de vérification avant mise en ligne du site aux étudiants.

- [ ] **Remplacer le template Word `cdcf-ecole-template.docx`** — référencé en lien dans `specification-technique.md` étape 6 et `cahier-des-charges-fonctionnel.md`, mais le fichier présent dans `ressources/templates/` n'est PAS le bon (mauvais fichier repéré en relecture 08/06). **MAJ 19/08 (suite 6)** : une collègue de Tim rédige actuellement le document — **dépendance externe sans échéance**, à ne plus compter comme dette de session ni comme bloquant de publication. Vigilance « EAT » : réserver l'abréviation à *état de l'art technique*, ne pas réintroduire « matrice EAT » (renommée « matrice éco-critères »).
- [ ] **Captures C29** — item fondu dans la feuille de route du 18/08 (suite 2), voir plus haut. Le compteur « 13 en attente » était un comptage manuel périmé, démenti par le balayage.
- [ ] **Vérifier les images** de toutes les fiches (présence + rendu). *(Les 11 SVG du 29/05 — algorithme/logigramme/MAE/grafcet/chronogramme/analyse-de-schema — ont été validés au rendu lors des relectures du 11/06, géométrie vérifiée au calcul ; recoupe le peigne SVG — cf. section Reprise visuelle.)*
- [ ] **Reconcilier la cartographie AA Phase 1** : `RA-PROJET-C03-3/EEE/1` est porté à la fois par `lire-une-datasheet` et `analyse-de-schema-electronique` (multi-couverture C20) ; confirmer le statut de `EEE/2` et des `EEE/5` (logigramme/MAE/grafcet/chronogramme) pour étayer le « MVP strict 100 % ». Vérifier aussi `phases: dossier-technique` sur `analyse-de-schema` (repris du cadrage, non revérifié) et le lien `[[del|LED]]` (**vérifié 18/08 suite : aucune occurrence dans `content/`** — l'audit wikilinks ne le trouve nulle part, donc ni fiche `del.md` à créer ni lien à délier ; item clos, gardé en trace).
- [ ] **Vérifier toutes les fiches du lot Phase 0 (gestion de projet)** avant publication : `bom`, `mind-map`, `fast`, `amdec`, `matrice-eco-criteres`, `ecodesign` + fiches GP/outils antérieures.

### 3. Horizon long terme (post-publication) — nouveaux domaines

> Ambition élargie, **après** la couverture des compétences du projet et la publication (priorité Tim 12/06 : les tutos du socle d'abord). Hors socle AA — à cadrer en sessions dédiées le moment venu.

- [ ] **Électronique numérique** (domaine complet à construire) : logique combinatoire et séquentielle, portes, bascules, compteurs… et le **module « architecture des ordinateurs »** (envie 12/06, née de la relecture `processeur`/`memoire`) : von Neumann vs **Harvard** (l'AVR est Harvard !), RISC vs CISC, registres, ALU, pile (stack), cycle d'instruction. Contenant à arbitrer au cadrage (`processeur` promu hub mère vs hub frère) ; prisme « comprendre ce qu'on achète et ce qu'on programme ». ⚠ **Moore et Mealy** : modèles d'automates → extension de `machine-a-etats` (§5), pas du module processeur.
- [ ] **Électronique analogique** (domaine complet à construire) : transistors, AOP, filtres, conversion… Périmètre à cadrer.
- Note de cohérence : plusieurs délégations actuelles « territoire du cours d'élec » (TTL/CMOS glossé sans développement dans `niveaux-de-tension`, topologie des convertisseurs dans `alimentation-electronique`, borne C55) pourront être **repointées en interne** quand ces domaines existeront — à inventorier au cadrage.

## Reprise visuelle SVG (voie A — sessions dédiées)

> **Priorité tranchée le 18/08** : un placeholder est un **trou visible**, un SVG imparfait est du **contenu existant qui pourrait être plus beau** — les deux files ne se traitent pas ensemble. Ces reprises attendent l'après-rentrée.

- [ ] **`analyse-de-schema-zones`** — reprise ponctuelle : le condensateur de découplage **C3 passe derrière le bloc MCU** (signalé à la validation du 11/06). Reste de la géométrie validée par Tim.
- [ ] **`bete-a-cornes-generique`** — reprendre en voie A. Signalé en relecture de `specification-technique` (08/06) ; embarqué sur `specification-technique` + `bete-a-cornes`.
- [ ] **`pieuvre-generique`** + **`pieuvre-bras-3-axes`** — reprendre en voie A. Signalés en relecture de `specification-technique` (08/06) ; embarqués sur `specification-technique` (générique aussi sur `pieuvre`). **MAJ 12/06** : `pieuvre-bras-3-axes` doit intégrer le lien **FC3** vers le milieu opérateur (FC3 ajoutée à la fiche) ; resync texte↔SVG du triptyque `bete-a-cornes`.
- [ ] **`decomposition-fonctionnelle-bras-3-axes`** (920 px) + **`fast-bras-3-axes`** (960 px) — recomposer ≤ 760 de large / polices ≥ 12 px (lisibilité mobile, signalé 12/06) ; mesurer aussi `mauvais`/`moyen`/`fast-generique` au passage.

## Tâches techniques en suspens

> **Priorité tranchée le 18/08** : une seule chose presse, et pas parce qu'elle est importante — parce qu'elle est **asymétrique**.

- [ ] **Bump des actions GitHub** — **FAIT le 18/08 (suite)** : checkout@v7, setup-node@v7, upload-pages-artifact@v5, deploy-pages@v5, runner passé en **ubuntu-24.04** (dépréciation du 22.04 au 17/09/2026). Item gardé en trace jusqu'à confirmation d'un premier déploiement réussi, puis à supprimer.
- [ ] Archivage du JOURNAL (n'affecte que les sessions de travail, pas les étudiants — après la rentrée).
- [ ] **Regroomer la pile de flèches TODO** — une vingtaine d'entrées historiques dont l'utilité est nulle puisque le JOURNAL les porte. Hygiène interne pure, dernier de la liste.

## Décisions éditoriales en attente

*Portées par `BACKLOG.md` (section Discussions/décisions en attente, conventions à éprouver) et `conventions.md` §8 (candidates en vol). Pas de duplication ici.*
