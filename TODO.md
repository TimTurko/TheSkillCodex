# TODO — TheSkillCodex

> Fichier privé (non publié). Listes courtes et actionnables. Items faits :
> supprimer plutôt qu'archiver — l'historique est dans `JOURNAL.md`.

## Prochaines sessions (ordre logique)

> **➤ MAJ 17/08 (suite 2)** — **RELECTURE DE FOND TERMINÉE — §12 TEENSY (5/5), §15 PIC (1/1), §16 PCB (2/2) + RATTRAPAGE §11.** Fin du chantier : **les 20 sections du parcours étudiant sont lues**. 8 fiches relues + corrigées, **6 SVG créés**. **§12 Teensy** — **C86 : trois occurrences dans `teensy-arduino-core`** (l'« occurrence unique » du 17/08 ne valait que pour le périmètre relu), corrigées **en mixte** (variable d'état au sketch d'ouverture, **`digitalToggleFast`** à l'Exemple et au corrigé — primitive vérifiée dans `PaulStoffregen/cores`) + **piège « relire une broche de sortie »** ajouté ; **ADC faux corrigé** (« 12 bits et plus » → 12 matériels / **10 par défaut** / ~10 utiles, plage figée 0-3,3 V, `analogReference()` sans effet) — **3ᵉ exemplaire** après Uno R4 et STM32duino, la fiche **se contredisait avec `teensy-usb`** ; **C71 reconstruit** sur trois angles (3,3 V strict / aucune radio / écosystème mince), **panorama réduit aux 4.1-4.0** (LC, 3.2, 3.5, 3.6 **arrêtées depuis 2023**) ; `aa: PROJ/5` (**4ᵉ trou de parité**) ; version d'outil précisée (paquet « for Arduino IDE 2.0.4 or later », **1.8.x abandonné sous macOS depuis Teensyduino 1.60**) ; 2 embeds C74 + **alts-fleuves raccourcis** ; `phases` +dossier-technique sur `-usb`. **§15 PIC** — **C71 tranchée absolue** (arbitrage Tim, contre ma recommandation) : 4 mentions retirées, y compris le coût unitaire en série ; rachat d'**Atmel par Microchip en 2016** ajouté ; contrôle « parcours fantasmé » **RAS**. **§16 PCB** — **C45 confirmée 4ᵉ épreuve** ; **ERC nommé à l'étape 1** du hub (le sigle n'apparaissait que dans les pièges) ; **« soutenue par le CERN » périmé** → **Linux Foundation depuis 2019**, CERN au passé (dons clos 09/2023) ; format C29 normalisé ×3 ; bibliothèque CERN (17 000 composants, mai 2026) + IPC-2581. **§11 rattrapé** — contradiction JOURNAL/dashboard levée sur demande de Tim : relecture de contrôle de `stm32-cubemx`/`-hal`/`-registres`, **elles étaient bien relues le 17/08**, seule la clôture s'était arrêtée ; 3 annotations + bandeau écrits, **aucune correction**. Conventions : **aucune numérotée (reste C86)** + 7 notes §8. **PROCHAINE SESSION = `falstad`, REFONTE PÉDAGOGIQUE** (décision Tim, révisée en fin de session) : **pas une intégration de captures** — Tim n'en a aucune et n'en veut pas encore. Session de **scénarisation** : reprendre la fiche entièrement, la transformer en **vrai tutoriel** (progression, gestes montrés, modification de circuits, exercices). Échange d'abord (options argumentées → arbitrage Tim), production ensuite. **Inversion assumée de la méthode `wokwi`** : ici le scénario **détermine** la liste des captures à shooter, au lieu que les captures déterminent le texte. **RESTE À TIM** : commit/push ; **clic-test des 6 SVG neufs** (`teensy/comparaison-arduino`, `teensy-arduino-core/paliers-d-acces`, `teensy-prise-en-main/montage-led-externe`, `teensy-audio/empilage-audio-shield`, `teensy-usb/montage-bouton` + `montage-potentiometre`) **et des 3 embeds à largeur modifiée** (`teensy-audio/flux`, `teensy-usb/personnalites`, `pcb/flux`) → cocher §12 et `pcb` ; clic-tests **§11 et §8 toujours en attente** (`trois-modes`, `flux`, `abstraction-couches`, `cycle`, `lecture-courbe`, hub `stm32`) ; **captures : lot porté à 15** (Falstad 2, LTspice 3, §10 six, **KiCad 3**). `pic` **cochée sans clic-test** (aucun média). **Pendances inchangées** : fiche transverse `filtrage`, fiches de lecture C85, audit liens `[[micropython-eeprom]]`, `.bak` de la racine, **pile de flèches TODO à regroomer**.

> **➜ MAJ 17/08 (suite)** — **RELECTURE DE FOND : §9bis XIAO CLOS (4/4), §8 SIMULATION (3/4), §10 ESP8266 CLOS (3/3), §11 STM32 RELU (6/6, 2 cochées).** Suite de la session du jour, PC perso, mode inversé **en lot par module**. **§9 ESP32 CLOS (12/12)** après clic-test du hub. **16 fiches relues + corrigées** + 3 transverses patchées (`wifi`, `analyseur-logique`, `micropython-simulation`). **4 SVG créés** (`esp8266/comparaison-esp32`, `esp8266-prise-en-main/montage-led-externe`, `stm32-hal/trois-modes`) + **9 captures Wokwi intégrées** (déposées par Tim, renommées en kebab-case — une majuscule initiale et une typo `helloEPS32` corrigées). **10 embeds mis au gabarit C74.** **Corrections de fond web-vérifiées** : charge LiPo XIAO **50 → 100 mA** (recharge 8 h → 4-5 h), crête caméra Sense **0,6 A → 350 mA**, **deep sleep Sense ≠ 14 µA** (64 µA à 4 mA), exemple Wokwi **Uno → ESP32** (la capture annonçait un ESP32), **ADC STM32duino 10 bits par défaut** (`analogReadResolution(12)` = appel nécessaire, même motif que l'Uno R4), **HAL/LL = Advanced Settings → Driver Selector** (pas Code Generator), dimensions XIAO **17,8 mm**, URL ESP8266 en **https**. **Argumentaire ESP8266 reconstruit** (C71 : la fiche *reposait* sur le prix — refaite sur parc existant / objets du commerce déjà équipés / encombrement). **Conventions : C86** (ne jamais dériver l'état d'une sortie en la relisant ; borne = primitives matérielles de bascule OK) + **précision C65** (c'est le **tutoiement** qui est proscrit, « vous » et « on » cohabitent — **pas de dette de conversion**) + 5 notes §8. **PROCHAINE SESSION = FIN DE LA RELECTURE** : **§12 Teensy (5)**, **§15 PIC (1)**, **§16 PCB (2)** — 8 fiches, même mode inversé en lot, contrôles d'office C62/C65/C68/C69/C71/C74/C77/C78/C79/C80/C81/C85/**C86** + parité `aa: PROJ/5` sur les prise-en-main + version d'outil imposée + porte famille. **RESTE À TIM** : commit/push ; **clic-tests** — `trois-modes.svg` (neuf), `flux.svg` + `abstraction-couches.svg` + `cycle.svg` + `lecture-courbe.svg` (largeurs modifiées) → cocher les 4 fiches §11 restantes et le hub §8 ; **captures Falstad (2) + LTspice (3)** — le §8 reste **non clos** sans elles (item posé § Rédaction) ; 6 placeholders C29 du §10. **Pendances inchangées** : fiche transverse `filtrage`, audit liens `[[micropython-eeprom]]`, `.bak` de la racine, **pile de flèches TODO à regroomer**.

> **➜ MAJ 17/08** — **RELECTURE DE FOND §9 ESP32 (11/12) + OUVERTURE §9bis XIAO.** (PC perso, reprise après congés.) ⚠ **Ouverture décalée** : le dépôt local était en retard d'une session — le travail du **01/07** (hub `esp32` relu + **3 fiches bus créées** `esp32-uart`/`-i2c`/`-spi`) était poussé mais **non tiré**, et sa clôture documentaire n'avait jamais été écrite (seul le dashboard avait été sauvé). `git pull` fait par Tim en cours de session. **10 fiches relues+corrigées** : les 9 du §9 + le hub `xiao-esp32-s3`. **`potentiometre` cochée → §6 socle commun CLOS (37/37)**. **5 SVG créés** (`montage-led-externe`, `couches-arduino-core`, `arbre-de-decision`, `montage-potentiometre`, `montage-reveil-bouton`) + `variantes.svg` retouché (C71). **Corrections de fond web-vérifiées** : `analogSetAttenuation(ADC_11db)` présenté à tort comme nécessaire (**défaut déjà à 11 dB** → `analogReadMilliVolts()`), **`BLEScan::start` en API 2.x qui ne compile pas en ≥ 3.0**, piège `LED_BUILTIN` faux sur C3/S3/C6 (**LED WS2812** → `RGB_BUILTIN`), « `Serial` à 115200 par défaut », caveats **mono-cœur** ×2, `app_main()` qui retourne. **Conventions : C85** — encart C77 réservé aux **idiomes embarqués**, mécanismes de langage renvoyés à deux **fiches de lecture** à produire (item TODO § Rédaction). **PROCHAINE SESSION = POURSUITE §9bis XIAO** à **`xiao-prise-en-main`**, puis `-alimentation`, `-sense` — même mode inversé, contrôles d'office C62/C65/C68/C69/C71/C74/C77/C78/C79/C80/C81/C84/**C85** + porte famille + specs en source. **⚠ Arbitrage ouvert** : les 3 tutos XIAO sont en « tu » alors que l'étalon des tutos MCU (`arduino-prise-en-main`) est en « vous » — à trancher sur pièces dès la première fiche. **RESTE À TIM** : commit/push ; **clic-test rendu** des 5 SVG neufs + `reveil.svg` + `ordonnancement.svg` + les **4 SVG XIAO** (jamais rendus depuis le 13/06, largeurs forcées à 640) + `variantes.svg` ; **clic-test du hub `esp32`** (`positionnement-variantes`, en attente depuis le 01/07) → cocher pour clore le §9 ; **supprimer les `.bak`** de la racine ; **regroomer la pile de flèches TODO**. **Pendances inchangées** : captures C29, fiche transverse `filtrage`, audit liens `[[micropython-eeprom]]`.

> **➜ MAJ 01/07** — **RELECTURE §9 ESP32 OUVERTE (mode inversé, PC pro) : hub + carte C68 + 3 fiches bus créées (C47a) + §14 clos.** Hub `esp32` relu : C71 ×2 (« prix contenu » + « économique » C3 retirés/reformulés), coquille liste, **carte C68 `positionnement-variantes` créée + embarquée** (8 variantes, connectivité × puissance ; dark + markers en classes), **C47 bloc *Communication* repointé** `arduino-*`→`esp32-*` ; **fond variantes web-vérifié Espressif** (RAS ; C5 = seul 5 GHz, P4 400 MHz assumé hors chemin connecté). **§14 Raspberry Pi coché** (4 fiches, rendu validé Tim). **C47(a) : 3 fiches bus créées + validées Tim** — `esp32-uart` (Serial2 GPIO16/17, 3 UARTs, WROVER), `esp32-i2c` (Wire GPIO21/22, scanner, Wire1, pull-ups), `esp32-spi` (VSPI 18/23/19/5, carte SD, HSPI) + **3 SVG câblage audités C80/C81** ; trame étalon `esp32-serie` (C49), `aa: PROJ/5` (C20, tally inchangé) ; **portes famille `[[uart]]/[[i2c]]/[[spi]]` ouvertes vers ESP32** (d'office). Broches vérifiées doc arduino-esp32. `esp32-prise-en-main` **critiquée, NON appliquée** (fond web-vérifié RAS ; `aa: PROJ/5` d'office + décision C79 [montage LED Ex.2] en attente). Conventions : aucune numérotée (reste **C84**) ; **méthode candidate §8** : passe de validation hands-on par l'auteur-testeur naïf. **PROCHAINE SESSION = PASSE HANDS-ON ESP32 (Tim exécute sur matériel réel).** Tim n'a jamais utilisé d'ESP32 → il déroule les tutos sur carte au fil de la relecture (cobaye naïf), **récolte les captures C29** du module (tous placeholders) + captures de guidage en cas de friction. Reprendre à **`esp32-prise-en-main`** (appliquer `aa: PROJ/5` + trancher C79 d'abord), puis `-arduino-core`, `-idf`, `-gpio`, `-wifi`, `-ble`, `-deep-sleep`, `-freertos` ; contrôles d'office C62/C65/C68/C69/C71/C77/C78/C79/C80/C81/C84 + porte famille + specs en doc Espressif. **RESTE À TIM** : commit/push ; **clic-test carte C68 du hub** (`positionnement-variantes`, clair/sombre/mobile) → cocher hub §9 ; brancher un ESP32 pour la passe hands-on ; **regroomer la pile de flèches TODO** (purge des flèches historiques — l'historique vit au JOURNAL).

> **➜ MAJ 30/06** — **RELECTURE DE FOND §13 MicroPython TERMINÉE (38/38) + CLÔTURE.** Fin du mode inversé (PC perso) : **28 fiches restantes relues+corrigées** (`entree-tor` → `watchdog`). **Rendus Quartz validés Tim → dashboard §13 clos** (14 dernières cases cochées, bandeau « TERMINÉE » ; §13 = **1er module famille MCU entièrement relu**). **~15 SVG** créés/régénérés dont **5 SVG-concepts neufs** (`bloquant-vs-non-bloquant`, `persistance-fichier`, `profil-courant-veille` « principe », `boucle-fermee-pid`, `fragmentation-tas`). **3 corrections de fond web-vérifiées (doc RP2)** : `timers` (Timer **virtuel** `id=-1` + callback soft/hard IRQ `hard=True`), `deep-sleep` (réveil-broche `lightsleep` = bug ouvert #7035/#16442 ; `deepsleep`≈`lightsleep`, pas de µA), `watchdog` (plafond 8388 ms) ; + specs ponctuelles (`afficheur` framebuf sans échelle, `sortie-pwm` duty_u16/ns, `capteur-analogique` temp RP2040). **Lien cassé** `[[micropython-eeprom]]`→`[[micropython-stockage]]` corrigé (memoire) — **audit des autres entrants au BACKLOG**. Conventions §8 : **C84 candidate** (SVG-concept neuf quand le jumeau n'est pas transposable, éprouvée 3/N) + notes (vérif source RP2, verdict C82 « clean », divergence « principe »). **JOURNAL > 100 ko assumé** (réduction prochaine session). **PROCHAINE SESSION = MAJ DOCUMENTAIRE dédiée** : **archiver les entrées anciennes du JOURNAL** (cible 08-19/06 → `JOURNAL-archive.md`, marqueur `<!-- INSERT_JOURNAL_HERE -->` préservé, invariant antichrono) pour repasser sous 100 ko ; puis **reprise relecture §14 Raspberry Pi** (mode inversé, médias en place) — `_drafts/relecture-ordre.md`, contrôles d'office C62/C65/C68/C69/C71/C77/C78/C79/C80/C81/C82/C83/**C84** + porte famille + câblage audité vs code. **RESTE À TIM** : commit/push ; **relire l'ajout de section `micropython-servomoteur`** (retour de position, [gros ajout]) ; captures C29 §13 (Thonny, multimètre veille, REPL `gc.mem_free()`, carte porteuse, alim étape 3) ; **audit liens `[[micropython-eeprom]]`** résiduels. **Pendances inchangées** : fiche transverse `filtrage` (lien rouge), captures C29 antérieures.

> **➜ MAJ 29/06** — **RELECTURE DE FOND §13 MicroPython OUVERTE (mode inversé) : parcours langage + gpio (10 fiches), PC pro.** Décisions d'ouverture tranchées : **(a) relecture de fond** (pas production médias), **(b) câblage MicroPython strict**, **(c) `aa: PROJ/5` sur `micropython-repl`** — les 3 décisions ouvertes des flèches 19–27/06 sont **résolues**. **10 fiches relues+corrigées** (hub famille, prise-en-main, simulation, hub langage, repl, types, contrôle, fonctions, modules, gpio) — **non cochées dans `relecture-ordre` (attente clic-test rendu Tim)**. Faits : hub (« Par où commencer ? » + REPL dédoublonné + `Pin("LED")`), repl + prise-en-main `aa: PROJ/5`, types (piège « simple précision » + caveat float), modules (4× `Pin("LED")`), accents commentaires (7 fiches), gpio (SVG `montage-led-bouton` audité **C80-ok**, parité C77 vérifiée vs `arduino-gpio`). Conventions §8 : **C82** (float simple précision RP2 → corrigés en ≈ + piège sur `types`), **C83** (`Pin("LED")` portable réel, Wokwi excepté) + notes (précision C65 registre, accents, parité C77, câblage-sim=capture). **⚠ Acquis méthodo** : sur-application de C65 (dévoussage abusif de `prise-en-main`+`simulation`) **détectée et corrigée — 20 conversions annulées** après vérif des jumeaux Arduino (procédure/Exemple en « vous »). **PROCHAINE SESSION = POURSUITE §13** à **`micropython-entree-tor`** puis sortie-tor, capteurs, PWM, bus, moteurs… (28 fiches §13 restantes) **+ §14 Raspberry Pi**, même mode inversé + contrôles d'office C62/C65[corrigé]/C68/C69/C71/C77/C78/C79/C80/C81/**C82**/**C83** + porte famille ; **câblage strict** = audit SVG vs code sur chaque fiche matérielle. **RESTE À TIM** : commit/push ; **clic-test rendu des 10 fiches** (surtout SVG `montage-led-bouton`, clair/sombre/mobile) ; **archivage JOURNAL** (seuil 100 ko largement franchi — à passer) ; **regroomer la pile de flèches TODO** (rebâtie depuis le 11/06). **Pendances inchangées** : captures C29, fiche transverse `filtrage` (lien rouge), slug eeprom (`micropython-stockage` renommé au dashboard).

> **➜ MAJ 27/06** — **MÉDIAS FAMILLES MCU : RELECTURE/CORRECTION + CLÔTURE.** Boucle l'intégration du dépôt Martin (PC perso, `filesystem:*`). **6 SVG de câblage régénérés contre le code (C81)** + embeds posés à la place des placeholders : `micropython-sortie-tor` (LED GP8/buzzer GP9/relais GP10), `-capteur-numerique` (HC-SR04 Trig GP9 + Echo pont diviseur GP10), `-spi` (GP2-5), `-moteur-cc` (DRV8833 GP12/11), `-moteur-pas-a-pas` (ULN2003 GP2-5), `raspberry-pi-gpio` (bouton GPIO2). **2 SVG replacés** (`roles-ble`→esp32-ble, `brochage-d1-gpio`→esp8266-arduino-core). **4 brochages + attributions** (Pico CC BY-ND Raspberry Pi, Teensy PJRC, STM32 Nucleo-64 + Blue Pill maison) + **cartographie 1→2 STM32**. **Rendu Tim : 12 pages acceptables**, 3 réserves tracées BACKLOG non bloquantes (2 SVG à retravailler `montage-relais`/`branchement-stepper` ; image `brochage-pico.png` à reprendre avant publi). Conventions : **aucune nouvelle, C81 ré-éprouvée 2/N**. **PROCHAINE SESSION = RELECTURE DE FOND (mode inversé) des familles à médias en place** — **MicroPython §13** + **Raspberry Pi §14** en priorité (dashboard annoté « médias validés, relecture de fond restant à faire »), même mode inversé + contrôles d'office C62/C65/C68/C69/C71/C77/C78/C79/C80/C81 + porte famille ; **alternative** : produire les médias résiduels (ESP32/RPi gaps, ESP8266). **DÉCISIONS TIM OUVERTES** : couverture câblage MicroPython stricte ~14 SVG / allégée ; `aa: PROJ/5` sur `micropython-repl` ; trancher relecture-de-fond vs production-médias pour la prochaine session. **RESTE À TIM** : commit/push ; captures matérielles en placeholder (Shell Thonny, oscilloscope, navigateur+IP, vue Registers CubeIDE) ; reprise visuelle ultérieure des 2 SVG + brochage Pico ; **archivage JOURNAL** (seuil 100 ko franchi). **Pendances inchangées** : captures C29, fiche transverse `filtrage` (lien rouge), slug eeprom.

> **➜ MAJ 25/06** — **INTÉGRATION DES MÉDIAS FAMILLES MCU : dépôt vérifié + 16 EMBEDS INSÉRÉS (15 fiches).** Session d'intégration (PC pro). Dépôt Martin (28 fichiers / 22 dossiers `img/<slug>/`) **vérifié** (structure, UTF-8/XML, 2 PNG officiels) ; **anomalie STM32 corrigée** (`stm-32` typo + `brochage-nucleo(-64).svg` parenthèses → `stm32/brochage-nucleo-64.svg`, `stm-32` supprimé). **16 embeds conformes posés** (MicroPython 11, ESP32 3, RPi 1, STM32 1) au format gabarit Arduino (placeholder→SVG, photo→schéma C78, alt|largeur 600/640). Conventions : **C81 candidate** (média câblé audité contre le code de la fiche, pas la cartographie). ⚠ **6 SVG divergents NON insérés** (broches ≠ code, à régénérer en relecture) : `micropython-sortie-tor` (relais GP16/fiche GP8-9-10), `-capteur-numerique` (générique/fiche HC-SR04 GP9-GP10+diviseur), `-spi` (GP18+/fiche GP2-5), `-moteur-cc` (L298N/fiche DRV8833 GP12-11), `-moteur-pas-a-pas` (GP10-13/fiche GP2-5), `raspberry-pi-gpio` montage (bouton GP27/fiche GPIO2). ⚠ **2 SVG mal ciblés** : `roles-ble`→`esp32-ble`, `brochage-d1-gpio`→`esp8266-arduino-core`. **PROCHAINE SESSION = RELECTURE/CORRECTION CLASSIQUE des fiches familles MCU** (mode inversé, gabarit Arduino) avec **validation du rendu de chaque embed** (`npx quartz build --serve` ; cocher `relecture-ordre.md`), **régénération des 6 SVG divergents** contre le code (Claude écrit direct), **replacement des 2 SVG mal ciblés**. **DÉCISIONS TIM OUVERTES** : attributions `brochage-pico.png` (Raspberry Pi CC BY-ND) + `teensy/brochage.png` (PJRC) ; cible(s) fiche(s) des 2 brochages STM32 (`brochage-blue-pill`/`-nucleo-64`) + **cartographie 1→2** ; captures matérielles laissées en placeholder (Shell Thonny, oscilloscope, navigateur+IP, vue Registers CubeIDE). **RESTE À TIM** : commit/push ; clic-test rendu des 16 embeds (surtout les **7 placés sans placeholder** : temporisation, prog-non-bloquante, esp32-gpio ×2, esp32-wifi, rpi brochage-40, stm32 registre). **Pendances inchangées** : captures C29 Arduino, fiche transverse `filtrage`, slug eeprom.

> **➜ MAJ 19/06** — **CARTOGRAPHIE DES MÉDIAS DES FAMILLES MCU PRODUITE** (inventaire, pas de fiches ; PC pro → perso, changement de serveur MCP en cours de session). Livrable : **`_drafts/cartographie-medias-familles.md`** — **tableau unique** (`Fiche | Nom | Chemin | Description | Type`), **62 lignes** (~24 SVG à dessiner, 6 photos, ~30 captures), densité **modulée** C47 (cible = spécifique carte/outil ; conceptuel reste en [T]), + synthèse chiffrée, « déjà existant », ordre de prod. Constats : placeholders C29 **déjà inline** dans les fiches (captures à shooter en place) ; câblages MicroPython en **photo** à convertir en **SVG broches Pico** (C78/C80). **Déviation `micropython-serie` → `micropython-repl` VALIDÉE** (C57) — section « Tracer une grandeur dans Thonny » ajoutée à la fiche (+ placeholder Plotter). Conventions : **aucune nouvelle (reste C80)**. **PROCHAINE SESSION = DÉBUT DE PRODUCTION DES MÉDIAS** (sortie inventaire) — ordre suggéré : **(1) brochages + photos (1/famille, réutilisables)** → **(2) ESP32** (brochage annoté + montage GPIO ; Wi-Fi/BLE en option) → **(3) Raspberry Pi** (brochage 40 br. + câblage GPIO) → **(4) MicroPython** (gros câblage — **décider strict ~14 SVG / allégé**) → **(5) STM32/Teensy/ESP8266** (captures-centrées). Mode **production**, gabarit SVG auto-contenu dark + vrais caractères UTF-8 + markers en classes C69 + broches du code C80 ; dossiers par fiche C73. **DÉCISIONS TIM OUVERTES** : `aa: PROJ/5` sur `micropython-repl` (parité `arduino-serie`) ? ; sous-arbitrage câblage MicroPython (strict/allégé) ; valider l'ordre de production. **RESTE À TIM** : commit/push ; clic-test section REPL au rendu. **Pendances inchangées** : captures C29 (13), fiche transverse `filtrage` (lien rouge), slug eeprom. *(`micropython-serie`→`repl` retiré des pendances — résolu.)*

> **➜ MAJ 17/06 (suite 3)** — **§7 ARDUINO TERMINÉ (10 dernières fiches) + NOTION `asservissement` CRÉÉE + CLÔTURE.** Dernière tranche relue/cochée : `arduino-gpio-boot`, `-programmation-non-bloquante`, `-machine-a-etats`, `-eeprom`, `-interruptions`, `-timers`, `-deep-sleep`, `-pid`, `-memoire`, `-watchdog` (**14 SVG créés** ; corrections de fond : 0xFFFF eeprom, réveil-redémarrage ESP32 deep-sleep, bootloop watchdog reformulé, coup de dérivée PID ; **notes de portabilité familles MCU** systematisées). **Notion transverse `content/embarque/asservissement.md` créée** (mère du tuto PID, + SVG `boucle-fermee`). **Toutes les fiches Arduino sont relues** (`_drafts/relecture-ordre.md` — §7 clos). Conventions : **aucune nouvelle, reste C80** (note d'éprouvage « portabilité familles MCU », affinement C47). **PROCHAINE SESSION = NOUVEAU SUJET — CARTOGRAPHIE DES MÉDIAS DES FAMILLES MCU.** Inventaire structuré (**pas de fiches**) de **tous les SVG à créer et captures à prendre** pour amener §8+ (ESP32, ESP8266, STM32, Teensy, XIAO, MicroPython, Raspberry Pi) **au format/densité de la famille Arduino** (= gabarit). **Brief : `_drafts/cadrage-cartographie-medias-familles.md`.** **RESTE À TIM** : ~~commit/push~~ (fait) ; clic-test des **14 SVG** de la session (clair/sombre/mobile) ; **décision placeholder pinout** `arduino-interruptions` (capture C29 ou retrait) ; **relire la notion neuve `asservissement`** + créer ses liens réciproques (schema-bloc-fonctionnel / timer / pwm / arduino-moteur-cc / arduino-capteur-analogique) + confirmer son emplacement (`embarque/` vs `embarque/mcu/`) ; **créer la fiche transverse `filtrage`** (lien rouge actif, dépendance restante) ; captures C29 + validations en attente inchangées (micropython-serie→repl, slug eeprom).

> **➜ MAJ 17/06 (suite 2)** — **RELECTURE §7 ARDUINO : bus + actionneurs + afficheur + debug (8 fiches).** `arduino-uart`/`-i2c`/`-spi` (**couche famille** C47, théorie renvoyée aux notions [[uart]]/[[i2c]]/[[spi]] ; SVG branchement croisé/pont-série/I2C/microSD ; **erreur I2C « Mega→plusieurs bus » → Uno R4 WiFi** ; encarts C77 ; prix C71 retirés), `arduino-servomoteur` (Coût retiré, MG996R recentré, **section « Servos à retour de position » ajoutée** [retour analogique A0 + calibration ; variante PWM Parallax 360], SVG retour-position + sg90 réutilisé), `arduino-moteur-cc` (SVG L298N, anti-rebond non bloquant C77, candidate [[pont-en-h]] BACKLOG), `arduino-moteur-pas-a-pas` (SVG 28BYJ-48+ULN2003, prix € retirés, piège step() bloquant + Vref A4988), `arduino-afficheur` (SVG I2C OLED/LCD, encarts buffer/clear, code LCD commenté), **`arduino-debug` ENRICHIE** (demande Tim — fiche « particulièrement riche » : SVG triage `ou-est-le-probleme` + 3 sections [Comprendre où / B compile / A comportement‑12 pièges] + **section code↔câblage** ; liens [[cpp]]/[[arduino-gpio]]). **8 cochées.** Conventions §8 : **C80** (SVG de câblage = noms de broches du code, rétroactive) + note `write_file`/`\u`. **79 → 80.** ⚠ **6 SVG corrigés d'un défaut d'encodage `\u`** (write_file écrit littéralement, ≠ edit_file ; → vrais caractères + relecture post-écriture). **PROCHAINE SESSION = REPRISE LECTURE DES FICHES à `arduino-gpio-boot`** puis `-programmation-non-bloquante`, `-machine-a-etats`, `-eeprom`, `-interruptions`, `-timers`, `-deep-sleep`, `-pid`, `-memoire`, `-watchdog` (~9 fiches Arduino restantes, `_drafts/relecture-ordre.md`), même mode inversé + contrôles d'office C62/C65/C68/C69/C71/C77/C78/C79/**C80** + porte famille + vigilance pull-up + specs en doc constructeur. **CLIC-TESTS au rendu** : 9 SVG de la session (clair/sombre/mobile — vérifier que l'encodage `\u` corrigé rend bien). **RESTE À TIM** : commit/push ; **relecture humaine de `arduino-debug` enrichie** (gros ajout) ; capture C29 « IDE 2.x mode Debug » ; **valider `arduino-serie`** (en attente depuis 16/06) ; captures C29 en attente inchangées ; validations en attente inchangées (micropython-serie→repl, slug eeprom, fiche `filtrage` rouge).

> **➜ MAJ 17/06 (suite)** — **RELECTURE §7 ARDUINO : temporisation → alimentation (5 fiches).** `arduino-temporisation` (callout `[!danger]` delay aveugle, 2 SVG, C77, note R4 int), `arduino-bibliotheques` (SVG couches-abstraction + servo SG90, C77), `arduino-module` (⚠ **pull-up I2C** : bus qui s'effondre — étape 4 revulgarisée résistance/pull-up ; **DHT11 1 mesure/s** ; 2 SVG dont `pullups-paralleles` refait avant/après ; C71), `arduino-shield` (**exemple refait piloté par bibliothèque** `AFMotor` ; **3 produits démêlés** L298P / L293D / TB6612-I2C vérifiés web ; 2 photos déposées), `arduino-alimentation` (⚠ **régulateur corrigé** LM7805→**NCP1117** [R3] / **ISL854102** découpage [R4 6-24 V] vérifié docs ; SVG `alimentation-separee` ; PSU/BOM glosés ; renvoi `alimentation-electronique` ; aa PROJ/5 ; phases). **5 cochées** (`_drafts/relecture-ordre.md`). Éprouvage inchangé à **C79** (candidate au BACKLOG : exemple shield/module piloté par bibliothèque). **PROCHAINE SESSION = `arduino-uart`** puis `-i2c`, `-spi`, `-servomoteur`, `-moteur-cc`, `-moteur-pas-a-pas`, `-afficheur`… (~18 fiches Arduino restantes), même mode inversé + contrôles d'office C62/C65/C68/C69/C71/C77/C78/C79 + porte famille + **vigilance pull-up** + **specs en doc constructeur**. **CLIC-TESTS au rendu** : 6 SVG de la session (clair/sombre/mobile) + 2 photos shield. **RESTE À TIM** : commit/push ; déposer captures C29 (12, dont « Uno + 3 voies d'alim » neuve) ; **valider `arduino-serie`** (en attente depuis 16/06) ; créer la fiche transverse `filtrage` (lien rouge) ; validations en attente inchangées (micropython-serie→repl, slug eeprom).

> **➜ MAJ 17/06** — **RELECTURE §7 ARDUINO : capteur analogique + sortie PWM + notion potentiomètre.** 3 fiches : `arduino-capteur-numerique` médias finalisés (GIF principe IR `how-ir-sensor-works`), **`arduino-capteur-analogique` ✓** (⚠ **erreur Uno R4 ADC corrigée** [défaut **10 bits / 0-1023**, vérifié docs Arduino — table + piège inversé reformulés], warning AREF [EXTERNAL avant 1er analogRead], note TMP36, C77 conversion + encart, 2 SVG [branchement-potentiometre, pont-diviseur-ldr — bug masse corrigé], liens adc/precision-de-mesure/filtrage), **`arduino-sortie-pwm` ✓** (réflexe pinout/doc pour MCU hors liste, R4 analogWrite 0-255 défaut, C77 fondu, SVG branchement-led-pwm + SVG potentiomètre ressorti, liens potentiometre/dac/capteur-analogique/pwm). **Notion `potentiometre` créée** (transverse [T], `aa:[]`, webp triptyque migrée, réciproques capteur-analogique/niveaux-de-tension/sortie-pwm). Conventions §8 : **C79** (un montage/schéma de principe par bloc de code). **78 → 79.** **PROCHAINE SESSION = `arduino-temporisation`** puis `-bibliotheques`, `-module`, `-shield`, `-alimentation`, `-uart`, `-i2c`, `-spi`, `-servomoteur`, `-moteur-cc`… (`_drafts/relecture-ordre.md`, ~20 fiches Arduino restantes), même mode inversé + contrôles d'office C62/C65/C68/C69/C70/C71/C77/C78 + **C79** + porte famille + **vigilance pull-up**. **CLIC-TESTS au rendu** : capteur-analogique (2 SVG clair/sombre/mobile + capture **traceur série** à déposer), sortie-pwm (SVG LED + capture **oscillo** à déposer), notion potentiometre (webp + render). **RESTE À TIM** : commit/push ; déposer captures traceur série + oscillo ; **créer la fiche transverse `filtrage`** (lien rouge dans capteur-numerique + capteur-analogique) ; décider du filigrane « robocraze » du GIF IR. Validations en attente inchangées (micropython-serie→repl, slug eeprom).

> **➜ MAJ 16/06 (suite)** — **RELECTURE §7 ARDUINO REPRISE** (mode inversé ralenti à la fiche, PC pro, après la migration). **5 fiches traitées** : `tinkercad` ✓, `arduino-serie` (faite, **décochée** — attente clic-test rendu Tim), `arduino-gpio` ✓, `arduino-entree-tor` ✓, `arduino-sortie-tor` ✓ + **notion `ascii` créée** (transverse [T], `aa:[]`, SVG `caractere-vers-code`, liée serie/cpp-types/manip-bits). **Corrections de fond** : 2 inversions pull-up (`tinkercad`, `arduino-gpio` — LED toujours allumée = GND/bouton 4 pattes), Nano D0–D13, anti-rebond OK (C44). **4 SVG créés** : `ascii/caractere-vers-code`, `arduino-entree-tor/rebond` (chronogramme), `arduino-sortie-tor/transistor-bas-cote` (⚠ **à peaufiner avant mise en ligne** — BACKLOG) + `arduino-sortie-tor/montage` (carte d'interfaces, remplace une photo). Conventions §8 : **C77** (code commenté débutant + encart « Comment lire ce code ») et **C78** (montage → schéma SVG, pas photo). **76 → 78.** **PROCHAINE SESSION = POURSUITE §7 Arduino** à partir de **`arduino-capteur-numerique`** (`_drafts/relecture-ordre.md` ; ~22 fiches restantes), même mode inversé + contrôles d'office (C62/C65/C68/C69/C70 + porte famille + **C71 prix** + **C77 code commenté** + **C78 schéma vs photo** + **vigilance pull-up** sur fiches à bouton). **CLIC-TESTS au rendu** : 4 nouveaux SVG (clair/sombre/mobile) + affichage « \n » d'`ascii`. **RESTE À TIM** : commit/push ; **valider `arduino-serie`** au rendu (puis cocher) ; reprise du SVG transistor avant mise en ligne ; captures C29 (10) inchangées ; validations en attente inchangées (micropython-serie→repl, filtrage rouge, slug eeprom).

> **➜ MAJ 16/06** — **SESSION MIGRATION MÉDIAS C73 FAITE** (PC pro, exécution autonome par fiche). **121 SVG** déplacés du dossier plat vers `content/ressources/img/<slug-fiche>/`, **121 embeds repointés** en chemin absolu ; **3 branches** (conduite 29 — déjà validée ; embarque 86 ; meca 5) **+ `techno-sans-fil`** (1 SVG rattrapé). Racine `img/` = dossiers seuls (vérifié ; seuls `archive/`+`commun/` à part). Nommage : descripteur = nom moins préfixe de fiche (cas préfixe!=slug `analyse-de-schema-electronique`/`raspberry-pi-projet`) ; multi-fiches -> fiche éponyme + cross-réf absolue ; transverse -> `commun/` ; renommage `programmation-non-bloquante`->`arduino-programmation-non-bloquante/bloquant-vs-non-bloquant.svg`. **Validé par Tim** (relecture : aucune image manquante). Conventions §8 : **C76** (placement/nommage sous C73). ⚠ **Trou de carto rattrapé** : la famille `sans-fil/` manquait de la carte SVG du 15/06 -> règle C76 « lister la racine `img/` en fin de migration ». **PROCHAINE SESSION = POURSUITE RELECTURE §7 ARDUINO** (`_drafts/relecture-ordre.md`) : reprendre **au hub `arduino`** (re-vérifier ses 2 images au rendu post-migration) puis dérouler `tinkercad` -> `arduino-serie` -> `arduino-gpio` -> … (32 fiches restantes), même mode inversé ralenti à la fiche, contrôles d'office C62/C65/C68/C69/C70 + porte famille + **C71 prix** (batch antérieur). **CLIC-TESTS** : embeds migrés au rendu Quartz (chemins `/<slug>/`), + reliquat du 15/06 (`<video>` CH340, GIF Blink, `|largeur`, centrage/légendes). Recroisement `tinkercad`<->`simulation-electronique` à trancher. **RESTE À TIM** : commit/push ; **peigne SVG + liste « Reprise visuelle » du BACKLOG à rafraîchir** (noms à plat -> chemins par dossier) avant la passe voie A ; 10 captures C29 inchangées ; validations en attente inchangées (micropython-serie->repl, filtrage rouge, slug eeprom).

> **➜ MAJ 15/06** — **RELECTURE §7 ARDUINO OUVERTE** (hub + `arduino-prise-en-main` relus & validés, PC pro, mode inversé ralenti à la fiche). Hub : « par où commencer »→prise-en-main, plafond AVR, radio restreinte, FreeRTOS « est traité », **tutoriels en tête** (gabarit hub famille : `aa:[]`/`phases:[concept]`/marqueurs C26-C32). Prise-en-main : C71 prix retiré, AA PROJ/5, piège clone Nano, chemin DL IDE + don optionnel, lien **sparks.gogo.co.nz** CH340, captures scindées ; **7 médias intégrés à plat** (6 captures + GIF Blink + **1re vidéo du wiki**, install CH340). CSS : centrage images+vidéo + légende-figure. Conventions §8 : **C73** (images par dossier de fiche), **C74** (présentation images), **C75** (vidéo HTML5). **PROCHAINE SESSION — 2 volets** : (1) **SESSION MIGRATION MÉDIAS** (mode b décidé 15/06) : créer `content/ressources/img/<slug-fiche>/`, déplacer les ~120 médias à plat, corriger tous les chemins d'embed (dont le `src` vidéo de prise-en-main) ; (2) **POURSUITE RELECTURE §7 Arduino** à partir de `tinkercad` → `arduino-serie` → `arduino-gpio` → … (32 fiches restantes), même mode, contrôles d'office C62/C65/C69/C70 + porte famille + **C71 prix** (batch antérieur à la convention). **CLIC-TESTS au rendu** : `<video>` CH340 (lecture), GIF Blink (anim), syntaxe `|largeur`, centrage images/vidéo, légendes centrées, 9 chemins à plat. Recroisement `tinkercad` ↔ `simulation-electronique` à trancher au §8. **RESTE À TIM** : commit/push ; captures C29 en attente (10, inchangées) ; validations en attente inchangées (micropython-serie→repl, filtrage rouge, slug eeprom).

> **➜ MAJ 13/06 (suite 2)** — **MODULE XIAO ESP32-S3 PRODUIT** (PC perso, mode production, parcours par composition). **Hub `xiao-esp32-s3`** (alias `xiao` → résout le lien rouge `[[xiao]]` du panorama `microcontroleur`, aucun patch externe) + **3 fiches** (`xiao-prise-en-main` / `xiao-alimentation` / `xiao-sense`) + **7 SVG**. Specs vérifiées au pad sur le wiki Seeed (brochage 11 pads, 9 ADC, LED GPIO21 active bas, charge LiPo 50 mA, deep sleep 14 µA, antenne U.FL, B2B Sense). 2 corrections Tim (prix retiré → C71 ; intro hub primo-découvreur → C72). **CLIC-TESTS au rendu** : 7 SVG XIAO (clair + sombre + smartphone — guetter le défaut type `analyse-de-schema-zones`, condensateur derrière bloc) + résolution de l'alias `[[xiao]]` au survol du panorama. Maj documentaire faite : JOURNAL (suite 2), conventions §8 (C71/C72), `relecture-ordre.md` (§9bis, 4 fiches non cochées), BACKLOG (7 SVG à valider). **PROCHAINE SESSION = RELECTURE DES MODULES FAMILLES MCU §7–§16** (`_drafts/relecture-ordre.md` ; Arduino, simulation, ESP32, ESP8266, STM32, Teensy, MicroPython, Raspberry Pi, PIC, PCB **+ le module XIAO §9bis**), même mode inversé posture critique, contrôles d'office C62/C65/C68/C69/C70 + porte famille + recroisements ; **alternative** : session SVG dédiée pour valider/affiner les 7 SVG XIAO (+ backlog SVG). **RESTE À TIM** : commit/push ; 10 captures C29 inchangées ; validations en attente inchangées (micropython-serie→repl, filtrage rouge, slug eeprom).

> **➜ MAJ 13/06 (suite)** — **RELECTURE BRANCHE ESE TERMINÉE (§19, 9/9)** + flag `matrice-de-decision`→PROJ/6 **résolu** (retiré du front matter → `aa: []`, réaligné sur la cartographie ; PROJ/6 reste C via concept/schema-bloc-fonctionnel/dossier-technique ; tally 79 % inchangé). **Les deux branches d'interface (Méca + ESE) sont relues** ; ne reste à relire que les **modules familles MCU §7–§16**. Maj documentaire faite : entrée JOURNAL, conventions §8 (C58 désormais *promotion-ready*), dashboard `relecture-ordre.md` (§19 9/9 + annotation matrice). **PROCHAINE SESSION = PRODUCTION du module XIAO ESP32-S3** (décidée 13/06 ; carte retenue pour un projet ICAM l'an prochain) : **parcours autonome par composition** (esprit C47, réutilisation de l'existant par liens — pas de duplication), **focalisé S3** + ouverture famille (C3/C6/C5/S3 Sense), **les deux langages** (Arduino + MicroPython) ; **hub `xiao`** (résout le lien rouge `[[xiao]]` du panorama `microcontroleur`) + **1-3 fiches** (`xiao-prise-en-main` / `xiao-alimentation` [recharge accu + basse conso] / `xiao-sense`). Topics imposés : **brochage réduit ~11 pads + extendeur d'E/S** (I²C), **antenne** (interne/externe IPEX), **recharge d'accu** (charge LiPo). Mode **production**, registre **« tu »** (C65), contrôles **C62/C68/C69** ; SoC délégué à `esp32`, fondamentaux à [T], prog à `arduino`/`micropython`. **Cadrage complet : `_drafts/cadrage-xiao-esp32s3.md`.** **RESTE À TIM** : commit/push ; captures C29 (10) inchangées ; validations en attente inchangées (micropython-serie→repl, filtrage rouge, slug eeprom).

> **➜ MAJ 12/06 (suite)** — **RELECTURE BRANCHE CONDUITE DE PROJET TERMINÉE** (mode inversé, PC perso, validation en flux). **Première branche entièrement relue** : §1bis transverses (3) + §2 PROJ (22/22) + §3 MEO (5/5) + §20 hub `conduite/index` + 3 index de domaine remplis (`proj` 30 fiches en 5 grappes, `meo` 5, `ese` 8). Faits marquants : SVG `fils-transverses-generique` + `mecatronique-generique` créés, `jalons-generique` réécrit (6 losanges) ; **sweep registre du V** (22 conversions, grep-vérifié) ; ossature jalons à 6 ; **FC3 sécurité opérateur** propagée bête-à-cornes→pieuvre→CdCF→mind-map ; alim **24 V/3 A** + enveloppe **300 €** (cohérence securite-et-qualite/EAT/bom/amdec) ; chiffres Niryo/Moveo web-corrigés ; **5 aa ajoutés** (mind-map PROJ/1, matrice-de-decision PROJ/6, acv ESE/1+2, archivage MEO/6, revue-de-code MEO/3 — tous multi-couverture, **tally inchangé 79 %**) ; pushbacks aa validés (amdec & mecatronique `aa:[]`). **C70 candidate** (ancre double-tiret). **2 reverts** : liens `mecatronique` → `conduite/index` (≠ stub). **PROCHAINE SESSION** : reprendre `_drafts/relecture-ordre.md` — **modules familles MCU §7–§16** (branche Système embarqué, gardés pour la fin) puis **branche Méca** ; même mode inversé, contrôles d'office C62/C65/C69 + recroisements. **CLIC-TESTS au rendu** (cumul branche) : SVG fils-transverses ×3 + jalons 6 losanges + mécatronique + matrice-risques-station-météo (légende) + mind-map + retroplanning ; **ancres double-tiret** `#cas-particulier--…` ×2 ; liens slug-nu des 3 index ; `phases: integration-et-tests` au filtre Explorer ; text-anchor en classes (8 SVG planif + matrice). **RESTE À TIM** : commit/push ; reporter les 5 rattachements aa dans `couverture-en-cours.md` ; 10 captures C29 ; validations inchangées (micropython-serie→repl, filtrage rouge, slug eeprom).

> **➜ MAJ 12/06** — **RELECTURE §6 SOCLE MCU TERMINÉE (37/37)** en une session, mode inversé posture renforcée, PC perso. Global : **54 fiches validées** (§0+1+4+5+6+17 complets). Faits marquants : `deep-sleep` **réécrite au calibre interruption** (7,6 ko + SVG profil de conso) ; **trio bus gonflé** sur demande Tim (6 SVG : branchement + chronogramme par bus, max d'entités explicité, terminaisons traitées) ; grappe sans-fil enrichie (pics de courant Wi-Fi, BLE≠son, coordinateur Zigbee, LoRa/LoRaWAN) ; grappe langage : § ternaire ajouté (`cpp-conditions`), exercice cpp-logs corrigé, pont C57 `cpp`→`micropython-langage`. **9 SVG créés, 5 corrigés** (markers → **C69**). Fiche `dac` créée. Motif « porte famille manquante » appliqué en série (à garder en contrôle d'office). **PROCHAINE SESSION = RELECTURE BRANCHE CONDUITE DE PROJET** (arbitrage Tim 12/06) : **§1bis transverses** (`gestion-de-projet` → `ecoconception` → `securite-et-qualite`) puis **§2 PROJ** (22 fiches, ⚠ SVG bête-à-cornes/pieuvre à reprendre — relecture texte d'abord, SVG en session dédiée) puis **§3 MEO** (6 fiches) et **§20** ; même mode inversé, contrôles d'office : porte famille (peu probable côté PROJ), markers C69, pipes C62 dans les tableaux. **Les modules familles MCU (§7 à §16) sont gardés pour la fin** (arbitrage Tim 12/06). **RESTE À TIM** : commit/push ; 10 captures C29 ; validations en attente inchangées (micropython-serie→repl, filtrage rouge, slug eeprom).

> **➜ MAJ 11/06 (suite 2)** — **RELECTURE §4 TERMINÉE (8/8) + §5 ALGORITHME TERMINÉ (5/5)**, mode inversé posture renforcée, PC pro → PC perso. **`lire-une-datasheet` restructurée** en 2 parties + sommaire à ancres (méthode / **lecture commentée du L298N** : boîtiers, brochage + table de vérité, pseudocode 4 fonctions, AMR commentés, calcul thermique → radiateur, gabarit PCB — valeurs vérifiées datasheet ST rev 5 ; flèche moteur B corrigée) → **6 captures C29 à récolter + clic-test des 2 ancres du sommaire**. **3 fiches créées** : `generateur-de-signaux` (alias GBF, piège High-Z/50 Ω), `analyseur-logique` (tableau C66), `precision-de-mesure` (4 cibles) → **2 captures C29** (face avant GBF, trame PulseView). Enrichies : `instruments-de-mesure` (callout TBT), `multimetre` (I = V/R, recroisements protection/précision), `oscilloscope` (**section Brancher la sonde + SVG terre/pince**). **§5** : hub (quadriptyque quatre-regards, pseudocode, **tip Le test du récit**), `logigramme` (boucle prolongée, exception embarqué, ISO 5807), `machine-a-etats` (callout mauvais **resynchronisé avec le SVG**, état initial ×2, événement/garde remodélisés), `grafcet` (mode sombre double carré, **SVG divergences ET/OU**), `chronogramme` (**§ Un statut à part**). **7 SVG créés / 8 corrigés.** Candidates : C66 5/N, C67 étendue instruments, **C68 nouvelle** (un schéma explicatif par fiche-outil). **PROCHAINE SESSION = §6 SOCLE MCU** (`microcontroleur` →, ⚠ `gpio` rouge #B23A2E en sombre), même mode, C68 à éprouver fiche par fiche. **RESTE À TIM** : commit/push ; **8 captures C29** ; validations en attente inchangées (micropython-serie→repl, filtrage rouge, slug eeprom).

> **➜ MAJ 11/06 (suite)** — **RELECTURE §4 EEE FONDATIONS : 6/8 + PRODUCTION ALIMENTATION**. Session de relecture en **mode inversé** (Claude critique en premier passage — pédagogie, progression, notions connexes — puis arbitrage Tim) : `niveaux-de-tension`, `alimentation-electronique` (refonte : section *Choisir la source*, plan de masse, piège « puissance à travers le régulateur »), `chaine-energie` (lien rouge `[[boucle-fermee]]` posé, tip composants par bloc), `analyse-de-schema-electronique` (multi-rails, 6 contrôles, justification fausse de l'exemple corrigée, SVG netlabels + zones) — toutes validées et cochées. **2 fiches créées** : `decouplage` et `protection-electronique` (roue libre dédiée, ESD, organes complémentaires, 5 petits SVG de branchement). **9 SVG neufs** au total. Candidates **C66** (tableaux de références éprouvées famille-first, 4×) et **C67** (petit SVG de branchement par organe) en §8. **PROCHAINE SESSION = RELECTURE §4 fin puis §5+** : `lire-une-datasheet` (⚠ sens des 2 flèches moteur L298N) → `instruments-de-mesure` → `multimetre` (⚠ géométrie série/parallèle) → `oscilloscope`, puis §5 algorithme et au-delà — même mode inversé, **posture critique renforcée** (voir prompt de lancement). **RESTE À TIM** : commit/push de la session ; clic-test `[[<branche>/index]]` si pas encore fait. Validations en attente inchangées (micropython-serie→repl, filtrage rouge, slug eeprom).

> **➜ MAJ 11/06** — **NETTOYAGE DOCUMENTAIRE COMPLET (3 volets)**, à cheval PC perso / PC pro. **(1) Archivage JOURNAL (10/06)** : 6 entrées 08/06→10/06 conservées, JOURNAL 97→27,8 ko, archive →422 ko (coupe β, invariant antichrono vérifié au byte près) ; `*.bak` gitignorés puis supprimés des deux postes. **(2) Grooming conventions.md (10-11/06)** : nouveau cluster §6 *Modules MCU — conventions de famille* (C25/C26/C32/C45/C56/C57 promues, arbitrage Tim) ; élagage §8 (bloc 28/05 suite 2 → pointeur ; 7 blocs MCU 06/06 suite 6→12 condensés en une synthèse, ~14 ko) ; **anomalie C50** (numéro orphelin jamais défini) neutralisée + loggée BACKLOG — ne jamais réaffecter, numéroter à partir de C65 ; mentions « promouvable §6 » caduques corrigées (items 32/35/36/45/50) ; ancre d'intro `#7-`→`#8-en-cours-déprouvage` réparée ; 112→103 ko. **(3) Grooming TODO (11/06)** : pile de flèches historiques purgée (l'historique vit au JOURNAL), sections faites supprimées, 77→~10 ko. **Au passage** : premier pull post-réorg 3 branches sur PC pro — erreurs Windows de suppression de dossiers (verrous) **sans conséquence git** ; coquilles vides `content/hub` + `content/fiches` supprimées à la main par Tim ; `git status` propre. **PROCHAINE SESSION = REPRISE RELECTURE HUMAINE — branche Système embarqué** (arbitrage Tim 11/06) : dérouler `_drafts/relecture-ordre.md` à partir de **§4 · EEE — fondations électronique** (`niveaux-de-tension` →), puis §5 algorithme, §6+ socle MCU et familles ; **§1bis transverses** (`gestion-de-projet` / `ecoconception` / `securite-et-qualite`) **et §2-3 PROJ/MEO repoussées après**. Sur le rendu Quartz local (`npx quartz build --serve`), cocher à validation. **RESTE À TIM** : si pas encore fait, build + **clic-test des `[[<branche>/index]]`** (seul filet sur l'angle mort `x/index`, pré-requis promotion C60) ; commit/push du nettoyage. **Validations Tim en attente** (BACKLOG / conventions §8) : déviation `micropython-serie`→`micropython-repl` ; `micropython-filtrage` laissé rouge ; slug `micropython-eeprom`.

### 1. Cadrage pédagogique (suite)
- [ ] **Session "chronopédagogie"** : projeter le cycle en V à 5 phases sur les 15 semaines du semestre. Quelle phase à quel moment, combien de temps, quels jalons concrets ?
- [ ] **Format des compétences (AA)** : discussion sur la granularité et la convention de tag — en attente validation hiérarchie
- [ ] **Trame v2 — boucles itératives** : enrichir le schéma du cycle en V pour afficher honnêtement les retours arrière (PoC échec → revoir spec/concept, qualif échec → revoir dossier). À traiter après quelques fiches de phase rédigées.

### 2. Rédaction de fiches

- [ ] **Captures d'écran des fiches-outils logiciels — lot de 8** (§8 simulation + §16 PCB) : trois fiches restent **non relues** faute de copies d'écran, même verdict pour les trois (une fiche d'outil logiciel sans capture n'est pas utilisable). **`falstad`** — 2 placeholders : interface avec les points de courant animés ; circuit RC avec l'oscilloscope intégré. **`ltspice`** — 3 placeholders : interface avec la directive `.ac` visible ; schéma du filtre RC ; tracé du gain avec le point −3 dB repéré. **`kicad`** (ajout 17/08 suite 2) — 3 placeholders : fenêtre de démarrage avec Eeschema et Pcbnew côte à côte ; Pcbnew en cours de routage 2 couches (composants placés, pistes rouge dessus / vert dessous, contour visible) ; schéma de la petite carte capteur annoté. **`falstad` sort de ce lot** — décision Tim 17/08 (suite 2) : la fiche part en **refonte pédagogique** (session dédiée, scénarisation en tutoriel), et **la liste de ses captures sera redéfinie par le scénario retenu** — les 2 placeholders actuels ne valent plus. Restent donc ici **`ltspice` (3)** et **`kicad` (3)**. **Méthode éprouvée sur `wokwi` le 17/08** — Tim shoote et dépose dans `content/ressources/img/<slug>/` en kebab-case, Claude **lit les images** (`read_media_file`) avant de rédiger pour que le texte et les blocs de code collent à l'écran (C81), puis remplace les placeholders et cale les largeurs sur les dimensions réelles des fichiers. Les valeurs des deux exemples de simulation sont déjà vérifiées et figées : RC 10 kΩ / 10 µF → τ = 0,1 s et ~3,15 V à 63 % (Falstad), filtre RC 1 kΩ / 160 nF → fc ≈ 1 kHz (LTspice) — les captures doivent montrer **ces** montages.

- [ ] **Session dédiée — fiches « lire un programme » (×2)** : produire `cpp-lire-un-programme` (après `cpp-logs`) et `micropython-lire-un-programme` (après `micropython-modules`), en fin de parcours langage. **Conception déjà arrêtée 17/08** — voir **C85** (`conventions.md` §8) et l'item détaillé du BACKLOG : `aa: []`, section de méthode dupliquée assumément (copie locale C47), trois blocs visés (par où entrer dans un programme inconnu / mécanismes de langage aujourd'hui répétés d'encart en encart / lecture commentée d'un programme complet du wiki). À poser au passage : renvoi depuis `arduino-programmation-non-bloquante`, réciproques avec `arduino-debug` / `micropython-debug` (frontière lire vs déboguer) et `revue-de-code`. **Pas de passe rétroactive** sur §7 et §13.
- [ ] **Fiche-porte `raspberry-pi-pico`** (notion courte, demande Tim 12/06) : la carte et ses variantes (Pico / Pico W / Pico 2), le PIO, renvoi au module `micropython` comme parcours de cours + note Pico-SDK C/C++ (à raccrocher à l'item « Porte Pico-SDK » des fiches restantes, cf. BACKLOG). Ligne déjà ajoutée au panorama de `microcontroleur` + pastille SVG de positionnement (lien rouge `[[raspberry-pi-pico]]` posé, approche A).
- [ ] **Fiche `bus-de-terrain`** (notion, demande Tim 12/06) : RS-485 et surtout **CAN** (trame, arbitrage, paire différentielle, résistances de terminaison 120 Ω — le « si nécessaire » des terminaisons vit là), positionnement face aux bus de carte (UART/I2C/SPI). Lien rouge `[[bus-de-terrain]]` posé dans `bus-de-communication` (prose de clôture + Voir aussi, approche A). Calibre à cadrer : notion popover-long suffit probablement (culture générale, hors socle AA).
- [ ] **Fiche `cpp-tableaux`** — étape 6bis candidate du parcours langage (repéré 12/06 : tableaux utilisés dans le chenillard de `cpp-boucles` sans être enseignés) — détail au BACKLOG.
- [ ] **Fiche transverse `filtrage`** (« Filtrer des mesures », [T]) — lien rouge **actif** dans `arduino-capteur-numerique` + `arduino-capteur-analogique` (bruit ADC : moyenne/oversampling, passe-bas RC, médiane). Pas de jumeau Arduino du `micropython-filtrage` (reste rouge) — **une fiche transverse unique** sert les deux familles. À créer avant de boucler la famille Arduino.
- [ ] **Enrichir `lire-une-datasheet` partie 2** (« Lecture de la datasheet du L298N », restructurée 11/06) — souhait Tim 11/06 : approfondir la lecture commentée plus tard (autres extraits annotés, exercices, etc. — périmètre à cadrer en session dédiée).
- [ ] **Session de validation cohérence d'ensemble** : relecture bout en bout, ajustements de périmètre entre phases. *(Largement portée par la relecture humaine en cours via `_drafts/relecture-ordre.md`.)*

*Fiches restantes (alimentation continue, non bloquantes pour la publication) :*
- [ ] `circuitverse` (fiche-tuto simu **numérique/logique** en ligne — niche distincte de `falstad`, pertinence à évaluer)
- [ ] `easyeda` (fiche-tuto outil PCB altern, embranchement de choix) — **promesse posée** : le tableau « Choisir un outil » de `pcb` annonce « EasyEDA *(à venir)* » (arbitrage Tim 17/08 suite 2 : mention conservée, item tracé ici). Pas un lien rouge, donc rien ne casse au rendu — mais la promesse est écrite.
- [ ] `bom-electronique` (fiche-tuto compétence pro)
- [ ] `pcb-gravure-ecole` (fiche-tuto spécifique monoface interne)
- [ ] Porte **Pico-SDK C/C++** (optionnelle, hors module Raspberry — cf. BACKLOG)

#### Pré-publication MVP

> Passe de vérification avant mise en ligne du site aux étudiants.

- [ ] **Remplacer le template Word `cdcf-ecole-template.docx`** — référencé en lien dans `specification-technique.md` étape 6 et `cahier-des-charges-fonctionnel.md`, mais le fichier présent dans `ressources/templates/` n'est PAS le bon (mauvais fichier repéré en relecture 08/06). Vigilance « EAT » : réserver l'abréviation à *état de l'art technique*, ne pas réintroduire « matrice EAT » (renommée « matrice éco-critères »).
- [ ] **Captures C29 en attente (13)** — à récolter par Tim : datasheet L298N ×6 + face avant GBF + trame PulseView (11/06), **IDE Arduino annoté** pour la fiche `ide` (12/06), **traceur série** (`arduino-capteur-analogique`) + **oscilloscope** (`arduino-sortie-pwm`) + **Uno avec les 3 voies d'alim** (USB type B + jack DC 9 V + fil Vin/GND sur breadboard, `arduino-alimentation` étape 3) (17/06, placeholders posés) + **IDE 2.x en mode Debug** (breakpoint posé + panneau des variables, `arduino-debug`) (17/06 suite 2, placeholder posé).
- [ ] **Vérifier les images** de toutes les fiches (présence + rendu). *(Les 11 SVG du 29/05 — algorithme/logigramme/MAE/grafcet/chronogramme/analyse-de-schema — ont été validés au rendu lors des relectures du 11/06, géométrie vérifiée au calcul ; recoupe le peigne SVG — cf. section Reprise visuelle.)*
- [ ] **Reconcilier la cartographie AA Phase 1** : `RA-PROJET-C03-3/EEE/1` est porté à la fois par `lire-une-datasheet` et `analyse-de-schema-electronique` (multi-couverture C20) ; confirmer le statut de `EEE/2` et des `EEE/5` (logigramme/MAE/grafcet/chronogramme) pour étayer le « MVP strict 100 % ». Vérifier aussi `phases: dossier-technique` sur `analyse-de-schema` (repris du cadrage, non revérifié) et le lien `[[del|LED]]` (probablement rouge — créer `del.md` ou délier).
- [ ] **Vérifier toutes les fiches du lot Phase 0 (gestion de projet)** avant publication : `bom`, `mind-map`, `fast`, `amdec`, `matrice-eco-criteres`, `ecodesign` + fiches GP/outils antérieures.

### 3. Horizon long terme (post-publication) — nouveaux domaines

> Ambition élargie, **après** la couverture des compétences du projet et la publication (priorité Tim 12/06 : les tutos du socle d'abord). Hors socle AA — à cadrer en sessions dédiées le moment venu.

- [ ] **Électronique numérique** (domaine complet à construire) : logique combinatoire et séquentielle, portes, bascules, compteurs… et le **module « architecture des ordinateurs »** (envie 12/06, née de la relecture `processeur`/`memoire`) : von Neumann vs **Harvard** (l'AVR est Harvard !), RISC vs CISC, registres, ALU, pile (stack), cycle d'instruction. Contenant à arbitrer au cadrage (`processeur` promu hub mère vs hub frère) ; prisme « comprendre ce qu'on achète et ce qu'on programme ». ⚠ **Moore et Mealy** : modèles d'automates → extension de `machine-a-etats` (§5), pas du module processeur.
- [ ] **Électronique analogique** (domaine complet à construire) : transistors, AOP, filtres, conversion… Périmètre à cadrer.
- Note de cohérence : plusieurs délégations actuelles « territoire du cours d'élec » (TTL/CMOS glossé sans développement dans `niveaux-de-tension`, topologie des convertisseurs dans `alimentation-electronique`, borne C55) pourront être **repointées en interne** quand ces domaines existeront — à inventorier au cadrage.

## Reprise visuelle SVG (voie A — sessions dédiées)

- [ ] **`analyse-de-schema-zones`** — reprise ponctuelle : le condensateur de découplage **C3 passe derrière le bloc MCU** (signalé à la validation du 11/06). Reste de la géométrie validée par Tim.
- [ ] **`cycle-v-projet`** — re-coder en voie A (cohérence de style + mode sombre). Signalé en relecture du hub « Conduite de projet » (08/06) ; embarqué sur l'accueil + le hub. Le peigne complet des ~81 SVG est suivi dans `_drafts/peigne-svg-prepublication.md` et au BACKLOG.
- [ ] **`bete-a-cornes-generique`** — reprendre en voie A. Signalé en relecture de `specification-technique` (08/06) ; embarqué sur `specification-technique` + `bete-a-cornes`.
- [ ] **`pieuvre-generique`** + **`pieuvre-bras-3-axes`** — reprendre en voie A. Signalés en relecture de `specification-technique` (08/06) ; embarqués sur `specification-technique` (générique aussi sur `pieuvre`). **MAJ 12/06** : `pieuvre-bras-3-axes` doit intégrer le lien **FC3** vers le milieu opérateur (FC3 ajoutée à la fiche) ; resync texte↔SVG du triptyque `bete-a-cornes`.
- [ ] **`decomposition-fonctionnelle-bras-3-axes`** (920 px) + **`fast-bras-3-axes`** (960 px) — recomposer ≤ 760 de large / polices ≥ 12 px (lisibilité mobile, signalé 12/06) ; mesurer aussi `mauvais`/`moyen`/`fast-generique` au passage.

## Tâches techniques en suspens

*Aucune. (Réorganisation 3 branches faite 10/06 ; archivages JOURNAL au fil de l'eau — historique au JOURNAL.)*

## Décisions éditoriales en attente

*Portées par `BACKLOG.md` (section Discussions/décisions en attente, conventions à éprouver) et `conventions.md` §8 (candidates en vol). Pas de duplication ici.*
