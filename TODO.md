# TODO — TheSkillCodex

> Fichier privé (non publié). Listes courtes et actionnables. Items faits :
> supprimer plutôt qu'archiver — l'historique est dans `JOURNAL.md`.

## Prochaines sessions (ordre logique)

> **➜ MAJ 06/09 (suite)** — **PRODUCTION : colonne « Système embarqué » FAITE (hub + 5 fiches-étape).** PC pro (`theskillcodex:*` déférés). `eee/index` restructuré en **colonne de méthodo de réalisation** (7 H2, intro « point d'entrée » en **tu**, `[!info]` d'orthogonalité, un `[!livrable]` par étape — lien C61 + label « Livrable X/7 » + dénominations Tim, catalogue familles dissous → renvoi `microcontroleur`, clôture « Le management = cycle en V » ; `aa:[]`, tags `[branche,trame,eee]`). **5 fiches-étape créées** dans `eee/realisation/` (`type:trame`, `aa:[]`, tags `[eee,trame,realisation]`, en **tu**) : `choisir-le-materiel` · `concevoir-l-electronique` (pilote) · `programmer-l-embarque` · `faire-communiquer` (optionnelle) · `fiabiliser-et-deboguer`. Étapes 1 et 7 = pas de fiche neuve (rattachées `decomposition-fonctionnelle`/`concept` + `integration-et-tests`). **7 livrables du hub reliés, 0 lien rouge dans la colonne.** **Décisions Tim** : archi hub-cycle-V + fiches-étape (renverse Q1 « pas de fiches ») ; **livrable = artefact technique** (≠ jalon-revue ; objet physique/papier/simu/algo) ; dénominations livrables figées ; **registre = « tu »** (pilote reconvertie de « on ») ; étape 5 optionnelle ; production en bloc. **Conventions §8** : C63 (colonne orthogonale au V), C64 (fiche-étape de réalisation sans « Équipe » → « Ce qui relève d'ailleurs », `aa:[]`, tag `realisation`). **C61 confirmée au rendu.** **PROCHAINE SESSION = RELECTURE humaine, reprise** : commencer par **la colonne realisation** (hub `eee/index` + 5 fiches `eee/realisation/`, sur le rendu Quartz local), cocher à validation dans `_drafts/relecture-ordre.md`, **puis continuer le reste des fiches à relire** (§1bis transverses `gestion-de-projet`/`ecoconception`/`securite-et-qualite` toujours en attente, puis la suite de l'ordre). **Points d'attention relecture** : (1) **noms des livrables internes** des 4 fiches (Livrable X/N) — à valider/renommer comme la pilote ; (2) **`aa:[]` des 6 pages** — trancher si rattachement de critères EEE (carto `couverture-en-cours.md`) ; (3) **registre tu (colonne) vs on (trames V)** — harmonisation à arbitrer (→ BACKLOG) ; (4) **ancre `#3-concevoir-lélectronique`** du sommaire du hub à vérifier au rendu (apostrophe) ; (5) **rendu `[!livrable]` à lien-dans-le-titre** (C61) déjà confirmé OK. Prompt de lancement fourni en clôture.
>
> **➜ MAJ 06/09** — **Relecture humaine : session 2 — trames du V TERMINÉES + cadrage trame embarquée.** PC pro (`theskillcodex:*` déférés). **Validées + cochées** : `concept` (scores pondérés recalculés, pré-dim ré-harmonisé, pipe `\|` échappé, « comptes rendus »), `preuve-de-concept` (pipe `\|` échappé, « non concluant »/« ambiguë », *go / no-go*, « point de bascule du V »), `dossier-technique` (3× « phase N »→noms, exemple PoC recadré, « fraisage mécanique » ×3, 11 pièges dé-puçés), `integration-et-tests` (4× « phase N »→noms, « fraisage mécanique » ×2, 11 pièges dé-puçés, « NEMA 17 », « microcontrôleur », « au banc », `[[fonction|FC]]` lié). **§1 (cycle en V) intégralement relue.** **Décisions Tim** : « stepper » **conservé** (expose fr/eng — renverse la conversion session 1, assumée) ; anglicismes techniques admis ; « Xᵉ étape » intro toléré ; « gravure à l'anglaise » **proscrit** → « fraisage mécanique » (→ conventions §1) ; E2 (blocs « Pendant cette phase » d'integration) **tenu** en prose. **Conventions** : §1 enrichi (proscrit + bloc anglicismes admis) ; **§8 C62** (échappement pipe `\|` en cellule de tableau). **PROCHAINE SESSION = PRODUCTION — nouvelle trame « Système embarqué »** (cadrage D validé) : **restructurer `eee/index`** en **colonne de méthodo de réalisation** du sous-système embarqué (« où j'en suis / quoi faire ensuite »), **fil rouge bras 3 axes**, **orthogonale au V** (le V = gestion de projet, la trame = ingénierie embarquée ; délègue tout le management au V par liens). Colonne 7 étapes + mapping + sous-questions de restructuration dans **`_drafts/cadrage-trame-embarquee.md`**. **Relecture §1bis transverses** (`gestion-de-projet`/`ecoconception`/`securite-et-qualite`, cf. `_drafts/relecture-ordre.md`) **différée après** la session de production. Prompt de lancement fourni en clôture.
>
> **➜ MAJ 06/08 (suite)** — **Relecture humaine : session 1 (entrée + trames du V démarrées).** PC pro (`theskillcodex:*` déférés). **Validées + cochées** : `index`, `hub/index` (lexique « dérisquer »/« point dur » corrigé), `mecatronique`, `specification-technique`. **Passe liens parcours** : la refonte 08/06 n'avait laissé **qu'un straggler** `[[index]]` (`mecatronique` → `[[hub/index|…]]`) ; tout le reste de la sphère parcours (31 `proj/` + 5 index + hubs `eee`/`mme`) **grepé propre** (dumps `read_multiple_files` + bash) ; `hub/index`/`ecoconception` étaient **déjà** absolus (fausses alertes mémoire — cf. JOURNAL, renforce C14) ; `[[FP/FS/FC]]` & `[[critère/niveau/flexibilité]]` non-rouges (alias front-matter). **`afnor-nfx50-151` publié** (`draft:false` + `type:notion`) → 404 résolu. **`specification-technique`** : 12 patches validés (« point dur »→« verrou », abréviations explicitées, « steppers »→« moteurs pas-à-pas », tournures). **Émergés → TODO** : SVG `bete-a-cornes-generique` + `pieuvre-generique`/`-bras-3-axes` à reprendre (voie A) ; **template `cdcf-ecole-template.docx` présent = MAUVAIS fichier** (item requalifié « remplacer »). **Décisions Tim** : mélange tu/vous toléré ; `(stub)` afnor gardé ; récap Pièges en fin de fiche gardé. **PROCHAINE SESSION = poursuivre la relecture au rendu — fiche `concept` (phase 2)**, puis `preuve-de-concept`/`dossier-technique`/`integration-et-tests`, puis 1bis transverses (cf. `_drafts/relecture-ordre.md`). Prompt de lancement fourni en clôture.
>
> **➜ MAJ 06/08** — **Démarrage relecture humaine + refonte architecture 3 branches.** PC pro (`theskillcodex:*`, **outils déférés → `tool_search`** au démarrage). Synchro OK (rename `micropython-stockage` ramené). **Tableau de bord** `_drafts/relecture-ordre.md` (213 fiches ordonnées, cases, repères image/attention, gabarit de retour) = **fil inter-conversations** (synchronisé 2 PC). **Méthode** : 1 conversation = 1 session de relecture, au fil de l'eau (lecture sur **rendu Quartz local**, pas Obsidian), cocher à la **validation**, prompt de reprise en clôture. **Refonte 3 branches métier** (couche d'orientation, **fichiers non déplacés**) : accueil refondu (3 sections, branches en « Par où commencer ») **✓ validé** ; `hub/index`→**« Conduite de projet »** (PROJ+MEO+ESE ; titre, embed absolu, section Méthodes/organisation/cadre MEO+ESE ; **sommaire remonté avant SVG** ; **liens de phase intégrés aux callouts `[!livrable]`**, « À lire ensuite » retirées) — **EN COURS de relecture (non coché)** ; `eee/index`→**« Système embarqué »** (familles MCU complétées) ; `mme/index`→**« Méca »**. **Conventions §8** : **C60** (archi 3 branches), **C61** (callout livrable à lien intégré). **Décisions** : nom « Conduite de projet » (≠ fiche `gestion-de-projet`) ; cycle en V = hub de branche ; pas de déplacement (slug/embeds/traçabilité) ; **SVG voie A** (maison, sessions dédiées) ; ancres relatives gardées (pas URL absolues). **PROCHAINE SESSION = poursuivre la relecture du hub « Conduite de projet » au rendu**, puis enchaîner l'ordre du tableau de bord (`mecatronique`, trames du V…). **En attente Tim** : validation hub au rendu ; **pictos d'orientation** des 3 branches (🛠️/🔌/⚙️) ; **`ecodesign` doublon de `ecoconception` ?** (+ 4 fiches éco à dédoublonner) ; correction **lexique « dérisquer »/« point dur »** (hub phase 3) ; **Explorer Quartz** à reconsidérer (nav passe désormais par branches) ; **suppression manuelle `mia/`**. **SVG — sessions dédiées à venir** (voie A) : reprendre `cycle-v-projet` + peigne complet (cf. `peigne-svg-prepublication.md`).
>
> **➜ MAJ 06/07 (suite 3)** — **Arc LIENS ROUGES DÉFINITIVEMENT CLOS** (re-scan exhaustif `bash` + nettoyage annotations). **(1) Re-scan exhaustif** : 221 fiches lues (100 % de `content/`) via `read_multiple_files` + parsing python (univers = 212 basenames ∪ dossiers ∪ alias front-matter ∪ alias manuels) → **0 lien rouge réel** (~3260 tokens `[[…]]`, tous résolus). La faille d'inventaire `acv-simplifiee` (suite 2) est **close** — inventaire confirmé exhaustif. **(2) Stragglers = néant** (0 annotation vers cible absente). **(3) 43 annotations périmées nettoyées** sur 11 fichiers (règle **C59** : retirer le statut `(à créer)`/`(à venir)`, garder le descripteur) : `concept`(5)/`dossier-technique`(5)/`gestion-de-projet`(7)/`preuve-de-concept`(4)/`integration-et-tests`(4)/`securite-et-qualite`(6)/`specification-technique`(4, `afnor *(stub)*` gardé)/`ecoconception`(1)/`micropython-modules`(1)/`micropython-repl`(1)/`hub/index`(5). **Objectif 1** : 3 `[[notion]]` de guidage des templates passés en code. **Bilan édition** : 14 fichiers / 46 changements / **0 échec d'ancre** (aucun NBSP). **Conventions §8** : **C59** (hygiène d'annotation). **Faux positifs laissés** (corrects) : `*(à venir)*` de tableaux (`instruments-de-mesure`, `pcb`) = sous-outils, pas des liens. **PROCHAINE SESSION = bascule publication-readiness** (production des familles + arc liens rouges terminés ; **arbitrage à confirmer par Tim**) : **(a) RECOMMANDÉ — nettoyage pré-publication restant** : **filtre Explorer Quartz** (`quartz.layout.ts`, **PAS `draft:true`**) + **peigne Quartz des SVG** (grande liste BACKLOG) + **`[[ide]]` STM32** (lien possiblement rouge à trancher) ; **(b) relecture API en bloc** (MicroPython complète + PIC ; ESP32/Teensy/ESP8266/STM32/Raspberry ; captures C29) — garde-fou Tim, Claude applique ensuite ; **(c) quick wins — validations Tim reportées** (déviation « Communication »→`micropython-repl` ; `micropython-filtrage` rouge optionnel ; slug `micropython-eeprom`) ; **(d) MAJ carto Phase 2 EEE/info** (toujours reportée, non bloquante). **Séparé** : codes AA des 24 fiches (suite 2) — autorité Tim — + propagation `couverture-en-cours.md`. **Garde-fou** : relecture utilisateur de la passe — **3 choix de wording vétoables** (rétention « délégué cours normatifs » ; rephrase `acv-simplifiee` → « version complète déléguée aux cours collègues » ; `micropython-repl` → `(transverse)`). **2× U+FFFD cosmétiques** (`28/05 suite 2` archive, `06/06 suite 10` JOURNAL) — nettoyage manuel si souhaité.
>
> **➜ MAJ 06/07 (suite 2)** — **Pré-publication : arc LIENS ROUGES CLOS — sweep complet + 24 fiches cibles + lien `.docx` corrigé.** Sweep `edit_file` terminé — repoints `alimentation-stabilisee→alimentation-electronique`/`niveau-logique→niveaux-de-tension`/`pid→arduino-pid`/`controleur→microcontroleur`/`programmation-non-bloquante→arduino-programmation-non-bloquante` ; déliés `capteur`/`actionneur`/`effecteur`/`frontiere-systeme`/`boucle-ouverte-boucle-fermee`/`asservissement`/`del`/`gabarit`/`ppm`/`bras-3-axes`/`pilote`/`arduino-filtrage`/`micropython-filtrage`/`abs`/`ese`. **24 fiches créées** (notion, draft:false, aa:[]) : `mecatronique`+`acv-simplifiee` (proj) · `ide`/`bibliotheque`/`shield` (eee/mcu transverses) · `usinage`/`impression-3d`/`pla`/`soudure`/`comparateur`/`pied-a-coulisse` (mme) · `marquage-ce`/`basse-tension`/`emc`/`iso-12100`/`reach`/`rohs`/`deee`/`epi` (ese) · `unite-si`/`relation-client`/`revue-de-code`/`archivage-projet`/`cable-management` (meo). **`.docx`** : déjà présent `ressources/templates/`, **4 wikilinks → lien md chemin absolu** `/ressources/templates/cdcf-ecole-template.docx`. **Décisions** : `bibliotheque`/`shield`/`ide` en notions transverses (≠ repoint) ; `epi` reclassé délier→création ; `acv-simplifiee` = **rouge hors inventaire** trouvé par hasard → **l'inventaire des 45 n'était pas exhaustif**. Vérif `directory_tree` = 0 rouge sur surface connue (cibles repoint + liens des 24 fiches), **pas** une ré-énumération exhaustive. **PROCHAINE SESSION (bash requis)** : **(1)** `analyze2.py` re-scan frais → confirmer 0 rouge + chasser stragglers type `acv-simplifiee` ; **(2)** traiter stragglers ; **(3)** passe **PLACEHOLDERS PÉRIMÉS d'un bloc** (« (à créer) »/« (à venir) »/« (stub) » obsolètes — dont ceux rendus faux par mes créations : `securite-et-qualite` [marquage-ce/basse-tension/emc/iso-12100/revue-de-code/epi], `specification-technique` [relation-client/archivage-projet/cahier-des-charges-fonctionnel/bete-a-cornes], `ecoconception` [acv-simplifiee] ; + BACKLOG concept/gestion-de-projet/dossier-technique/preuve-de-concept). **Conventions §8** : pointeur d'interface léger (affine C55) ; asset téléchargeable = lien md chemin absolu (généralise SVG) ; anti-régression liens (ne jamais relier un concept délié). **Séparé** : codes AA sur les 24 fiches (autorité Tim) + carto ; relecture API (bloc) ; peigne Quartz SVG ; filtre Explorer Quartz. **Garde-fou** : relecture utilisateur des 24 fiches + des `(voir X)` inline rendus maladroits par les déliés.
>
> **➜ MAJ 06/07 (suite)** — **Famille SBC `raspberry-pi` : MODULE COMPLET** (hub + 3 fiches + 3 SVG ; cadrage D option A validé, puis production A). Livré `eee/mcu/raspberry-pi/` : hub `raspberry-pi.md` (notion, thèse **MCU vs SBC**, panorama Pi 5/4/Zero, warning 3,3 V, distinction Pico→`micropython`, **paliers adaptés** ≠ C25 ≠ clone C57) + `-prise-en-main` (Imager + Pi OS Lite + headless/SSH, `aa:[]`) + `-gpio` (`gpiozero`/`RPi.GPIO`/`lgpio`, LED+bouton, « pas de temps réel dur », `aa:PROJ/5`) + `-projet` (**architecture bicéphale** SBC+MCU sur bras 3 axes, `phases: integration`, `aa:PROJ/5`) + 3 SVG conceptuels. **Tally inchangé 79 %** (largeur, C20 ; aucun critère SBC-spécifique — le référentiel *nomme* « Raspberry » EEE/1 + « PC embarqué » PROJ/5, déjà C). **lien rouge `[[raspberry-pi]]` résolu, aucun patch → le panorama `microcontroleur` n'a plus AUCUN lien-famille rouge ; couche familles MCU/SBC COMPLÈTE.** Conventions §8 : borne C57 (SBC = contre-cas, structure propre ≠ clone), C55 étendue (3ᵉ frontière OS/sysadmin), C23 +1. **Reste Raspberry (garde-fou)** : relecture exactitude OS/API (Imager, `gpiozero`/Pi 5, numérotation BCM, courant/broche ~16 mA, alim Pi 5) + captures C29 (4 sur `-prise-en-main` + 1 sur `-gpio`) + 3 SVG au peigne Quartz. **Reporté hors module** : porte **Pico-SDK C/C++** (optionnelle). **Prochaine session = arbitrage ouvert** — production des familles terminée, **bascule vers la publication-readiness** : **(a) RECOMMANDÉ — nettoyage pré-publication, les 4 items** (inventaire global stubs/liens rouges/`[À rédiger]` ; triage des rouges restants + `[[ide]]` STM32 ; **filtre Explorer Quartz** `quartz.layout.ts`, **PAS `draft:true`** ; **peigne Quartz des SVG**) — Claude-actionnable, le verrou avant mise en ligne, le contenu étant désormais complet ; **(b) relecture API en bloc** (MicroPython complète + PIC ; ESP32/Teensy/ESP8266/STM32/Raspberry ; captures C29) — garde-fou Tim, Claude applique les corrections ensuite ; **(c) quick wins — validations Tim reportées** (déviation « Communication »→`micropython-repl` ; `micropython-filtrage` rouge ; slug `micropython-eeprom`) — court ; **(d) MAJ carto Phase 2 EEE/info** (toujours reportée, non bloquante). **Note** : (a) **absorbe** les rouges de (c). **2× U+FFFD cosmétiques** (`28/05 suite 2` archive, `06/06 suite 10` JOURNAL) — nettoyage manuel si souhaité.
>
> **➜ MAJ 06/07** — **Nettoyage pré-publication (PARTIEL — 2/7 items)**. **Fait** : (1) **archivage JOURNAL coupe α** via script `tools/archive-journal-0607.mjs` (11 entrées 28/05→05/06 → archive ; JOURNAL **~100→~55 ko** ; archive ~307→~353 ko ; `.bak` créés) ; (2) **suppression ciblée `mia`** (`git rm -r content/fiches/mia` côté Tim ; `callouts-demo.md` déjà absent) ; (3) **promotion C57** (motif « clone de curriculum + hub langage substitué », §8). **REPORTÉS — à reprendre AVANT publication** : (i) **inventaire stubs/liens rouges/[À rédiger]/placeholders** (cf. BACKLOG « Discussions/décisions en attente ») ; (ii) **triage liens rouges + `[[ide]]` STM32** ; (iii) **filtre Explorer Quartz** (`quartz.layout.ts`, **PAS `draft:true`**) ; (iv) **peigne Quartz des SVG** (grande liste BACKLOG « Reprise visuelle »). **Prochaine = Raspberry (session dédiée)** : **trancher d'abord le périmètre** (cadrage D obligatoire avant production) — **SBC Linux** (Pi 4/5, Raspberry Pi OS, GPIO `gpiozero`/`RPi.GPIO`, Python hors `machine`, vrai OS) **vs** **Pico RP2040** (déjà couvert côté MicroPython) ; ambiguïté à lever. **À valider Tim (reportés)** : déviation « Communication »→`micropython-repl` ; `micropython-filtrage` rouge ; divergence eeprom (slug `micropython-eeprom` conservé par défaut). **Relecture (BLOC FINAL, garde-fou)** : **API MicroPython COMPLÈTE** + exactitude **PIC** ; API **ESP32/Teensy/ESP8266/STM32** ; **peigne Quartz** (SVGs ESP32/Teensy/STM32 + `micropython-modele-execution`) ; captures C29 ; **`[[ide]]` STM32**. **2× U+FFFD cosmétiques** (`28/05 suite 2` → archive, `06/06 suite 10` → JOURNAL) — hors `normalize-pilotage.js`, nettoyage manuel si souhaité.
>
> **➜ MAJ 06/06 (suite 12)** — **MicroPython Vague 3 : 6 fiches Ingénieur — MODULE COMPLET** (régime décalque Cas A). Livré dans `eee/mcu/micropython/` : `micropython-interruptions` (`Pin.irq`) · `-timers` (`machine.Timer`) · `-deep-sleep` (`lightsleep`/`deepsleep`) · `-pid` · `-memoire` (`gc`) · `-watchdog` (`machine.WDT`). `manipulation-de-bits` reste **[T]**. **API/divergences soulignées** : **pas d'allocation en ISR** → `micropython.schedule` (+ `alloc_emergency_exception_buf`, `disable_irq`/`enable_irq`) · `machine.Timer` **uniforme** · **`deepsleep` redémarre** (sauver l'état en fichier, `reset_cause`) · PID `borne()` + `duty_u16` · **`gc` ramasse-miettes** (renverse `F()`/`PROGMEM` ; fragmentation/pauses) · **`WDT` non désarmable** (timeout max ~8,3 s). **Tally inchangé 79 %.** **Module MicroPython = 38 fiches** (hub + 37 enfants, 4 vagues) — **seul `micropython-filtrage` reste rouge** (optionnel, pas de jumeau arduino). **À valider Tim** : (1) **promotion du motif « clone de curriculum + hub langage substitué » en convention numérotée (C57)** — mûr après 3 vagues ; (2) déviation « Communication » → `micropython-repl` ; (3) `micropython-filtrage` rouge ; (4) divergence eeprom (persistance fichier). **Prochaine = nettoyage documentaire** (session dédiée, décidée suite 12 ; Tim accepte le JOURNAL > 100 ko en attendant) : **archivage JOURNAL** (→ `JOURNAL-archive.md`, motif MARKER + segments, garde EPERM), **inventaire des stubs/liens rouges** (cf. BACKLOG « Discussions / décisions en attente »), **peigne Quartz des SVG** (grande liste BACKLOG « Reprise visuelle des images SVG »), **suppressions manuelles différées à lister pour Tim** (dossier `mia/`, `callouts-demo.md` — MCP ne supprime pas), **filtre/retrait de l'Explorer Quartz**, **`[[ide]]` STM32** (lien possiblement rouge), décisions `micropython-filtrage` (rouge ?) + **promotion C57** (motif clone). **Puis Raspberry** (session suivante, dédiée) : scoping SBC Linux (Pi 4/5, Raspberry Pi OS, GPIO via `gpiozero`/`RPi.GPIO`) **vs** Pico RP2040 (déjà couvert côté MicroPython) — trancher le périmètre avant production. **En attente relecture (BLOC FINAL)** : **API MicroPython COMPLÈTE** (tout le module, des bases au watchdog) + exactitude **PIC** ; **peigne Quartz** (SVGs ESP32/Teensy + `micropython-modele-execution`) ; captures C29 ; **`[[ide]]` STM32**. **ARCHIVAGE JOURNAL URGENT** (> 100 ko) — à faire avant la prochaine session.
>
> **➜ MAJ 06/06 (suite 11)** — **MicroPython Vague 2 : 12 fiches Avancées** (régime décalque **mixte**, C27/C48). Livré dans `eee/mcu/micropython/` : **bus** `micropython-uart`/`-i2c`/`-spi` (→ [[bus-de-communication]]) · **actionneurs** `-servomoteur`/`-moteur-cc`/`-moteur-pas-a-pas` · `-afficheur` (OLED) · `-debug` · `-gpio-boot` · `-programmation-non-bloquante` · `-machine-a-etats` · `-eeprom` (Stockage persistant). **API/divergences soulignées** : `i2c.scan()` intégré · **REPL sur USB ≠ UART matériel** (2 UART libres, pas de SoftwareSerial) · CS = `Pin` manuel · **servo = PWM 50 Hz + `duty_u16`** (pas de Servo.h) · **DRV8833/TB6612 plutôt que L298N** sur 3,3 V · OLED `ssd1306` + `show()` obligatoire · **`asyncio` intégré** (+ second cœur `_thread`) · machine à états = constantes + `if/elif` (pas de switch/enum) · **persistance par fichier ≠ EEPROM** (`json`, `try/except OSError`) · Pico sans strapping + `Pin(...,value=)`. **`micropython-filtrage` laissé rouge** (pas de jumeau `arduino-filtrage`, optionnel). `micropython-machine-a-etats` porte **EEE/5 + PROJ/5** ; **tally inchangé 79 %** (EEE/5 déjà couvert, C20). Module = **32 fiches** (hub + 31 enfants). **Prochaine = Vague 3 Ingénieur (~6)** : `micropython-interruptions` (→ [[interruption]]) · `-timers` (`machine.Timer` → [[timer]]) · `-deep-sleep` (`machine.lightsleep`/`deepsleep` → [[deep-sleep]]) · `-pid` · `-memoire` (`gc` → [[memoire]]) · `-watchdog` (`machine.WDT`) — transposition des `arduino-*` ; `manipulation-de-bits` reste [T]. Puis **Raspberry (session dédiée)**. **À valider Tim** : (1) déviation « Communication » → `micropython-repl` (éprouvée V1-V2) ; (2) `micropython-filtrage` rouge ; (3) divergence eeprom (persistance fichier). **En attente relecture (BLOC FINAL)** : API MicroPython V2 (UART/I2C/SPI, `i2c.scan`, `duty_u16` servo, DRV8833, `ssd1306`/`show()`, `asyncio`, `json`/`OSError`, broches internes Pico) + exactitude PIC ; **peigne Quartz** (SVGs ESP32/Teensy + `micropython-modele-execution` ; SVG prog-non-bloquante MicroPython optionnel) ; captures C29 (Thonny débogueur, Wokwi, `i2c.scan`, OLED) ; **`[[ide]]` STM32**. **ARCHIVAGE JOURNAL** (~100 ko, seuil atteint) — à faire.
>
> **➜ MAJ 06/06 (suite 10)** — **MicroPython Vague 1 : 11 fiches Bases hands-on + SVG modèle d'exécution** (régime décalque Cas A, C27/C48). Livré dans `eee/mcu/micropython/` : `micropython-gpio` · `-entree-tor` · `-sortie-tor` · `-capteur-numerique` · `-capteur-analogique` · `-sortie-pwm` · `-temporisation` · `-bibliotheques` · `-module` · `-shield` · `-alimentation` (toutes type tuto, renvoi vers la jumelle `arduino-*`, exercices C40, captures C29). **API spécifiques soulignées** : `ticks_diff()` obligatoire (jamais soustraction directe — LA différence vs `millis()`) · `duty_u16()`/`read_u16()` **tous deux 16 bits** → pot→PWM **sans mise à l'échelle** · `time_pulse_us()` (≡ pulseIn) · module **`dht` intégré** · toutes broches PWM · ~12 mA/broche · 3,3 V non tolérant 5 V · alim **VSYS** 1,8-5,5 V / **3V3(OUT)** sortie limitée · **shield = cartes porteuses** (pas de shield Uno). **SVG** `micropython-modele-execution.svg` (compilé vs scripté) **embarqué** dans `## Pourquoi` du hub. « Communication » (bases) → `micropython-repl` confirmé (pas de `-serie` ; **déviation à valider Tim**). **Tally inchangé 79 %**. Module = **19 fiches** (hub + prise-en-main + simulation + langage[+5] + 11 bases) sur ~28 prévues. **Prochaine = Vague 2 Avancées (~12)** : `micropython-uart`/`-i2c`/`-spi` (→ [[bus-de-communication]]) · `-debug` · `-gpio-boot` · `-servomoteur`/`-moteur-cc`/`-moteur-pas-a-pas` · `-afficheur` (OLED) · `-programmation-non-bloquante` · `-machine-a-etats` · `-eeprom` (stockage flash/EEPROM externe) · `-filtrage` — transposition des `arduino-*`. Puis **V3 Ingénieur (~6)** : `-interruptions`/`-timers`/`-deep-sleep`/`-pid`/`-memoire`/`-watchdog`. Puis **Raspberry (session dédiée)**. **En attente relecture (BLOC FINAL — validé Tim)** : API MicroPython (`ticks_diff`, `duty_u16`/`read_u16`, `time_pulse_us`, `dht`, formule temp `ADC(4)`, VSYS/3V3, `Pin.irq`) + exactitude **PIC** ; **peigne Quartz du SVG** `micropython-modele-execution` (+ SVGs ESP32/Teensy toujours en attente) ; captures C29 (Thonny, Wokwi) ; **`[[ide]]` STM32 toujours à vérifier**. **Archivage JOURNAL** (~95 ko, seuil 100) — à faire bientôt.
>
> **➜ MAJ 06/06 (suite 9)** — **PIC (notion)** + **ouverture du module MicroPython, Vague 0**. (1) `eee/mcu/pic.md` (notion de positionnement : ancien µC Microchip MPLAB/XC, pas d'Arduino-core natif, **détrôné par STM32 en industrie / Arduino en école** ; **pas de module** ; `[[pic]]` rouge de `microcontroleur` résolu). (2) **MicroPython** (format Arduino, ancré **Pico 2**, **4 vagues**) — **Vague 0** = `eee/mcu/micropython/` : hub `micropython.md` (Tutoriels en tête + parcours complet 4 paliers [enfants V1-V3 rouges pour l'instant] ; panorama Pico 2 / 2 W / RP2040 `[!info]` ; **note portabilité** ESP32/Pyboard/Arduino ; callout 3,3 V) + `micropython-prise-en-main` (Thonny + flash firmware BOOTSEL + REPL + blink `while True` + `main.py`, aa:[]) + `micropython-simulation` (Wokwi, aa:PROJ/5) + **hub langage `micropython-langage`** (calque `cpp`, 5 filles) + filles `micropython-repl`/`-types`/`-controle`/`-fonctions`/`-modules` (aa:[]). **Partage [A]/[T]** : `cpp` remplacé par le **hub langage propre** ; `niveaux-de-tension`/`lire-une-datasheet`/`manipulation-de-bits` + notions-concept (machine-a-etats/interruption/timer/deep-sleep/memoire) **référencés [T]**. **« Communication » (bases) → `micropython-repl`** (pas de fiche `-serie` ; le REPL/`print()` est la console ; déviation au 1:1 Arduino assumée, à confirmer). **Tally inchangé 45 C / 5 E / 4 HS / 3 HS-D / 0 NC (79 %)** (multi-couverture C20). Aucune convention numérotée nouvelle (motif **« clone de curriculum + hub langage substitué »** à éprouver). **Prochaine session = Vague 1 (Bases hands-on, ~11 fiches)** : `micropython-gpio` · communication(REPL) · `-entree-tor` · `-sortie-tor` · `-capteur-numerique` · `-capteur-analogique` · `-sortie-pwm` · `-temporisation` · `-bibliotheques` · `-module`/`-shield` · `-alimentation` — transposition Python/`machine` des `arduino-*`. Puis **V2 Avancées (~12)**, **V3 Ingénieur (~6)**, puis **Raspberry (session dédiée)**. **En attente de relecture (garde-fou, EN BLOC FINAL — validé Tim, avec intégration SVG au fil)** : **API MicroPython** (Thonny/firmware Pico 2 `.uf2`, `machine`/`time`, `Pin("LED")` vs GP25, `read_u16()`, REPL `Ctrl-C`/`Ctrl-D`, `main.py`) + exactitude **PIC** ; **SVG module MicroPython** (candidat : modèle d'exécution *firmware une fois + REPL + `.py`* vs *compiler→binaire*) à produire en V1 ; captures C29 (Thonny : install firmware / interpréteur / REPL / save `main.py` ; Wokwi) ; **`[[ide]]` STM32 toujours à vérifier**. **Archivage JOURNAL à envisager** (~89 ko, seuil 100).
>
> **➜ MAJ 06/06 (suite 8)** — **Ouverture de la famille MCU `esp8266`** : hub lean + 2 enfants (décalque C18, **pas de SVG**). Livré : hub `eee/mcu/esp8266/esp8266.md` (thèse **« le petit frère Wi-Fi de l'ESP32 »** — Wi-Fi seul, **pas de BLE**, moins de GPIO, un seul ADC, moins cher ; une porte ESP8266 Arduino core ; table variantes NodeMCU/D1 mini/ESP-01 **`[!info]`** ; **2 callouts** 3,3 V non tolérant 5 V **+** broches de boot GPIO0/2/15 & ADC 0–1 V ; 4 paliers [A]/[T] ; **lien rouge `[[esp8266]]` de `microcontroleur` résolu, aucun patch**) + **vague 1 décalque** (`esp8266-prise-en-main` core ESP8266 + URL Boards Manager + **pilote USB-série CH340/CP2102** + NodeMCU + blink **LED active-bas** + auto-reset, `aa:[]` ; `esp8266-arduino-core` **Dxx≠GPIO** + **broches de boot** + **ADC unique A0** + **pile Wi-Fi en tâche de fond → yield()/watchdog** + **exemple Wi-Fi** connexion/IP/RSSI renvoi `esp32-wifi`, `aa:PROJ/5`). **Pas de SVG** (famille « moins »). **Tally inchangé 45 C / 5 E / 4 HS / 3 HS-D / 0 NC (79 %)** (largeur, multi-couverture C20). **lean reconduit + un cran plus loin** : pas de `esp8266-wifi`/`-ble`, Wi-Fi essentiel dans l'exemple + concepts renvoyés `esp32-wifi`, deep-sleep renvoyé `esp32-deep-sleep` → **C56 3/N** (famille « moins-disante »), **nuance C47** (renvois cross-famille tolérés pour un « petit frère », parcours autonome sur les spécificités). Aucune convention nouvelle (C18/C25/C26/C27/C48/C29/C40/C43/C49 réutilisées). **Prochaine session = arbitrage ouvert** : (a) **passe consolidation pré-publication** [mûre] — peigne visuel des SVG (nombreux), `cdcf-ecole-template.docx`, filtre Explorer Quartz, inventaire stubs/liens rouges ; (b) **dernières familles MCU** : **`raspberry-pi`** (cadrage dédié SBC Linux vs Pico RP2040) et **`pic`** (écosystème MPLAB/XC distinct) — les 2 restantes, chacune un cadrage propre ; (c) enfants additionnels STM32/Teensy (périphériques) ; (d) MAJ carto Phase 2 EEE/info (toujours reportée). **En attente de relecture (garde-fou)** : **exactitude API ESP8266** (URL core `package_esp8266com_index.json`, pilote USB-série, LED intégrée active-bas, mapping Dxx↔GPIO [D1=GPIO5, D2=GPIO4, D5=GPIO14, D6=GPIO12], broches de boot GPIO0/2/15 + GPIO16/deep-sleep, ADC A0 0–1 V/0–3,3 V, `ESP8266WiFi`/`WiFi.RSSI()`, watchdog/`yield()`) ; captures C29 (Préférences+URL, gestionnaire de cartes, menu Outils, moniteur série connexion Wi-Fi) ; **vérifier `[[ide]]` dans le hub STM32** (lien possiblement rouge — encore en attente).
>
> **➜ MAJ 06/06 (suite 7)** — **Ouverture de la famille MCU `teensy`** : hub + 4 enfants + 2 SVG (décalque C18). Livré : hub `eee/mcu/teensy/teensy.md` (thèse **« l'Arduino mais rapide + couteau-suisse USB/audio »**, une seule porte Teensyduino ; table variantes par cœur 4.1/4.0 M7 600 MHz · LC · 3.2/3.6 legacy **`[!info]` à confronter doc PJRC** ; callout **3,3 V** nuance générationnelle — **4.x NON tolérant 5 V** ≠ STM32/FT ; 4 paliers [A]/[T]) + **vague 1 décalque** (`teensy-prise-en-main` Arduino IDE+Teensyduino+Teensy Loader, blink broche 13, `aa:[]` ; `teensy-arduino-core` Arduino musclé `digitalWriteFast`/`elapsedMillis`/F_CPU 600 MHz, noyau PJRC **sur registres NXP sans HAL**, `aa:PROJ/5`) + **vague 2 distinctifs** (`teensy-audio` Audio Library + **Audio System Design Tool** + objets/cordons + DMA + exemple→I2S/Audio Shield, pas de DAC sur 4.x ; `teensy-usb` menu **USB Type** clavier/souris/manette/MIDI/audio + exemple bouton→Keyboard détection de front C44) — `aa:PROJ/5`. **2 SVG conceptuels** (`teensy-audio-flux` ; `teensy-usb-personnalites`). **Tally inchangé 45 C / 5 E / 4 HS / 3 HS-D / 0 NC (79 %)** (largeur, multi-couverture C20). **lean-Bases reconduit** (pas de `teensy-gpio`/`-serie`) → **C56 éprouvée 2/N et élargie** (Teensy n'a PAS de porte native ≠ STM32 ; déclencheur = couverture des périphériques de base par [T]+porte, pas la nature native). Pas de fiche `teensy-performance` (diluée). Aucune convention nouvelle (C18/C25/C26/C27/C48/C29/C40/C43/C44/C47/C49/C55 réutilisées). **Prochaine session = arbitrage ouvert** : (a) **passe consolidation pré-publication** [de plus en plus mûre] — peigne visuel des SVG (très nombreux désormais), `cdcf-ecole-template.docx`, filtre Explorer Quartz, inventaire stubs/liens rouges ; (b) **dernières familles MCU** (`raspberry-pi` = cadrage dédié SBC Linux vs Pico ; `esp8266` ≈ note de variante dans `esp32` ; `pic` = écosystème MPLAB distinct) ; (c) enfants additionnels STM32/Teensy (périphériques) ; (d) MAJ carto Phase 2 EEE/info (toujours reportée). **En attente de relecture (garde-fou)** : **2 SVG Teensy 1ers jets** au rendu Quartz (chaîne audio + personnalités USB) ; **exactitude API Teensy** (Teensyduino + URL Boards Manager PJRC, broches 4.0/4.1, `digitalWriteFast`/`elapsedMillis`, USB Type & API `Keyboard`/`usbMIDI`, Audio Library objets/`AudioMemory`, **pas de DAC sur 4.x**, Audio Shield SGTL5000/I2S) ; captures C29 (gestionnaire de cartes, menu Outils, Teensy Loader, Audio Design Tool, moniteur série) ; **vérifier `[[ide]]` dans le hub STM32** (lien possiblement rouge — à confirmer/retirer).
>
> **➜ MAJ 06/06 (suite 6)** — **Ouverture de la famille MCU `stm32`** : hub + 5 enfants + 2 SVG (décalque C18 des hubs arduino/esp32). Livré : hub `eee/mcu/stm32/stm32.md` (thèse **deux portes** — continuité STM32duino / métier CubeMX-HAL-registres ; table variantes 7 lignes par cœur Cortex-M **`[!info]` à confronter au ST product selector** ; callout **3,3 V / broches FT** ; 4 paliers [A]/[T] + marqueur C32 sur `stm32-registres` ; SVG couches embarqué ; **lien rouge `[[stm32]]` de `microcontroleur` résolu automatiquement, aucun patch**) + **vague 1 décalque** (`stm32-prise-en-main` CubeIDE+Nucleo `aa:[]` ; `stm32-arduino-core` STM32duino sur HAL `aa:RA-PROJET-C03-3/PROJ/5`) + **vague 2 pivots** (`stm32-cubemx` arbre d'horloge/NVIC/génération ; `stm32-hal` 3 modes scrutation-IT-DMA + HAL/LL ; `stm32-registres` CMSIS/blink bare-metal/BSRR vs ODR/renvoi `manipulation-de-bits`) — tous `PROJ/5`. **2 SVG conceptuels** (couches d'abstraction = thèse ; flux CubeMX). **Tally inchangé 45 C / 5 E / 4 HS / 3 HS-D / 0 NC (79 %)** (multi-couverture C20 — largeur, pas couverture). Décision **lean-Bases** : pas de `stm32-gpio`/`-serie`, GPIO/UART portés par les exemples travaillés de CubeMX/HAL (écart assumé au décalque ESP32, noté §8 C56). Aucune convention majeure nouvelle (C18/C24/C25/C26/C27/C48/C29/C32/C40/C43/C47/C49 réutilisées). **Prochaine session = arbitrage ouvert** : (a) **enfants STM32 additionnels** (périphériques : `stm32-timers`/PWM, ADC, I2C/SPI) ; (b) **passe consolidation pré-publication** [de plus en plus mûre] — peigne visuel des SVG accumulés (désormais nombreux), `cdcf-ecole-template.docx`, filtre Explorer Quartz, inventaire stubs/liens rouges ; (c) autre famille MCU (`raspberry-pi` = cadrage dédié SBC Linux vs Pico ; `esp8266` ≈ note de variante) ; (d) MAJ carto Phase 2 EEE/info (toujours reportée). **En attente de relecture (garde-fou)** : **2 SVG STM32 1ers jets** au rendu Quartz (couches d'abstraction + flux CubeMX, lisibilité smartphone) ; **exactitude API STM32** (fonctions HAL, broches Nucleo LD2/PA5 & B1/PC13, macros CMSIS `RCC_AHB1ENR_GPIOAEN`/`GPIO_MODER_MODE5_0`/`GPIO_BSRR_BS5`, URL Boards Manager STM32duino, modes de flashage SWD/DFU, fréquences cœur F411/G431) ; captures C29 (CubeIDE Board Selector/build/Debug+Registers ; CubeMX pinout/horloge/USART+NVIC ; moniteur série).
>
> **➜ MAJ 06/06 (suite 5)** — **Clôture du domaine MME** via la fiche `optimisation-mecanique` (`mme/`, **notion interface**, décalque `schema-cinematique`). Livré : fiche ~8 ko (`aa RA-MME-C03-1/MME/6` objet central — concevoir vs optimiser, 3 leviers matériau-procédé/allègement/nombre-de-pièces + renvoi `ecoconception`, démarche itérative via `matrice-de-decision`, exemple bras 3 axes, frontière calcul/FEA/topologie → cours méca) + **1 SVG conceptuel** (`optimisation-mecanique-generique`, avant/après allègement) + entrée `mme/index` + **carto `MME/6` E→C** propagée partout (ligne passe A, bilan MME, 2 tables, pourcentages, prose synthèse, compteurs, bloc dédié, mention obsolète `dossier-technique`). **MME refermé : 5 C / 5 E / 1 HS-D / 0 NC**, tally global **45 C / 5 E / 4 HS / 3 HS-D / 0 NC (79 %)**. MME rejoint EEE/ESE/MEO sans trou ; reste PROJ (2 critères non-HS portés ailleurs, non bloquants). Aucune convention nouvelle (réutilisation gabarit interface + C55 + C23). **Prochaine session = la prochaine grappe MCU** (validé Tim) — candidat recommandé **`stm32`** (Arduino-core compatible → couche [T] s'applique + vrai palier ingénieur neuf : HAL/LL, CubeMX, registres ; `esp8266` ≈ note de variante dans `esp32` ; `raspberry-pi` = cadrage dédié à part, lever l'ambiguïté SBC Linux vs Pico RP2040). **En attente de relecture (garde-fou)** : SVG `optimisation-mecanique-generique` au rendu Quartz (avant/après, 1er jet).
>
> **➜ MAJ 06/06 (suite 4)** — **Fiche cœur EEE `alimentation-electronique` (transverse [T]) + 3 SVG + liens entrants + carto EEE/3 E→C**. Arbitrage post-Phase 3 tranché → (a). Livré : `eee/alimentation-electronique.md` (~14,3 ko, **notion** [T], `aa RA-EEE-C03-2/EEE/3` objet central + `RA-PROJET-C03-3/EEE/5` effleuré) — 5 principes (réguler / découpler / router les masses / CV-CC / protéger) + exemple incarné bras 3 axes + Pièges + cas particulier (rails multiples) ; **profondeur *Réguler* bornée** (choisir/dimensionner ; topologie buck/boost déléguée au cours d'élec de puissance). **3 SVG conceptuels** (régulation linéaire vs découpage ; duo masses étoile/chaînage ; archi incarnée bras 3 axes). **3 liens entrants** (`chaine-energie`, `arduino-alimentation`, `eee/index` → plus semi-orpheline). **Carto** : `RA-EEE-C03-2/EEE/3` **E→C** (1er vrai reclassement depuis le gel passe B ; les fiches Phase 2/3 étaient toutes multi-couverture C20) → **EEE 10 C/0 E**, tally global **44 C/6 E/4 HS/3 HS-D/0 NC (77 %)** ; compteur 84→85, bloc de session + 2 tables + matrice + reprises prose toutes propagées (carto **entièrement cohérente**). **Frontière 3 étages confirmée** : `chaine-energie` (la carte ⬆) / `alimentation-electronique` (les principes [T]) / `arduino-alimentation` (la recette carte [A] ⬇) — pas de redite. **Squelette pro cœur désormais complet** (Phase 3 + alimentation). **Prochaine session = arbitrage ouvert** : (b) **passe consolidation pré-publication** [recommandée] — peigne visuel des SVG accumulés, `cdcf-ecole-template.docx`, filtre Explorer Quartz, inventaire stubs/liens rouges ; ou (c) nouvelle famille MCU (`raspberry-pi`/`esp8266`/`stm32`) ou `optimisation-mecanique` (MME). **En attente de relecture (garde-fou)** : **3 nouveaux SVG** au rendu Quartz (priorité duo masses + bras incarné, coordonnées 1er jet) **+ 3 SVG Phase 3** encore en attente (priorité circuit multimètre) ; captures C29 (multimètre, oscillo, debugger, KiCad).
>
> **➜ MAJ 06/06 (suite 3)** — **Phase 3 squelette pro EEE bouclée** : 6 fiches (mini-hub `instruments-de-mesure` + `multimetre`/`oscilloscope` ; `debugger-embarque` transverse [T] ; mini-hub `pcb` + `kicad`) + passe de câblage (5 edits, `eee/index` curaté) + **3 SVG conceptuels** (multimètre série/parallèle, confrontation mesure, flux PCB) + carto (bloc Phase 3, 78→84 fiches, **tally inchangé 43 C/7 E/4 HS/3 HS-D/0 NC** — EEE/5 était déjà C). **MVP étendu structurellement complet** (squelette pro + Arduino + ESP32). **Prochaine session = arbitrage ouvert post-Phase 3** : (a) `alimentation-electronique` (dernière fiche cœur du squelette pro, raccroche dossier-tech/intégration) ; (b) **passe consolidation pré-publication** (peigne visuel des SVG accumulés, `cdcf-ecole-template.docx`, filtre Explorer Quartz, inventaire stubs) ; (c) autre famille MCU (`raspberry-pi`/`esp8266`/`stm32`) ou `optimisation-mecanique` (MME). **En attente de relecture (garde-fou)** : 3 SVG Phase 3 au rendu Quartz (surtout circuit multimètre) ; captures C29 (face multimètre, écran oscillo, session debugger, éditeurs KiCad).
>
> **➜ MAJ 06/06 (suite 2)** — **Module ESP32 : 9 tutos enfants + 2 SVG + hub** (batch en 2 régimes). Constat : track (c) PROJ AF/outils (`amdec`/`bom`/`fast`/`mind-map`/`ecoconception`/`ecodesign`/`matrice-eco-criteres`) + `caracteriser-une-exigence` **déjà écrits** — seul vrai vide = enfants ESP32. Écrites : `esp32-prise-en-main`/`-gpio`/`-serie`/`-arduino-core`/`-wifi`/`-ble`/`-deep-sleep`/`-idf`/`-freertos` (course-grade, autoportantes, **avec exercices**, zéro renvoi `arduino-*`). 2 SVG conceptuels (deep-sleep réveil, freertos ordonnancement). Hub : variantes +C5/H2/P4 + tendance RISC-V (Espressif live), `[[esp32-idf]]` lié, `arduino-core` en Bases. Conventions §8 : **C47** (parcours MCU autonome / redites [A] OK), **C48** (batch 2 régimes), **C49** (trame tuto MCU + exercices). **Prochaine session = Phase 3 squelette pro (proposé)** : `oscilloscope`/`multimetre`/`debugger-embarque`/`pcb`+`kicad` — cœur EEE, dérouge des liens vivants. **En attente de relecture (garde-fou)** : 9 fiches + 2 SVG ESP32 — exactitude API (LEDC 3.x, BLE2902, pile FreeRTOS en octets, USB CDC On Boot), rendu Quartz des SVG, **vérifier `[[deep-sleep]]`/`[[wifi]]`/`[[ble]]`/`[[techno-sans-fil]]`/`[[pwm]]` non rouges** ; captures C29 à récolter ; asymétrie exercices `arduino-*` à aligner.
>
> **➜ MAJ 06/06 (suite)** — **Arbitrages design/MME tranchés + fiches & SVG méca + carto AA refermée**. Autonomie totale (2 chantiers). **3 décisions niveau D tranchées** : catégorie **HS-D** instaurée (3 critères design) ; **5 critères MME effleurés** actés *Effleuré terminal par délégation* (pas de fiche MME phase 2) ; **`schema-cinematique` créé** (frontière interface). Livré : 2 fiches (`schema-cinematique` mme/ `aa MME/5` ; `chaine-energie` eee/ transverse `aa:[]`) + **8 SVG** (4 méca fil-rouge bras 3 axes + 4 élec conceptuels : simulation cycle/lecture-courbe, masquage, firmware) + 5 embeds outils simulation → placeholders C29. **`couverture-en-cours.md` refermée : 43 C / 7 E / 4 HS / 3 HS-D / 0 NC (75 %)**. `conventions.md` §7 : HS-D = 5e catégorie, décisions tranchées. **Prochaine session = arbitrage ouvert (post-relecture méca)** : (a) **tutos enfants ESP32** (Phase 4, hub validé : `esp32-prise-en-main`/`-arduino-core`/`-idf`/`-gpio`/`-serie`/`-wifi`/`-ble`/`-deep-sleep`/`-freertos`) ; (b) **Phase 3 squelette pro** (`oscilloscope`/`multimetre`/`pcb`+`kicad`/`debugger-embarque`) ; (c) reprise rédaction fiches (`caracteriser-une-exigence` prioritaire, `amdec`, fiches-notion outils) ; (d) `optimisation-mecanique` (MME phase 2). **En attente de relecture** : 8 SVG premiers jets — 4 méca relus 06/06 (schema-cinematique validés, chaine-energie corrigés), 4 élec simulation à valider au rendu ; captures C29 à récolter.
>
> **➜ MAJ 06/06** — **Réconciliation carto AA Phase 2** (`couverture-en-cours.md`, passe complète : **74 front matters vérifiés un par un**, EEE→7C puis **9C/1E/0NC**, global **42/57 = 74 %**, NC 6→4) + **module `simulation`** sur pattern hub (C18) : `simulation-electronique` (hub [T], ferme **EEE/3+/4**) + `falstad`/`ltspice` (analogique) + `wokwi` (MCU, PROJ/5). 2 écarts JOURNAL corrigés (`lire-une-datasheet` ≠ [], grappe PROJ/5 = **40** fiches). Fritzing tenu hors hub. Conventions nouvelles §8 : **C45** (AA tutos-outils = [] porté par le hub) + **C46** (« simuler ≠ représenter »). 5 SVG conceptuels simulation en placeholder. **Prochaine session = AUTONOMIE TOTALE** (Claude définit périmètre + priorités) : (a) **design MME/PROJ** — 2 NC `PROJ-C03-3/1`+`/2` + 3 arbitrages niveau D (catégorie « HS par délégation » pour les 3 critères design, statut des 4 critères MME effleurés sans fiche centrale, statut `schema-cinematique` wiki vs délégation) ; (b) **passe SVG** — créer les SVG conceptuels en attente (placeholders C29 : module simulation = cycle / lecture-courbe / 3 interfaces ; bitmasking ; couches firmware ; cpp ×2 ; etc.), **fil rouge bras 3 axes pour les SVG méca**.
>
> **➜ MAJ 05/06 (suite)** — **Relecture/enrichissement complète du parcours Arduino** (4 paliers, ~33 fiches : hub + 32 filles + module cpp), bout en bout. Livrables : `PROJ/5` effleuré propagé sur ~28 fiches `arduino-*` (pas `arduino-alimentation`, matérielle) ; **bug « verrou anti-doublon » corrigé** (sous-débordement unsigned `millis()+offset`, systémique → `temporisation`/`moteur-cc`/`moteur-pas-a-pas`, fix = détection de front `etatStable`) ; **`const`/`#define` harmonisé** (décision A : `const` pour les valeurs, `#define` pour compil. conditionnelle + macros ; revert `temporisation`/`module`/`afficheur`) ; correction `alimentation` (watchdog → brown-out) ; **lien rouge `mise-en-service` résolu** (décision B : pas de 6ᵉ phase, `eeprom` repointé vers `integration-et-tests`, bullet redondant retiré d'`afficheur`) ; 2 SVG cpp (`cpp-execution-cycle`, `cpp-portee-locale-globale`). Conventions nouvelles §8 : **C43** (`const` partout) + **C44** (anti-rebond détection de front). **Prochaine session = arbitrage ouvert (post-relecture Arduino)** : (a) **tutos enfants ESP32** (Phase 4, hub Arduino = modèle validé) ; (b) MAJ `couverture-en-cours.md` (PROJ/5 propagé + Phase 2 EEE/info, toujours reportée) ; (c) Phase 3 squelette pro (`oscilloscope`/`multimetre`/`pcb`/`kicad`) ; (d) reprise visuelle SVG. **Micro-items émergés** : « Spécification » en gras non liée dans `arduino-deep-sleep`/`-watchdog` (à lier à `[[specification-technique]]` ou laisser) ; captures C29 à récolter ; reprise visuelle SVG toujours ouverte.
>
> **➜ MAJ 05/06** — **Module C++ complet** : la fiche transverse `cpp` devient un **hub** (`eee/mcu/cpp/cpp.md`) + **7 sous-tutoriels** progressifs avec *Code à lire* et exercices corrigés (`cpp-execution` étalon / `-structure` / `-types` / `-portee` / `-conditions` / `-boucles` multi-notions / `-logs`). **Factorisation transverse** (pas de duplication par MCU ; `cpp-logs` seule famille-spécifique — réutilisable ESP32/Teensy). Aussi : **hub Arduino refondu** (Tutoriels remontés en 2e, 3 frictions corrigées) + **prise-en-main réorganisée** (dépannage par symptôme, pilote CH340 en dernier) + **2 corrections AA** (`lire-une-datasheet` retrait EEE/1, `microcontroleur` EEE/5→RA-EEE/2 ; EEE **7C/1E/2NC**, ~40C/70 %). **Correctifs rendu Quartz** : corrigés dé-imbriqués (C40) + CSS fond des blocs code dans callouts. **Prochaine session = relecture/enrichissement du parcours Arduino** (étape 0 hub+prise-en-main faite → suite `tinkercad` puis *Apprendre les bases*), en intégrant la relecture du module cpp. **Calls en attente de relecture** : captures C29 (`cpp-execution` moniteur série, `cpp-logs` messages IDE) ; 2 SVG conceptuels candidats (cycle `setup()`/`loop()`, portée locale/globale) ; MAJ `couverture-en-cours.md` Phase 2 EEE/info toujours reportée ; mode sombre callouts toujours ouvert.
>
> **➜ MAJ 02/06 (suite)** — 5 fiches transverses/hub : `cpp` (ferme *Apprendre les bases*), `fonction-informatique`, `manipulation-de-bits`, `firmware`, hub `esp32` (Phase 4 ouverte). Archivage 1-pour-1 repris (`28/05` → archive). **Prochaine session = arbitrage ouvert (post-push)** : (a) **consolidation recommandée** — reconciliation AA Phase 1 + vérif images (dont 6 SVG 02/06) + relecture concentrée (dette : 21 arduino-* + 7 Niveau ingénieur + 5 transverses 02/06 suite) ; (b) remplir `esp32` (`esp32-prise-en-main` / `esp32-arduino-core` / `wokwi`) ; (c) Phase 3 squelette pro (`oscilloscope` / `multimetre` / `debugger-embarque` / `pcb`+`kicad`). **Calls en attente de relecture** : AA `PROJ/5` effleuré ×3 (`cpp`/`manipulation-de-bits`/`firmware`), `firmware`/`manipulation-de-bits` sans SVG (candidats notés), `firmware` typé **notion**, variantes `esp32` à confronter à la doc Espressif.
>
> **➜ MAJ 02/06** — Squelette Phase 1 EEE **scellé** (`gpio` créée, dernier rouge résorbé) + **palier *Niveau ingénieur* du hub Arduino complet (7/7)** : grappes `interruption`+`timer` (notion transverse + tuto chacune), `arduino-programmation-non-bloquante`, batch 4 (`deep-sleep`/`pid`/`memoire`/`watchdog`). Hub Arduino aux 4 paliers. **Prochaine session = rédiger `cpp`** (trou des bases C++, lien rouge depuis le hub ; `memoire`/`programmation-non-bloquante`/`timers` s'y appuient). Émergé : notion `manipulation-de-bits` (registres/masques), 2 SVG candidats (schéma-bloc PID, profil deep-sleep), item explorateur Quartz au BACKLOG. **Relecture concentrée** des fiches Niveau ingénieur + grappes en attente (garde-fou).
>
> **➜ MAJ 29/05 (suite)** — Phase 1 squelette EEE **close** (grappe `algorithme` : algorithme / MAE / logigramme / grafcet / chronogramme + tuto `analyse-de-schema-electronique` ; reste `gpio` rouge). **MVP strict structurellement atteint** — couverture AA 100 % **à reconcilier** (cf. *Pré-publication MVP*). **Prochaine session = à décider ensemble (post-push)** : pistes ouvertes — clore `gpio` ; chaîne `arduino-machine-a-etats` (notion MAE désormais disponible) ; 1ʳᵉ fiche Niveau ingénieur ; hub `esp32` (Phase 4) ; ou passe relecture/images du backlog livré.
>
> **➜ Prochaine session = arbitrage utilisateur ouvert (post-batch Arduino)** — Phase 2 a livré 21 fiches arduino-* (Bases 11 + Avancées straight 10) en batch le 29/05 soir, hub Arduino Avancées à 19/21 (manquent `arduino-machine-a-etats` + `arduino-filtrage` optionnel) + Niveau ingénieur entièrement à faire (7 fiches, cadrage propre par fiche). **Choix probable à proposer en démarrage** : (a) chaîne `machine-a-etats` (notion transverse) + `arduino-machine-a-etats` ; (b) 1ère fiche Niveau ingénieur (`arduino-interruptions` ou `arduino-pid` comme têtes naturelles) ; (c) hub `esp32` (second embranchement, ouverture Phase 4) ; (d) `gpio` transverse (la grappe E/S reste rouge). **Relecture des 21 fiches arduino-* en attente** de la récolte du premier set d'images utilisateur (garde-fou explicite acté le 29/05).
>
> **Sessions 28/05** : Phase 0 GP **close** (6 fiches ; entrée JOURNAL reconstruite a posteriori 28/05 suite). Squelette EEE/mcu démarré 28/05 suite 2 — hubs `microcontroleur` + `arduino`, SVG archi MCU, Tutoriels Arduino en 4 paliers, **C18 épreuve 1/N**. Suite 3 : `lire-une-datasheet` (tuto transverse + 2 SVG). Suite 4 : `niveaux-de-tension` + grappes `bus` et `sans-fil` + 7 briques MCU **en batch** (18 fiches + 4 SVG), **C18 promue § 6** (épreuve 3/3).
>
> **Session 29/05** : Phase 2 embranchement Arduino — matinée PC perso, 3 fiches *Prendre en main* (`arduino-prise-en-main` + `tinkercad` + `arduino-serie`) hors-batch. Soir PC pro (MCP `theskillcodex:*`), **batch 21 fiches Bases + Avancées straight** après pushback D et arbitrage utilisateur Option A. Hub Arduino à 22/24 fiches en vert (manquent `arduino-machine-a-etats` + `arduino-filtrage`). **C27 épreuve 3/N étendue à batch de fiches-tuto pleines** — borne déplacée, reformulation à acter avant promotion. Relecture utilisateur des 21 fiches reportee a la recolte du premier set d'images.
>
> **Cadrage stratégique phase 2 elec/info acté 26/05 suite 5 (niveau D)** : (a) elec/info devient le **cœur du wiki**, ~50-60 fiches phase 2 assumées, plusieurs mois ; (b) structure **squelette transverse** (AA + compétences pro communes, indépendantes du choix techno) + **embranchements technologiques** (familles MCU + outils spécifiques, modules autonomes) ; (c) 5 mini-hubs à structurer (`microcontroleur`, `algorithme`, `pcb`, `bus-de-communication`, `techno-sans-fil`) ; (d) roadmap structurée en 5 phases (Phase 0 clôture → Phase 5 alimentation continue) ; (e) 3 cercles de priorité publication : MVP strict (AA 100 %, ~21 sessions) / MVP étendu (~35 sessions) / cible complète (~70 sessions). MVP rentrée scolaire atteignable en 2-3 mois.
>
> **Chantier clôture méthodologique terminé 26/05 suite 4** : (a) promotions C1-C6 dans §§ 1-2 + C15-C16 dans nouveau § 7 *Référentiel AA* de `conventions.md` ; (b) synthèse globale 8 sections dans `_drafts/referentiel/couverture-en-cours.md` (ambition au-delà du référentiel actée comme posture éditoriale) ; (c) archivage 3e vague JOURNAL.md (sessions 25/05 → 25/05 suite 4) via pattern MARKER + N segments éprouvé, JOURNAL 122→86 ko.

### 0. Session annexe — Nettoyage documentaire (complète)
**Décision 25/05 suite 2 → exécutée 25/05 suite 3** : compactage du set documentaire pour réduire les tokens chargés au démarrage. Démarrage post-refonte : ~11-12 k tokens (vs ~55 k initial, réduction ~80 %).

- Palier 1 — Création `conventions.md` ✅ fait 25/05 suite 3
- Palier 2 — Archivage JOURNAL pré-22/05 dans `JOURNAL-archive.md` ✅ fait 25/05 suite 3
- Palier 3 — Compactage TODO (51 ko → 12,5 ko) ✅ fait 25/05 suite 3
- Palier 4 — Compactage BACKLOG (22 ko → 20,2 ko) ✅ fait 25/05 suite 3
- Palier 5 — Refonte v2 du prompt projet (9 ko → 5,5 ko, 13 sections → 9) ✅ fait 25/05 suite 3
- Palier 6 — Test de démarrage post-refonte + patch §8 tail→head ✅ fait 25/05 suite 3

### 1. Cadrage pédagogique (suite)
- [ ] **Session "chronopédagogie"** : projeter le cycle en V à 5 phases sur les 15 semaines du semestre. Quelle phase à quel moment, combien de temps, quels jalons concrets ?
- [ ] **Format des compétences (AA)** : discussion sur la granularité et la convention de tag — en attente validation hiérarchie
- [ ] **Trame v2 — boucles itératives** : enrichir le schéma du cycle en V pour afficher honnêtement les retours arrière (PoC échec → revoir spec/concept, qualif échec → revoir dossier). À traiter après quelques fiches de phase rédigées.

### 2. Rédaction de fiches (parcours prioritaire)

**Stratégie actée (20/05 suite)** : squelettisation des 5 trames + 3 fils transverses bout en bout, puis cartographie AA, puis validation cohérence, **puis seulement** approfondissement trame par trame dans l'ordre.

- [x] ~~**Squelette + approfondissement `securite-et-qualite`**~~ (fil transverse — créée ex nihilo + approfondie 26/05 suite, 3 blocs co-actifs Sécurité produit / Sécurité projet / Qualité documentaire, 11 Pièges, 4 paragraphes Équipe, cohérence finale 4 passes → 3 patches wiki-links uniquement)
- [x] ~~**Session de cartographie AA**~~ — bouclée 26/05 suite 3 (passes B + A + écriture champ aa:). 21 fiches cartographiées, matrice par domaine produite, 17 fronts matters écrits + 4 skip légitimes. **Reste : synthèse globale** dans `_drafts/referentiel/couverture-en-cours.md` (niveau B/C, 1 session).
- [ ] **Session de validation cohérence d'ensemble** : relecture bout en bout, ajustements de périmètre entre phases.
- [x] ~~**Approfondissement des fiches phases du V**~~ : `concept.md` complet (25/05 suite 4-6, 27 patches). `preuve-de-concept.md` complet (25/05 suite 7, 5 étapes + 11 pièges + 4 ¶ équipe + 4 passes cohérence + round 2 wiki-links outils). `dossier-technique.md` complet (25/05 suite 8, alignement v2.1 + 5 étapes [étape 3 pré-rédigée autre Claude] + 11 pièges + 4 ¶ équipe + 0 patch cohérence + round 2 PCB). `integration-et-tests.md` complet (26/05, 27 patches : alignement v2.1 + 5 étapes voie C [2/3/3/2/2 H4 dont 3 H4 disciplinaires étape 2 et 3 H4 pyramide compressée étape 3] + 11 pièges + 4 ¶ équipe + 3 patches cohérence + round 2 validé). **Phase 1 du wiki = complète côté trames du V.**

*Notions à produire en parallèle (popovers posés dès les squelettes)* :

> **Roadmap phase 2 elec/info structurée** (actée 26/05 suite 5, niveau D). 3 sous-sections : Phase 0 clôture phase 1 GP / Phase 1 squelette transverse critique AA / Embranchements technologiques. Voir cadrage stratégique en tête de fichier.

#### Phase 0 — Clôture phase 1 GP (~10 sessions, MVP rentrée)

- [x] ~~`decomposition-fonctionnelle` (fiche-tuto, calque bete-a-cornes complet + 4 SVG bras 3 axes + triptyque mauvais/moyen/bon + 2 AA mappés PROJ/1 et /6 multi-couverture + patch concept étape 1 + patch schema-bloc-fonctionnel)~~ — **fait 27/05 suite**
- [x] ~~**Dette** : restructurer `decomposition-fonctionnelle.md` au format fiche-tuto (sections *Procédure pas à pas*, *Captures d'écran*, *Raccrochage projet*) une fois le template `fiche-tuto.md` figé.~~ — fait 27/05 suite 4 (procédure en 3 H3 numérotées, Raccrochage projet ajouté entre Cas particulier et Voir aussi, contenu intact ailleurs)
- [x] ~~`etat-de-l-art-technique` (fiche-tuto, popover posé `specification-technique.md` étape 2) — **occasion de figer le template `fiche-tuto.md`**~~ — fait 27/05 suite 4 (méthode N×M, exemple bras 3 axes 3 réfs × 6 critères Niryo/uArm/Moveo, 7 pièges, AA mappés multi-couverture PROJ/2 + MEO/1)
- [x] ~~`bom` · `mind-map` · `fast` · `amdec` · `matrice-eat` (→ renommé `matrice-eco-criteres`) · `ecodesign`~~ — **6 fiches créées 28/05** (session Phase 0 non journalisée sur le moment, entrée reconstruite a posteriori : JOURNAL 28/05 suite). Renommage `matrice-eat`→`matrice-eco-criteres` acté (collision EAT = état de l'art technique). Vérification finale reportée au gate *Pré-publication MVP*.
- [x] ~~**Template `fiche-tuto.md`** à figer en transition Phase 0 → Phase 1 (modèle : calque `fiche-notion.md` + sections *Procédure pas à pas* + *Captures d'écran* + *Raccrochage projet*)~~ — figé 27/05 suite 4 (Captures d'écran option B = inline dans étapes, ordre A des sections finales avec Raccrochage projet en pénultième position, fil rouge bras 3 axes en commentaire de la section *Exemple* en éprouvage)
- [ ] **Patches conventions** : ~~C18 mini-hub~~ **promue § 6 le 28/05 suite 4** (épreuve 3/3 : microcontroleur + bus + sans-fil) ; ~~C19~~ éprouvée (faible) sur `lire-une-datasheet`, vrai test reste `analyse-de-schema`/`firmware` ; **C24 `write_file`** promotion § 6 mûre (~18 créations, 0 faux positif) ; nouvelles candidates § 8 : **C27** batch de grappe homogène (2/2), **C28** rouge danger #B23A2E SVG (1/1) ; candidates 4 paliers + [A]/[T] à confirmer sur `esp32`.

#### Phase 1 — Squelette transverse critique AA elec/info (~8 sessions)

Ordre pédagogique : datasheet → schéma → chronogramme → méthodes formelles. Couvre 100 % des critères NC EEE.

- [x] ~~`microcontroleur`~~ (hub mère — panorama 7 familles + aide au choix + SVG archi MCU + 11 popover-notions) — **fait 28/05 suite 2**
- [x] ~~`lire-une-datasheet`~~ — **fait 28/05 suite 3** (fiche-tuto transverse ~15,3 ko + 2 SVG : anatomie datasheet générique + L298N deux mondes ; procédure générique 5 étapes + *Exemple* L298N autonome 2 moteurs CC ; C19 variante-(c) épreuve faible ; AA élargi C20 ; cas autonome ≠ fil rouge bras 3 axes — borne C23).
- [x] ~~**`niveaux-de-tension`** (fiche-notion transverse — 3,3 V vs 5 V, adaptation de niveau)~~ — **fait 28/05 suite 4** (~8,6 ko + 2 SVG, AA EEE/4 + /1, cas autonome ESP32/HC-SR04, rouge danger #B23A2E)
- [x] ~~**`gpio`** (fiche transverse substantielle : modes INPUT/OUTPUT/INPUT_PULLUP/open-drain + état à l'allumage)~~ — fait 02/06 (+ 3 SVG : modes push-pull/drain ouvert/tirage, bouton flottant, courant max #B23A2E ; `aa: RA-EEE-C03-2/EEE/4` multi-couverture C20). **Squelette Phase 1 scellé.**
- [x] ~~**`analyse-de-schema-electronique`** (fiche-tuto transverse)~~ — fait 29/05 suite (+ 2 SVG ; aa `EEE/1` multi-couverture avec `lire-une-datasheet` + `EEE/2` ; cas autonome pont diviseur → MCU → LED)
- [x] ~~**`chronogramme`** (fiche-notion, 4ᵉ représentation du hub algorithme)~~ — fait 29/05 suite (+ 2 SVG : générique axe-temps + PWM/UART ; `EEE/5` ; raccrochage oscilloscope/datasheet)
- [x] ~~**`algorithme`** (mini-hub mère)~~ — fait 29/05 (début session ; hub léger : panorama 4 représentations + aide au choix)
- [x] ~~**`logigramme`** (fiche-notion fille hub algorithme)~~ — fait 29/05 suite (+ générique symboles + triptyque thermostat ; `EEE/5`)
- [x] ~~**`machine-a-etats`** (fiche-notion fille, alias `MAE`)~~ — fait 29/05 (début session ; + générique + triptyque portail ; `EEE/5`)
- [x] ~~**`grafcet`** (fiche-notion fille hub algorithme)~~ — fait 29/05 suite (+ générique IEC 60848 + cycle perçage ; `EEE/5`)

→ **Phase 1 squelette quasi complète** (reste `gpio`, laissé rouge) + embranchement Arduino livré → **MVP strict structurellement atteint**. ⚠️ Couverture AA 100 % **à reconcilier** : `RA-PROJET-C03-3/EEE/1` est déjà porté par `lire-une-datasheet`, donc le sous-critère qui ferme réellement le 100 % (EEE/1 vs EEE/2) est à confirmer sur la cartographie avant de clamer le site publiable.

#### Phase 2 — Premier embranchement Arduino (~3 sessions, MVP strict publiable)

- [x] ~~`arduino`~~ (hub Arduino — panorama cartes Uno R3/R4/Mega/Nano + écosystème + section *Tutoriels* en 4 paliers) — **fait 28/05 suite 2**
- [x] ~~`arduino-prise-en-main` (fiche-tuto, palier *Prendre en main*)~~ — fait 29/05 matin (cas Blink LED_BUILTIN)
- [x] ~~`tinkercad` (fiche-tuto simu, palier *Prendre en main*)~~ — fait 29/05 matin (Blink simulé + bouton `INPUT_PULLUP`)
- [x] ~~**Module Arduino — batch Bases + Avancées straight (21 fiches)**~~ — fait 29/05 soir (PC pro, MCP `theskillcodex:*`). **Reste à écrire** : `arduino-machine-a-etats` (chaîne avec notion `machine-a-etats`) ; `arduino-filtrage` (optionnel) ; 7 Niveau ingénieur (`arduino-programmation-non-bloquante`, `arduino-interruptions`, `arduino-deep-sleep`, `arduino-pid`, `arduino-timers`, `arduino-memoire`, `arduino-watchdog`) — chacun en session dédiée avec cadrage propre. **Relecture/amélioration utilisateur** post-récolte premier set d'images à conduire avant publication.
  - **02/06 — complété** : `arduino-machine-a-etats` + **7 Niveau ingénieur** (`arduino-programmation-non-bloquante`/`interruptions`/`deep-sleep`/`pid`/`timers`/`memoire`/`watchdog`) écrits → **palier *Niveau ingénieur* 7/7, hub Arduino complet (4 paliers)**. Reste `arduino-filtrage` (optionnel).

- [x] ~~**`cpp`** (fiche transverse [T] — bases du langage)~~ — **fait 02/06 (suite)** (`eee/mcu/cpp.md` 14,7 ko ; tour du langage 5 blocs + exemple classificateur de seuil + *Aller plus loin* tremplin ; `aa: RA-PROJET-C03-3/PROJ/5` effleuré C20). Ferme le rouge *Apprendre les bases* du hub Arduino.
- [x] ~~**`manipulation-de-bits`** (notion transverse — binaire, masque, registres)~~ — **fait 02/06 (suite)** (`eee/mcu/manipulation-de-bits.md` 6,2 ko ; 4 idiomes agnostiques + 1 illustration registre AVR ; `aa: .../PROJ/5` effleuré ; pas de SVG — motifs binaires monospace). Également créée : `fonction-informatique` (popover-notion). Résout le double rouge `cpp` ↔ `arduino-timers`.

#### Pré-publication MVP (après Phase 2)

> Passe de vérification avant mise en ligne du site aux étudiants (MVP strict atteint).

- [ ] **Remplacer le template Word `cdcf-ecole-template.docx`** — référencé en lien dans `specification-technique.md` étape 6 et `cahier-des-charges-fonctionnel.md`, mais le fichier présent dans `ressources/templates/` n'est PAS le bon (mauvais fichier repéré en relecture 08/06). Vigilance « EAT » : réserver l'abréviation à *état de l'art technique*, ne pas réintroduire « matrice EAT » (renommée « matrice éco-critères »).
- [ ] **Vérifier les images** de toutes les fiches (présence + rendu) — **dont les 11 SVG du 29/05** (algorithme : générique + triptyques logigramme/MAE ; grafcet, chronogramme, analyse-de-schema), hand-codés au premier jet : valider la géométrie au rendu Quartz, en priorité `analyse-de-schema-exemple` (symboles composants) et les SVG placés dans les callouts de triptyque.
- [ ] **Reconcilier la cartographie AA Phase 1** : `RA-PROJET-C03-3/EEE/1` est porté à la fois par `lire-une-datasheet` et `analyse-de-schema-electronique` (multi-couverture C20) ; confirmer le statut de `EEE/2` et des `EEE/5` (logigramme/MAE/grafcet/chronogramme) pour étayer le « MVP strict 100 % ». Vérifier aussi `phases: dossier-technique` sur `analyse-de-schema` (repris du cadrage, non revérifié) et le lien `[[del|LED]]` (probablement rouge — créer `del.md` ou délier).
- [ ] **Vérifier toutes les fiches du lot Phase 0 (gestion de projet)** avant publication : `bom`, `mind-map`, `fast`, `amdec`, `matrice-eco-criteres`, `ecodesign` + fiches GP/outils antérieures.

#### Phase 3 — Squelette transverse pro (~10 sessions, MVP étendu)

- [x] ~~`oscilloscope` (fiche-tuto compétence pro instrumentation)~~ — **fait 06/06 (suite 3)** (eee/mesure/, fille du hub instruments-de-mesure, aa:[])
- [x] ~~`multimetre` (fiche-tuto compétence pro)~~ — **fait 06/06 (suite 3)** (eee/mesure/, fille hub, aa:[], + SVG série/parallèle)
- [x] ~~`firmware` (transverse, archi code embarqué)~~ — **fait 02/06 (suite)** (`eee/mcu/firmware.md` 5,7 ko ; typé **notion** finalement, pas tuto ; progression super-loop → coopératif → MAE → modules → RTOS + section « selon la famille » ; C19 vrai test → **option (c)** ; `aa: .../PROJ/5` effleuré ; SVG couches = candidat)
- [x] ~~`debugger-embarque` (fiche-tuto compétence pro)~~ — **fait 06/06 (suite 3)** (eee/mcu/, tuto transverse [T], aa PROJ/5)
- [x] ~~`pcb` (mini-hub mère, gravure école / monoface interne vs double face JLCPCB)~~ — **fait 06/06 (suite 3)** (eee/pcb/, hub C18, aa EEE/5 dédiée, + SVG flux ; frontière fab = Gerber)
- [x] ~~`kicad` (fiche-tuto outil PCB principal, simu intégrée mention)~~ — **fait 06/06 (suite 3)** (eee/pcb/, fille hub pcb, aa:[])
- [x] ~~`bus-de-communication` (hub mère) + `uart` / `i2c` / `spi`~~ — **fait 28/05 suite 4** (grappe batch dans `eee/mcu/bus/` ; hub tableau comparatif + aide au choix + SVG topologies ; C18 épreuve 2/N ; AA hub EEE/4, filles [])

#### Phase 4 — Second embranchement ESP32 (~4 sessions)

- [x] ~~`esp32` (hub fille de `microcontroleur`)~~ — **fait 02/06 (suite)** (`eee/mcu/esp32/esp32.md` 6,0 ko ; décalque `arduino`, 4 paliers + [A]/[T], alerte 3,3 V ; C25/C26 épreuve 2/N ; `aa: []`). Tutos `esp32-*` rouges = roadmap Phase 4 ci-dessous.
- [x] ~~`esp32-arduino-core`~~ — **fait 06/06 (suite)** (l'API Arduino sur ESP-IDF/FreeRTOS ; `loop()`=tâche, 2 cœurs ; PROJ/5)
- [x] ~~`wokwi` (fiche-tuto simu spécifique ESP32)~~ — fait 06/06 (module `simulation`, MCU + `aa: PROJ/5`)
- [x] ~~`alimentation-electronique`~~ — **fait 06/06 (suite 4)** (finalement **notion transverse [T]** `eee/alimentation-electronique.md` ~14,3 ko, pas tuto ; 5 principes réguler/découpler/masses/CV-CC/protéger + exemple bras 3 axes + 3 SVG ; `aa RA-EEE-C03-2/EEE/3` **objet central → E→C** + `EEE/5` effleuré ; 3 liens entrants ; carto EEE 10C/0E, tally **44C/6E/77 %**)

#### Phase 5+ — Alimentation continue (~25 sessions)

*Reste squelette transverse :*
- [x] ~~`techno-sans-fil` (hub mère) + `wifi` / `ble` / `zigbee` / `xbee` / `lora`~~ — **fait 28/05 suite 4** (grappe batch dans `eee/mcu/sans-fil/` ; hub tableau + carte portée×débit ; C18 épreuve 3/N)
- [ ] `circuitverse` (fiche-tuto simu numérique en ligne, retenue 26/05 suite 5)
- [x] ~~`ltspice` (fiche-tuto simu analogique desktop)~~ — fait 06/06 (module `simulation`, SPICE / dimensionnement, exemple filtre RC + fc)
- [ ] `easyeda` (fiche-tuto outil PCB altern, embranchement de choix)
- [x] ~~`cable-management`~~ — **fait 06/07 (suite 2)** (notion `meo/`, tags eee+meo)
- [ ] `bom-electronique` (fiche-tuto compétence pro)
- [x] ~~`falstad` (fiche-tuto simu altern, à évaluer si pertinent après CircuitVerse)~~ — fait 06/06 (module `simulation`, analogique navigateur, exemple charge RC). `circuitverse` reste à évaluer (simu **numérique/logique**, niche distincte).

*Embranchements technologiques restants :*
- [x] ~~`esp32-idf`~~ — **fait 06/06 (suite)** (orientation : quand/pourquoi le natif ; `app_main`/`idf.py`/`menuconfig` ; `aa:[]`)
- [x] ~~`raspberry-pi` (hub fille SBC) + `raspberry-pi-prise-en-main`~~ — **module complet fait 06/07** (hub + 3 fiches `-prise-en-main`/`-gpio`/`-projet` + 3 SVG ; paliers adaptés ≠ clone C57 ; tally 79 % inchangé ; rouge `[[raspberry-pi]]` résolu). **Couche familles MCU/SBC complète.**
- [x] ~~`esp8266` (hub fille) + `esp8266-prise-en-main`~~ — fait 06/06 (suite 8).
- [x] ~~**Hubs prio 2** : `stm32`, `teensy`, `pic`~~ — faits 06/06 : `stm32` (suite 6, hub+5), `teensy` (suite 7, hub+4), `pic` (suite 9, notion).
- [ ] `pcb-gravure-ecole` (fiche-tuto spécifique monoface interne)

#### Tutos EEE/info embarquée — méthodes commande hors mini-hub algorithme

- [ ] **`chronogramme`** : voir Phase 1 ci-dessus (transverse, jonction datasheet/algorithme/oscilloscope)

#### Tutos délégués cours collègues (mécanique / fabrication / ACV)

- [x] ~~`usinage`~~ — **fait 06/07 (suite 2)** (notion pointeur `mme/`, aparté délégation cours fabrication)
- [x] ~~`impression-3d`~~ — **fait 06/07 (suite 2)** (notion pointeur `mme/`)
- [x] ~~`soudure`~~ — **fait 06/07 (suite 2)** (notion pointeur `mme/`, tags eee+mme)
- [x] ~~`acv-simplifiee`~~ — **fait 06/07 (suite 2)** (notion pointeur `proj/` ; était un rouge hors inventaire)

#### Tutos MME (phase 2)

- [x] ~~`optimisation-mecanique` (fiche-tuto MME). Lien référentiel `RA-MME-C03-1/MME/6`.~~ — **fait 06/06 (suite 5)** : finalement **notion interface** (`mme/`, pas tuto), décalque `schema-cinematique` ; `RA-MME-C03-1/MME/6` **E→C** ; 1 SVG conceptuel (avant/après allègement). MME refermé (5 C / 5 E / 1 HS-D / 0 NC).

### 3. Templates à rédiger
- [x] ~~**Template fiche-tuto** dans `templates/fiche-tuto.md`~~ — **existe déjà** (`content/templates/fiche-tuto.md`, figé 27/05 suite 4). Ligne stale (le chemin réel est `content/templates/`, pas la racine).

## Reprise visuelle SVG (voie A — sessions dédiées)

- [ ] **`cycle-v-projet`** — re-coder en voie A (cohérence de style + mode sombre). Signalé en relecture du hub « Conduite de projet » (08/06) ; embarqué sur l'accueil + le hub. Le peigne complet des ~81 SVG est suivi dans `_drafts/peigne-svg-prepublication.md` et au BACKLOG.
- [ ] **`bete-a-cornes-generique`** — reprendre en voie A. Signalé en relecture de `specification-technique` (08/06) ; embarqué sur `specification-technique` + `bete-a-cornes`.
- [ ] **`pieuvre-generique`** + **`pieuvre-bras-3-axes`** — reprendre en voie A. Signalés en relecture de `specification-technique` (08/06) ; embarqués sur `specification-technique` (générique aussi sur `pieuvre`).

## Tâches techniques en suspens

- [x] ~~**Archivage JOURNAL — masse 8 entrées (`25/05 suite 7` → `27/05 suite 5`)**~~ — fait 29/05 suite 2 (flux 1-pour-1 fidèle, JOURNAL 97→29,7 ko, archive →302,9 ko, somme conservée, invariant antichronologique restauré ; incident D8 résolu — graphies d'origine « traçée »/« 2ᵉ » à recopier verbatim, jamais « corriger », cf. C14). **Pas de backlog d'archivage** : JOURNAL repart à `28/05`, bien sous le seuil 100 ko. Rappel procédure (au prochain franchissement) : archivage **hors clôture, en tête de session**, plus ancienne entrée d'abord, marqueurs `<!-- DEBUT DES SESSIONS … -->` par groupe-date.

## Décisions éditoriales en attente

- [ ] **6 conventions transverses (25/05) + conventions concept/PoC/dossier-technique (25/05 suite 4-8)** à éprouver. Conventions 1-6 à éprouver sur `securite-et-qualite` (3ᵉ transverse). Documentation dans `conventions.md` § 7.
  - Convention 6 mode d'application affiné (au fil + filet passe dédiée, 25/05 suite 7).
  - **Convention 10** (matrice incarnée dans `[!example]`) **promue § 2 le 26/05 début session** après 12 contextes éprouvés (concept 4 + PoC 4 + dossier-tech 4).
  - **Convention 11** (rythme H4 par étape) **promue § 6 le 25/05 suite 7**. **Amendée § 6 le 26/05** : 2 nouveaux cas inédits acquis sur `integration-et-tests` (étape multi-disciplinaire = 3 H4 par discipline ; pyramide compressée = 3 H4 dense pour 4 niveaux conceptuels).
  - **Convention 12** (Pièges nourris a posteriori) **promue § 6 le 26/05 début session** après ratio stable 37-45 % sur 3 trames du V (concept 37 %, PoC 45 %, dossier-tech 45 % ; integration ~55 %).
  - **Convention 13** (relire amont avant section avale) **promue § 5 le 26/05 fin session** après épreuve 2/2 sur dossier-tech (suite 8) + integration-et-tests.
  - **Convention 14** (seuil 30 ko MCP payload) **enrichie 26/05 suite** à deux niveaux : (a) **pattern MARKER + N segments** validé comme procédure standard pour déplacements de blocs > 30 ko (archivage JOURNAL 22→24/05 réussi, 60 ko net via 3 segments de 17-24 ko) ; (b) **nouveau piège EPERM Windows distinct du seuil de payload** : verrou de fichier quand Obsidian a le focus dessus, `edit_file` échoue au rename final, remède = changer d'onglet Obsidian. Zone intermédiaire 30-40 ko à éprouver pour calibrage exact (24 ko passe, 50 ko échoue suite 8).

*Note : les autres décisions éditoriales en attente (mode sombre callouts v2, pliage callouts, alignement rétroactif trames, alias Quartz, station météo, 2 images par fiche-notion, format date, tag `transverse`, section équipe transverses, etc.) sont désormais portées par BACKLOG.md (conventions à éprouver) ou par conventions.md (conventions acquises). Pas de duplication.*




