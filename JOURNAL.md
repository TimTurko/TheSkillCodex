# JOURNAL — TheSkillCodex

> Mémoire du projet. Ce qui a été fait, daté, avec les décisions prises et leur justification.
> Nouvelles entrées en HAUT (ordre antichronologique).
> Sessions antichronologiques antérieures au 30/08 (suite 11) archivées dans `JOURNAL-archive.md`
> (mise en place initiale du dépôt, trame projet cycle en V, squelettes du V,
> rédaction de `specification-technique.md`, trames transverses et fiches-notion,
> modules MCU complets — ESP32, STM32, Teensy, ESP8266, MicroPython, Raspberry Pi —,
> clôture MME, squelette pro EEE, sweep liens rouges pré-publication, relecture de fond des modules, prises de vue, chantier de traduction mené à son terme (quatorze lots, 242 fiches, 291 261 mots) ;
> dernière coupe : 30/08, sessions 22/08 → 30/08 (suite 10)).

<!-- INSERT_JOURNAL_HERE -->
## 2026-08-31 (séance 16) — Le chantier « site exploitable par une IA » posé en entier, sept réfutations qui sont toutes la même, et le seul anglais publié que rien ne mesure

- **Périmètre** — **PC perso, onglet Code**, exécution directe, **vingt-quatre blocs (162 à 185)**. Objet unique : les blocs **A + B + C + A5** du § 8 de `_drafts/cadrage-ia-quatre-mains.md`, dans l'ordre du brief. **Chantier des puces non ouvert**, bloc 0 et bloc 1 intacts. **Aucun commit, aucun push** (C121).
- **Livrables** — **Code** : `quartz/plugins/emitters/rawMarkdown.ts` et `llmsTxt.ts` (neufs), `Head.tsx` (`hreflang` FR↔EN + `text/markdown`), `quartz.layout.ts` (filtre `ia`, pied de page vidé), `contentIndex.tsx` (déclaration XML du sitemap), `quartz.config.ts`, `quartz/plugins/emitters/index.ts`. **Contenu** : `content/ia/index.md` (150 lignes, bilingue), `content/robots.txt`, carte des cinq phases et `description:` sur les deux racines, phrase du hub `conduite/` et de sa jumelle, `lang: en` sur **242** fiches. **Outils** : `tools/ajouter-lang-en-3108s16.mjs` (jetable), exemption `bilingue: true` dans `compter-mots.mjs` et `creer-fiche-en.mjs`, `lang: en` au squelette du générateur.
- **Décisions Tim** — **aucune sollicitée, aucune rendue** : la séance est un lot de production sous brief validé. **Une décision prise seule à coût de revert nul** : ne poser **aucune exemption nommée** pour l'orpheline `/ia/` — l'arbitrage annoncé au brief n'avait pas d'objet (voir ci-dessous).
- **Conventions** — **aucune neuve, la numérotation reste à 131.** **Six entrées § 8** : *une prédiction de `numstat` se compte sur les lignes que git compare* ; *une page bilingue sort de tous les contrôles de la zone anglaise* ; *une spécification qui présuppose une structure du corpus se vérifie sur le corpus* ; *avant d'arbitrer sur ce qu'un outil va signaler, lire ce qu'il compte* ; *la fin de ligne d'un fichier n'existe pas toujours, l'unité est la ligne d'ancre*.
- **Prédictions** — **150 publiées avant leur bloc, 143 tenues, 7 réfutées — 95,3 %**, sur vingt blocs (162 à 181). **Sept tests négatifs délibérés, sept refus, zéro octet écrit à tort.** Les sept réfutations sont **toutes** des comptes de lignes, d'entrées ou de textes de mes propres éditions ; **aucune ne porte sur un verdict, une mesure du corpus ou un contrôle en ligne**. ⚠ *Ce total s'arrête au bloc 181 : les blocs de correction qui l'ont suivi ajoutent chacun leurs prédictions et leur test négatif, donc périment le chiffre qu'ils écrivent. Le registre `tools/predictions-260831.md` fait foi (C130).*
- **Tailles** — mesurées par `wc -c` **avant** écriture du pilotage : conventions **604 375 o**, TODO **299 770**, BACKLOG **226 997**, JOURNAL **53 495**. Corpus : **243 fiches FR publiées, 293 413 mots** ; **242 traduites (291 261 mots)**, **1 bilingue exemptée (2 152)**, **0 restant**. `public/llms.txt` **121 237 o**, **243 lignes de fiche**, **25 groupes**. `public/` : **485 `.md`**, **485 `<url>`** au sitemap.

✅ **LE DISPOSITIF EST COMPLET ET MESURÉ EN LOCAL, PIÈCE PAR PIÈCE.** `llms.txt` rend `242 paires, 1 sans jumelle` — la carte s'apparie par `source_fr` et jamais par le slug, ce qui rattrape les sept `index.md` que la règle du suffixe `-en` ratait. Le Markdown brut sort à **485 fichiers**, front matter conservé, **0 commentaire HTML** dans `ecoconception.md` contre **3** dans sa source. `hreflang` **1 / 1** de part et d'autre d'une paire, `<html lang="en">` sur une page anglaise et `<html lang="fr">` sur sa jumelle — le défaut de césure au dictionnaire français est fermé par le même champ. Le sitemap ouvre sur `<?xml`, `robots.txt` est copié **identique à l'octet**, le titre de `/ia/` n'apparaît **0 fois** dans la racine servie (filtre de l'Explorateur), et `discord` **0 fois**. ✅ **Le contrôle qui discrimine sur la forme de `/ia/`** : les titres du bloc anglais sortent en **douze `<h3>` et non six** — sans les lignes vides autour de `<section lang="en">` (décision 5 de Tim), le Markdown anglais ne serait pas interprété et le compte tomberait de moitié.

⚠ **LE SEUL ANGLAIS PUBLIÉ DU DÉPÔT QUE RIEN NE MESURE EST CELUI DE LA PAGE QUI EXPLIQUE LE DÉPÔT AUX MACHINES.** Le bloc EN de `/ia/` vit dans `content/ia/index.md`, donc **hors de `content/en/`** — et les six contrôles du chantier de traduction (`--controle`, `--style`, `--alt`, `--libelles`, `derive-traduction`, `compter-mots --paires`) balaient tous la zone anglaise. C'est le **prix non chiffré** de la décision « une page unique, bilingue », prise le 31/08 pour de bons motifs : une adresse, un entretien, pas de lot de traduction. ⚠ *L'exemption `bilingue: true` posée au bloc C2 referme la **population** des compteurs — `RESTANT A TRADUIRE` passe de 1 à 0 sans qu'aucune fiche ait été traduite, c'était le test négatif — **mais exempter n'est pas contrôler**.* Entrée § 8, et deux issues au BACKLOG.

⚠ **L'ARBITRAGE ANNONCÉ AU BRIEF N'AVAIT PAS D'OBJET, ET LE CODE LE DISAIT.** Le brief attendait qu'`audit-wikilinks` signale `/ia/` comme orpheline, et demandait de trancher entre exemption nommée et acceptation. **L'outil indexe des cibles de wiki-liens**, pas des pages : une page sans lien entrant n'est la cible de rien, elle n'entre dans aucun seau. Mesuré après la pose : `MORT 0`, `CASSE 0`, code 0, et `/ia/` n'apparaît nulle part. *Il n'y avait ni exemption à écrire, ni décision à rendre.* Le seul instrument qui la nomme est `derive-traduction --manquantes`, sous `ORPHELINE`, et il n'est **pas** bloquant — ni lancé par `batterie.ps1`, ce qui part au BACKLOG. **C'est C116 (7) portée d'un chiffre à un arbitrage** : la même erreur coûtait un nombre faux, elle coûte ici une exemption qui aurait pollué un outil pour rien.

⚠ **LES SEPT RÉFUTATIONS SONT LA MÊME, ET ELLE N'EST PAS CELLE QU'ON CROIT.** Trois portent sur un `git diff --numstat`, deux sur `git status`, deux sur un texte du pilotage — une ancre et une population d'occurrences. Aucune ne vient d'avoir prédit sans ouvrir le code — l'amendement du 30/08 a été tenu à chaque fois. Elles viennent d'avoir compté **le geste d'édition** au lieu **des lignes que git compare** : une ligne vide de fermeture oubliée (`12 0` pour `11 0`) ; une ligne conservée à l'identique comptée dans un bloc remplacé (`12 1` pour `13 2`) ; un `.set()` oublié entre la déclaration et la lecture ; et **deux fois le total d'un `git status` où le sous-compteur était juste** — le fichier neuf compté, le fichier qui devenait modifié oublié, alors qu'il était nommé deux lignes plus haut. ✅ **Parade posée au bloc 168 et tenue neuf blocs de suite** : chaque déclaration C131 recompte **nominativement, entrée par entrée**, avec son numéro. Les six déclarations suivantes sont justes sur leurs deux termes, y compris à **270** entrées et sur deux `numstat` **cumulés depuis HEAD** (`15 1` et `3 2`).

⚠ **UN DÉFAUT TROUVÉ EN RELISANT UNE SORTIE, ET AUCUNE PRÉDICTION NE LE PORTAIT.** Le § 8 spécifiait les groupes de `llms.txt` par « titre du `index.md` du dossier ». **Dix-sept des vingt-quatre dossiers FR n'ont pas d'`index.md`** — C18 y loge un hub en fichier nommé — et le fichier est sorti avec dix-sept en-têtes en chemin brut. Les prédictions du bloc portaient sur le nombre de paires, la taille et l'absence de commentaires : **toutes justes, et aveugles à ce qui était illisible**. Le repli posé (hub `<dossier>/<segment>.md`) en résout **dix** ; les sept derniers ont un hub dont le nom n'est pas celui du dossier, et ils gardent leur chemin plutôt qu'une table codée en dur. Entrée § 8.

**Reste à Tim (C121)** — sur le PC perso :

```powershell
cd C:\Users\turko\Documents\TheSkillCodex
git status
git add -A
git commit -m "seance 16 : chantier IA pose en entier, blocs A B C, lang en sur 242, cinq entrees SS8"
git push
```

**Puis, une fois la CI passée**, le contrôle en ligne (bloc C5, **non fait, il attend le déploiement**) :

```powershell
cd C:\Users\turko\Documents\TheSkillCodex
"llms.txt","ia/","conduite/proj/concept.md","robots.txt" | ForEach-Object { $u = "https://timturko.github.io/TheSkillCodex/$_"; try { $r = Invoke-WebRequest -Method Head -Uri $u -UseBasicParsing; "$($r.StatusCode)  $u" } catch { "ERR $($_.Exception.Message)  $u" } }
(Invoke-WebRequest -Uri "https://timturko.github.io/TheSkillCodex/conduite/proj/concept" -UseBasicParsing).Content -match 'hreflang'
```

**Reste aussi, hors dépôt** : Search Console « Vérifier » → Sitemaps `sitemap.xml` → inspection d'URL sur `/` et `/ia/` → Bing par import. Et le **clic « TheSkillCodex en français » depuis `/en/`**, dû depuis la séance 14 ; le **patch § 7 côté claude.ai**.

**Arbitrages en attente** — ✅ **un CLOS sans arbitrage** : l'orpheline `/ia/`, l'outil ne pouvant pas la voir. ⚠ **Un NEUF, et c'est le plus lourd de la séance** : comment contrôler l'anglais d'une page `bilingue: true` (chemin explicite passé aux outils, ou balayage des blocs `<section lang="en">` de la zone FR). **Deux neufs mineurs** : les sept en-têtes en chemin brut de `llms.txt` ; `derive-traduction --manquantes` absent de `batterie.ps1`. **Inchangés** : exemption ou correction des six hubs à double titre, relecture ciblée des quatre fiches, acheminement invisible vers `/ia/` (à rouvrir après la recette APRÈS), URL absolues sans contrôle, et les quatorze entrées de la file.

⚠ **Chantiers FERMÉS, redits pour mémoire** : le **chantier de traduction** depuis le 30/08 (séance 10), 242/242 ; la **coupe C128** ; la **rédaction des textes IA** (séance 15) ; et ce soir la **pose des blocs A, B et C** — il ne reste du chantier IA que le déploiement, le contrôle en ligne et la **recette APRÈS**.

**Prochaine session** — **chat Desktop, séance de recette** : les **neuf prompts** de `_drafts/recette-ia-3108-avant.md` rejoués dans les **mêmes conditions**, trois assistants, conversations neuves sans relance, résultats dans `_drafts/recette-ia-AAMMJJ-apres.md`, **grille contre grille**. Condition d'entrée : le commit poussé, la CI passée, les **quatre `200`** vérifiés. Prompt de lancement livré à l'écran (C129).


## 2026-08-31 (séance 15) — Les quatre textes du chantier IA écrits et validés, la posture argumentée avant d'être rédigée, et un dispositif dont le lien d'entrée n'est visible de personne

- **Périmètre** — **PC pro, chat Desktop**, séance courte annoncée par la séance 14, objet unique : **rédiger les textes**. **Aucune fiche touchée, rien dans `content/`, aucun outil versionné modifié, chantier des puces non ouvert.** Lectures d'ouverture selon le § 6 cas A — `conventions.md` tête + queue, `JOURNAL.md` head 250 — puis `_drafts/cadrage-ia-quatre-mains.md` §§ 7-8-9 et `_drafts/recette-ia-3108-avant.md`, plus dix fichiers du corpus appelés pour prendre les cinq phases à la source. ⚠ **Note d'horloge (C119)** : le plan du § 9 plaçait cette séance à J+1 ; l'horloge lit **31/08**, même jour que la séance 14. La date est lue, pas incrémentée.
- **Livrables** — `_drafts/textes-ia-brouillon.md`, **seul fichier écrit hors pilotage** : (1) corps FR de `content/ia/index.md` ; (2) le même en anglais, à envelopper dans `<section lang="en">` ; (3) carte des cinq phases pour `index.md` et `en/index.md` avec leurs `description:` explicites ; (4) phrase du hub `conduite/index.md` et de sa jumelle. **Validé par Tim, les quatre sections**, marque portée en tête du fichier.
- **Décisions Tim** — **cinq sollicitées, cinq rendues.** (1) **Rôle de `/ia/`** : elle porte l'explication du site **et** les règles d'encadrement de l'étudiant ; l'étudiant n'y arrive pas de lui-même, c'est l'assistant parti de la racine qui va l'y chercher. Titre : `Utiliser ce site avec un assistant IA`. (2) **URL absolues sur `/ia/`**, wiki-links ailleurs. (3) **Deux-points, aucune glose au cadratin créée.** (4) `en/index.md` sans l'adresse de `/ia/`. (5) Lignes vides autour de `<section lang="en">`.
- **Conventions** — **aucune neuve, la numérotation reste à 131.** Deux entrées § 8 : *un texte neuf s'écrit à la forme cible d'un chantier cadré* ; *une validation nomme sa surface*.
- **Tailles** — mesurées par `list_directory_with_sizes` **avant** toute écriture : conventions **588,18 Ko**, TODO **289,99**, BACKLOG **220,15**, JOURNAL **44,69**. **Après** écriture des trois premiers, avant cette entrée : conventions **590,21** (+2,03), TODO **292,74** (+2,75), BACKLOG **221,68** (+1,53). Brouillon **22 861 → 24 041 o**. **Corpus non relancé** : aucune fiche ouverte en écriture.

✅ **LA POSTURE A ÉTÉ ARGUMENTÉE AVANT D'ÊTRE RÉDIGÉE, ET L'ARGUMENT A CHANGÉ LE TEXTE.** La ligne entre aider et faire à la place ne se trace pas entre écrire et réfléchir : elle se trace sur **la propriété de l'artefact évalué**. Les trois interdictions demandées sont alors **trois mécanismes distincts** — livrer le CdCF **retire l'apprentissage** (le livrable *est* le résultat évalué) ; valider une revue **usurpe une autorité** (la revue de CdCF est un jalon enseignant, la faute porte sur le verbe et non sur le contenu) ; trancher le choix de la carte **contredit ce que le corpus enseigne** (un choix se justifie par une matrice, et Gemini P3 rend une réponse techniquement sérieuse et pourtant non conforme, parce qu'elle tranche « sans appel »). ✅ **Un quatrième mécanisme, non demandé, a été ajouté sur la mesure** : Claude P2 cite juste, ne se trompe sur rien et **restitue la trame entière** — rien n'y est faux, c'est la fiche qui n'est plus ouverte. D'où la quatrième règle d'usage, *on renvoie plutôt que de restituer*, qu'aucune des trois interdictions n'attrapait. ⚠ **Principe de rédaction qui en sort** : *aucune interdiction sans son « à la place, on »* — un texte qui ne liste que des refus produit un assistant qui refuse, pire pour l'étudiant qu'un assistant qui déborde.

⚠ **LE DISPOSITIF ENTIER TIENT À UN LIEN QU'AUCUN HUMAIN NE VOIT, ET C'EST LA DÉCISION 1 QUI LE VEUT.** Trois canaux le portent, aucun n'étant un texte caché : le `<meta description>` de la racine, le Markdown brut de la racine (le front matter y reste, bloc A1), et `/llms.txt` dont le préambule **est** le corps de `/ia/`. ⚠ **Le trou est nommé** : l'assistant qui fetche la racine, ignore la description et ne sonde pas `llms.txt` — exactement la famille « racine seule » mesurée la veille. Pour lui, la **carte des cinq phases** est le filet, et elle suffit sur P2 sans suffire sur la posture. **Un pointeur visible en bas de racine lèverait le doute et n'a pas été posé** : la recette APRÈS répond en trois prompts, et poser la ligne avant reviendrait à payer la contrainte sans avoir mesuré le besoin.

✅ **DEUX CONTRAINTES MÉCANIQUES ONT COMMANDÉ LA FORME, ET AUCUNE N'EST UN GOÛT.** (1) `/ia/` porte des **URL absolues** parce que le préambule de `llms.txt` est son corps **sans résolution de wiki-link** : un `[[concept|Concept]]` arriverait tel quel dans l'index destiné à la machine. Prix versé au BACKLOG : le domaine est codé en dur et **aucun contrôle ne lit les liens absolus**. (2) **Aucune glose au cadratin n'a été créée** : suivre le style local aurait ajouté une dizaine d'occurrences à la population de 1 164 qu'on s'apprête à traiter, sur deux fichiers qui sont déjà des porteuses. Entrée § 8.

⚠ **LE DÉFAUT DE LA SÉANCE EST UNE SURFACE NON NOMMÉE, ET IL A FAILLI FAIRE VALIDER LE MAUVAIS OBJET.** Un même tour a rendu l'argumentaire de posture **à l'écran** et le brouillon **sur disque**, et ma clôture de tour n'a dit ni que les deux étaient distincts, ni où lire le second — elle en donnait la taille en octets, ce qui suppose déjà de savoir qu'il existe. Tim a demandé où lire le brouillon **en le validant dans la même phrase**. *C129 signale les transitions de surface entre séances ; rien ne les signalait à l'intérieur d'un tour.* Entrée § 8, et règle appliquée dès la réponse suivante.

**Reste à Tim (C121)** — sur le PC pro :

```powershell
cd C:\Users\timothe.turko.ICAMAD\Documents\TheSkillCodex
git status
git add -A
git commit -m "seance 15 : textes du chantier IA valides, deux entrees SS8, pilotage"
git push
```

**Reste aussi** : le clic « TheSkillCodex en français » depuis `/en/` ; le statut du sitemap à relire dans Search Console ; le patch § 7 côté claude.ai.

**Arbitrages en attente** — ✅ **cinq RENDUS et sortis de la file** : rôle et titre de `/ia/`, URL absolues, forme des gloses, `description:` de `en/index.md`, blancs de `<section>`. ⚠ **Deux NEUFS versés au BACKLOG** : l'**acheminement vers `/ia/` par un canal invisible et non mesuré** (à rouvrir après la recette APRÈS) et les **URL absolues sans contrôle**. **Inchangés** : forme du champ de fiche bilingue (`bilingue: true`, à fixer au bloc C avec son test négatif), exemption ou correction des six hubs à double titre, relecture ciblée des quatre fiches, et les quatorze entrées de la file.

⚠ **Chantiers FERMÉS, redits pour mémoire** (règle du 31/08, séance 13) : le **chantier de traduction** l'est depuis le 30/08 (séance 10), 242/242 fiches, 291 261 mots ; la **coupe C128** est faite ; la **rédaction des textes IA** l'est ce soir.

**Prochaine session** — **onglet Code, séance de production** : blocs **A** (émetteurs `rawMarkdown.ts` et `llmsTxt.ts`, `Head.tsx` `hreflang` — jeton déjà posé —, layout, **A5** déclaration XML du sitemap), **B** (`robots.txt`, passe `lang: en` sur 242, générateur), **C** (`ia/index.md` et exemption `bilingue: true` avec test négatif, racines et `--recaler`, hub). Spécification et prédictions au **§ 8 de `_drafts/cadrage-ia-quatre-mains.md`**, textes au **`_drafts/textes-ia-brouillon.md`**. Prompt de lancement livré à l'écran (C129).

## 2026-08-31 (séance 14) — Le site mesuré tel qu'une IA le lit, un dispositif en cinq pièces arbitré, et une recette AVANT qui montre trois assistants, trois familles d'accès

- **Périmètre** — **PC pro, chat Desktop (Fable 5)**, séance intermédiaire annoncée par la séance 13, objet neuf et hors chantier des puces (bloc 0 et bloc 1 intacts). **Aucune fiche touchée, rien dans `content/`.** Après clôture, **une ligne dans `quartz/components/Head.tsx`** (jeton Search Console), seul fichier de code versionné touché. Un script de mesure jetable (`tools/seance.ps1`, hors git), deux brouillons dans `_drafts/`, un témoin C124, les quatre fichiers de pilotage.
- **Livrables** — `_drafts/cadrage-ia-quatre-mains.md` : inventaire de sept dispositifs avec coûts de mise en place, d'entretien, côté étudiant et risque pédagogique (§ 0-6), mesures rendues (§ 6 bis), arbitrages (§ 7), **spécification des blocs A-B-C-D avec prédictions** (§ 8), plan (§ 9). `_drafts/recette-ia-3108-avant.md` : protocole, trois prompts, attendus lus dans le corpus, **neuf réponses intégrales et la grille**. `tools/seance-sortie-3108s14.txt`, témoin des sept étapes de mesure ; `tools/seance-sortie-2708s4-serie3.txt`, copie C124 de la sortie précédente du poste.
- **Décisions Tim** — (1) `llms.txt` **et** Markdown brut par page ; (2) codes AA non publiés ; (3) dispositif **automatique, sans geste étudiant** — l'étudiant donne la racine, rien d'autre — puis **page `/ia/` unique, bilingue, hors navigation** ; (4) émetteurs Quartz ; (5) `robots.txt` tout permis + `Sitemap:` ; (6) **indexation oui** ; (7) phrase du hub réécrite ; (8) analytics `null` ; carte des cinq phases sur la racine ; les trois prompts de recette ; **le plan dans l'ordre, ce chantier devant le bloc 0 des puces**. Pris par Claude à coût de revert nul et consignés au § 7 du cadrage : B entier, `hreflang`, JSON-LD abandonné, un seul `llms.txt`, préambule = corps de `/ia/`, premier paragraphe lu par l'émetteur, commentaires HTML retirés du brut, `aliases: [IA]`.
- **Conventions** — **aucune neuve, la numérotation reste à 131.** Trois entrées § 8 : *un artefact pour la machine se dérive du texte que l'humain lit* (avec ses quatre corollaires : projections, hors navigation et non caché, règles d'usage et non ordres, profondeur = curseur renvoi / substitution) ; *une fiche bilingue déclare qu'elle l'est* (champ à fixer au bloc C, proposition `bilingue: true`, test négatif obligatoire) ; *un prédicat de ligne compte des lignes* (`^# ` et les blocs de code).
- **Prédictions** — **20 termes publiés avant mesure, 17 tenus, 3 réfutés (15 %)** : deux sur le même objet (H1 dans le corps), un après clôture sur le sitemap (déclaration XML attendue, absente). Sept sondes HTTP et corpus tenues à l'unité (`404`, `404`, `200`, `200`, `242`, `0`, JSON dans la bande 2-8 Mo : **4 171 260 octets**). Script : 12 termes, 10 tenus.
- **Tailles** — mesurées par `list_directory_with_sizes` avant toute écriture : conventions **585,34 Ko**, TODO **286,27**, BACKLOG **216,11**, JOURNAL **33,88** ; après écriture des trois premiers, avant cette entrée : conventions **588,18** (+2,84), TODO **289,63** (+3,36), BACKLOG **220,15** (+4,04). Corpus non relancé (aucune fiche touchée) ; `content/en` **242 `.md`**, FR hors `en` et templates **243** dont **1** brouillon.

✅ **CE QUE LA MACHINE VOIT DU SITE, MESURÉ AVANT D'INVENTORIER.** Deux `web_search` nominatives, **zéro résultat vers le site** sur 19 documents. Fetch de la racine : trois liens de branche et `/en/`, Explorateur et graphe vides (construits côté client), `meta description` polluée par le H1 et le callout anglais. Fetch de `/conduite/` : **la description machine est la première phrase de la fiche** — la convention du popover du 19/05 a déjà écrit le résumé machine de chaque fiche, et le script le confirme sur **476 fiches sur 476** hors hubs. Fetch de `/tags/proj` : 64 entrées FR et EN mêlées sans marqueur de langue. Dépôt : `static/contentIndex.json` porte le texte intégral aplati et le graphe de liens de 484 pages — **4,17 Mo en ligne, existant mais inavalable en un fetch** ; `sitemap.xml` **484 = 242 + 242**, sans page d'étiquette ni de dossier ; `<html lang="fr">` sur les 242 pages EN (aucun front matter EN ne porte `lang`), et `custom.scss` césure en `hyphens: auto` : **l'anglais est césuré au dictionnaire français sur écran large** — un défaut humain et un défaut machine pour le même champ manquant. `assets.ts` copie tout non-`.md` de `content/` à la racine du site ; `path.ts` **retire l'extension `.html`** au passage.

✅ **L'AXE QUI ORDONNE L'INVENTAIRE : RENVOYER OU RÉPONDRE À LA PLACE, ET LES DEUX ÉCHECS NE SONT PAS SYMÉTRIQUES.** Un dispositif qui donne titres, définitions et URL met l'assistant dans la position d'un wiki-link — popover puis redirection. Un corpus en bloc le fait paraphraser sans que l'étudiant ouvre une fiche. Mais l'état actuel est le pire des deux : **l'assistant qui ne trouve pas le corpus répond depuis le général et le contredit sans le savoir.** Sept options inventoriées de A (rien) à G (service dédié) ; retenu **C + F + B + D1** et une page d'entrée. Tim a d'abord délégué la forme du mode d'emploi, puis tranché sur un fait qu'il connaît mieux que moi : *les étudiants ne colleront rien*. D'où le pivot — la seule lecture garantie est celle de l'URL donnée, donc **tout ce que l'assistant doit savoir vit sur la racine ou à un lien de distance**, et la `description:` de la racine, lue par tout outil de fetch et jamais affichée, porte l'adresse de `/ia/`.

⚠ **« INVISIBLE POUR L'HUMAIN » A ÉTÉ REFORMULÉ EN « HORS DU PARCOURS, JAMAIS CACHÉ », ET TIM L'A VALIDÉ.** Un texte masqué par CSS ou en commentaire HTML est jeté par les outils de fetch, traité en injection par les assistants et en cloaking par les moteurs — et il contredirait la règle de Tim lui-même, *publié pour l'IA = publié pour tous*. `/ia/` sera une page normale, sans étiquette ni lien entrant, filtrée de l'Explorateur, présente dans la recherche et le sitemap, et ses consignes s'écrivent comme des règles d'usage du corpus : un assistant relaie ce qu'un site préconise, il refuse ce qu'une page lui commande. **La page unique traite les deux langues** parce que chaque fiche a une jumelle : la carte est bilingue par construction, une ligne par paire, et un lot de traduction, un sha et la moitié de l'entretien disparaissent. Prix : la première fiche FR publiée sans jumelle, que les contrôles de clôture doivent apprendre à lire.

⚠ **LA RECETTE AVANT EST LE RÉSULTAT LE PLUS UTILE DE LA SÉANCE, ET C'EST LE PROMPT DE TIM QUI L'A RENDU.** Trois prompts étudiants, trois assistants, neuf conversations neuves sans relance. **Claude** suit les liens : 3/3 cite, 3/3 conforme, mais **P2 restitue la trame `concept` en entier et offre de faire l'étape 1 à la place de l'équipe** — la substitution par le meilleur assistant, sur une demande de l'étudiant qui la réclamait ; P3 montre le comportement visé, la grille du site appliquée puis renvoi à `choisir-le-materiel`. **ChatGPT** lit la racine seule : P1 rend écodesign et écoconception synonymes *« d'après TheSkillCodex »* et écrit lui-même qu'il n'a pas trouvé l'index du site ; **P2 attribue au site un V à douze étapes qui renvoie un étudiant en phase de concept refaire le besoin et le CdCF**, avec un vocabulaire plausible et une structure fausse que l'étudiant ne peut pas voir. **Gemini** ne lit rien et **invente un « framework de The Skill Codex »** en cinq points. Zéro terme proscrit, zéro argument par le prix : le piège C71 ne s'est pas refermé. Trois familles d'accès, trois leviers — la carte de la racine et `llms.txt` pour la famille « racine seule », les consignes pour la substitution, **l'index de Google pour Gemini** : le point 6, d'abord décliné (*je ne cherche pas à être référencé*), a été rouvert sur cette mesure et retenu, indexation et non classement.

⚠ **DEUX RÉFUTATIONS SUR LE MÊME CHIFFRE, ET UN CHIFFRE ÉCRIT DE MÉMOIRE.** « H1 dans le corps : 3 » est sorti **29** : le prédicat `^# ` comptait vingt commentaires Python de blocs de code, et six hubs de domaine manquaient à mon inventaire du monde — ils portent un H1 sous leur `title:`, **double titre visible par le lecteur** (BACKLOG). Les 9 vrais sont mesurés par l'étape 7, indépendante, et **476 + 9 = 485** ferme la population. Puis, en écrivant le BACKLOG, j'ai donné aux huit commentaires HTML des numéros de ligne **de mémoire** ; la relecture contre la sortie du script les a tous trouvés faux (45, 146, 170 ; 50, 150, 170 ; 41, 161) et corrigés avant le commit. *La réfutation était dans mon propre relevé, écrit vingt minutes plus tôt.*

✅ **LE POINT 6 CHANGE DE MÉTHODE PAR UNE LECTURE DE `path.ts`.** `slugifyFilePath` supprime `.md` et `.html` : un `google<jeton>.html` déposé dans `content/` sortirait sans extension. Vérification par balise `<meta name="google-site-verification">` dans `Head.tsx`, la ligne prévue au bloc A3(iii). **Après clôture, le soir même** : la ligne posée par anticipation, commit poussé, balise mesurée en ligne (`True`), **propriété vérifiée, sitemap déposé** — statut « Impossible de lire » à l'instant du dépôt, file de Google, à relire à J+1 ; Bing attend le bloc A. ⚠ **Une vingtième prédiction, réfutée en passant** : j'attendais `<?xml …` en tête du sitemap, de mémoire du code amont ; l'émetteur lu le soir même ouvre sur `<urlset` — déclaration optionnelle en XML 1.0, Google lit les sitemaps Quartz tels quels, une ligne **A5** au bloc A lève le doute. **Rien dans `content/` ce soir, la règle de la séance tient jusqu'au bout.**

**Reste à Tim (C121)** — sur le PC pro :

```powershell
cd C:\Users\timothe.turko.ICAMAD\Documents\TheSkillCodex
git status
git add -A
git commit -m "seance 14 : cadrage IA quatre mains, mesures, recette avant, pilotage, jeton Search Console"
git push
```

**Fait le soir même** : commit poussé, propriété Search Console vérifiée, sitemap déposé. **Reste** : clic « TheSkillCodex en français » depuis `/en/` ; patch § 7 côté claude.ai ; un commit pour les mises à jour d'après-clôture (cadrage A5, TODO ①, cette entrée) ; relire le statut du sitemap à J+1.

**Arbitrages en attente** — forme du champ de fiche bilingue (`bilingue: true` proposé) ; exemption ou correction des six hubs à double titre ; relecture ciblée des quatre fiches avant ou après l'ouverture des puces (inchangé, après la rentrée).

**Prochaine session** — **chat Desktop, séance courte : les textes.** `/ia/` en FR et en EN, carte des cinq phases sur `index.md`, phrase du hub, dans `_drafts/textes-ia-brouillon.md`, relus par Tim ; sa clôture livre le prompt de la séance Code (blocs A + B + C, § 8 du cadrage). Prompt de lancement livré à l'écran (C129).


## 2026-08-31 (séance 13) — Quatre arbitrages rendus, et le périmètre du chantier des puces était chiffré sans être nommable

- **Périmètre** — **PC perso, chat Desktop**, modèle **Opus 5**, **séance 13**. ⚠ **Première séance du dépôt dont l'objet est un arbitrage de cadrage pur** : aucun bloc d'exécution, aucune prédiction publiée, **zéro octet écrit hors des quatre fichiers de pilotage**. Lectures d'ouverture selon le § 6 cas A — `conventions.md` tête + queue, `JOURNAL.md` head 250 — puis six relevés du dépôt appelés en cours d'échange (`perimetre-fr-3008s12.txt`, `style-avant-en-3008s12.txt`, `desalignees-3008s11.txt`, `puces-tiret.mjs`, les deux fragments BACKLOG de la séance 12).
- **Livrables** — **4 arbitrages rendus**, **1 spécification de bloc 0** (trois correctifs d'instrument, chacun avec son autocontrôle), **1 spécification de bloc 1** (outil d'appariement des 13 suppressions), **1 prompt de lancement** pour l'onglet Code, **1 prompt de lancement** pour une séance chat intermédiaire. **Aucun outil écrit, aucune fiche touchée.**
- **Décisions Tim** — **trois sollicitées, trois rendues, une déléguée.** **(a)** Découpe du chantier des puces : **HYBRIDE** — tranche 1 = les **193 items de liste numérotée** (FR) / 183 (EN), puis les **971 / 945 puces par module**. **(b)** Sens de travail : **FR d'abord, report un pour un en EN**, avec **seau nommé déclaré au cadrage** pour le résidu non bijectif (~36 côté FR). **(c)** Report des trois items au BACKLOG **refusé après instruction** (voir ci-dessous) : bloc 0 part maintenant. **(13)** Les 13 `C109 supprimées en EN` : **délégation explicite de l'arbitrage à Claude**.
- **Décision prise seule** — les 13 suppressions se lisent **une par une** ; le cas `easyeda-en` **n'est pas un précédent de classe**. Motif : une suppression de cadratin recouvre au moins quatre choses — rendu par virgule, réécriture en subordonnée, coupe en deux phrases, perte réelle d'articulation — dont **une seule se corrige**. **Coût de revert nul** : si les quatre premières lectures rendent le même verdict, on bascule en précédent et on l'écrit.
- **Conventions** — **aucune neuve, la numérotation reste à 131.** **Deux entrées neuves au § 8.** ⚠ **C132 annoncée sous condition** : la distinction *puce / item de liste numérotée*, absente de C109, ne se rédige **qu'après la tranche 1** — on ne saura qu'à ce moment-là si les deux espèces se traitent vraiment différemment.
- **Tailles** — mesurées par `get_file_info` **avant** toute écriture de la présente clôture : `JOURNAL.md` **27 655 o**, `conventions.md` **596 763 o**, `TODO.md` **289 466 o**, `BACKLOG.md` **218 392 o**. **Corpus inchangé au mot** : la séance n'a ouvert aucune fiche en écriture.

⚠⚠ **LE FAIT DE LA SÉANCE EST TOMBÉ D'UNE QUESTION SIMPLE — « DONNE-MOI LE NOM DES FICHES » — ET LE PÉRIMÈTRE N'Y A PAS RÉPONDU.** Le chantier des puces est chiffré au mot près depuis la séance 12 : **FR 1 164 gloses sur 178 porteuses**, **EN 1 128 sur 173**. **Aucune de ces 178 n'est nommée nulle part.** `--style` imprime `porteuses du perimetre : 178 fichier(s)`, un cardinal ; `puces-tiret.mjs --corpus` imprime `porteuses` et `fichiers` sans les nommer non plus ; et son mode `--fiches`, qui rendrait le compte fiche par fiche, **exige en argument la liste qu'aucune sortie ne produit**. ✅ **Conséquence immédiate sur l'arbitrage (a)** : aucune des trois découpes — module, espèce, densité — n'était constructible au moment où elle a été tranchée. Le correctif est **une ligne** et il remonte en tête de bloc 0. ⚠ *C'est la sœur de la règle du 30/08 (séance 12) sur l'exemption muette, prise par l'autre bout : là une population n'était pas comptée ; ici elle est comptée et pas nommable.*

⚠⚠ **LE REPORT DEMANDÉ EN (c) PORTAIT SUR UNE CONDITION DÉJÀ REMPLIE, ET LE MOTIF N'EST PAS UNE INATTENTION.** La demande était de verser les trois items « au BACKLOG pour quand on aura terminé la traduction ». **Le chantier de traduction est fermé depuis le 30/08 (séance 10) — 242 fiches sur 242, 291 261 mots sur 291 261, `RESTANT A TRADUIRE : 0 fiches, 0 mots`** —, et les trois items **étaient déjà au BACKLOG**, versés aux séances 11 et 12. Le report était donc sans objet dans ses deux termes. ⚠ *Douze séances d'onglet Code séparent la clôture du chantier de la demande, et une séance de production ne relit pas le JOURNAL en entier : **l'état d'un chantier FERMÉ s'efface plus vite que celui d'un chantier ouvert**, parce que rien ne le rappelle.* ✅ **Ce qui a permis de le voir sans mesure** : la clôture de la séance 12 le porte en toutes lettres dans son propre en-tête de périmètre.

⚠ **UNE IDENTIFICATION FAITE À LA MAIN, ET LA MÉTHODE DÉLIBÉRÉMENT NON GÉNÉRALISÉE.** L'orpheline de `concept` a été trouvée en appariant les deux relevés de style : **FR 8 occurrences contre EN 7**, et la manquante est la **ligne 336**, `**Interfaces métiers — mécanique, fabrication, informatique…**`. Les douze autres se trouveraient de la même façon. **Elles ne le seront pas** : les extraits des relevés font 65 caractères tronqués des deux bouts, ils disent *où* le tiret a disparu et jamais *par quoi il a été remplacé* — or c'est exactement ce qu'il faut lire. L'outil de bloc 1 devra **retrouver seul** la ligne 336 sans qu'on la lui donne, ce qui en fait son autocontrôle.

**Reste à Tim** (C121) — les quatre écritures de pilotage sont posées :

```powershell
cd C:\Users\turko\Documents\TheSkillCodex
git add -A
git commit -m "seance 13: quatre arbitrages de cadrage, specification des blocs 0 et 1, deux entrees SS8"
git push
```

**Arbitrages en attente** — ✅ **quatre SORTENT de la file** : la **découpe du chantier des puces** et sa **question préalable** (versées à la séance 12), l'**ordre des trois chantiers**, et les **13 `C109 supprimées en EN`** quant à leur *méthode* — leur *lecture*, elle, reste à faire. ⚠ **Un NEUF, non rendu** : **relecture ciblée des 4 fiches avant ou après l'ouverture du chantier des puces** — non bloquant, bloc 0 précède les deux branches, à rendre en ouverture de la prochaine séance Code. ⚠ **Un NEUF versé au BACKLOG** : l'**absence de sortie nominative des porteuses**. **Restent les quatorze entrées de la file**, inchangées depuis la séance 12.

⚠⚠ **Prochaine session — chat Desktop, séance intermédiaire**, sur un objet neuf et hors chantier : **rendre le site exploitable par une IA en travail à quatre mains avec l'étudiant**. *Motif porté par Tim : les étudiants travaillent massivement avec des assistants, et un site que l'IA ne sait pas parcourir ne sera pas utilisé du tout.* **La séance est exploratoire — inventaire des options, pas décision.** ⚠ **Elle n'écrit rien dans `content/`** et ne déplace pas bloc 0, qui reste le premier bloc de la séance Code qui suivra.


## 2026-08-30 (suite 12) — La coupe C128 rend le JOURNAL à 14 Kio, le correctif #10 fait parler une exemption muette, et la « borne haute » du chantier des puces minorait de 24 %

- **Périmètre** — **PC perso, onglet Code**, modèle **Opus 5**, **douzième séance du 30/08**, terminée le **31/08 après minuit**. Les trois points du brief de la suite 11, dans son ordre : coupe C128, correctif #10 de `--style`, mesure du périmètre qu'il débloque. **Onze blocs d'exécution (149 à 159), deux gardes de péremption au vert, `HEAD 6428d1c` stable au caractère sur les deux, un incident d'ouverture, zéro arrêt, ZÉRO OCTET ÉCRIT DANS `content/`.** Horloges : ouverture **30/08 23:37:44**, seconde garde **31/08 00:07:27**.
- **Livrables** — **2 outils de coupe one-off** (`coupe-journal-chantier-3008s12.mjs`, `coupe-predictions-3008s12.mjs`), **1 correctif d'instrument** en trois gestes sur `creer-fiche-en.mjs --style`, **1 mise à jour de `tools/README.md`**, **10 relevés datés**, **4 fragments d'écriture**, **1 table d'insertion**. **Trois tests négatifs délibérés, trois refus, zéro octet écrit à tort.**
- **Décisions Tim** — **deux RENDUES en ouverture, aucune sollicitée en cours de séance.** **(a)** le terme (2) de C116 — *une prédiction de compteur s'écrit comme la ligne exacte attendue* — **confirmé**, et éprouvé **1/N → 2/N** dès le bloc 149 (treize prédictions littérales, treize tenues). **(b)** le seau `C109 supprimees en EN` **reste hors du code de sortie** — **confirmé**, coût de revert d'une ligne **non dépensé**, et vérifié en acte : `C109 supprimees en EN : 13` sur le corpus entier avec un **code de sortie 0**. **Quatre décisions prises seules**, chacune avec son coût de revert publié avant exécution : la **coupe ne garde qu'une entrée** ; le **point-virgule de fin d'item** est compté avec le tiret, dans un seau séparé ; la **mesure du périmètre se fait DANS `--style`** et non dans un outil séparé ; le **fichier de prédictions suit la séance et non l'horloge** quand minuit passe.
- **Conventions** — **aucune neuve, la numérotation reste à 131.** **Huit entrées neuves au §8** et **un amendement de C109 refermé** — la question *« corriger le motif de `--style` reste ouvert »*, ouverte le 29/08 (suite 8), est close avec son chiffre. **Marques d'épreuve** : *terme (2) de C116* **1/N → 2/N** ; *une exemption muette est une perte* **1/N** ; *une « borne haute » ne borne que si son prédicat couvre la population* **0/N** ; *un filtre en sous-chaîne attrape les outils* **0/N** ; *un échantillon C110 riche rate la famille pauvre* **0/N, contre elle** ; *un contrôle d'intégrité se borne à son objet* **0/N, contre elle** ; *un bloc `--dry` verse sa sortie* **0/N** ; *la coupe instrument / monde* **0/N → 1/N**.
- **Tailles** — mesurées **après** l'écriture de `conventions.md` et de `BACKLOG.md`, **avant** celle de la présente entrée (C118, la borne est nommée) : `conventions.md` **571,2 → 580,1 Kio** (594 016 o), `BACKLOG.md` **210,4 → 213,3 Kio** (218 392 o), `TODO.md` **282,7 inchangé** (289 466 o). ⚠ **`JOURNAL.md` 737,2 → 14,3 Kio** (14 618 o) avant cette entrée, `JOURNAL-archive.md` **565,6 → 1 288,8 Kio**, `tools/predictions-260830.md` **1 206,4 → 88,4 Kio** à la coupe, `tools/predictions-260830-chantier.md` **1 118,6 Kio**, neuf. **Corpus FR inchangé au mot, `RESTANT A TRADUIRE : 0 fiches, 0 mots`** : la séance n'a touché aucune fiche.

⚠⚠ **LA COUPE C128 REND LE JOURNAL À 14,3 KIO, ET SA FRONTIÈRE NE POUVAIT PAS ÊTRE UNE DATE.** Le chantier de traduction s'ouvre le **22/08** et se ferme le **30/08 (suite 10)** : la frontière tombe **à l'intérieur d'une journée**, entre deux entrées de même date, là où `archive-journal-coupe-2208.mjs` comparait un seuil `SEUIL = '2026-08-22'`. L'ancre est donc le **titre exact de la première entrée déplacée**, et la garde d'extension se réécrit : ancre unique, ancre au **rang 1**, liste gardée **égale à la déclaration**, dates déplacées **≤** celle de l'entrée gardée. ⚠ *Cette dernière ne peut pas être une comparaison monotone : le fichier porte une rupture d'ordre **préexistante**, `## 2026-08-25` logé entre deux entrées du 24/08.* **58 entrées, 1 gardée, 57 déplacées**, décompte par date juste **aux neuf termes** — 11 + 12 + 2 + 7 + 6 + 9 + 4 + 5 + 1. ✅ **Intégrité vérifiée à l'octet des deux côtés** : l'entrée gardée fait **13 429 octets** avant comme après, et le bloc déplacé, **740 390 octets**, se retrouve **verbatim** dans l'archive. Même coupe sur le fichier de prédictions, **1 145 121 octets archivés** contre **90 229 conservés**, conservation vérifiée à l'octet.

⚠⚠ **LE CORRECTIF #10 NE RÉPARE PAS UN MOTIF AVEUGLE : IL FAIT PARLER UNE EXEMPTION MUETTE, ET C'EST UN DÉFAUT D'UNE AUTRE ESPÈCE.** `--style` **voyait** parfaitement les puces et les listes numérotées — `exemptions()` les reconnaît depuis le 23/08. Ce qu'il faisait, c'est `continue` : l'occurrence n'était **ni comptée, ni affichée, ni rangée hors périmètre, elle disparaissait**. ⚠ *Et le code portait déjà la bonne règle **dix lignes plus bas**, sur la garde d'intervalle numérique — « L'exemption sort en hors-perimetre et non en silence [...] elle reste comptée, donc mesurable ». **Deux exemptions voisines, deux régimes opposés, et c'est celle qui portait le chantier qui était muette.*** ✅ **Un second défaut, que le brief ne nommait pas, est tombé du même geste** : la glose était cherchée comme *le premier tiret de la ligne*, des deux espèces, si bien qu'une puce portant un **intervalle numérique** avant sa glose voyait son demi-cadratin pris pour la glose **et son vrai cadratin signalé en faux positif C109**. **Quatre lignes du corpus dans ce cas, et ce sont elles qui ont fait l'échantillon C110.**

⚠⚠ **LA « BORNE HAUTE » DU CHANTIER DES PUCES N'EN EST PAS UNE : ELLE MINORE DE 24 %.** Périmètre mesuré — **FR 1 164 gloses (puce 971 / liste numérotée 193) sur 178 fichiers**, **EN 1 128 (945 / 183) sur 173** —, contre la référence `puces-tiret.mjs --corpus` motif A, **FR 937 / EN 902**, que le BACKLOG et le brief présentaient comme un plafond. ✅ **La décomposition se referme au chiffre dans les deux langues, et son premier terme n'est pas une subtilité de typographie** : **193 FR et 183 EN items de LISTE NUMÉROTÉE**, que le prédicat `PUCE = /^\s*[-*]\s/` du motif A ne peut pas voir, quand l'amendement C109 du 29/08 (suite 8) ne distingue nulle part la puce de l'item numéroté. Le reste, sur les puces seules : **+39 par langue** de puces logées dans un **blockquote**, **+4** de gloses au **demi-cadratin**, **0** de cadratin sans espaces, **−9 FR** de lignes de commentaire HTML de gabarit que le motif A compte à tort — *et l'asymétrie 9 / 0 tient à ce que `content/en/` ne porte aucun gabarit*. ⚠ **Le taux de chute reste inconnu** : l'unique échantillon est le **20 sur 32** du lot 3 d'`esp32/`, et C119 interdit de l'extrapoler. **Ce qui est acquis est combien il y en a et où, pas combien tomberont.**

⚠⚠ **LE MODE D'ÉCHEC DE LA SÉANCE EST UN SEUL ET IL EST TOMBÉ QUATRE FOIS : J'AI PRÉDIT LA POPULATION QUE J'AVAIS EN TÊTE, LE CODE EN ADDITIONNAIT UNE AUTRE.** (1) `hors artefacts de seance` prédit **8**, mesuré **6** — le filtre `$saleHors` cherche `predictions-` **en sous-chaîne** et attrape `coupe-predictions-3008s12.mjs`, un **outil**. (2) `hors perimetre` prédit **+2**, mesuré **+9** — l'échantillon C110 était bâti sur la famille à **deux** fautes et ignorait celle à **une**. (3) `C109 sur ligne de liste` prédit **36**, mesuré **75** — le compteur additionne `x.cat === 'C109'`, donc les tirets **et** les points-virgules. (4) les `+N octets` d'`inserer-pilotage` prédits en **octets**, affichés en **caractères** — `remplacant.length - ancre.length`. ⚠ *Les quatre compteurs ont été écrits par ce dépôt, deux d'entre eux **par moi, dans le bloc précédent**.* ✅ **Trois des quatre ont été réparés dans le code le soir même** : le seau `C109 sur ligne de liste` déclare désormais sa population (`tiret 36 / point-virgule 39`), l'exemption parle, et le filtre `$saleHors` est versé au BACKLOG avec son correctif d'une expression régulière. *L'amendement (a) rendu ce matin ne suffit donc pas : écrire la ligne littérale attendue **oblige à ouvrir le code**, mais rien n'oblige à ouvrir le code **au bon endroit** — à la ligne du `+= 1` et non à celle du `console.log`.*

**Bilan de prédiction** : **116 prédictions à décompte plein, 108 tenues, 8 réfutées — 93,1 %**, sur les blocs 149 à 158 ; le bloc 159, qui a écrit la présente entrée, et le bloc 160, qui corrige ces deux lignes, ferment leur propre bilan dans `tools/predictions-260830.md`. **La coupe de la suite 11 rejouée, et elle tient dans sa forme mais pas dans ses valeurs** : **instrument 95 sur 100, 95,0 %** ; **monde 13 sur 16, 81,3 %**. ⚠ *Le taux de monde est deux fois celui de la séance 11 (41,9 %), et le motif est lisible : **presque toutes les prédictions de monde de ce soir sont adossées à une reconnaissance mesurée le soir même** — le compteur de glose a été estimé par un script jetable avant d'être écrit dans l'outil, et il est tombé **juste aux huit termes**. **Une prédiction de monde adossée à une mesure du jour se comporte comme une prédiction d'instrument** ; c'est le mélange, encore, et non la qualité du travail.* **Deux gardes de péremption au vert**, copies C124 sous `3008b67` et `3108b1`, **trois tests négatifs délibérés, trois refus, zéro fichier écrit à tort**.

**Reste à Tim** (C121) — les écritures de pilotage sont posées :

```powershell
cd C:\Users\turko\Documents\TheSkillCodex
git add -A
git commit -m "seance 12: coupe C128 journal et predictions, correctif 10 de --style, perimetre des puces mesure"
git push
```

**Arbitrages en attente** — ✅ **deux RENDUS et sortis de la file** : **(a)** le terme (2) de C116, **(b)** le seau `C109 supprimees en EN` hors du code de sortie. ✅ **Un SORT de la file, traité** : le **motif de `--style` aveugle aux tirets de puce et de listes numérotées** — correctif #10. ⚠ **Quatre NEUFS versés au BACKLOG** : le **filtre `$saleHors` en sous-chaîne**, qui rend invisible tout fichier nommé `predictions-…`, outil compris ; les **13 `C109 supprimees en EN` sur 12 fiches**, jamais lues ; le **cadrage du chantier des puces**, avec ses trois découpes possibles ; les **9 lignes de commentaire de gabarit** comptées à tort par `puces-tiret.mjs`. **Restent quatorze entrées de la file** : la **glose des libellés d'interface** ; `CIBLES SANS FICHE` de `--anneau`, **6 faux positifs sur les alias** ; les **34 lignes de commentaire français dans des blocs de code EN** ; le **`title:` d'`esp8266-arduino-core-en`** ; le **faux négatif de la garde d'intervalle de `--style`** ; l'absence d'un **contrôle des titres de section et de callout EN contre les formes de production** ; l'**écart FR/EN d'une puce sur `esp32-arduino-core`** ; les **32 libellés parenthésés de désambiguïsation** ; les **treize `MOT FR` de la classe `tinkercad`** et le total **`MOT FR 41`** sans relevé d'avant ; la **forme des annotations incrustées** ; les **trois autres captures `tinkercad/`** ; les **libellés français en production dans `content/en/`** ; les **12 `ABSENT` de `audit-medias`** ; l'**écart du TODO** ; le **contrôle du front matter entier**. ⚠ *La coupe du JOURNAL et du fichier de prédictions **sort** de la file, faite ce soir.*

⚠⚠ **Prochaine session — chat Desktop**, critère C129 : ce qui vient n'est pas un lot mais un **arbitrage de cadrage**, et il porte sur trois questions qu'aucune mesure ne tranchera. **(1) La découpe du chantier des puces à tiret** — par **module** comme le chantier de traduction, par **espèce** (les 193 + 183 items de liste numérotée d'abord, famille homogène et jamais traitée), ou par **densité** de porteuses ; **et la question préalable : en FR puis report un pour un, ou des deux côtés à la fois ?** *Le chantier fait 1 164 gloses FR sur 178 fichiers, soit l'ordre de grandeur du chantier de traduction en nombre de gestes.* **(2) Les 13 `C109 supprimees en EN`**, qui sont à **lire** une par une et non à traiter mécaniquement — douze fiches nommées, relevé daté au dépôt. **(3) L'ordre des trois chantiers ouverts** : les puces, la relecture ciblée par la mesure (`gestion-de-projet-en` à −6,13 %, les trois fiches à mise en ligne divergente), et le contrôle du nombre de lignes absent de tous les instruments. ⚠ **Un lot de production est possible sans cet arbitrage** — le correctif de trois lignes du **contrôle de mise en ligne** en phase `etat`, et celui d'une expression régulière du **filtre `$saleHors`** —, mais il ne vaut pas une séance à lui seul.


## 2026-08-30 (suite 11) — BILAN DE CHANTIER : la prédiction d'instrument tient à 97 % et celle de monde à 42 %, le foisonnement se mesure à la ligne et pas à la fiche, et le premier arbitrage de la file tient en deux lignes

- **Périmètre** — **PC perso, onglet Code**, modèle **Opus 5**, **onzième séance du 30/08**. ⚠ **PREMIÈRE SÉANCE DU DÉPÔT QUI N'EST NI UN LOT NI UN CORRECTIF D'ARBITRAGE DE LOT** : le corpus est fermé depuis la séance 10 (242 / 242 fiches, 291 261 / 291 261 mots). Les quatre points du brief, dans son ordre : bilan de chantier, file d'arbitrages, cause du foisonnement négatif, suite. **Quinze blocs d'exécution (134 à 148), deux gardes de péremption au vert, un incident de protocole, zéro arrêt, ZÉRO OCTET ÉCRIT DANS `content/`.** Horloges : ouverture **22:34:18**, deuxième garde **23:13:58**. ⚠ **`HEAD` a bougé en cours de séance** — le commit de Tim est passé pendant l'échange, `a421335` → `6dcb912` —, ce que la deuxième garde a relevé et confirmé.
- **Livrables** — **1 correctif d'outil** : le seau symétrique **`C109 supprimees en EN`**, arbitrage **1 sur 18** de la file, **annoncé à deux lignes depuis le lot 13 et rendu en deux lignes de logique et une d'affichage**. **1 outil de mesure versionnable**, `tools/decompo-registre.mjs` (deux axes en partition exacte, autocontrôle à `0 ecart` sur 484 fiches). **1 outil d'écriture versionnable**, `tools/inserer-pilotage.mjs` — garde d'ancre unique **tout ou rien** transposée aux fichiers de pilotage, que `renommer-titres` et `remplacer-passe` ne couvraient pas. **5 outils jetables**, **10 sorties datées**, **3 fragments d'écriture**. **`compter-mots.mjs` exporte désormais `corpsC110`, `MOTIF_MOT` et `EST_MOT`**, pour qu'aucune décomposition ne réimplémente la règle C110.
- **Décisions Tim** — **une sollicitée, une rendue** : **arbitrage (a), le terme (2) de la sous-règle C116 est amendé** — *une prédiction de compteur se publie sous la forme de la **ligne exacte attendue**, jamais d'un nombre isolé*. **Deux décisions prises seules, chacune avec son coût de revert publié avant exécution (C116 (8))** : (1) la **lecture de la règle d'écrivain unique** — la plume est *libre* et non *interdite*, la séance 10 l'ayant rendue à sa clôture —, avec l'engagement tenu de **ne rien écrire dans `content/` ni dans les fichiers de pilotage avant réponse** ; (2) le seau `C109 supprimees en EN` **n'entre pas dans le code de sortie**, une suppression n'étant pas un défaut mécanique — **coût de revert : une ligne**, et c'est l'**arbitrage (b), non rendu**.
- **Conventions** — **aucune neuve, la numérotation reste à 131.** **Un amendement arbitré** (terme (2) de C116) et **quatre entrées neuves au §8**, dont **trois nées d'une hypothèse mesurée fausse**. **Marques d'épreuve** : *terme (2) amendé* **0/N → 1/N** ; *un contrôle écrit dans un seul sens ne garde qu'une moitié* **1/N** ; *la coupe instrument / monde* **0/N** ; *le foisonnement se mesure à la ligne* **0/N** ; *un contrôle de mise en ligne manque au chantier* **0/N**.
- **Tailles** — mesurées **après** l'écriture de `conventions.md` et de `BACKLOG.md`, **avant** celle de la présente entrée (C118, et la borne est nommée) : `conventions.md` **563,1 → 571,2 Kio** (584 945 o), `BACKLOG.md` **206,0 → 210,4 Kio** (215 429 o), `TODO.md` **282,7 inchangé**, `JOURNAL.md` **724,1 avant cette entrée** — sa taille d'après est relevée au bloc 148 de `tools/predictions-260830.md`. **Corpus FR 291 261 mots, `RESTANT A TRADUIRE : 0 fiches, 0 mots`, inchangés au mot** : la séance n'a touché aucune fiche.

⚠⚠ **LE FAIT DE LA SÉANCE EST DANS LA COUPE DU DÉCOMPTE, ET IL VAUT POUR LES QUINZE SÉANCES.** Les prédictions se coupent en deux populations que le protocole n'avait jamais séparées. **Prédictions d'INSTRUMENT** — ce qu'un outil va imprimer, ce qu'un `git status` va porter, ce qu'un contrôle croisé va refermer : **58, dont 56 tenues, 96,6 %**. **Prédictions de MONDE** — ce que le corpus va se révéler être : **31, dont 13 tenues, 41,9 %**. *La séance 10 l'avait entrevu sans le chiffrer — « le registre garde sa valeur sur les compteurs de DENSITÉ et la perd sur le SIGNE du foisonnement ». **Conséquence pratique : un taux global de tenue ne veut rien dire tant que les deux populations ne sont pas séparées, parce qu'il varie avec le MÉLANGE et non avec la qualité du travail.** Un lot de production sortira toujours au-dessus de 90 %, une séance d'enquête toujours en dessous, et c'est le signe qu'elle a servi à quelque chose.*

⚠⚠ **LA CAUSE DU FOISONNEMENT NÉGATIF EST INSTRUITE, ET TROIS HYPOTHÈSES SONT MORTES EN CHEMIN.** Quatre blocs, un outil neuf, **et à chaque fois l'hypothèse a été mesurée fausse au lieu d'être plaidée**. ✅ **(1)** Le corpus est **homogène par registre** — `titre` +6,7 %, `callout` +3,4 %, `tableau` +2,2 %, `liste` +3,7 %, `paragraphe` +3,3 % — et par construit — `alt` +0,4 %, `chemin d'embed` +0,0 %, `libellé de wikilink` −0,1 %, `code inline` −0,2 %, `prose nue` +3,9 %. **Aucune catégorie n'est structurellement négative**, `alt` compris : l'observation d'`easyeda` au lot 13 était **locale à cette fiche**. ✅ **(2)** Le lot 14 n'est donc **pas** un effet de mélange : son registre `liste` sort à **−3,9 %** contre **+3,7 %** au corpus, sur **35,3 %** de son volume contre 20,5 %. ✅ **(3)** Ligne à ligne, la **densité de génitif français** rend une **échelle monotone sur cinq seaux** — **+6,89 / +3,92 / +3,49 / +2,00 / +0,63 %** sur **270 857 mots**, `r = −0,188` sur 7 450 lignes — et le mécanisme est lisible sur pièce : `le hub de la carte` **4** → `the board's hub` **3**, `le bus de réglage de la caméra` **6** → `the camera's control bus` **4**. ❌ **(4)** À l'échelle de la **fiche**, il n'en reste **rien** : `r = −0,029`. ❌ **(5)** Comme **prédicteur**, il bat la constante de **1,6 %** — le modèle ne sort jamais de la bande +1,6 % à +5,0 % quand le réel va de **−6,1 % à +18,2 %**. ⚠ **Ce qui reste ouvert est plus étroit qu'avant et se dit en une phrase : la variance de fiche à fiche, 24 points d'amplitude, est sans cause — et trois hypothèses sont désormais interdites de rejeu.**

⚠⚠ **UNE TROUVAILLE NON CHERCHÉE, ET AUCUN DES SIX CONTRÔLES DE CLÔTURE NE POUVAIT LA VOIR.** Sur 242 paires, **9 n'ont pas le même nombre de lignes** dans leur corps C110. Trois d'entre elles, toutes dans `conduite/proj/`, perdent **−31, −32 et −32 lignes de paragraphe** en anglais **pendant que tous les autres registres sont égaux au chiffre** — `titre` 10/10, `callout` 22/22, `liste` 40/35, 22/20, 24/24. *Ce n'est pas une perte de contenu : c'est une **divergence de mise en ligne**, leurs paragraphes anglais étant écrits en lignes longues quand toutes les sources sont coupées à la main.* ⚠ **Pourquoi c'est invisible** : `--controle` compare des **nombres**, `derive-traduction` des **empreintes de source**, `compter-mots` **somme**, `--style` lit des **motifs**, `--alt` des alt de même rang, `--libelles` des libellés — **le nombre de lignes n'est lu nulle part**. ⚠ **Et un fait séparé sort du même relevé** : `gestion-de-projet` foisonne à **−6,13 %**, le plus bas du corpus, quand ses deux jumelles de même défaut font −0,37 % et −1,61 %. **C'est la première fiche à relire, et le motif est chiffré.**

✅✅ **C116 (7) INSTRUITE : LA RÈGLE N'EST PAS MAL FORMULÉE, ELLE ÉTAIT SANS DÉCLENCHEUR.** Les trois chutes du 30/08 (séance 10) ne sont pas trois fois la même chose. **(1)** `hors perimetre` : la ligne **nomme** déjà « titres, tableaux et alt », mais sa parenthèse se lit comme une *exclusion* quand elle est une *définition du seau* — **défaut d'outil**. **(2)** `remplacements` : la ligne ne dit rien de sa population, ancres ou occurrences — **manque d'outil**. **(3)** `audit-medias` : la ligne dit `fiches lues`, exactement — **défaut de lecture**. *Le correctif qui couvre les trois est l'amendement (a) : on ne peut pas écrire la ligne attendue sans avoir ouvert le code ou une sortie précédente, donc le geste que C116 (7) demandait comme un rappel devient une **condition d'écriture de la prédiction**.* ✅ **Première application le soir même : les onze prédictions du bloc 134 écrites comme les lignes littérales de `batterie.ps1 -Phase garde`, onze tenues.** ⚠ *Et la réfutation P147.3 montre la limite exacte de l'amendement : elle porte sur une **addition d'octets**, pas sur un compteur — j'ai compté le `+6 612` de la deuxième entrée en oubliant le `+1 437` de la première, **alors que les deux étaient dans la même table et que l'essai à blanc les affichait tous les deux**. C'est le défaut du lot 14 rejoué à l'identique.*

⚠ **L'INCIDENT DE LA SÉANCE EST UN BLOC LANCÉ SANS PRÉDICTIONS, ET IL EST CONSIGNÉ COMME TEL (terme (3)).** L'écriture de `inserer-pilotage.mjs`, de sa table et **le lancement du test négatif délibéré** ont été enchaînés à la suite du bloc 146 comme s'ils en faisaient partie, alors qu'ils écrivent deux fichiers neufs et lancent un outil. *Le résultat du test négatif — `REFUS : 1 defaut(s). AUCUN FICHIER ECRIT.`, code 1, quatre entrées valides et pourtant non appliquées — **ne compte pas au décompte** puisqu'il n'a pas été prédit.* **Le bloc a été repris, prédictions publiées, avant l'essai à blanc et l'écriture.**

**Bilan de prédiction** : **89 prédictions à décompte plein, 69 tenues, 20 réfutées — 77,5 %**, sur les blocs 134 à 147 ; le bloc 148, qui écrit la présente entrée, ferme son propre bilan dans `tools/predictions-260830.md`. ⚠ *Le taux est le plus bas depuis le lot 4 du 29/08, et **c'est le résultat attendu** : dix-neuf des vingt réfutations sont des **prédictions de monde**, dans une séance qui n'a produit aucune fiche et n'a fait qu'interroger le corpus.* **Deux gardes de péremption au vert**, **deux lancements de `batterie.ps1`** copiés sous `3008b65` et `3008b66`, **un test négatif délibéré, un refus, zéro fichier écrit à tort**.

**Reste à Tim** (C121) — les trois écritures de pilotage sont posées :

```powershell
cd C:\Users\turko\Documents\TheSkillCodex
git add -A
git commit -m "seance 11: entree JOURNAL, amendement C116 terme 2, quatre entrees SS8, BACKLOG"
git push
```

**Arbitrages en attente** — ✅ **un RENDU** : **(a)**, le terme (2) de C116, écrit au §8. ⚠ **Un NEUF, non rendu** : **(b)**, le seau `C109 supprimees en EN` hors du code de sortie, **coût de revert une ligne**. ✅ **Un SORT de la file, traité** : le **seau symétrique** lui-même. ⚠ **Deux NEUFS versés au BACKLOG** : le **contrôle du nombre de lignes** absent de tous les instruments (correctif : trois lignes en phase `etat`), et **`gestion-de-projet-en` à −6,13 %**, première fiche à relire. **Restent les dix-sept de la file** : la **glose des libellés d'interface** ; `CIBLES SANS FICHE` de `--anneau`, **6 faux positifs sur les alias** ; les **34 lignes de commentaire français dans des blocs de code EN** ; le **`title:` d'`esp8266-arduino-core-en`** ; le **faux négatif de la garde d'intervalle de `--style`** ; l'absence d'un **contrôle des titres de section et de callout EN contre les formes de production** ; l'**écart FR/EN d'une puce sur `esp32-arduino-core`** ; les **32 libellés parenthésés de désambiguïsation** ; ⚠ le **motif de `--style` aveugle aux tirets de puce et de listes numérotées, qui BLOQUE le chantier des puces** ; les **treize `MOT FR` de la classe `tinkercad`** et le total **`MOT FR 41`** sans relevé d'avant ; la **forme des annotations incrustées** ; les **trois autres captures `tinkercad/`** ; les **libellés français en production dans `content/en/`** ; les **12 `ABSENT` de `audit-medias`** ; l'**écart du TODO** ; ⚠ **la coupe du JOURNAL (724 Kio) et du fichier de prédictions du 30/08 (1 744 Kio)** ; le **contrôle du front matter entier**.

⚠⚠ **Prochaine session — onglet Code**, lot de production mécanique (critère C129), **et l'ordre est justifié par une mesure** : **(1) la coupe C128** du JOURNAL et du fichier de prédictions — c'est la **seule tâche dont le coût augmente si on la retarde**, chaque ouverture de séance lisant `JOURNAL` head 250 et `conventions` head+tail, et **la fermeture du corpus EST la frontière de chantier** que C128 exige ; **(2) le correctif du motif de `--style`**, qui débloque le chiffrage du chantier des puces à tiret — tant qu'il est aveugle, le périmètre se dimensionnerait **à l'œil**, ce que C118 interdit ; **(3) la mesure** de ce périmètre, la seule référence disponible n'étant qu'une **borne haute** (`puces-tiret.mjs --corpus`, motif A, **FR 937 puces / 166 porteuses / 248 fichiers**). ⚠ **La relecture EN de bout en bout n'est PAS retenue comme chantier suivant** : 242 fiches, c'est un chantier de la taille de la traduction, quand la séance a produit une **liste courte et mesurée** de fiches à relire — `gestion-de-projet-en`, les trois fiches à mise en ligne divergente, et les huit que le modèle rate le plus. **Une relecture ciblée par la mesure coûte une séance ; une relecture de masse coûte un chantier.** ⚠ **Arbitrage (b) à rendre en ouverture.**

---

<!-- Sessions antérieures au 30/08 (suite 11) déplacées dans `JOURNAL-archive.md` lors des nettoyages documentaires successifs (détail des lots dans l'intro de l'archive et les marqueurs de groupe). Dernière passe : 30/08 — coupe C128 à la frontière du chantier de traduction, sessions 22/08 → 30/08 (suite 10) archivées en bloc. -->
