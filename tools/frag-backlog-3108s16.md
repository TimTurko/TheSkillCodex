### 31/08 (séance 16) — ce que la pose du chantier IA laisse ouvert

- [ ] **L'anglais de `/ia/` est le seul anglais publié du dépôt qu'aucun instrument ne mesure.** Le bloc EN vit sous `<section lang="en">` **dans `content/ia/index.md`**, donc hors de `content/en/` — et `--controle`, `--style`, `--alt`, `--libelles`, `derive-traduction` et `compter-mots --paires` balaient tous la zone anglaise. ⚠ *L'exemption `bilingue: true` du bloc C2 referme la **population** des compteurs ; elle ne crée **aucun** contrôle sur le texte exempté.* **Deux issues, à arbitrer** : (a) faire accepter un chemin explicite à `--style` et `--alt` et les lancer à la main sur cette page à chaque retouche ; (b) apprendre aux balayages à descendre dans les blocs `<section lang="en">` de la zone FR — plus juste, plus cher, et sans autre porteuse aujourd'hui. *Population actuelle : une page, 67 lignes d'anglais.*
- [ ] **Sept en-têtes de groupe de `llms.txt` restent en chemin brut, et aucune règle mécanique ne les résout.** `## embarque/mcu`, `## embarque/mcu/bus`, `## embarque/mcu/sans-fil`, `## embarque/mcu/xiao`, `## embarque/mesure`, `## embarque/realisation`, `## embarque/simulation`. Leur hub C18 existe mais **ne porte pas le nom du dossier** — `microcontroleur`, `bus-de-communication`, `techno-sans-fil`, `instruments-de-mesure`, `simulation-electronique` — et `embarque/realisation` n'a pas de hub propre (la colonne est décrite par `embarque/index`). ✅ *Le repli posé au bloc 166 (hub en fichier nommé) en a résolu **dix sur dix-sept**.* **Issue possible** : un champ `hub:` dans le front matter de ces sept fiches, lu par l'émetteur — sept éditions FR **et** sept `--recaler`, contre une table codée en dur dans un émetteur qui périmerait au premier renommage.
- [ ] **`derive-traduction --manquantes` est le seul instrument qui nomme une page FR sans jumelle, et il n'est dans aucune phase de `batterie.ps1`.** La phase `etat` lance `derive-traduction` **sans le drapeau** : le seau `ORPHELINE` n'est donc jamais imprimé en clôture. Sans conséquence tant qu'il n'y a qu'une orpheline connue et voulue (`ia/index.md`) ; le jour où une fiche FR neuve est créée sans jumelle, **rien ne le dit**. *Correctif : un mot sur une ligne de `batterie.ps1`.*
- [ ] **La ligne `+N octets` de `inserer-pilotage.mjs` affiche des POINTS DE CODE.** `remplacant.length - ancre.length` compte des unités UTF-16, et le libellé dit « octets » — sur un fragment français, l'écart est systématique et minorant. *Déjà relevé le 30/08 (P158.4) et non corrigé ; reversé ici parce que l'outil a servi trois séances de suite depuis.* **Correctif : `Buffer.byteLength` des deux côtés, une ligne.**
- [ ] **`/ia/` porte des URL absolues et n'est vérifiée par rien — mais le motif s'est déplacé.** L'entrée du 31/08 (séance 15) parlait d'un domaine codé en dur dans la page. Mesuré depuis : la page porte **~20 URL absolues** et le contrôle manque toujours ; ⚠ **et le préambule de `llms.txt` en recopie l'intégralité**, si bien qu'un changement de `baseUrl` casserait **deux** artefacts au lieu d'un, l'un d'eux étant celui que les machines lisent. *Correctif inchangé : comparer les `https://` internes de `content/` à la `baseUrl` de `quartz.config.ts`, une dizaine de lignes.*
- [ ] **Le contrôle en ligne de la séance 16 n'a pas été fait : il attend le commit et la CI de Tim.** Quatre `Invoke-WebRequest -Method Head` → `/llms.txt`, `/ia/`, `/conduite/proj/concept.md`, `/robots.txt`, quatre `200` attendus ; `hreflang` sur une page FR publiée ; clic EN → FR depuis `/en/` (dû depuis la séance 14). *Tout est vérifié **en local sur `public/` régénéré**, rien ne l'est en ligne.* **Puis, hors dépôt** : Search Console « Vérifier », dépôt du sitemap, inspection d'URL sur `/` et `/ia/`, Bing par import.

