

- [ ] **Le `robots.txt` du bloc B1 n'est lu par aucun crawler, et c'est mesuré.** Il est publié à `https://timturko.github.io/TheSkillCodex/robots.txt` (`200`), mais un robot ne lit le `robots.txt` qu'à la **racine de l'hôte** — `https://timturko.github.io/robots.txt`, qui appartient au site utilisateur GitHub et **pas à ce dépôt** : mesuré **`404`** le 31/08 au soir. Conséquences, aucune n'étant grave : le `Allow: /` n'autorise rien que le défaut n'autorise déjà (pas de `robots.txt` à la racine ⇒ tout permis), et la directive `Sitemap:` qu'il porte **n'est vue par personne** — c'est la soumission manuelle en Search Console qui est le vrai canal, et elle est faite. ⚠ *L'arbitrage 5 de la séance 14 (« `robots.txt` tout permis + `Sitemap:` ») est donc **à moitié décoratif** : il reste une déclaration d'intention lisible par un humain, il n'a aucun effet mécanique.* **Rien à corriger dans le dépôt** ; à savoir avant d'en tirer une conclusion sur le crawl. *Le seul moyen d'avoir un vrai `robots.txt` serait un dépôt `timturko.github.io` séparé, hors périmètre.*
