### 31/08 (séance 12) — ce que la coupe C128 et le correctif #10 laissent ouvert

- [ ] **Le compteur `hors artefacts de seance` de `batterie.ps1` est aveugle à tout fichier dont le NOM porte `predictions-`, outil compris.** `$saleHors` filtre par sous-chaîne : `tools/coupe-predictions-3008s12.mjs` et `tools/coupe-predictions-dry-3008s12.txt` — un outil et un relevé — disparaissent du compteur qui existe pour voir les outils. ⚠ *Correctif possible : ancrer le motif sur `tools/predictions-\d{6}` au lieu de `predictions-`, deux caractères dans une expression régulière. Le coût de ne rien faire est un compteur qui minore silencieusement, et il a déjà fait publier un chiffre faux.* **Non fait ce soir : c'est `batterie.ps1`, pas l'objet de la séance.**
- [ ] **Douze fiches EN portent TREIZE occurrences C109 supprimées par la traduction, et aucune n'a jamais été lue.** Premier balayage du corpus entier par le seau symétrique (arbitrage Tim du 30/08) : `C109 supprimees en EN : 13`, sur `concept-en`, `ecoconception-en`, `specification-technique-en` (**2**), `cpp-conditions-en`, `cpp-en`, `esp32-gpio-en`, `esp32-prise-en-main-en`, `ide-en`, `micropython-controle-en`, `micropython-simulation-en`, `easyeda-en`, `ltspice-en`. ⚠ *`easyeda-en` est le cas qui a fait naître le seau, et il **se retrouve tout seul** dans le balayage ; les onze autres n'avaient jamais été vus. Relevé nominatif dans `tools/style-avant-en-3008s12.txt`.* **Une suppression n'est pas un défaut mécanique — elle est à LIRE, une par une.**
- [ ] **Le chantier des puces à tiret est CADRABLE, et il faut décider de sa découpe avant de l'ouvrir.** Périmètre mesuré : **FR 1 164 gloses sur 178 fichiers**, **EN 1 128 sur 173**, plus le cas 3 déjà compté (**FR 75 / EN 73**). ⚠ *Le taux de chute est inconnu — l'unique échantillon est le **20 sur 32** du lot 3 d'`esp32/`, et C119 interdit de l'extrapoler.* **Trois découpes possibles à arbitrer** : par **module** (comme le chantier de traduction), par **espèce** (les 193 + 183 items de liste numérotée d'abord, famille homogène et jamais traitée), ou par **densité** (les fichiers les plus porteurs d'abord). ⚠ **Et une question préalable** : le chantier se mène-t-il **en FR puis report un pour un** comme les lots de traduction, ou **des deux côtés à la fois** ?
- [ ] **Neuf lignes de commentaire HTML de gabarit sont comptées par `puces-tiret.mjs` comme des puces à tiret du corpus.** `templates/fiche-notion.md` et `templates/fiche-tuto.md`, à l'intérieur de blocs `<!-- … -->` : le motif A ne masque pas les commentaires HTML. ⚠ *Effet mesuré petit — 9 lignes sur 937 — et permanent. `masquerHorsProse` de `--style` les masque déjà, donc les deux compteurs divergent de ce montant par construction.*

