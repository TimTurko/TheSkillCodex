> **➤ MAJ 31/08 (séance 16)** — **LE CHANTIER « SITE EXPLOITABLE PAR UNE IA » EST POSÉ EN ENTIER. IL NE RESTE QUE LE DÉPLOIEMENT ET LA RECETTE.** PC perso, onglet Code, exécution directe, **seize blocs (162 à 177)**, **aucun commit** (C121). Chantier des puces **non ouvert**, bloc 0 et bloc 1 intacts.
>
> ✅ **BLOC A, machinerie Quartz** : `rawMarkdown.ts` (485 `.md` émis, front matter conservé, commentaires HTML retirés — **3 → 0** sur `ecoconception`), `llmsTxt.ts` (**242 paires, 1 sans jumelle**, 121 237 o, 243 lignes, appariement par `source_fr` et jamais par le slug), `Head.tsx` (`hreflang` **1/1**, `text/markdown`, jeton **non redoublé**), `quartz.layout.ts` (filtre `ia` dans les deux Explorateurs, pied de page vidé — `discord` **0**), **A5** déclaration XML du sitemap (**485 `<url>`**).
>
> ✅ **BLOC B, passes mécaniques** : `content/robots.txt` (copié **identique à l'octet**), **`lang: en` sur les 242 fiches EN** (`+2178` octets, `numstat` **242 × `1 0`**, `DERIVE 0 / A JOUR 242`), et `lang: en` au squelette de `creer-fiche-en.mjs`. **Effet mesuré sur le HTML servi** : `<html lang="en">` sur une page anglaise, `<html lang="fr">` sur sa jumelle.
>
> ✅ **BLOC C, contenu** : `content/ia/index.md` (**150 lignes**, textes de la séance 15 **extraits par `sed`, jamais retapés**, **12 `<h3>` et non 6** — la décision 5 de Tim est vérifiée par un chiffre qui discrimine) ; exemption `bilingue: true` avec son **test négatif fermé** (`RESTANT A TRADUIRE` **1 → 0** sans qu'une fiche soit traduite, population **242 + 1 + 0 = 243**) ; carte des cinq phases et `description:` sur les deux racines ; phrase du hub `conduite/` et de sa jumelle — les deux jumelles **`--recaler`** après un `DERIVE 1` visible.
>
> ⚠ **UN ARBITRAGE NEUF, ET C'EST LE PLUS LOURD** : **l'anglais de `/ia/` est le seul anglais publié qu'aucun instrument ne mesure** — il vit hors de `content/en/`, et `--controle`, `--style`, `--alt`, `--libelles`, `derive-traduction` et `--paires` balaient tous la zone anglaise. *Exempter n'est pas contrôler.* Deux issues au BACKLOG. ✅ **Un arbitrage CLOS sans être rendu** : l'orpheline `/ia/` — `audit-wikilinks` indexe des **cibles**, pas des pages, il ne peut pas la voir ; aucune exemption écrite.
>
> ⚠ **137 prédictions, 132 tenues, 5 réfutées (96,4 %), quatre tests négatifs délibérés, quatre refus.** Les cinq réfutations sont **toutes** des comptes de lignes ou d'entrées de mes propres éditions — le geste d'édition compté au lieu des lignes que git compare. Parade posée au bloc 168 (recompte nominatif entrée par entrée), tenue **neuf blocs de suite**. Entrée § 8.
>
> **À FAIRE** : ① [Tim] **commit + push de clôture** (bloc PowerShell au JOURNAL) ; ② [Tim] **une fois la CI passée, le contrôle en ligne** — quatre `200` sur `/llms.txt`, `/ia/`, `/conduite/proj/concept.md`, `/robots.txt`, `hreflang` sur une page FR publiée, **clic EN → FR depuis `/en/`** (dû depuis la séance 14) ; ③ [Tim] **Search Console** « Vérifier » → sitemap → inspection d'URL sur `/` et `/ia/` → **Bing par import** ; ④ **séance suivante = chat Desktop, recette APRÈS** — neuf prompts, mêmes conditions, `_drafts/recette-ia-AAMMJJ-apres.md`, **grille contre grille** ; ⑤ [Tim] **patch § 7 côté claude.ai** ; ⑥ **après la rentrée, le TODO dans son ordre** : bloc 0 des puces → bloc 1 → arbitrage relecture ciblée → tranche 1 → C132 (numérotation à **131**).

